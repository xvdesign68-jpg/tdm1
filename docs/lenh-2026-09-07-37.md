# LỆNH #37 (07/09/2026) — func-path theo ma trận + lead rác đọc theo trang + index log reply

> Từ output KHỐI 2 LỆNH #36. Cloud Shell, 1 lần dán (2 dòng ngoài heredoc, không `!`). Không có secret.

## Vì sao
- **(a) `stepNick` (đường func)** tới nay chỉ có van react theo brand (LỆNH R), còn thiếu: ma trận nhiệt độ×hành động, cổng vai LỆNH #31 (chủ bài/người bán), né lead-là-bình-luận (func chưa tym/reply đúng comment). Nick anh chạy AdsPower nên chưa ảnh hưởng, nhưng nếu bật lại nick func thì cần gate này. Patch chỉ sửa TRONG thân `stepNick` theo dump 06/09 (3 mốc, fail-closed): lead loop bỏ junk/lost, `brandAllow(temp,'react')`, `roleBlockOf`, `commentIdOf`; thread mới mang `allow:{comment,inbox}`; `drive()` nhảy bước bị tắt (không tốn lượt) + van theo `brandCaps` (hết → hẹn lại 1 giờ). Thread cũ không có `allow` → hành vi cũ. Harness `docs/harness-lenh37-2026-09-07.mjs` **14/14**.
- **(b) `_l36_junk.mjs` bản 2**: đọc `leads` theo trang 300 + `select()` vài field (bản 1 đọc nguyên collection → `DEADLINE_EXCEEDED 300 s`). Chỉ liệt kê; xoá là lệnh riêng.
- **(c) index** `outreach_log(status ASC, at DESC)` để `_wk_check.mjs` mục 4 (log reply 7 ngày) chạy được.

## Dán
```bash
cat > /tmp/l37.sh <<'L37'
#!/bin/bash
# LỆNH #37 (07/09/2026) — (a) outreach.js: đường func stepNick theo ma trận/van brand/cổng vai/né comment-lead → deploy outreachTick · (b) lead rác Lan Anh Nguyễn đọc theo trang (sửa lỗi DEADLINE_EXCEEDED #36d) · (c) index outreach_log(status, at desc) cho log reply. Fail-closed, backup .bak-<TS>, idempotent.
set -o pipefail
cd ~/firebase-s13/functions || { echo 'KHONG VAO DUOC ~/firebase-s13/functions'; exit 1; }
TS=$(date +%Y%m%d-%H%M%S); echo "backup TS=$TS (giờ UTC)"
cat > _l37_stepnick.cjs <<'EOF_SN'
/* LỆNH #37 (07/09/2026) — outreach.js: ĐƯỜNG FUNC (`stepNick`) theo MA TRẬN nhiệt độ×hành động + VAN theo brand + cổng vai (LỆNH #31) + né lead-là-bình-luận
   — đóng mục tồn "func path chưa gate theo matrix" (v119-35/36). Chỉ sửa TRONG thân stepNick (mốc `async function stepNick(` … `async function handleAuth(`),
   content-anchored theo dump 06/09, fail-closed (thiếu mốc → không ghi), idempotent (marker LENH #37). Dùng: node _l37_stepnick.cjs outreach.js */
const fs = require('fs'); const F = process.argv[2] || 'outreach.js'; const src = fs.readFileSync(F, 'utf8');
if (src.includes('LENH #37')) { console.log('outreach.js: ĐÃ patch LENH #37 (idempotent) — bỏ qua'); process.exit(0); }
const i0 = src.indexOf('async function stepNick('), i1 = src.indexOf('async function handleAuth(');
if (i0 < 0 || i1 < 0 || i1 <= i0) { console.error('KHONG THAY MOC stepNick/handleAuth (i0=' + i0 + ', i1=' + i1 + ')'); process.exit(1); }
let body = src.slice(i0, i1);
const A1 = "      prevStep = t.step;\n      let ok = false;";
const A2 = "    const lead = ld.data(); lead.id = ld.id;\n    if (lead.dropped) continue;\n    const tref = threads.doc(ld.id);";
const A3 = "      pid, step: 'react', active: true, nextAt: nowMs(), createdAt: FieldValue.serverTimestamp() });";
for (const [n, a] of [['A1', A1], ['A2', A2], ['A3', A3]]) { const c = body.split(a).length - 1; if (c !== 1) { console.error('KHONG THAY MOC ' + n + ' trong stepNick (đếm ' + c + '): ' + a.slice(0, 70).replace(/\n/g, '⏎')); process.exit(1); } }
body = body.replace(A1, () => `      prevStep = t.step;
      // LENH #37: func-path theo MA TRẬN + VAN theo brand (như AdsPower). Bước bị brand tắt (t.allow[kind]===false) → nhảy bước kế, không tốn lượt;
      //   hết van brand cho bước này → hẹn lại 1 giờ (không chặn nick nạp lead khác ở tick sau). Thread cũ không có t.allow → hành vi cũ.
      const kind37 = t.step === 'react' ? 'react' : t.step === 'comment' ? 'comment' : t.step === 'inbox' ? 'inbox' : null;
      if (kind37 && t.allow && t.allow[kind37] === false) { const nx = kind37 === 'react' ? 'comment' : kind37 === 'comment' ? 'inbox' : 'done'; await tref.set(nx === 'done' ? { step: 'done', active: false } : { step: nx, nextAt: nowMs() }, { merge: true }); if (nx === 'done') return; continue; }
      if (kind37) { const u37 = await usageOf(pid); const c37 = brandCaps(brand); if ((Number(u37[kind37]) || 0) >= (Number(c37[kind37]) || 0)) { await tref.set({ nextAt: nowMs() + 60 * 60000 }, { merge: true }); return; } }
      let ok = false;`);
body = body.replace(A2, () => `    const lead = ld.data(); lead.id = ld.id;
    if (lead.dropped || lead.lost) continue;
    // LENH #37: gate như AdsPower — bỏ junk · ma trận brand tắt react cho nhiệt độ này · chủ bài/người bán (LỆNH #31) · lead-là-bình-luận (func chưa tym/reply đúng comment → để AdsPower lo)
    const temp37 = lead.temp || 'cold';
    if (temp37 === 'junk') continue;
    if (typeof brandAllow === 'function' && !brandAllow(brand, temp37, 'react')) continue;
    if (typeof roleBlockOf === 'function' && roleBlockOf(lead)) continue;
    if (typeof commentIdOf === 'function' && commentIdOf(lead)) continue;
    const tref = threads.doc(ld.id);`);
body = body.replace(A3, () => `      pid, step: 'react', active: true, nextAt: nowMs(), createdAt: FieldValue.serverTimestamp(),
      allow: { comment: (typeof brandAllow === 'function') ? !!brandAllow(brand, temp37, 'comment') : true, inbox: (typeof brandAllow === 'function') ? !!brandAllow(brand, temp37, 'inbox') : true } }); // LENH #37: ma trận brand cho các bước sau`);
fs.writeFileSync(F, src.slice(0, i0) + body + src.slice(i1));
console.log('PATCH OK outreach.js (LENH #37: stepNick gate ma trận/van brand/vai/comment-lead — 3 mốc trong thân stepNick)');
EOF_SN
cat > _l36_junk.mjs <<'EOF_JUNK2'
/* LỆNH #37 (b) = LỆNH #36 (d) bản 2: liệt kê lead tên chứa "Lan Anh Nguyễn" — ĐỌC THEO TRANG 300 doc + chỉ lấy vài field (bản 1 đọc nguyên collection → DEADLINE_EXCEEDED 300 s).
   --delete = xoá thật (recursiveDelete kèm ghi chú). */
import { initializeApp, applicationDefault, getApps } from 'firebase-admin/app';
import { getFirestore } from 'firebase-admin/firestore';
if (!getApps().length) initializeApp({ credential: applicationDefault(), projectId: 'smartlead-z15' }); const db = getFirestore();
const fold = s => String(s || '').normalize('NFD').replace(/[\u0300-\u036f]/g, '').replace(/đ/g, 'd').replace(/Đ/g, 'D').toLowerCase();
const DEL = process.argv.includes('--delete'); const PAGE = 300;
let last = null, scanned = 0; const hit = []; const t0 = Date.now();
for (;;) {
  let q = db.collection('leads').select('name', 'brand', 'stage', 'score', 'detected_at', 'source', 'source_name').orderBy('__name__').limit(PAGE);
  if (last) q = q.startAfter(last);
  const s = await q.get(); if (s.empty) break;
  s.docs.forEach(d => { scanned++; const l = d.data() || {}; if (fold(l.name).includes('lan anh nguy')) hit.push({ id: d.id, l }); });
  last = s.docs[s.docs.length - 1]; if (s.size < PAGE) break;
}
console.log('Quét', scanned, 'lead trong', Math.round((Date.now() - t0) / 1000) + ' s → tìm thấy', hit.length, 'lead tên chứa "Lan Anh Nguyễn":');
for (const { id, l } of hit) { const det = l.detected_at && l.detected_at.toDate ? l.detected_at.toDate().toISOString().slice(0, 16) : String(l.detected_at || '').slice(0, 16);
  console.log(' ', id, '|', l.name, '| brand=', l.brand, '| stage=', l.stage, '| score=', l.score, '| detected=', det, '| nguồn=', String(l.source || l.source_name || '').slice(0, 30));
  if (DEL) { await db.recursiveDelete(db.collection('leads').doc(id)); console.log('    → ĐÃ XOÁ (kể cả ghi chú)'); } }
if (!DEL && hit.length) console.log('Xoá thật: node _l36_junk.mjs --delete');
EOF_JUNK2
echo "=== (a) outreach.js: stepNick (đường func) theo ma trận + van brand + cổng vai + né comment-lead ==="
cp outreach.js "outreach.js.bak-$TS" && node _l37_stepnick.cjs outreach.js || { echo 'DỪNG (a): patch không áp — gửi em output (không ghi gì)'; exit 1; }
node --check outreach.js || { echo 'DỪNG (a): lỗi cú pháp — khôi phục: cp outreach.js.bak-'"$TS"' outreach.js'; cp "outreach.js.bak-$TS" outreach.js; exit 1; }
grep -n "LENH #37" outreach.js | cut -c1-80 | head -4
set -a; . ./.env; set +a
node --input-type=module -e "const m=await import('./index.js'); console.log('IMPORT OK · outreachTick =', typeof m.outreachTick, '· scheduledScan =', typeof m.scheduledScan)" || { echo 'DỪNG: import lỗi — khôi phục: cp outreach.js.bak-'"$TS"' outreach.js'; exit 1; }
cd ~/firebase-s13 && firebase deploy --only functions:outreachTick > /tmp/l37_deploy.log 2>&1; tail -3 /tmp/l37_deploy.log
grep -q "Deploy complete" /tmp/l37_deploy.log || { echo 'DEPLOY LỖI — gửi em: grep -iE "error" /tmp/l37_deploy.log | head'; exit 1; }
gcloud functions describe outreachTick --region asia-southeast1 --gen2 --format='value(state,updateTime)' 2>/dev/null || echo '(describe bỏ qua)'
echo "=== (b) lead rác 'Lan Anh Nguyễn' — đọc theo trang, CHỈ LIỆT KÊ (xoá thật: node _l36_junk.mjs --delete) ==="
cd ~/firebase-s13/functions && node _l36_junk.mjs || echo '(b) LỖI — gửi em output'
echo "=== (c) index outreach_log(status ASC, at DESC) — cho truy vấn log reply (tạo nền, vài phút) ==="
gcloud firestore indexes composite create --collection-group=outreach_log --query-scope=COLLECTION --field-config=field-path=status,order=ascending --field-config=field-path=at,order=descending --quiet --async 2>&1 | tail -2 || echo '(c) có thể index đã tồn tại — bỏ qua'
echo "LỆNH #37 XONG — exit=0"
L37
bash /tmp/l37.sh
```
**Mong đợi**: `PATCH OK outreach.js (LENH #37 …)` · 3 dòng marker · `IMPORT OK · outreachTick = function` · `Deploy complete` · `ACTIVE` · `(b) Quét N lead trong S s → tìm thấy K lead …` · `(c)` in tên operation hoặc "already exists" · `LỆNH #37 XONG — exit=0`.
Sau đó: (b) đúng là rác → `cd ~/firebase-s13/functions && node _l36_junk.mjs --delete`.

## ★ KẾT QUẢ — ĐÃ CHẠY OK (06/09 22:55 UTC = 07/09 05:55 VN, `exit=0`)
- backup `outreach.js.bak-20260906-225555`; (a) `PATCH OK` marker dòng 136 (drive gate) / 163 (lead loop) / 178 (`allow`) · `IMPORT OK · outreachTick = function · scheduledScan = function` · `Deploy complete` · `outreachTick` ACTIVE 2026-09-06T22:57:15Z.
- (b) quét **1855 lead trong 2 s** (bản 1 chết 300 s vì tải nguyên doc) → 1 lead: `B01VaRplT2m36J1LAiVP` Lan Anh Nguyễn · brand `z15mrc-tts-1` · new · 50 · 2026-08-22 · nguồn "Tuyển dụng thực tập sinh Hà Nội". Xoá: `node _l36_junk.mjs --delete` (anh quyết).
- (c) index `outreach_log(status ASC, at DESC)`: `Create request issued` (operation nền).
- **(c) KIỂM 07/09 ~06:05 VN: index READY** — `gcloud firestore indexes composite list` lọc `/outreach_log/`: `READY status:ASCENDING,at:DESCENDING,__name__:DESCENDING` (mới) + `READY brandCode:ASCENDING,at:DESCENDING,__name__:DESCENDING` (cũ CICAgNjpgYIK). `_wk_check.mjs` mục 4 (log reply 7 ngày) chạy được từ giờ.
