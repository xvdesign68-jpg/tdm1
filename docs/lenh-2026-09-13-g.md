# LỆNH G (13/09/2026) — Đợt 2.5 PC-3 "BỘ NHỚ NGƯỜI VIẾT" (author_memory): người bán quen bỏ TRƯỚC AI · khách cũ quay lại có cờ + ghi chú + push · CF clearAuthorMemory · Rules · TTL

Mốc trên mã **SAU LỆNH F** (index.js có marker `LENH F`; stats.js/push.js có marker `LENH C`). Chạy SAU LỆNH E và F. File: `lenh-2026-09-13-g.sh` (KHỐI 1, nhúng `_lg_patch.cjs` + `_lg_authmemcf.js` + `_lg_rules.cjs` + `_lg_after.mjs`; sha256 `22e574a575adc0f0508c36a6c7dc2f6e102d82f3df4d231842a65482abbdc458`, 41.761 byte / 251 dòng) · `lenh-2026-09-13-g-patch.cjs` (3 file index.js 13 mốc · stats.js 2 · push.js 2, marker `LENH G`, fail-closed NGUYÊN TỬ, idempotent) · `lenh-2026-09-13-g-authmemcf.js` (CF mới) · `lenh-2026-09-13-g-rules.cjs` · `lenh-2026-09-13-g-after.mjs` (KHỐI 2 chỉ đọc) · harness `harness-lenhg-2026-09-13.mjs` **46/46** · FE zip **v119-93 / v120-esm-aq** (`fe-2026-09-07-v119-61/m35.py`).

## 1. Vì sao (L-4 / Q-1 / D-2 / N2-6 báo cáo rà soát 11/09 — PC-3, critic: CHỈ khoá theo link hồ sơ)
- Scanner không nhớ AI: mỗi bài mới của cùng một người bán lại tốn tầng 1 + tầng 2 (số liệu A4: 158/359 comment-lead là chủ bài, 47 "mùi người bán", ban ngày ~5 seller/lượt). Vai `seller` chỉ dùng cho đúng bài đó.
- Không có khái niệm "khách cũ": `identityKey` rỗng với ~69 % lead thiếu author_url trước #48; sau #48 lead-bài 100 % có uid số → giờ nhận diện được người quay lại.
- Anh chốt (12/09): khoá theo **link hồ sơ / uid số**, KHÔNG theo tên (2 người trùng tên trong cùng group).

## 2. Thay đổi
### 2.1 `index.js` (13 mốc)
- **G1** helper + bộ đếm: `authorKeyG(url, uid)` (= `__slProfileKey31` → `id:<uid>` | `u:<username>`, hoặc `author_uid` số ≥5 chữ số), `memDocG`, `brandKeyG`, `sellerKnownOfG(m)` (= sellerHits ≥ 2 & buyerHits 0 & lastSellerAt < 30 ngày), `returningOfG(m, brand)` (= `brands.<brand>.lastStage ∈ responded/booked/closed` + lastLeadId), cache lượt `memCacheG`, bộ đệm ghi `memBufG` (memNoteG/memFlushG: increment + arrayUnion, dedup theo post_id — mỗi bài đếm 1 lần dù chấm lại).
- **G2** `SEEN_DEC_B.seller_known = 'seller'` · **G3** `scanned_posts` thêm `author_key` + `memHits` (web: nút sửa bộ nhớ) · **G4** Pha 2 đọc author_memory theo LÔ (1 `getAll`/lô 300, như seen) · **G5** cổng: người bán quen → `recordPost(decision 'seller_known', role seller, memHits)` không gọi AI; `force` (Quét lại từ đầu) vẫn chấm.
- **G6** Pha 3b: chỉ phán quyết THẬT của AI (`ai._llm !== false`) nuôi bộ nhớ — `seller` khi vai `seller`; `buyer` khi thành lead (isLead). Vai `poster_self`/`reseller` KHÔNG tính người bán (fail-closed).
- **G7/G8** khách cũ: đọc bộ nhớ theo brand của bài (`x.brandB`), lead cũ còn tồn tại → lead mới mang `returning, returning_lead_id, returning_stage, returning_at, returning_assignee_uid, returning_assignee` + ghi chú hệ thống 🔁 (giai đoạn cũ · ngày · người phụ trách · lead cũ); `commitLeadNow` trả **id doc mới** (G9; gộp multitouch vẫn `true`) → bộ nhớ ghi `leadIds` + `brands.<brand>.lastLeadId`.
- **G10** sweeper #46 (chấm lại điểm tạm) cũng nuôi bộ nhớ · **G11** `memFlushG()` trước khi tính `durationMs` · **G12** `scans.sellerKnown/returning` · **G13** `export * from './authmemcf.js'`.
### 2.2 `stats.js` (2 mốc) — `authorKeyG/memIdG/brandKeyG/stageMemG` (thuần, export) + trong `statsOnLead`: lead đổi giai đoạn `responded/booked/closed` (kể cả tạo mới đã ở giai đoạn đó) và có khoá → `author_memory/{key}.brands.<brand> = { lastStage, lastStageAt, lastLeadId }` (set merge, expireAt +180 ngày).
### 2.3 `push.js` (2 mốc) — `returningGateG(before, after)` thuần: lead có `returning:true` + `returning_lead_id` + brand/brand_hint, before chưa có cờ, không dropped/lost → push **"🔁 Khách cũ quay lại (Nđ): tên"** (body: từng <giai đoạn> · người phụ trách cũ · nhu cầu; `require:'1'`, `actionTitle 'Mở lead'`, tag `ret-<id>`) tới `returning_assignee_uid`, không có → `brandAdmins(brand)` (admin brand + super). Nhánh này `return` → KHÔNG push "Lead nóng mới" ở cùng lần ghi (lần ghi 2 gán brand: before đã có cờ + brand_hint → không push nữa).
### 2.4 `authmemcf.js` (MỚI) — CF `clearAuthorMemory` (onRequest cors, region asia-southeast1, **chỉ Super Admin**): body `{ key | author_url + author_uid, action: 'clear' | 'seller', post_url?, note? }` → `clear` = sellerHits 0 / sellerIds [] / lastRole '' / clearedBy; `seller` = sellerHits 2 + buyerHits 0 + lastSellerAt now (bỏ trước AI 30 ngày); mỗi thao tác ghi `ai_feedback` {kind not_seller | mark_seller, key, by, at, post_url, note} (nuôi PC-1). Khoá theo tên (`n:`) → 400.
### 2.5 Rules — 2 block đọc `author_memory` + `ai_feedback` (read super, write false) sau `match /databases/{database}/documents {` (cách #23/C). TTL `author_memory.expireAt` (180 ngày; doc được ghi lại mỗi lần có bài/giai đoạn mới).
### 2.6 KHÔNG đổi: worker, outreach.js, scraper, content.js, index Firestore (getAll theo id).

## 3. KHỐI 1 (dán vào Cloud Shell) — khối trong chat = `cat > /tmp/lg.sh <<'EOF_LG'` … `EOF_LG` rồi `[ sha256 = 22e574a5… ] && bash /tmp/lg.sh; echo "exit=$?"`
Thứ tự trong script: viết 4 file phụ → `node --check` → guard (marker LENH F / LENH C / hotGateC / firestore.rules) → backup `.bak-<TS>` (index/stats/push + firestore.rules + authmemcf.js nếu có; chạy lại không đè) → `mv` authmemcf.js → patch 3 file (nguyên tử) → Rules → `node --check` ×4 → import test (.env: scheduledScan/manualScan/statsOnLead/pushOnLead/clearAuthorMemory + authorKeyG/stageMemG/returningGateG/authorKeyOfG) → deploy `firestore:rules + scheduledScan + manualScan + statsOnLead + pushOnLead + clearAuthorMemory` (gated "Deploy complete"; lỗi → in cách chạy lại / quay lui) → TTL `author_memory.expireAt` → describe 5 function.

## 4. KHỐI 2 (chỉ đọc, sau ≥ 15′; bộ nhớ đầy dần sau vài ngày quét)
```bash
cd ~/firebase-s13/functions && node _lg_after.mjs
```
In: lượt quét 6 h (sellerKnown/returning) · author_memory (tổng, người bán quen, seller 1 lần, khách cũ, super đánh dấu/gỡ) · scanned_posts 24 h seller_known (mẫu) · lead 24 h returning (mẫu) · ai_feedback 7 ngày. Kỳ vọng ngày đầu: sellerKnown 0–vài (bộ nhớ cần ≥2 bài/người bán), returning 0 (tăng khi sales đổi giai đoạn lead rồi khách đăng lại). Đọc theo trang ≤300 + select().

## 5. Kiểm (harness `harness-lenhg-2026-09-13.mjs` 46/46; dry-run `.sh` fake HOME/firebase/gcloud ×2 exit 0 idempotent, deploy lỗi → exit 1 + describe)
- patch: OK 3 file / idempotent / --check ×4 / fail-closed NGUYÊN TỬ (1 mốc lệch → không ghi file nào) / thiếu LENH F → dừng / vá dở (marker ở 1 file) → dừng · Rules OK / idempotent / mốc thiếu.
- scanAll trọn vòng (Firestore + BrightData + LLM giả): bài 1 AI seller → sellerHits 1 · bài 2 → 2 · bài 3 → `seller_known` KHÔNG gọi AI (0 lượt tầng 1/2), seen 'seller', scanned_posts author_key/memHits 2, scans.sellerKnown 1 · force → AI vẫn chấm, bài đã đếm không đếm lại · có bài thành lead (buyerHits 1) → sellerHits 3 vẫn KHÔNG seller_known · bộ nhớ 40 ngày → chấm lại, AI seller → tươi lại → seller_known · super gỡ nhãn → lead · khách cũ (brands.b1 closed) → returning + ghi chú 🔁 + scans.returning 1 + bộ nhớ ghi lead mới (giữ lastStage, không đụng brand khác) · brand b2 chỉ từng lost → không · lead cũ đã xoá → không · sweeper #46 → sellerHits 1 + dropped rescore_role.
- stats.js: authorKeyG 7 dạng · stageMemG 8 ca · statsOnLead new→booked→closed ghi brands.b1 · lead không khoá → không ghi. push.js: returningGateG 6 ca · pushOnLead → 1 push tới uAn (không push hot cùng lần) · bước 2 không push lại · không assignee → admin brand · không cờ → nhánh hot cũ. CF: 401/403/400 (khoá tên)/clear/seller + ai_feedback ×2.

## 6. Rủi ro / ghi chú
- **Sai chiều an toàn**: seller_known chỉ khi ≥2 bài KHÁC NHAU AI nói seller và 0 bài thành lead; sai → chỉ bỏ lỡ bài của 1 người, super gỡ được ("Không phải người bán"), nhãn tự hết hạn 30 ngày; force (Quét lại từ đầu) bỏ cổng.
- Bộ nhớ toàn hệ thống cho `seller` (người bán là người bán với mọi brand); `returning` theo TỪNG brand (khách của A không phải khách cũ của B).
- Push "Khách cũ quay lại" thay push "Lead nóng mới" ở cùng lần ghi (tránh 2 thông báo) — lead lạnh/ấm quay lại cũng được push (giá trị cao hơn nóng mới).
- Chi phí: +1 `getAll` theo lô ở Pha 2 (rẻ hơn 1 lượt tầng 1) + 1 batch ghi cuối lượt; đọc lead cũ 1 doc chỉ khi returning.
- `ai_feedback` là collection mới (Admin SDK ghi) — khác field `leads.ai_feedback` (PC-6). FE v119-93 chỉ super mới thấy nút; user thường thấy chip "Khách cũ quay lại" + quyết định "Người bán quen · bỏ trước AI".

## 7. KẾT QUẢ KHỐI 1 — ĐÃ CHẠY OK 13/09 13:07–13:09 VN (anh dán ảnh Cloud Shell, `exit=0`, ngay sau LỆNH F)
- sha256 khối chat khớp `22e574a5…c458` · backup TS `20260913-060707` (UTC) → `index.js / stats.js / push.js / firestore.rules .bak-20260913-060707`.
- **PATCH OK 3 file (LENH G): index.js 13 mốc · stats.js 2 · push.js 2** (author_memory đọc lô Pha 2 · seller_known · returning + ghi chú · sweeper · flush cuối lượt · scans sellerKnown/returning · scanned_posts author_key · stats lastStage theo brand · push khách cũ quay lại · export authmemcf.js) · **PATCH OK firestore.rules** (block đọc `author_memory` + `ai_feedback`, super, write=false). Marker `LENH G`: index.js 16 · stats.js 2 · push.js 2 · Rules 2 dòng · authmemcf.js 36 dòng — **khớp 100 % bản dựng lại ở máy em** (mã sau F + patch G: 16/2/2/2/36).
- **IMPORT OK** (.env): `scheduledScan · statsOnLead · pushOnLead · clearAuthorMemory` = function · `authorKeyG` → `id:100012345678901` / `u:nguyen.van.a` ✓ · `stageMemG booked` → `{"key":"id:100012345678901","stage":"booked"}` ✓ · `returningGateG` → `returning` ✓ (= kỳ vọng harness 46/46).
- **Deploy Rules + `scheduledScan · manualScan · statsOnLead · pushOnLead · clearAuthorMemory` → Deploy complete**; describe 5 function **ACTIVE 2026-09-13T06:08Z** (URL `clearAuthorMemory` = `https://asia-southeast1-smartlead-z15.cloudfunctions.net/clearAuthorMemory`). **TTL `author_memory.expireAt` (180 ngày) OK** (ACTIVE sau vài phút).
- Web: anh đã deploy **v119-93** (chip Khách cũ quay lại · Bài đã quét `seller_known` · nút Không phải người bán / Đánh dấu người bán → CF `clearAuthorMemory`) ⇒ chuỗi G trọn từ máy chủ tới web. Bộ nhớ dày dần theo lượt quét; `seller_known` chỉ xuất hiện khi 1 người viết đã bị AI chấm `seller` ≥ 2 bài trong 30 ngày.
## 8. KẾT QUẢ KHỐI 2 — (chờ: `cd ~/firebase-s13/functions && node _lg_after.mjs` sau ≥15′; sau vài ngày quét mới có `sellerKnown/returning` > 0)
