# LỆNH #46 (11/09/2026) — LEAD "ĐIỂM TẠM – AI CHƯA CHẤM" (ai_scored:false): vá triệt để

> Bản lưu; **khối lệnh thật dán trong chat** (anh copy lên Cloud Shell). File: `lenh-2026-09-11-46.sh` (KHỐI 1, gồm `_l46_patch.cjs` + `_l46_after.mjs` heredoc quoted, không `!` ngoài heredoc) · `lenh-2026-09-11-46-patch.cjs` · `lenh-2026-09-11-46-after.mjs` (KHỐI 2 chỉ đọc) · harness `harness-lenh46-2026-09-11.mjs` (**69/69**) trên `lenh-2026-09-11-46-fixture/` (engine dựng lại từ dump #20/#31b/#43/#44a — không phải bản đầy đủ, chỉ mốc patch) · dry-run `.sh` với fake firebase/gcloud + thư mục functions giả: **exit 0 ×2**, idempotent, fail-closed nguyên tử (thiếu 1 mốc → 4 file không đổi, "LỆCH" khi 1 file đã patch). FE đi cùng: **zip `v119-89` / `v120-esm-am`** (`fe-2026-09-07-v119-61/m31.py`, smoke 135/135 cả 2 cây).

## KẾT LUẬN LỆNH #45 (anh chạy 11/09 ~15:xx VN, 4 ảnh)
- **KHÔNG phải credit/key/rate-limit**: `.env` và 4 service đã deploy (scheduledscan/manualscan/outreachtick/gencontent) cùng 1 key (sha8 `ded3c6d4`), `LLM_MODEL=gpt-5.6-sol`, `LLM_PREFILTER_MODEL=gpt-5-nano`, `MIN_KEEP_SCORE=40`; `GET /models` 200; gọi đơn 200 (10–15 s/lượt, nano 24 s vì 2.304 token suy nghĩ); rate-limit 5.000 RPM / 2 M TPM còn gần nguyên.
- **GỐC hôm nay = OpenAI trả HTTP 500 `server_error` ("The server had an error while processing your request. Sorry about that!") TỪNG ĐỢT khi gọi song song**: burst 3 lượt song song → **2/3 lỗi 500 trong 1,7 s**. `scoreLead` cũ chỉ thử lại **1 lần sau 1,5 s** (vẫn trong đợt lỗi) → rơi `heuristic()` từ khoá → "Thuê mb chỉ 5 triệu…" = 35 + 45 ("thuê") + 10 ("5 triệu") = **90 NÓNG**, `reply` rỗng, `ai_scored:false` → chip "Điểm tạm – AI chưa chấm" (chỉ super thấy). Log 24h khớp: 15:06 ×3 "LLM lỗi, dùng heuristic: LLM 500" (lead #399, kèm vòng miss-reply gọi lại 2 lần cũng 500) + 14:39 ×2 (lead #398 lạnh 45 "vốn góp 200tr").
- **Phụ**: 4 dòng "prefilter 400 Invalid body: failed to parse JSON value" = `buildPostContent` `slice(0, maxLen)` cắt đôi emoji → surrogate lẻ trong JSON body → OpenAI từ chối (tầng 1 fail-open nên chỉ tốn 1 lượt, không mất lead).
- Hệ quả trước #46: lead rác thành NÓNG → push "Lead nóng mới", Hộp việc, Nên gọi tiếp theo, KPI; engine automation vẫn bốc (chỉ lọc dropped/junk/vai/người thật); `scans.scoreErrors` vẫn 0 (hàm không ném lỗi) → **im lặng hoàn toàn**, không bao giờ chấm lại. 14 ngày: 7 lead điểm tạm (5 ngày 28/08, 2 ngày 11/09), 0 lead bị sales/máy chạm.

## LỆNH #46 làm gì (marker `LENH #46`, 4 file)
1. **`lib/scorer.js` — `llmChat46`** dùng chung cho prefilter + scoreLead: `sanitize46` (bỏ surrogate lẻ + ký tự điều khiển ở mọi message) · `AbortController` **60 s** (`LLM_TIMEOUT_MS`) · **thử lại 4 lần** (`LLM_TRIES`) giãn **2 / 5 / 12 s + jitter**, tôn trọng `Retry-After` (≤ 30 s) cho **429 / 5xx / mạng / JSON hỏng**; **KHÔNG thử lại** `auth` (401/403/invalid_api_key) · `quota` (insufficient_quota, kể cả 429) · `model` (404) · `badreq` (400/422) · API từ chối `response_format` → gửi lại không json mode · đọc JSON khoan dung (kẹp ```json, regex `{…}`) · lỗi mang `e.kind` · bộ đếm `scoreLead.health46` (ok/fail/attempts/lastKind/preFail…). **`scoreLead` LLM hỏng → NÉM LỖI** (không còn heuristic ngầm); `scoreLead(…,{heuristicOnly:true})` = heuristic **KẸP ≤ 59 (lạnh)**, intent "Điểm tạm – AI chưa chấm được lúc quét…", `_fallback`. Không cấu hình key → heuristic như cũ (cố ý). prefilter 2 lần, fail-open, `_err`.
2. **`index.js` (scanAll)**: LLM hỏng → `deferPost46` ghi **`score_retry/{R_<post_id>}`** {post gọn (chỉ primitive, text ≤4000), src{name,url,industry,brand}, tries, firstAt, nextAt, lastErr, kind} — chờ **3′ → 10′ → 30′ → 1 h → 3 h → 6 h**, tối đa **6 lần / 24 h**; bài ghi `scanned_posts` decision **`ai_wait`**, **KHÔNG tạo lead**. Đầu Pha 3b của lượt quét theo lịch (không backfill/không quét riêng nguồn/không force): nạp ≤ 30 bài tới hạn vào `toScore` (nguồn đã xoá → bỏ) → AI chấm → lead như bình thường (`ai_scored:true`) + xoá doc chờ. Quá hạn / `badreq` / không post_id → lead **dự phòng kẹp ≤ 59** + `ai_scored:false` (`llmFallback`). Miss-reply: không gọi lại khi đang dự phòng; 2 lượt gọi lại chỉ thử 2 lần. **Sweeper cuối lượt**: lead `ai_scored:false` (mở, ≤ 30 ngày, `rescore_tries` < 8) → AI chấm lại **≤ 12 lead/lượt** (pool 3, mới nhất trước) → ghi đè score/temp/intent/need/service/reply/role + `ai_scored:true, rescored_at, ai_prev{score,temp,intent}` + ghi chú hệ thống "AI đã chấm lại…"; AI nói không phải lead → `dropped:true, dropped_by:'rescore'|'rescore_role', dropped_reason`; **sales đang chăm (first_care_at/last_touch_at/stage≠new/assignee) → chỉ cập nhật điểm, không loại** (`rescore_note`); lỗi → `rescore_tries+1`, dừng sớm khi LLM lỗi; chỉ chạy khi lượt này LLM khoẻ (có OK hoặc 0 lỗi); tắt bằng `RESCORE_FALLBACK=false`, số/lượt `RESCORE_PER_RUN`. **Giám sát**: lượt ≥ 2 lượt chấm hỏng & 0 OK (hoặc lỗi auth/quota/model) → `console.log({severity:'ERROR', message:'[LLM-DOWN] …'})` (kích alert LỆNH #9 → email) **1 lần** khi vừa xuống + `system_status/llm {ok:false, since, at, runs, kind, sample, model, fails, deferred, fallback}`; có lượt OK sau khi down → `[LLM-UP]` WARNING + `ok:true, recoveredAt`. `scans` doc thêm `llmDeferred/llmFallback/llmRescored/llmRescoredLeads/llmOk/llmFail/llmErr/llmPreFail`.
3. **`outreach.js`**: `stepNickAdspower` (trước `humanBusy44`) + `stepNick` func-path (sau `junk`) **bỏ lead `ai_scored === false`** → máy không chạm lead điểm tạm.
4. **`push.js`**: không push "Lead nóng mới" khi `ai_scored === false`; AI chấm lại điểm tạm → nóng (không dropped) → push 1 lần.

Không cần Rules/index mới: `score_retry` chỉ Admin SDK ghi/đọc (không có block Rules → web không đọc được, đúng ý); `system_status/llm` đọc qua block `system_status/{sid}` read=super (LỆNH #23); truy vấn `score_retry where nextAt<= orderBy nextAt` và `leads where ai_scored==false` / `rescored_at >` đều single-field (tự có index).

## Vận hành / lưu ý
- Thời gian tệ nhất 1 lượt chấm khi OpenAI sập hẳn: 4 lượt × 60 s + 19 s ≈ 4,3′/bài, pool 6 → lượt quét ≤ ~5′ rồi mọi bài vào hàng chờ (timeout scheduledScan 1800 s). Bình thường 500 lẻ tẻ: thử lại sau 2–5 s là qua.
- `daily_stats`: lead chấm lại đổi nhiệt độ không trừ counter hot cũ (hạn chế sẵn có của statsOnLead khi đổi điểm); lead bị loại → `dropped` như thao tác loại tay.
- Lead điểm tạm CŨ (7 lead 14 ngày) tự được chấm lại ở lượt quét theo lịch đầu tiên sau deploy (≤ 12/lượt); ghi chú hệ thống ghi rõ điểm cũ → mới.
- FE v119-89: chip "Điểm tạm – AI sẽ chấm lại" mọi vai trò (thẻ + modal); Nên gọi tiếp theo −30; Bài đã quét "Chờ AI chấm lại"; `system_status/llm` → Cảnh báo hệ thống + thẻ (Overview/Lịch sử quét) + chip thanh nhịp quét; ok:true tự gỡ.

## KHỐI 1 (dán vào Cloud Shell) → xem chat. KHỐI 2 sau ≥ 15′ (có bài mới): `cd ~/firebase-s13/functions && node _l46_after.mjs`
Kỳ vọng KHỐI 1: `PATCH OK 4 file` · marker scorer 4 / index 9 / outreach 2 / push 1 · `IMPORT OK … tries 4 · timeout 60000 ms` · `LIVE OK · gpt-5.6-sol · ~10–15 s · hotness … · lượt gọi 1 (hỏng 0)` (hoặc `lượt gọi 2–3` nếu trúng đợt 500 = thử lại có tác dụng) · `Deploy complete` · 4 function ACTIVE.
Kỳ vọng KHỐI 2: `system_status/llm ok=true` · `score_retry` 0–vài bài · lead `ai_scored:false` mở giảm về 0, "Đã AI chấm lại 24h: N lead · loại k" · scans có cột `llmOk/llmFail/…` · log `[LLM-RESCORE] chấm lại …`.

## KẾT QUẢ (điền sau khi anh chạy)
_(chờ output)_
