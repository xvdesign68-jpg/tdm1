# LỆNH #42 (09/09/2026) — bộ đếm `daily_stats` KHÔNG tính người bán / chủ bài là "lead hợp lệ" (anh chốt sau khối kiểm 09/09)

**Phát hiện** (khối kiểm `_l41_kpi14.mjs`, 09/09 ~08:00 VN): 14 ngày có 1.029 lead "hợp lệ" theo bộ đếm, trong đó **91 lead là vai người bán / chủ bài** (LỆNH #31b đã Loại) vẫn được cộng vào
`new/hot` vì bộ đếm tính theo ngày phát hiện và LỆNH #31b gắn vai SAU khi lead đã được đếm. Ô KPI "Lead hợp lệ / Lead nóng" bị thổi ~9% (hscl-01 ~15%).

**Vá** (`stats.js`, marker `LENH #42`, áp lên bản đã qua #41): `roleBad(l)` = `role` seller|poster_self hoặc `self_comment===true` → `countable` thêm điều kiện `!roleBad`;
lead đã đếm rồi MỚI bị gắn vai (rescore, `_l31_fix`) → **trừ lại** `new −1, <temp> −1` (rác → `junk −1`) đúng ngày phát hiện; gỡ vai → cộng lại. Lead sales tự bấm "Loại" (không vai) **vẫn tính hợp lệ**.
`_l42_recount.mjs` = `_l41_recount` + bỏ lead có vai → dựng lại tuyệt đối `new/hot/warm/cold/junk` 60 ngày (mọi brand 1.029 → ~938). Harness `docs/harness-lenh42-2026-09-09.mjs`; dry-run `.sh` fake ×2 idempotent.
FE v119-81 đi cùng: caption ô "Lead hợp lệ" = "AI chấm từ 40 điểm, đã bỏ rác", tooltip ghi rõ không tính người bán/chủ bài, chip "còn mở N".

## KHỐI 0 — dán 2 file vào `~/firebase-s13/functions`
```bash
cd ~/firebase-s13/functions
cat > _l42_stats.cjs <<'EOF_L42S'
<dán nguyên docs/lenh-2026-09-09-42-stats-patch.cjs>
EOF_L42S
cat > _l42_recount.mjs <<'EOF_L42R'
<dán nguyên docs/lenh-2026-09-09-42-recount.mjs>
EOF_L42R
node --check _l42_stats.cjs && node --check _l42_recount.mjs && echo OK
```

## KHỐI 1 — chạy
```bash
cat > /tmp/l42.sh <<'EOF_L42'
<dán nguyên docs/lenh-2026-09-09-42.sh>
EOF_L42
cd ~/firebase-s13/functions && bash /tmp/l42.sh 2>&1 | tee ~/l42-$(date +%Y%m%d-%H%M).log
```
**Mong đợi**: `PATCH OK stats.js (LENH #42…)` · `SYNTAX OK` · `IMPORT OK · statsOnLead = function · người bán không đếm ✓ …` · deploy `Successful update operation` + `ACTIVE` ·
(d) DRY `quét ~2000 lead (bỏ qua: … vai người bán/chủ bài ~160)` · (e) `ĐÃ GHI 90 doc daily_stats` · (f) `_l41_kpi14.mjs` in ô "Lead hợp lệ" mọi brand ≈ 938 và dòng đếm trực tiếp
`✗ LỆCH` là BÌNH THƯỜNG (script kiểm #41 vẫn đếm cả lead vai; hiệu 2 số = số lead vai trong 14 ngày = 91). Anh dán output cho em check.

## ★ ĐÃ CHẠY OK (09/09/2026 10:31 VN = 03:31 UTC, anh dán 3 ảnh output)
- **(b) Patch**: `PATCH OK stats.js (LENH #42…)` · `SYNTAX OK` · marker `LENH #42` dòng 64 · `IMPORT OK · statsOnLead = function · người bán không đếm ✓ · gắn vai sau khi đếm → trừ lại ✓ · gỡ vai → cộng lại ✓ · update thường không đếm ✓` (4/4 kịch bản).
- **(c) Deploy**: `firebase deploy --only functions:statsOnLead` → `Successful update operation` · describe **ACTIVE `2026-09-09T03:31:40Z`** (10:31 VN). Từ mốc này lead mới có vai người bán/chủ bài không vào bộ đếm; lead đã đếm rồi mới bị gắn vai → trừ lại đúng ngày phát hiện.
- **(d) DRY 4 ngày gần nhất**: so bộ đếm ↔ dựng lại đều `✓` (không lệch — vì recount #41 lúc 07:10 mới chạy, chênh chỉ ở lead vai).
- **(e) Recount thật 60 ngày**: quét **2.005 lead**, bỏ qua **158 lead vai người bán/chủ bài** (= đúng số LỆNH #31b đã Loại) → **ĐÃ GHI 90 doc `daily_stats`**. Theo brand 60 ngày (hợp lệ/nóng): hscl-01 **570/137** (trước #42: 637/140) · test-agency 223/76 (225/76) · z15mrc-tts-1 615/226 (656/236) · z15mrc-tuyendung1 275/34 (316/35).
- **(f) Khối kiểm `_l41_kpi14.mjs` (14 ngày, mọi brand)**: Bài đã quét 25.246 (+58,4% so 15.939) · **Lead hợp lệ 939** (+43,4% so 655; trước #42: 1.029) · **Lead nóng 250** (+33,7% so 187; trước: 258) · Đã chốt 1 (0,1%). Theo brand: **hscl-01 291/72/0** (bài quét 9.921; ảnh KPI anh chụp trước đó là 333/73) · test-agency 39/3/1 (2.291) · tts 434/164/0 (6.613) · tuyendung 175/11/0 (6.421). Dòng đếm trực tiếp in `1030 = hợp lệ 1030 (nóng 258) + rác 0 → ✗ LỆCH (939)` = **BÌNH THƯỜNG, đúng như dự báo**: script kiểm bản #41 vẫn cộng lead vai vào "hợp lệ"; 1.030 − 91 vai = **939 ✓**, nóng 258 − 8 = **250 ✓**. Trong 1.030: đã Loại 91 (chính là 91 lead vai bị #31b Loại) · không thành 1 · còn mở thật sự 938.
- **Sau #42 file kiểm đã sửa** (`docs/lenh-2026-09-09-41-kpi14check.mjs`, selftest cập nhật): đếm trực tiếp tách riêng "vai người bán/chủ bài" khỏi hợp lệ → chạy lại `node _l41_kpi14.mjs` (dán bản mới đè) sẽ in `✓ khớp` và dòng "còn mở thật sự" = chip "còn mở" của v119-81.
- **Kết luận**: LỆNH #42 chạy đúng, số liệu nhất quán 3 chiều (bộ đếm = kho lead − lead vai; trigger mới không đếm vai; recount = 158 lead vai đúng bằng #31b). Anh chỉ còn **deploy zip v119-81** (Cmd+Shift+R) để web hiện dải kỳ + chip "còn mở" theo bộ đếm mới; bản riêng v120-esm-ae deploy sau khi v119-81 ổn.
