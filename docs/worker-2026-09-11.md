# Worker AdsPower `2026-09-11` — Đợt 1 sau rà soát automation 10/09 (R-15 · R-9 · R-8 · R-2 · R-1 phía worker · dryRun)

> Bản kế tiếp của `worker.mjs 2026-09-06d`. Áp bằng script fail-closed `docs/worker-2026-09-11-patch.cjs`
> (`node docs/worker-2026-09-11-patch.cjs <worker 06d> <worker mới>`; idempotent; từ chối file không phải 06d; ghi file CHỈ khi khớp đủ mọi mốc).
> Harness: `docs/harness-worker-2026-09-11.mjs` **67/67** + 8 harness cũ (t_flow 16/16 · t_zombie 13/13 · t_refund 6/6 · t_meta 7/7 · t_cmtapi 12/12 · t_reply 19/19 · t_reply2 9/9 · t_uid 9/9).
> `node --check` OK · 1.768 dòng · 51 mốc `v2026-09-11`. **KHÔNG cần đổi backend/Rules/index** (đã có LỆNH #44). **Không có secret.**

## 1. Thay đổi (theo mã phát hiện trong báo cáo rà soát 10/09)

| Mã | Vấn đề (bản 06d) | Sửa trong 2026-09-11 |
|---|---|---|
| **R-15 / W-19** | Bình luận BÀI qua API dùng bộ biến 29/08 (`feedLocation GROUP` · `feedback_source PROFILE` · `AUTO_TRANSLATE`) với doc_id mới `28781864408106143` → FB từ chối mọi bài group **gid số** (20/20 fail từ 06/09), DOM chỉ cứu ~½ | `gqlComment` gửi **đúng bộ biến capture 06/09 cho MỌI bài**: `feedLocation POST_PERMALINK_DIALOG` · `feedbackSource 2` · `input.feedback_source OBJECT` · `translationType ORIGINAL` (+ relay `CommentAutoTranslationType ORIGINAL`) · giữ `groupID` khi URL có gid số. FB trả **lỗi rõ** → thử đúng 1 lần bộ cũ (`legacy`) rồi mới rơi DOM. |
| **W-20** | API trả 200 không `errors` nhưng không có `comment_create` → coi là fail → DOM gõ lại = **có thể đăng 2 lần**, và retry 5 lượt = 5 bình luận | Kết quả **`ambiguous`** → KHÔNG gọi lại API, `verifyComment` (đã thấy chữ trên bài?) — thấy → done, không gõ; chưa thấy → DOM. Áp cả `runFunnel` lẫn `execOnPage`. |
| **W-19b** | DOM bình luận bài chỉ tìm ô có nhãn "Viết bình luận…" → nhiều bài không có ô sẵn → "không thấy ô Bình luận (đúng nhãn)" 65 lần/7 ngày | Không thấy ô → đánh dấu ô cũ, **bấm nút "Bình luận"** của bài, poll ≤6 s ô **mới xuất hiện / đang focus** (không dựa vào nhãn — bài học 05c), gõ + Enter; còn chữ → kiểm đã đăng chưa rồi mới Enter lần 2; vẫn còn → thất bại thật (không báo giả). |
| **R-9 / W-18** | Safety Score trừ 2 điểm cho MỌI lỗi bước kể cả lỗi selector của chính worker → nick t7 ok 74/fail 43 → Safety 28 → tự tắt oan | `onFail(t, msg, {kind})`: **`ui`** (không thấy nút/ô, timeout DOM…) → `uiFailCount` (KHÔNG trừ điểm) · **`fb`** (API lỗi/soft-block, verify không thấy) → `failCount` **đếm 1 lần/PHIÊN nick** · **`none`** (checkpoint/đăng xuất — đã có bộ đếm riêng). Bước thất bại trong phễu mang `failKind` theo đường lỗi (throw = ui, verify-false = fb). |
| **W-3** | 1 lần đăng xuất/checkpoint đếm N lần theo số task trong phiên → nâng maxConcurrent là nick tự tắt | `onNeedLogin` đếm **1 lần/phiên** (các task còn lại chỉ hẹn lại, không đếm). |
| **W-8** | Decay Safety neo vào `safetyAt` (ghi mỗi phiên) → nick chạy hằng ngày KHÔNG BAO GIỜ giảm lỗi | Neo vào **`safetyDecayAt`** tiến theo NGÀY TRÒN: lỗi FB −1/ngày · lỗi giao diện −2/ngày · đăng nhập lại −1/3 ngày · checkpoint −1/7 ngày. Ghi bộ đếm tuyệt đối + **`safetyWhy`** ("checkpoint N · đăng nhập lại N · lỗi FB N · thành công N · lỗi giao diện N (không trừ điểm)") để web hiện lý do. **Di trú tự động** `safetyV<2`: `failCount` cũ (toàn lỗi selector) → `uiFailCount`, `failCount=0` → nick t7 lên lại điểm cao ở phiên đầu (vẫn cần bấm ✓ Đã xử lý trên web để gỡ `safetyPaused`). |
| **R-8 / W-10** | Ghi log/KPI rồi mới `markDone` → chết giữa chừng = retry làm lại bước = double; Ctrl+C thoát ngay giữa bước | `logStep`: **`markDone` TRƯỚC** log/KPI. `SIGINT/SIGTERM` lần 1 = **tắt êm**: không nhận việc mới, bước đang làm xong thì dừng, việc còn lại hẹn 5′ "worker đang tắt", `goOffline` khi không còn nick chạy (trần `stopGraceMs` 90 s); lần 2 = thoát ngay. `gateCheck` cũng hoãn khi đang tắt. |
| **R-2 / W-1 / W-5** | `checkReplies` chỉ chạy cuối `runNick` → nick không có việc mới / brand tắt / hết van → **không bao giờ đọc inbox**; bỏ thread sau 6 lần (~6 h) | **`replyLoop()` độc lập** (chạy 2′ sau khởi động rồi mỗi `max(5, everyMin/2)` phút): mở nick CHỈ để đọc inbox khi có thread tới hạn, không mở nick đang chạy phễu, nick đang đọc không bị claim task; **backoff theo thread** `replyNextAt` = `inbox.backoffH` [1,3,6,12,24] giờ theo số lần chưa thấy phản hồi, trong cửa sổ `inbox.days` kể từ `inboxAt`. |
| **W-6** | Regex nhận diện bong bóng khớp cả nhãn trạng thái "Đã gửi/Đã xem" → có thể báo phản hồi giả | Chỉ nhận "`<Tên> đã gửi`/`sent`" có NỘI DUNG sau tin của mình; loại "Đã gửi/Sent/Đã xem/Seen/…" đứng riêng. |
| **R-1 phía worker / W-4** | Task xếp hàng nhiều giờ trước; sales đã chăm/hẹn/chốt trong lúc đó → máy vẫn chạm | **`preflight(t)`** trước khi mở lead: thread đã tắt/`human`/`expired`/`skipped_role` → task lỗi thời (huỷ êm); lead có `first_care_at`/`last_touch_at`/`stage≠new`/`closed_at`/`lost`/`dropped`/`outreach_replied` (assignee chỉ khi `brand.outreach.skipAssigned`) → **`onHuman`**: thread `step human`, task `cancelled`, hoàn van cùng ngày, log 🙋. Kiểm lại **trước MỖI bước** trong phễu (khách chốt giữa phễu → dừng ngay). |
| **E-5 phía worker** | `refundQuota` increment(−1) → van âm, hoàn cả sang ngày khác | Hoàn **cùng `reservedDay`** (thread cũ không có → coi như hôm nay; `'moved'` = hôm nay), transaction **kẹp ≥0**, không tạo doc. |
| **W-11** | `nextMorningVN` 0–8 h sáng → +1 ngày (mất trọn ngày) | 0–8 h → 8 h **hôm nay**. |
| **W-9** | `detectChallengeStrict` quét cả dialog bài permalink | Bỏ vùng dialog có `div[role=article]`. |
| **W-13** | `Object.assign` nông → config.json ghi `inbox:{enabled:true}` là mất `maxThreads/everyMin/days` | Merge sâu cho `graphql/hardCaps/inbox/safety`. |
| **dryRun** | Không có cách chạy thử an toàn sau khi đổi selector/doc_id | `"dryRun": true`: mở nick + đi hết phễu **không thao tác** (mở bài, kiểm `fb_dtsg`, mở trang cá nhân, kiểm nút Kết bạn/Nhắn tin, giải mã uid), log `[DRY] …` status `dry` lên web, thread hoãn 2 h; đọc inbox thấy phản hồi cũng chỉ log, **không ghi** lead/thread. |
| heartbeat | — | `workers/{id}` thêm `hardCaps · dryRun · stopping · replyLoop` (web F-2 đọc trần van thật). |

Không đổi: van tầng 2 `hardCaps` 80/30/20/15 · khung giờ 8–22 h VN · gate nick/brand/pauseAll · claim theo `workerId` · reaper · recycle 6 h · comment-lead (tym/reply API 06b + DOM 05c) · kết bạn/inbox DOM · `resolveProfileUid` · TTL log.

## 2. `config.json` — khoá mới (tuỳ chọn, thiếu = mặc định)

```json
{
  "dryRun": false,
  "stopGraceMs": 90000,
  "replyLoop": true,
  "inbox": { "enabled": true, "maxThreads": 5, "everyMin": 60, "days": 7, "backoffH": [1, 3, 6, 12, 24] },
  "hardCaps": { "react": 80, "comment": 30, "friend": 20, "inbox": 15 },
  "safety": { "enabled": true, "pauseBelow": 30, "slowBelow": 60 }
}
```
`graphql` giữ nguyên 4 doc_id bản 06/09 (comment/reply `28781864408106143`). Không cần đổi gì khác trong file đang chạy trên VPS.

## 3. Trường mới trên Firestore (worker ghi bằng Admin SDK — không cần Rules)
- `fb_accounts/{pid}`: `safetyV:2` · `uiFailCount` · `uiFailLegacy` · `safetyDecayAt` · `safetyWhy` · `lastFailAt/lastFailKind/lastFailMsg` · `replyCheckAt` (đã có).
- `outreach_threads/{leadId}`: `replyNextAt` · `inboxAt` · `stopReason` (khi `step:'human'`).
- `outreach_log`: status mới **`dry`** (chạy thử) — FE v119-88 hiện nhãn "Chạy thử"; bản cũ hiện mã thô.

## 4. Nghiệm thu (thứ tự khuyên dùng)
1. Chép `worker.mjs` đè MỌI VPS (giữ `config.json`). Tắt worker cũ bằng **Ctrl+C 1 lần** (giờ là tắt êm — chờ tối đa 90 s) hoặc 2 lần = thoát ngay. Chạy `run.bat`. Dòng đầu phải có `| v2026-09-11` và dòng `Trần cứng/nick/ngày: …`.
2. **Chạy thử trước**: `config.json` đặt `"dryRun": true` → khởi động lại → đăng nhập lại 1 nick trên AdsPower + bấm **✓ Đã xử lý** trên web (gỡ needLogin/safetyPaused) → bật nick + brand → trong ~5′ "Hoạt động gần đây" hiện các dòng `[DRY] … mở được bài ✓ · fb_dtsg ✓ · trang cá nhân mở ✓ · nút kết bạn ✓ · nút Nhắn tin ✓` (status Chạy thử). Không có react/comment thật.
3. Đặt lại `"dryRun": false` → khởi động lại → chạy thật 1 nick. Kỳ vọng trên web: *Thả cảm xúc ✓ → Bình luận vào bài ✓* (cửa sổ worker: `comment API: OK capture`) → Kết bạn → Inbox; nick t7 sau phiên đầu `safetyWhy` hiện "lỗi giao diện 43 (không trừ điểm)" và điểm 🛡 lên cao.
4. Sau ≥2′ khởi động, cửa sổ worker có dòng `💬 nick …: mở để đọc phản hồi (N thread tới hạn)` khi có thread đã inbox tới hạn (vòng độc lập); không thread → chỉ ghi `replyCheckAt`.
5. Van hscl-01 trên web nên về Cân bằng **40/12/10/8** (engine LỆNH #44 đã kẹp trần 80/30/20/15; FE v119-88 cũng kẹp).

## 5. Harness cũ đổi kỳ vọng (cố ý)
- `t_refund` A: hoàn van kẹp ≥0 (seed friend 1 → 0) thay vì tạo doc −1 (E-5).
- `t_cmtapi` F: comment bài `feedLocation POST_PERMALINK_DIALOG` thay `GROUP` (R-15).
