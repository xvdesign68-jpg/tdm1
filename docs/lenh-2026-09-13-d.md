# LỆNH D (13/09/2026) — Đợt 2.1: TTL `scans`/`seen` + lưới bài quá cũ + hoãn AI không đẻ doc + `prefiltered_out` 14 ngày (PB-9) · bình luận ↔ bài cha FAIL-CLOSED (PB-12)

> Sau LỆNH C (mốc trên mã có marker `LENH B` + `LENH C`). Chưa chạy C → script tự DỪNG, không ghi gì.
> File: `lenh-2026-09-13-d.sh` (KHỐI 1, nhúng `_ld_patch.cjs` · `_ld_backfill.mjs` · `_ld_after.mjs`) + `-d-patch.cjs` (18 mốc, 3 file, marker `LENH D`) + `-d-backfill.mjs` + `-d-after.mjs` + harness `harness-lenhd-2026-09-13.mjs` **33/33**.
> **sha256 `lenh-2026-09-13-d.sh` = `649534fa8a703bf8840e2a17dd2ec82cc236742b158e7f4b17f9a3af3875e9c9`** (29.471 byte / 210 dòng). Dry-run fake HOME + fake firebase/gcloud ×2 exit 0 (lần 2 idempotent), không `!` ngoài heredoc, không secret.

## 1. Vì sao
- **PB-9** (N1-8/N3-4/Q-2/N2-10/N4-9): `scans` ~200–480 doc/ngày không TTL (~12–14 k/tháng, mỗi doc `bySource` 23 dòng); `seen` 54.663 doc vĩnh viễn (~1.300/ngày); mỗi lần hoãn AI (score_retry) lại ghi thêm 1 doc `scanned_posts` `ai_wait` → 1 bài lỗi tới 7 dòng, chip "Chờ AI chấm lại" thổi số; `prefiltered_out` (52 % bài) xoá sau 3 ngày → không đối soát được tầng 1 (PC-7). Critic: bỏ id `SP_` + merge (giữ `watchScanned` đếm doc mới), chỉ guard `deferredDoc` + TTL; FE limit 300 + nhãn phạm vi đã làm ở zip v119-90.
- **PB-12** (N1-4/N3-7): `fetchComments` và `harvestComments` (đường gặt theo lịch, LỆNH #23) khi record BrightData có `post_url` lệch dạng → rơi về **bài ĐẦU lô** (`batch[0]` / `first`) → bình luận dán nhầm bài: AI đọc `BÀI GỐC` sai, `self_comment`/`parent_kind` (#31/#34) sai, brand Bài đã quét sai (LỆNH B đã sửa brand theo ctx bài cha, còn khớp bài cha thì chưa). Comment-lead là lead giá trị nhất → sửa **fail-closed có bậc**: khớp URL chuẩn hoá → id số bài → lô chỉ có ĐÚNG 1 bài cha → còn lại orphan (bỏ + đếm), không đoán.

## 2. Thay đổi (patch `_ld_patch.cjs`, fail-closed NGUYÊN TỬ 3 file, idempotent theo marker)
- **lib/config.js** (+4 khoá, `.env` ghi đè): `SCANS_TTL_DAYS` 90 · `SEEN_TTL_DAYS` 180 · `TOO_OLD_DAYS` 45 · `PREFILTERED_TTL_DAYS` 14.
- **lib/scraper.js**: helper `pidNumD(url)` (id số ≥6 chữ số từ `/posts|permalink|videos|photos/<id>`, `story_fbid=`, `fbid=`, `post_id=`, hoặc số trần) · **`matchParentD(c, byKey, byPid, metas, uk)`** (export) · `byPidMapD(metas)`. `harvestComments`: `res.orphanCmt/matchedById`, vòng khớp dùng `matchParentD`, không khớp → `orphanCmt++` + bỏ; `parentUrl` = URL bài cha đã gieo (không phải `c.post_url` thô). `fetchComments` (quét tay/backfill): lô = metas, cùng logic, `out.orphanCmt/matchedById`. **Sổ tiền BrightData giữ nguyên** (record lỗi/lạc vẫn cộng bài đầu lô như cũ).
- **index.js**: bộ đếm `cmtOrphanB/cmtByIdB/tooOldB` → `scans.cmtOrphan/cmtById/tooOld` · `summary.expireAt` = now + 90 ngày (cả doc `status:'aborted'` của `abortLog48`) · `seenDocB` (create) + 2 chỗ merge `{brands}` thêm `expireAt` = now + 180 ngày · `tooOldD(post)` (`tsMsB(post.time)`; time không đọc được → không chặn) — **chỉ lượt theo lịch (`sowMode`)**, đặt TRƯỚC `self_comment`/exclude/AI: `recordPost(decision:'too_old')` + `seen.brands.<b>='old'` (SEEN_DEC_B) — quét tay/backfill KHÔNG qua lưới (cố ý lấy bài cũ) · hoãn AI: `if (r46 === 'error' || !x.deferredDoc) recordPost(ai_wait)` → lần hoãn 2+ không đẻ doc · `recordPost.expireAt`: `prefiltered_out` = +14 ngày, còn lại `TTL_MS` (3 ngày) như cũ.
- **KHÔNG đổi**: `recordPost.brand` (đã theo ctx bài cha từ LỆNH B — `x.brandB`), Rules, index, worker, FE (zip kế: DECIS `too_old` "Bài quá cũ · bỏ"; hiện chip rơi về "Đã chấm · chưa đạt" — chỉ nhãn).

## 3. KHỐI 1 (dán vào Cloud Shell) — khối trong chat = `cat > /tmp/ld.sh <<'EOF_LD'` … `EOF_LD` rồi `bash /tmp/ld.sh` (nội dung = file `lenh-2026-09-13-d.sh`, sha256 ở đầu)
Thứ tự: `--check` 3 script → guard `LENH C` có / `LENH D` chưa (chạy lại → giữ `.bak` gốc qua `_ld_backup_ts`) → backup `.bak-$TS` 3 file → patch → `--check` → import test `.env` (in TTL 90/180 · too_old 45 · prefiltered 14 + tự kiểm `matchParentD` 2 ca) → deploy `scheduledScan + manualScan` (xích `&&`; lỗi → describe + cách quay lui) → TTL policy `gcloud firestore fields ttls update expireAt --collection-group=scans|seen --enable-ttl` (lỗi = cảnh báo) → `node _ld_backfill.mjs` (paged 400, chỉ doc thiếu expireAt; seen ~55 k doc ≈ vài phút; lỗi = cảnh báo, chạy lại được).

## 4. KHỐI 2 (chỉ đọc, sau ≥ 15′ — PB-12 (0) cần vài lượt có bình luận, tốt nhất 1–2 giờ ban ngày)
```bash
cd ~/firebase-s13/functions && node _ld_after.mjs
```
Kỳ vọng: (1) TTL `scans.expireAt` + `seen.expireAt` ACTIVE (CREATING vài phút đầu) · (2) 10 lượt gần nhất là bản D (`cmtOrphan` có số, `expireAt +90d`), 24 h: orphan/(gặt+orphan) **< 10 %** (>10 % → gửi em mẫu `post_url` để nới `urlKey`), `cmtById` > 0 nếu BrightData trả URL dạng khác · (3) scans/seen thiếu expireAt = 0 sau backfill · (4) `ai_wait` trùng (cùng bài+brand) 0, `prefiltered_out` expireAt ≈ +14d cho doc sau D · (5) bình luận 7 ngày gid(bài cha) ≠ gid(nguồn) ≈ 0 % cho doc sau D.

## 5. Kiểm (harness `harness-lenhd-2026-09-13.mjs` 33/33)
Dựng mã đang chạy = fixture B + patch B + push.js #46 + **patch C** → patch D: PATCH OK 18 mốc / idempotent / --check 3 file + 2 script / fail-closed nguyên tử (1 mốc scraper lệch → 0 file ghi) / LỆCH (config đã D) / chưa C → DỪNG · `matchParentD` 7 ca thuần (URL chuẩn hoá, id số qua m.facebook+permalink+query, post_id số trần, story_fbid, 2 bài không khớp → null, 1 bài duy nhất → nhận, id <6 chữ số không nhận) · scanAll trọn vòng trên Firestore + BrightData + LLM giả: `scans.expireAt` +90d · `tooOld` 2 (1 bài × 2 brand dùng chung) · seen MERGE (doc cũ không expireAt + thêm brand) → +180d giữ brand cũ · seen CREATE bài too_old `brands.b1='old'` +180d · too_old không gọi AI/không lead, bài mới vẫn lead · `prefiltered_out` +14d · backfill bài 60 ngày vẫn lead · hoãn AI: lần 1 → 1 doc ai_wait + score_retry tries 1, lần 2 → vẫn 1 doc, tries 2, AI hồi → lead + score_retry gỡ · gặt bình luận: khớp URL + khớp id số (m.facebook/permalink) → 2 lead đúng bài cha (parent_url/parent_text), bình luận lạc → orphan 1/byId 1, KHÔNG dán bài đầu · quét tay: khớp URL + `post_id` số (không post_url) → 2 lead, lạc → orphan 1 · 1 bài cha duy nhất → bình luận URL lạ vẫn nhận, orphan 0.

## 6. Rủi ro / ghi chú
- Fail-closed có thể bỏ lỡ bình luận ở record có URL dạng chưa biết KHI lô ≥2 bài — KHỐI 2 mục (2) đo ngay; >10 % thì nới `pidNumD/urlKey` (LỆNH nhỏ), không quay lại đoán bài đầu.
- `seen` TTL 180 ngày: bài >45 ngày tái xuất bị lưới `too_old` chặn trước AI (không tốn tiền, không lead trùng); lead id tất định `L_<post_id>__<brand>` + `create()` vẫn là lưới cuối.
- Backfill seen ≈ 137 trang × 400 (đọc `select` + batch update) — chạy trong Cloud Shell vài phút; ngắt giữa chừng chạy lại được (chỉ doc thiếu expireAt).
- TTL policy cần quyền `datastore.owner`/`firestore.fields.update` — LỆNH B đã bật TTL `outreach_locks` bằng cùng lệnh nên tài khoản anh có quyền.

## 7. KẾT QUẢ (điền sau khi anh chạy)
- KHỐI 1: …
- KHỐI 2: …
