# LỆNH B (12/09/2026) — HƯỚNG B "1 GROUP NHIỀU BRAND" + TỐC ĐỘ + PC-4 — CHỜ ANH CHẠY

> Bản lưu; **khối lệnh thật dán trong chat** (anh copy lên Cloud Shell). File: `lenh-2026-09-12-b.sh` (KHỐI 1 = nhúng `_lb_patch.cjs` + `_lb_sources.js` + `_lb_rules.cjs` + `_lb_tag_patch.cjs` + `_lb_after.mjs`, heredoc quoted, 0 dấu `!` ngoài heredoc) · `lenh-2026-09-12-b-patch.cjs` (patch 7 file, **73 mốc**, marker `LENH B`) · `lenh-2026-09-12-b-sources.js` (CF mới `createSource` · `sourceOnWrite` · `bdReady`) · `lenh-2026-09-12-b-rules.cjs` (block `lead_links`) · `lenh-2026-09-12-b-tag-patch.cjs` (codebase2 `tagLeadBrand`) · `lenh-2026-09-12-b-after.mjs` (KHỐI 2 chỉ đọc) · `lenh-2026-09-12-b-fixture/` (mã ĐANG CHẠY sau #48: `index.js · lib/scraper.js · lib/config.js · lib/scorer.js` = fixture #48 + patch #48; `outreach.js` = fixture #46 + patch #46; `stats.js · scanstats.js · lib/multitouch.js · codebase2/tagLeadBrand.js` = dump #47/#47b/#47e; `stubB.mjs` Firestore giả deep-merge) · `harness-lenhb-2026-09-12.mjs` (**98/98 PASS**).
> Theo thiết kế `docs/rasoat-scan-2026-09-11/thiet-ke-group-nhieu-brand.md` §8.1–8.4 + §9.1–9.21 (anh chốt 12/09) + KHỐI 0 (`docs/lenh-2026-09-12-b0.md`) + LỆNH #47/#48.

## 0. 3 số BrightData dashboard — CHƯA CÓ → mặc định an toàn (đổi trong `.env`, KHÔNG cần chạy lại LỆNH)
| Số anh chưa cho | Mặc định LỆNH B dùng | Khi có số thì đặt |
|---|---|---|
| Trần snapshot đồng thời / request-phút | sàn nhịp gieo **5′** (`SCAN_INTERVAL_MIN_FLOOR=5`), gộp ≤ **40** group/1 trigger (`BD_MAX_TRIGGERS_PER_RUN=40`), hỏi progress chỉ với snapshot ≥ **120 s** (`BD_PROGRESS_MIN_AGE_S=120`), 429/5xx = "busy" → hoãn (không tính là ngưng) | có trần rộng → `SCAN_INTERVAL_MIN_FLOOR=3` (group sôi động gieo 3′); trần hẹp → giảm `BD_MAX_TRIGGERS_PER_RUN` |
| Đơn giá posts/comments + `dead_page` có tính tiền? | record lỗi **coi là CÓ tính tiền** (`BD_ERROR_RECORD_BILLED=true` → `bdRecords`/`bd_month` đếm theo raw) | không tính tiền → `BD_ERROR_RECORD_BILLED=false`; đơn giá vào ô benchmark ROI trên web như trước |
| Gói có `notify` webhook? | **không dùng** (`BD_NOTIFY_URL` trống) → gặt ở lượt kế sau ≥ 120 s | có → `.env`: `BD_NOTIFY_KEY=<chuỗi ngẫu nhiên>` + `BD_NOTIFY_URL=https://asia-southeast1-smartlead-z15.cloudfunctions.net/bdReady?key=<chuỗi đó>` (KHÔNG dán key vào chat) → snapshot chín được đánh dấu `ready` → gặt ngay lượt kế (−1–2′) |
Muốn tick nhanh hơn (đã có khoá lượt): `.env` `POLL_MINUTES=2` rồi `firebase deploy --only functions:scheduledScan` (Scheduler đọc từ code). Không bắt buộc.

## 1. Sửa gì (7 file + 1 file mới + Rules + codebase2; marker `LENH B`; fail-closed NGUYÊN TỬ: đủ 73 mốc cả 7 file mới ghi)
| Mã | File | Thay đổi |
|---|---|---|
| B-1/B-4 | `index.js` Pha 1 mới `pha1B` | **Nhóm nguồn theo group gid số** (`buildGroupsB`: gid từ URL/field `gid` → `g_<num>`; slug → `g_<num>` nếu đã học `group_state/s_<slug>.gidNum`, không thì `s_<slug>`; nguồn chủ = `sharedAt` nhỏ nhất/không có). `group_state/{gkey}` (migration đọc doc cũ theo URL). **Gặt** `harvestPostsB` (claim transaction, route record theo `group_id` → gkey, học gid slug→số → ghi `sources.gid` + `group_state.gidNum`) → **fan-out**: mỗi record × mỗi source doc trong group = 1 ứng viên riêng (`brandB`, `post.__brand`, `gid`, `shared`), `seen` đọc 1 lần/bài (cache). **Gieo gộp** 1 trigger/lượt cho group tới nhịp (`sowPostsB`, chunk ≤ 40, `pending_snapshots/PB_<snapshot>` kèm `groups[]`). |
| T-8 | `index.js` `ivB` | **Nhịp thích ứng** EWMA bài mới/giờ (τ 60′, chỉ cập nhật ở lượt gặt probe, không sweep/deep): ≥4/giờ → sàn 5′ · ≥0,4/giờ → 10′ · ít hơn → trần 30′; hysteresis ±20 %; group mới (<2 mẫu) = sàn; vừa có bài mới (<30′) = sàn; im ≥6 h ban ngày = trần; đêm 23–6 h ×2 (trần); super ghi đè từng nguồn `sources.scanIntervalMin`; sàn/trần = `config/app.scanIntervalMin`/`scanIntervalMaxMin` > `.env`. `group_state.iv/band` để FE hiện. |
| T-4/S8 | `index.js` sow | **Sweep** = 20 bài + `start_date = lastSweepAt − 6 h` + `end_date` + `notInclude = recentIds`; **escalate** (probe đầy bài mới) = gieo đủ 20 NGAY lượt này (`notInclude = vừa gặt ∪ recentIds`); probe 10 cho group ≥4 bài/giờ; `lastTriggerAt/lastSweepAt` ghi SAU gieo OK. |
| T-3 | `index.js` bdwatch + sow | Đang ngưng → cần **2 lượt OK liên tiếp** mới `[BRIGHTDATA-UP]` (`okStreak`); ngưng ≥ `BD_DEEP_SWEEP_AFTER_MIN` (30′) → `system_status/brightdata.deepDue` → lượt sau **gieo sâu** mọi group (`start_date = lúc ngưng − 1 h`, `numPosts = rate×giờ + 10 ≤ 200`) 1 lần (`deepDue.doneAt`, log `[BRIGHTDATA-DEEP]`). 429/5xx khi hỏi progress = busy → hoãn, **không** tính là ngưng. |
| T-1 | `index.js` `scanAll` bọc | **Khoá lượt** `system_status/scan_lock` (transaction, TTL `SCAN_LOCK_TTL_S` 1200 s) cho MỌI trigger: lịch gặp khoá → bỏ lượt (`busy:true`, log `[SCAN-BUSY]`, không ghi scans); Quét ngay/backfill → `manualScan` trả **409 `{error:'busy'}`**. `runId48` = holder. Nhả ở `finally` (kể cả lượt ném lỗi). Lỗi transaction → fail-open. |
| T-6 | `index.js` | **Lượt thuần skip** (không gieo/gặt/ứng viên, không bài chờ AI tới hạn, không busy, chưa tới nhịp việc phụ) → KHÔNG ghi `scans`, chỉ `system_status/scan.skipRuns++`. **Việc phụ theo nhịp** `HOUSEKEEPING_MIN` 5′ (`lastHousekeepingAt`): hồ sơ BD collect, quét-vét eKYC, sweeper chấm lại điểm tạm. Quét ngay/backfill luôn chạy đủ. |
| B-4 | `index.js` Pha 2 | `seen/{post_id}` = `{at, run, brands:{<brand>:'pending'|'lead'|'low'|'pre'|'ex'|'self'|'seller'}}` — 1 doc/bài `create()` (gộp brand), brand thêm sau → set-merge `brands.<X>='pending'`; **doc cũ không `brands`**: brand X = ĐÃ xử lý nếu nguồn X không `sharedAt` hoặc `sharedAt < seen.at` (brand vào sau mốc mới được chấm). ALREADY_EXISTS (2 lượt chồng) → đọc lại, bỏ bài/brand lượt kia đã nhận. Quyết định ghi lại theo brand qua `recordPost → seenDecB → flushSeenB` (batch). |
| B-2 | `index.js` lease/retry | `score_retry` id **`R_<post_id>__<brand>`** (`retryRef48`/`deferPost46` theo `post.__brand`), doc `src` thêm `source_id/gid/fanout`; nạp lại tìm nguồn theo `source_id` → (url + brand) → url; `row` theo `source_id`. |
| §9.2/§9.12 | `lib/scraper.js` | `normalizePost.post_id = postIdB(p)` = `post_id ‖ id ‖ urlKey(url) ‖ h_<hash tác giả+nội dung>` — **bỏ `Math.random()`**. |
| B-4/§9.2 | `index.js` `commitLeadNow` | Lead fan-out (`brand_hint` + `post_id`) → id **`L_<post_id>__<brand>` + `create()`** (ALREADY_EXISTS → `dupLead`, không đè); lead mang `post_id/gid/source_id/brand_hint/shared_post/num_comments/textKey`; **GIỮ 2 bước ghi brand**: create không `brand` → birthstamp `update({brand: brand_hint …, brand_tagged_by:'scanner-inline'})` (ZBS `justTagged`); bỏ v-fixgrp cho lead fan-out; `lead_links/{gid}_{post_id}.brands{<brand>:leadId}` cho bài dùng chung (Rules read super, lead KHÔNG mang mã brand khác). Đường cũ (không brand_hint) giữ nguyên. |
| B-8/PC-4 | `lib/multitouch.js` | `tryMergeTouch(db, lead, {brand})`: **chỉ gộp trong cùng brand** (`brand_hint`/`brand`), limit 12; **PC-4** `textKeyB` (fold 160 ký tự đầu, bỏ link/dấu, <24 ký tự → không) → `tryMergeTextB`: cùng brand ≤14 ngày cùng `textKey` lead mở → gộp thành touch (`merged_by:'textKey'`), query `where textKey ==` single-field (không cần index ghép). *Lệch thiết kế §9.8 (simhash 64 bit) → hash prefix 160 ký tự: đủ cho "rải cùng 1 bài đi nhiều nhóm", rẻ, không cần thư viện; bài sửa vài chữ đầu không gộp — chấp nhận.* |
| §9.4/9.20/S6 | `index.js` Pha 1b | `ctxAllB`: **mọi brand dùng chung group nhận bình luận** (ứng viên riêng, gid, shared, `__brand`); gieo snapshot bình luận **CHỈ khi `num_comments > 0`** (bài watch cũng vậy) → `noCmt`; `cmt_scrape.lastAt` ghi **SAU** gieo OK (`fetchComments` trả `sownUrls`; gieo lỗi → lượt sau gieo lại). |
| §9.20 | `index.js` watch_posts | Bài ra lead nhưng 0 bình luận lúc gặt → `group_state.watch{post_id:{url,at,checks}}` (≤5/nguồn, 2 ngày, ≤4 lần): lượt **sweep** loại các bài này khỏi `notInclude` → BrightData trả lại record với `num_comments` mới → gieo bình luận; `checks+1` mỗi lần. |
| B-3 | `index.js` bySource/bd_month/scans · `scanstats.js` | `bySource` dòng có `brand/source_id/gid/bdShared` (tiền BrightData ở nguồn chủ); `bd_month.src` key `s_<url>` (chủ) / `s_<url>__<brand>` (dùng chung) + field brand/source_id/shared; `scanstats.scanEvents` lấy `r.brand` trước `normUrl` → `daily_stats.scanned` đúng brand; `scans` thêm `groups sharedGroups sown harvested escalated bdBusy deepSweep sweepRecords noCmt dupLead hvOrphan gidLearned housekeeping`. |
| §8.2 | `outreach.js` | **`acquireLocksB`** (transaction `outreach_locks/{post_<post_id>|cmt_<comment_id>|person_<uid>|person_u_<username>|person_<pfbid>}` = `{brand, leadId, pid, state:'reserved' 6 h | 'touched' 14 ngày, expireAt}`) gọi ở **CẢ `apEnqueueFunnel` lẫn `stepNick` TRƯỚC `tryConsume`/`genForLead`**; thua → `skipLeadSharedB`: thread `step:'skipped_shared'`, `active:true`, `nextAt = retryAt = hết hạn khoá` (touched → `active:false`), log ⏭ Nhường; **xét lại**: `stepNickAdspower` nhánh retry (lead vẫn mở → `apEnqueueFunnel` lại), `drive()` func-path (lấy khoá → `step:'react'`); nhả: `!steps.length`, sweep44 đóng phễu quá 72 h, `stats.stopMachine` (người thật). Lỗi transaction → fail-open. Cùng brand → dùng lại khoá. |
| B-5 | `sources.js` (MỚI) | **`createSource`** (onRequest, super hoặc admin đúng brand): URL chuẩn hoá, gkey (gid số/slug đã học), 409 khi cùng brand + cùng group / cùng brand + trùng tên bỏ dấu; id **`<gid|slug>__<brand>`**; group đã có brand khác → `sharedAt/sharedBy/sharedWith`. **`sourceOnWrite`** (onDocumentWritten `sources/{id}`, so before/after gid/brand/active/url/sharedAt/name): group ≥2 brand → `system_status/sources.shared[gkey]` `{brands, gid, name nguồn chủ, since, by, sources[], ackAt}` + **push FCM super** + WARNING `[SOURCE-SHARED]` (chỉ khi thêm brand mới); còn ≤1 brand → gỡ. **`bdReady`** webhook notify (`?key=BD_NOTIFY_KEY`) → `pending_snapshots PB_/C_ ready:true`. |
| §9.21 | codebase2 `tagLeadBrand` | dòng 75 `const brand = hint || await getBrandForSource(sourceName)` (`hint = lead.brand_hint`), `brand_tagged_by: hint ? 'brand_hint' : 'tagLeadBrand'` → 2 source doc trùng TÊN khác brand không còn gán sai. Deploy `--only functions:tagLeadBrand` từ `~/codebase2` (lỗi = cảnh báo, không chặn KHỐI 1). |
| — | Rules | `match /lead_links/{lid}` read super / write false (bản sao block `workers`). `outreach_locks`, `group_state` gkey, `pending_snapshots PB_` = Admin SDK (không cần block). |
| — | `lib/config.js` | 10 khoá tuỳ chọn (mục 0 + `SCAN_INTERVAL_MAX_MIN 30`, `SCAN_LOCK_TTL_S 1200`, `BD_DEEP_SWEEP_AFTER_MIN 30`, `HOUSEKEEPING_MIN 5`, `BD_NOTIFY_KEY`). |

**Đường cũ giữ nguyên cho Quét ngay / backfill / nguồn nick / URL không phải group** (`legacyPha1B`), nhưng **1 lần/group** (chỉ nguồn chủ gọi BrightData) rồi `fanoutLegacyB` cho brand còn lại → lead id tất định cả ở Quét ngay. **Không đổi worker VPS** (thiết kế nói worker kiểm `outreach_map`/`person_<uid>` và chuyển `touched` — CHƯA làm: khoá `reserved` 6 h + engine first-come đủ chặn 2 brand cùng xếp phễu 1 bài/người; worker 2026-09-12 sẽ thêm `touched` khi có bước thành công). **Không có FE trong LỆNH này** (zip FE sau: chip "Dùng chung", form tạo nguồn qua `createSource`, toast 409 busy, `sourceId` cho "Quét thử nguồn này").

## 2. Kiểm trước khi gửi
- **Fixture = mã đang chạy**: harness tự dẫn xuất (fixture #48 + patch #48 → byte-identical với fixture B 4 file; fixture #46 + patch #46 → outreach.js; stats/scanstats/multitouch == VĂN BẢN DUMP #47/#47b) rồi đối chiếu **73 mốc mỗi mốc đúng 1 lần**; tagLeadBrand 2 mốc (dump #47e).
- Harness `harness-lenhb-2026-09-12.mjs` **98/98**: patch áp/idempotent/`node --check` 8 file/fail-closed nguyên tử (thiếu 1 mốc scanstats → exit 1, không ghi file nào)/LỆCH · tag patch + rules script idempotent · **scraper 10 ca** (postIdB tất định, gid, sowPostsB 2 group/1 trigger + chunk 40, harvestPostsB chờ ≥120 s / claim / route group_id / học gid / dead_page tính tiền / 429 busy / failed) · **multitouch 5 ca** (brand khác không gộp, cùng brand gộp, PC-4 gộp/không gộp chéo brand, văn bản ngắn) · **index.js chạy TRỌN `scanAll` 13 kịch bản** (C1 lượt đầu chỉ gieo 2 group 1 trigger · C2 gặt → 3 bài × 2 brand + 1 × 1 = 7 lead id tất định, brand đúng nguồn, 2 bước brand, AI 7+7, seen.brands, lead_links, group_state/học gid/watch, bySource bdShared, bd_month key __brand · C3 thuần skip · C4 khoá lượt: 2 lượt chồng → 1 busy, Quét ngay 409, TTL · C5 seen cũ + sharedAt · C6 dupLead · C7 nhịp: probe 10, escalate 20, EWMA, idle 30′, sweep start_date · C8 DOWN → 2 OK → UP + deepDue → deep sweep · C9 score_retry theo brand → nạp lại · C10 bình luận: num_comments gate, lastAt sau gieo, fan-out 2 brand, gieo lỗi không ghi lastAt · C11 Quét ngay 1 lần/group + fan-out · C12 watch loại khỏi notInclude · C13 busy) · **outreach 11 ca** (lockKeysB 4 dạng, first-come, thua khoá không đốt van/AI, người khác cùng bài OK, cùng người group khác bị khoá person, retry khi hết hạn, touched nhường vĩnh viễn, stopMachine nhả, sweep44 nhả, func stepNick thua/lấy lại) · **scanstats/sources/tag 15 ca** (createSource 6 đường, sourceOnWrite 3, bdReady 2, notify gặt ngay, tagLeadBrand 2).
- Dry-run `lenh-2026-09-12-b.sh` với fake HOME (fixture UNPATCHED + node_modules giả) + fake `firebase`/`gcloud`: lần 1 PATCH OK 7 file → sources.js → Rules chèn → IMPORT OK 13 hàm + CFG B → deploy giả 8 function + rules → tagLeadBrand patch + deploy giả → exit 0; lần 2 idempotent (7 file/Rules/tag đều "ĐÃ CÓ") exit 0. `_lb_after.mjs` chạy trên shim firebase-admin: in đủ 8 mục, exit 0. `bash -n` OK; 0 dấu `!` ngoài heredoc; quét secret sạch.
- **Rà đối kháng (workflow 3 lens → refuter)**: xem mục 6.

## 3. KHỐI 1 (dán vào Cloud Shell) — xem file `lenh-2026-09-12-b.sh` (khối trong chat = `cat > /tmp/lb.sh <<'EOF_LB'` … `EOF_LB` rồi `bash /tmp/lb.sh`)
Trình tự: backup 7 file + `firestore.rules` `.bak-<TS>` → `_lb_patch.cjs` (fail-closed) → `sources.js` → `node --check` ×8 → Rules `lead_links` → import test `.env` (13 hàm + CFG B + `postIdB/gidNumOfB/lockKeysB`) → deploy **`firestore:rules` + 8 function** `scheduledScan manualScan outreachTick statsOnLead scanStatsOnRun createSource sourceOnWrite bdReady` (xích `&&`) → describe → codebase2 `tagLeadBrand` patch + deploy (lỗi = cảnh báo). Lỗi ở (a)/(b)/(c) tự khôi phục từ `.bak`. KHÔNG đụng `.env`.

## 4. KHỐI 2 (sau ≥ 15′, tốt nhất 1–2 giờ ban ngày; chỉ đọc)
```bash
cd ~/firebase-s13/functions && node _lb_after.mjs
```
Kỳ vọng: `system_status/scan` có `skipRuns` tăng (≈ 30–60 % lượt), `scan_lock` holder trống · `brightdata ok=true` · `group_state` 20+ doc `g_<gid>` với `rate/band/iv` (nguồn slug học gid → `s_<slug>.gidNum`) · `pending_snapshots PB_` 0–3 (tuổi < 5′) · 12 lượt: `sown` vài group/lượt, `harvested` 1 ở lượt gặt, `busy 0`, `dup 0`, `hvOrphan 0`, `hk ✓` mỗi ~5′ · seen mới 100 % có `brands` · lead 24 h: id `L_<post_id>__<brand>`, `brand == brand_hint`, `brand_tagged_by scanner-inline` (hoặc `brand_hint` từ tagLeadBrand) · **LAG trung vị 5–7′ / p95 ≤ 20′** (mốc gốc 11′/36′) · `lead_links` = 0 tới khi có group 2 brand · log `[B]` mỗi lượt gieo/gặt, `[SCAN-BUSY]` hiếm, `[SCAN-ABORTED]` 0.
**Nghiệm thu "1 group nhiều brand"** (khi anh muốn): thêm nguồn thứ 2 cùng group cho brand khác qua CF (FE chưa có nút → tạm dùng curl với idToken super, hoặc chờ zip FE): sau 2–3 lượt → `system_status/sources.shared` có group, push super "Group dùng chung", lead mới của group đó có 2 doc `L_<post_id>__<brandA>` / `__<brandB>` + `lead_links`; automation: brand tới trước giữ khoá, brand kia thread `skipped_shared` xét lại sau 6 h.

## 5. Rủi ro & quay lui
- Quay lui code: `cd ~/firebase-s13/functions; for f in lib/config.js lib/scraper.js index.js outreach.js stats.js scanstats.js lib/multitouch.js; do cp "$f.bak-<TS>" "$f"; done; rm -f sources.js; cp ~/firebase-s13/firestore.rules.bak-<TS> ~/firebase-s13/firestore.rules` rồi deploy lại 8 function + rules (xoá `createSource/sourceOnWrite/bdReady` bằng `firebase functions:delete` nếu muốn sạch). Dữ liệu mới (`group_state/g_*`, `seen.brands`, `lead_links`, `outreach_locks`, `pending_snapshots/PB_*`) không phá mã cũ: mã cũ đọc `group_state` theo URL (doc cũ còn), `seen` chỉ xem `exists`, pending `PB_` bị bỏ qua (mã cũ chỉ đọc `P_<url>`/`C_`).
- Lượt đầu sau deploy: mọi group chưa có `group_state/g_*` → lấy từ doc URL cũ (lastTriggerAt/lastSweepAt/recentIds) → không gieo dồn; group_state mới ghi `iv/band` từ lượt đầu (rate 0, mẫu 0 → sàn 5′ tới khi có ≥2 mẫu).
- Lead id đổi dạng (`L_<post_id>__<brand>`) — FE không phụ thuộc dạng id; `notes` subcollection theo id lead như cũ.
- `dupLead` > 0 ở lượt đầu là bình thường nếu 2 lượt chồng trước khi khoá có hiệu lực; sau đó phải = 0.
