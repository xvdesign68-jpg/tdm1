# LỆNH C (13/09/2026) — PB-10 push · PB-6 bộ đếm trừ khi máy loại/đổi nhiệt + recount · Rules whitelist · ZBS guard · source_health `[SCAN-NO-LEAD]` · backfill_done theo brand · dọn group_state cũ

> Sau LỆNH B (mã đang chạy = fixture B + patch B; push.js = fixture #46 + patch #46). Anh chốt 12/09 câu 1: **máy loại/hạ điểm → TRỪ bộ đếm; sales bấm Loại giữ như cũ**. 13/09 anh chốt: toàn bộ lộ trình là task chính → làm hết.
> File: `lenh-2026-09-13-c.sh` (KHỐI 1, nhúng `_lc_patch.cjs` · `_lc_rules.cjs` · `_lc_zbs.cjs` · `_lc_recount.mjs` · `_lc_clean.mjs` · `_lc_after.mjs`) · `-c-patch.cjs` (3 file, marker `LENH C`, fail-closed NGUYÊN TỬ, idempotent) · `-c-rules.cjs` · `-c-zbs.cjs` · `-c-recount.mjs` · `-c-clean.mjs` · `-c-after.mjs` (KHỐI 2 chỉ đọc) · harness `harness-lenhc-2026-09-13.mjs` **42/42**.
> **sha256 `lenh-2026-09-13-c.sh` = `6061ed4ae537b75dd8f6ea65de0a1c7c246fa13b049afc44f9705d1ce8a05fa1`** (46.937 byte / 341 dòng; bản 2 13/09 sáng: Rules thêm 2 block đọc `source_health`/`group_state` cho web v119-90). Dry-run fake HOME + fake firebase/gcloud ×2 exit 0 (lần 2 idempotent), after-script chạy trên shim OK, không `!` ngoài heredoc, không secret.

## 1. Sửa gì
- **stats.js (PB-6)**: `MACHINE_BY` = token máy CHÍNH XÁC `rescore|rescore_role|lenh31b|ai|scanner|engine|worker|machine|system|sweep|auto` (tuỳ chọn `:hậu-tố`; email/uid người không bao giờ khớp) + `machineDroppedC(l)` (export). `countable` thêm `!machineDroppedC` → máy loại lead → trừ `new/<temp>` đúng ngày phát hiện (nhánh có sẵn từ #42). Nhánh mới: cả 2 bản đếm được mà `tempOf` đổi (AI chấm lại hạ/nâng nhiệt) → chuyển bucket cùng ngày (hot−1 cold+1 …; xuống/lên `junk` chỉnh cả `new`). `dropped` của máy → **`aiDropped`** (field mới), người → `dropped` như cũ. Sales khôi phục lead máy đã loại → đếm lại.
- **push.js (PB-10)**: `hotGateC(before, after)` thuần (export): push "Lead nóng mới" ĐÚNG 1 LẦN khi `brand || brand_hint` **xuất hiện** (`!before` → `new`; before không có → `tagged`), hoặc `ai_scored false → true` (`rescored`, giữ #46); bỏ `dropped/lost/ai_scored:false/role seller|poster_self/self_comment/không nóng/chỉ đổi điểm`. Người nhận theo `brand || brand_hint` (đường B: admin brand nhận ngay ở bước ghi 1; bước 2 gán `brand` không push lại). Log `push hot <id> <why> sent <n>`.
- **index.js**: (a) `bfKey(url, brand)` — `backfill_done` ghi khoá CÓ brand (+ field `brand`), đọc khoá mới rồi khoá cũ (không gieo lại BrightData cho backfill đã làm) → group dùng chung: brand thứ 2 backfill được. (b) cuối lượt: **`source_health/{source_id}`** set-merge `days.<ngàyVN>.{posts,leads,hot,err}` (increment), `lastPostAt/lastLeadAt`, `errStreak` (trigger lỗi +1; OK → 0), `lastError`, name/brand/gid/url — chỉ ghi dòng có bài/lead/lỗi hoặc vừa hồi sau lỗi (`system_status/scan.healthErrIds` lượt trước). (c) nhịp việc phụ (5′): **`healthEvalC`** (thuần, export) → dọn `days` >7 ngày, nguồn ĐANG BẬT có vấn đề = `noLead` (≥15 bài/7 ngày, 0 lead) · `silent` (có bài trước đây, >48 h không bài mới) · `err` (`errStreak ≥ 6`) → WARNING `[SCAN-NO-LEAD] <nguồn> [brand]: lý do` ≤1 lần/6 h/nguồn (`alertedAt`, reset khi khoẻ), ERROR `[SCAN-HEALTH]` (alert #9) khi ≥3 nguồn và ≥½ nguồn bật; `system_status/scan.health {at, n, active, bad[≤30]}` (FE zip sau đọc).
- **Rules** (`_lc_rules.cjs`, cwd `~/firebase-s13`): (1) `match /leads/{id}` update non-super `hasOnly([...])` thêm **`'dropped_reason', 'ai_feedback', 'phone'`** ngay sau `'dropped_by'` (mốc dump #47 dòng 177); (2) **2 block đọc cho web** chèn 1 dòng/block ngay sau `match /databases/{database}/documents {` (cách LỆNH #23): `match /source_health/{sid}` + `match /group_state/{gid}` — `read: isSuperAdmin()`, `write: false` (chỉ Admin SDK ghi) → zip v119-90 cột "Nhịp · Sức khoẻ" ở Nguồn quét + Cảnh báo hệ thống. Idempotent từng phần (đã có whitelist mà thiếu block → chỉ chèn block); fail-closed: thiếu mốc hoặc thiếu helper `isSuperAdmin()` → không ghi gì.
- **ZBS** (`_lc_zbs.cjs`, `~/smartlead-zalo-fn/functions/index.js`, tuỳ chọn — lỗi = cảnh báo): sau `if (!brand) return;` thêm guard `dropped || lost || ai_scored === false || role seller|poster_self || self_comment` → return; điều kiện `!justTagged && score không đổi → return` nới cho `ai_scored false → true`. `zalo_notified` vẫn chặn trùng. Deploy `firebase deploy --only functions:notifyBrandZalo` (cần `firebase.json` trong zalo-fn; không có → báo, không chặn).
- **`_lc_recount.mjs`** (= `_l42_recount` + bỏ lead máy loại; `tempOf` = nhiệt độ HIỆN TẠI sau AI chấm lại): dựng lại `new/hot/warm/cold/junk` 60 ngày (set merge, `recountBy: 'lenhC'`), doc trong khoảng không còn lead → về 0. **KPI 14 ngày sẽ GIẢM** (lead máy loại không còn đếm) = số thật. Chạy lại bao nhiêu lần cũng ra cùng số.
- **`_lc_clean.mjs`**: liệt kê doc `group_state` cũ khoá URL (B đã di trú sang `g_/s_`); `--apply` xoá — chỉ khi ≥15 doc mới và doc cũ không được ghi sau deploy B (12/09 12:11Z). KHỐI 1 chỉ dry.
- KHÔNG đổi: worker, FE (zip v119-90 đọc `system_status/scan.health`, `aiDropped`, `dropped_reason`, `ai_feedback`).

## 2. Kiểm trước khi gửi
- Harness **44/44** (bản 2: + Rules nửa vời chỉ chèn block thiếu · không có helper isSuperAdmin → dừng): patch OK/idempotent/fail-closed nguyên tử (1 mốc lệch → không ghi file nào)/LỆCH (push.js chưa) · `--check` 3 file · Rules (mốc dump #47) + ca không mốc · ZBS (mẫu dump #47d, guard đúng vị trí, `--check`) · statsEvents 11 ca (máy loại −new −hot +aiDropped; sales loại giữ + dropped; hạ nhiệt hot→cold; xuống rác; nâng rác→ấm; khôi phục; đổi giai đoạn; vai người bán) · machineDroppedC (email `aiden@…`/uid dài = người; `rescore:sweep` = máy) · hotGateC 6 ca · healthEvalC 5 ca (noLead/silent/err, không tính ngày >7, dọn ngày cũ cả nguồn tắt, alertedAt/6 h, ERROR ≥½) · scanAll trọn vòng trên Firestore + BrightData giả: lượt gieo không ghi source_health, lượt gặt ghi 3 nguồn (days/lastPostAt/errStreak 0/brand/gid), 6 lượt trigger lỗi → errStreak ≥6 → WARNING + ERROR + `scan.health`, không lặp trong 6 h, hồi → 0; backfill khoá có brand + khoá cũ vẫn được tôn trọng · recount `newAgg`.
- Bẫy đã gặp: (1) regex `ai[_-]?` khớp email `aiden@…` → đổi thành token chính xác + `$`; (2) `\w` trong template literal của patch .cjs → phải `\\w` (harness bắt: `rescore:sweep` không khớp); (3) nguồn hồi sau lỗi mà không có bài mới thì không ghi → errStreak treo → thêm `healthErrIds` lượt trước để ghi reset.

## 3. KHỐI 1 (dán vào Cloud Shell) — khối trong chat = `cat > /tmp/lc.sh <<'EOF_LC'` … `EOF_LC` rồi `bash /tmp/lc.sh` (nội dung = file `lenh-2026-09-13-c.sh`, sha256 ở đầu)
Thứ tự: `--check` 6 script → backup `.bak-$TS` (3 file + firestore.rules; chạy lại giữ `.bak` gốc qua `_lc_backup_ts`) → patch 3 file fail-closed → `--check` → Rules → import test `.env` (in `statsEvents máy loại → [{new:-1,hot:-1},{aiDropped:1}]`, `hotGateC brand_hint → new | bước 2 → (không push lại)`) → deploy `firestore:rules + scheduledScan + manualScan + statsOnLead + pushOnLead` (xích `&&`, lỗi → describe + hướng dẫn quay lui) → zalo-fn (backup `.bak-$TS`, patch, deploy, describe; lỗi = cảnh báo) → `_lc_recount.mjs` (áp) → `_lc_clean.mjs` (dry).

## 4. KHỐI 2 (sau ≥ 15′, tốt nhất 1–2 giờ ban ngày; chỉ đọc)
```bash
cd ~/firebase-s13/functions && node _lc_after.mjs
```
Kỳ vọng: (1) daily_stats 4 ngày có `recountBy lenhC`, `aiDropped` bắt đầu đếm · (2) lead 24 h: `dropped_by` người không khớp regex máy · (3) log pushOnLead: `push hot … new sent ≥1` cho lead nóng mới (admin brand/super có token) · (4) source_health có doc cho nguồn hoạt động, `scan.health` n nhỏ, `[LENH C]` lỗi ghi 0 · (5)(6) backfill/group_state · (7) Rules local có dropped_reason, zalo-fn có marker, notifyBrandZalo updateTime mới. Tuỳ chọn: `node _lc_clean.mjs --apply`.

## 5. Rủi ro & quay lui
- Quay lui code: `cd ~/firebase-s13/functions; TSB=$(cat _lc_backup_ts); for f in stats.js push.js index.js; do cp "$f.bak-$TSB" "$f"; done; cp ~/firebase-s13/firestore.rules.bak-$TSB ~/firebase-s13/firestore.rules; cd ~/firebase-s13 && firebase deploy --only firestore:rules,functions:scheduledScan,functions:manualScan,functions:statsOnLead,functions:pushOnLead`. ZBS: `cp ~/smartlead-zalo-fn/functions/index.js.bak-<TS> …` + `firebase deploy --only functions:zalo:notifyBrandZalo` (codebase `zalo`, xem mục 6.2).
- Recount đã ghi thì số daily_stats theo định nghĩa mới; muốn về định nghĩa cũ chạy `node _l42_recount.mjs` (vẫn còn trong functions/).
- source_health/healthEval nằm trong `try` riêng, lỗi chỉ `console.warn('[LENH C] …')`, không đụng lượt quét. Chi phí: ≤ số nguồn write/lượt gặt (thường vài dòng) + 1 read collection/5′.
- `phone` vào whitelist: sales sửa được SĐT lead (cần cho PA-5); super vốn đã sửa được.

## 6. KẾT QUẢ
### 6.1 KHỐI 1 — ĐÃ CHẠY 13/09/2026 07:08–07:10 VN (TS `20260913-000854`, `exit=0`) — PASS trừ bước (e) ZBS deploy
- sha256 `/tmp/lc.sh` = `6061ed4a…5fa1` ✓ khớp bản 2 · `--check` 6 script OK · backup `.bak-20260913-000854` (stats.js · push.js · index.js · firestore.rules; `_lc_backup_ts` ghi).
- **PATCH OK 3 file** (marker `LENH C`: stats.js 4 · push.js 4 · index.js 9) · **Rules PATCH OK** (whitelist + `dropped_reason, ai_feedback, phone`; 2 block đọc `source_health` + `group_state` cho super) · `node --check` OK.
- **IMPORT OK** (`.env`): `statsEvents máy loại → [{new:-1,hot:-1},{aiDropped:1}]` ✓ · `hotGateC brand_hint → new | bước 2 → (không push lại)` ✓.
- **Deploy `firestore:rules` + `scheduledScan · manualScan · statsOnLead · pushOnLead` — Deploy complete, cả 4 ACTIVE 00:10:23–00:10:32Z** (= 07:10 VN).
- **(e) ZBS**: `_lc_zbs.cjs` PATCH OK (marker `LENH C` trong `~/smartlead-zalo-fn/functions/index.js`, backup `index.js.bak-20260913-000854`, `node --check` OK) **NHƯNG `firebase deploy --only functions:notifyBrandZalo` LỖI `Error: No function matches given --only filters. Aborting deployment.`** → cảnh báo, không chặn (đúng thiết kế). Function đang chạy vẫn bản CŨ (ZBS vẫn bắn cho lead đã loại/điểm tạm/người bán) tới khi chạy **bước (e) bổ sung `lenh-2026-09-13-c-e.sh`** (mục 6.2).
- **`_lc_recount.mjs` (áp)**: quét **2.361 lead** · vai (#42) **406** · **MÁY loại 26** (định nghĩa mới, trừ khỏi bộ đếm) → **106 doc** `daily_stats` 60 ngày; so bản cũ **2 LỆCH = đúng dự kiến** (`test-agency 2026-09-11` 3/1/0 → 1/0/0 · `z15mrc-tts-1 2026-09-12` 45/23 → 45/24); theo brand: hscl-01 521/142 · test-agency 198/54 · z15mrc-tts-1 775/310 · z15mrc-tuyendung1 271/11 (hợp lệ/nóng) · **ĐÃ GHI 106 doc, đưa về 0: 1** (`recountBy:'lenhC'`). KPI 14 ngày trên web giảm nhẹ = số thật (anh chốt câu 1).
- **`_lc_clean.mjs` (dry)**: `group_state` 56 = mới (g_/s_) **30** + cũ (URL) **26** (3 doc "1970" = không có mốc thời gian); không doc cũ nào ghi sau mốc deploy B → `[DRY] sẽ xoá 26` → an toàn chạy `node _lc_clean.mjs --apply`.

### 6.2 Bước (e) bổ sung — `lenh-2026-09-13-c-e.sh` (sha256 `0e5d0401…c8bb`, 7698 byte) — CHỜ ANH CHẠY
Vì sao lỗi: Firebase CLI ở `~/smartlead-zalo-fn` **không thấy `notifyBrandZalo` trong codebase khai ở `firebase.json`** (mục `functions` trỏ source khác / codebase đặt tên / hoặc không có mục functions) — giống hệt tagLeadBrand ở LỆNH B (e) (deploy bằng gcloud, không phải Firebase CLI). Script: (1) chẩn đoán `firebase.json`/`.firebaserc`/`package.json`, export thật của `index.js` (chỉ nạp module), marker (0 → tự vá lại bằng `_lc_zbs.cjs`), `node --check`, describe TRƯỚC (**chỉ TÊN** env/secret, không in giá trị); (2) deploy A = Firebase CLI với **đúng codebase** đọc từ `firebase.json` (`functions:<codebase>:notifyBrandZalo`); lỗi/không thấy → deploy B = `gcloud functions deploy notifyBrandZalo --gen2 --region=asia-southeast1 --runtime=nodejs22 --entry-point=notifyBrandZalo --source=~/smartlead-zalo-fn/functions --quiet` (cập nhật function có sẵn: trigger/env/**secrets `ZALO_APP_ID`/`ZALO_SECRET_KEY`**/SA/maxInstances 10 giữ nguyên vì không truyền cờ nào khác); (3) describe SAU: `updateTime` phải đổi, ACTIVE, secret keys còn đủ — không đổi → rc 1. KHÔNG deploy trùm zalo-fn, KHÔNG đụng `~/codebase2`. Dry-run fake HOME + fake gcloud/firebase **5 kịch bản** (codebase đặt tên + Firebase OK · "No function matches" → gcloud OK · source khác → bỏ A → gcloud · gcloud lỗi → rc 1 + updateTime không đổi · marker 0 → tự vá) exit đúng; 1 dấu `!` duy nhất = shebang (trong heredoc).
- **★ Bước (e) bổ sung ĐÃ CHẠY OK 13/09 08:28 VN (= 01:28Z, exit 0)**: sha256 `0e5d0401…c8bb` khớp · `~/smartlead-zalo-fn/firebase.json` = `functions:[{source:"functions", codebase:"zalo", ignore:[…]}]` + `.firebaserc` default `smartlead-z15` · `functions/package.json` `smartlead-zalo-zbs` node 22, deps firebase-admin/firebase-functions/node-fetch, `node_modules` có · marker `LENH C` 2 (dòng 290 guard dropped/lost/ai_scored/vai/self_comment · 299 `!justTagged && !(ai_scored false→true) && score không đổi → return`) · SYNTAX OK · export thật: `bootstrapZaloTokens, refreshZaloToken, sendZaloZBS, notifyBrandZalo` · describe TRƯỚC: ACTIVE, updateTime `2026-08-12T14:00:53Z`, rev `notifybrandzalo-00004-hud`, trigger `document.v1.updated leads/{leadId}` (database + namespace default), maxInstances 10, timeout 60, `deployment-tool=cli-firebase`, `firebase-functions-codebase=zalo`, secret keys `ZALO_APP_ID, ZALO_SECRET_KEY` · **codebase = `zalo` → deploy A `firebase deploy --only functions:zalo:notifyBrandZalo --non-interactive` → "Successful update operation · Deploy complete!"** (cảnh báo `GOOGLE_CLOUD_QUOTA_PROJECT is not usable when uploading source` vô hại) → bỏ nhánh gcloud · describe SAU: ACTIVE, **updateTime `2026-09-13T01:28:32Z`, rev `notifybrandzalo-00005-hux`**, trigger/maxInstances/timeout/tool/codebase giữ nguyên, secret keys còn đủ, env keys thêm `FUNCTION_REGION` (Firebase CLI bản mới tự đặt, vô hại). ⇒ **LỆNH C trọn vẹn: Rules + 4 function s13 + notifyBrandZalo (zalo-fn) đều bản mới.** ZBS từ giờ KHÔNG bắn cho lead đã loại / không thành / điểm tạm / người bán / chủ bài; AI chấm lại lead điểm tạm → được bắn (`zalo_notified` vẫn chặn trùng).
- **BÀI HỌC (mới)**: zalo-fn khai `codebase: "zalo"` trong `firebase.json` → Firebase CLI ở đây KHÔNG khớp bộ lọc `functions:<tên>` trần ("No function matches given --only filters"), phải ghi **`functions:zalo:<tên>`**. Mọi LỆNH sau deploy function trong `~/smartlead-zalo-fn` dùng dạng có codebase. `~/codebase2` thì ngược lại: không có `firebase.json`, deploy bằng gcloud từ thư mục function (LỆNH B (e)). Quay lui ZBS: `cp ~/smartlead-zalo-fn/functions/index.js.bak-20260913-000854 ~/smartlead-zalo-fn/functions/index.js && cd ~/smartlead-zalo-fn && firebase deploy --only functions:zalo:notifyBrandZalo`.
### 6.3 `_lc_clean.mjs --apply` + KHỐI 2 `_lc_after.mjs` — ĐÃ CHẠY 13/09 08:35 VN — PASS
- **clean**: 56 doc = mới 30 + cũ 26 (ghi cuối ≤ 12/09 19:10 VN, 3 doc không mốc) → **ĐÃ XOÁ 26 doc group_state cũ** (khoá URL).
- **(1) daily_stats 4 ngày (10/09 →)**: 16 doc, **recountBy lenhC** đủ (hscl-01 4 · test-agency 3 · tts-1 4 · tuyendung1 4); hợp lệ/nóng: hscl-01 51/23 · test-agency 8/1 · z15mrc-tts-1 177/78 · z15mrc-tuyendung1 43/1; `aiDropped` (máy loại) 0 = đúng vì mới đếm từ deploy 07:10. **`dropped` ("sales loại") 109/38/64/63 = KHÔNG phải sales bấm**: bộ đếm cũ (trước C) cộng cả lead do MÁY loại theo ngày loại (sweeper #46/#48 ngày 11–12/09 ≈ 270 lead backlog điểm tạm) — recount KHỐI 1 chỉ dựng lại new/hot/warm/cold/junk, không đụng `dropped` → Bảng brand (30 ngày) đang hiện "loại 109 · AI loại 0" cho hscl-01 → **KHỐI 3 `_lc_drop.mjs`** (mục 6.4) dựng lại tuyệt đối 2 cột `dropped`/`aiDropped` theo ngày loại.
- **(2) lead 24 h**: 66 · máy loại 0 · sales loại 0 · AI chấm lại đổi điểm 0 · `dropped_reason` 0 (zip v119-90 chưa deploy / chưa ai bấm Loại) — không có ca `dropped_by` người khớp regex máy ✓.
- **(3) log pushOnLead 24 h**: 27 dòng `push hot` đều TRƯỚC deploy C (4 dòng gần nhất 12/09 16:22–20:19Z tts-1 `sent 1`); **sau C (00:10Z → 01:35Z) 0 dòng** `new/tagged/rescored` → **chưa kết luận được** (sáng sớm có thể chưa có lead nóng mới; nhịp trước C ≈ 1,2 lead nóng/giờ nên 1,5 h ra 0 là hơi thấp) → script `_lc_push.mjs` (mục 6.4) đối chiếu từng lead nóng đủ điều kiện với dòng push.
- **(4) source_health**: 12 nguồn có dữ liệu · `system_status/scan.health` lúc 08:31: **2 xấu / 12 bật** = "Chợ Hải Sản Vân Đồn Quảng Ninh" (hscl-01, 26 bài/7 ngày, 0 lead) + "Cá khô sỉ 1" (hscl-01, 17 bài, 0 lead) → đúng ngưỡng `noLead` (≥15 bài, 0 lead) — cảnh báo mềm, WARNING `[SCAN-NO-LEAD]` chỉ bắn khi nguồn thật sự im (log 24 h: 0 vì vừa deploy) · 10 nguồn còn lại bài 2–17/lead 0–1, errStreak 0 · `[LENH C] lỗi ghi 0` · `[SCAN-HEALTH]` 0 ✓ · days ≤ 7 khoá, bài cuối 07:34–08:31.
- **(5) backfill_done** 20 gần nhất: 3 doc, khoá mới (có brand) 0 = bình thường (chưa backfill sau C) · **(6) group_state 30 doc, cũ URL 0** ✓ · **(7) Rules local có `dropped_reason/ai_feedback/phone` · zalo-fn marker LENH C · notifyBrandZalo ACTIVE `2026-09-13T01:28:32Z`** ✓.

### 6.4 KHỐI 3 (13/09 sáng) — `_lc_drop.mjs` (recount `dropped`/`aiDropped`) + `_lc_push.mjs` (kiểm push) — LẦN 1 (DRY) ĐÃ CHẠY 08:59 VN; `--apply` + kiểm lại CHỜ ANH
- **`lenh-2026-09-13-c-drop.mjs`** → `~/firebase-s13/functions/_lc_drop.mjs`: quét `leads` theo trang 300 + select (brand/brand_hint/dropped/dropped_by/dropped_at/rescored_at/detected_at) → lead `dropped===true` → `dropped_by` khớp regex máy → `aiDropped`, còn lại → `dropped`; ngày = `dropped_at || rescored_at || detected_at` (VN); cửa sổ 60 ngày; so với `daily_stats` hiện có → in từng doc lệch; mặc định DRY, `--apply` set merge `{dropped, aiDropped, recountDropBy:'lenhC', recountDropAt}` (kể cả đưa về 0; `lost` không đụng); chạy lại = "KHỚP hết". Đã test trên shim Firestore có trạng thái: DRY → apply 4 doc → apply lần 2 = 0 ghi; lead không brand/không mốc/trước cửa sổ bị bỏ đúng.
- **`lenh-2026-09-13-c-push.mjs`** → `_lc_push.mjs` (chỉ đọc; tham số 1 = mốc ISO, mặc định `2026-09-13T00:10:00Z`): lead nóng từ mốc, đủ điều kiện push (có brand/brand_hint, không dropped/lost/vai/điểm tạm) ↔ dòng `push hot <id> new|tagged|rescored sent N` trong log `pushonlead` (gcloud logging) ↔ user có `fcmTokens` → kết luận tự động: đủ điều kiện mà không có dòng push = LỖI cổng; có dòng sent 0 = admin brand chưa bật thông báo; chưa có lead nóng = chạy lại sau. Test 3 kịch bản (thiếu → LỖI 1/2 · đủ → ✓ · mốc tương lai → chưa kết luận) trên shim + fake gcloud.
- **KẾT QUẢ KHỐI 3 lần 1 (13/09 08:59 VN, chỉ DRY + kiểm push)**:
  - **`_lc_drop.mjs` DRY**: quét **2.361 lead → 432 lead đã loại, TẤT CẢ là máy** (`dropped_by` khớp regex máy; người 0) · theo brand: hscl-01 177 · test-agency 40 · z15mrc-tts-1 110 · z15mrc-tuyendung1 105 · cửa sổ 60 ngày có 110 doc `daily_stats` · **8 doc lệch** = 2 ngày × 4 brand: **06/09** (`dropped → 0`, `aiDropped` hscl-01 68 · tts-1 46 · tuyendung1 42 · test-agency 2 = **158 = đúng số lead chủ bài LỆNH #31b KHỐI 2**) và **11/09** (`aiDropped` 109 · 64 · 63 · 38 = **274 = backlog điểm tạm sweeper #46/#48**). Tổng 158 + 274 = 432 ✓ khớp từng con số đã biết → an toàn `--apply` (8 doc, không đụng `lost`/`new`/nhiệt độ).
  - **`_lc_push.mjs` (mốc 00:10Z)**: **0 lead phát hiện kể từ mốc** (nên 0 lead nóng đủ điều kiện, 0 dòng `push hot`), user có token: 1 (superadmin, brand test-agency) → **CHƯA KẾT LUẬN** (kết luận tự động "chạy lại sau"). Ghi nhận: từ 07:10 đến 08:59 VN `source_health` cộng ≈ 74 bài mới ở 12 nguồn nhưng 0 lead → sáng Chủ nhật bài ít lead hay AI đang hoãn? → kiểm bằng `node _l48_after.mjs` (12 lượt gần nhất: bài/lead · llmOk/llmFail/deferred · tầng 1 · `system_status/llm` · `score_retry`) trước khi chạy lại `_lc_push.mjs` lúc 11–12 h hoặc 14–16 h VN.
- **KHỐI 3b (CHỜ ANH CHẠY)** — (1) ngay: `cd ~/firebase-s13/functions && node _lc_drop.mjs --apply` (kỳ vọng `ĐÃ GHI 8 doc`; F5 Bảng brand → cột "Không thành / loại" hscl-01 hiện `loại 0 · AI loại 177`); (2) lúc 11–12 h VN: `node _l48_after.mjs && node _lc_push.mjs && node _lb_lag.mjs 24` (sức khoẻ AI/lượt quét · push lead nóng · LAG ban ngày LỆNH B khối 4c).
- KẾT QUẢ KHỐI 3b: …
