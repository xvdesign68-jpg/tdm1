# C — Rà đối kháng ENGINE automation backend (outreach.js · content.js · stats.js) — 10/09/2026

> Dựng lại từ patch đã áp (`docs/lenh-2026-09-07-37-stepnick-patch.cjs`, `…31b-patch.cjs`, `…34-patch.cjs`, `…30-c30.cjs`, `…32-patch.cjs`, `…33-patch.cjs`, `…36-stats-patch.cjs`), harness (`harness-lenh34/37/selfcmt`, `harness-worker-refund`), script vận hành (`…34-move.mjs`, `…30c-regen.sh`, `…32-brand.mjs`, `…31b-fix.mjs`) và CLAUDE.md (ENGINE · v2 · v119-29/35/36 · PHASE 2 · FUNNEL 1-LEAD-1-PHIÊN · v120-scale · COMMENT-LEAD · LỆNH #31/#34/#37 · Safety Score). Không có `outreach.js` đầy đủ → mục chưa thấy code ghi "Suy luận". Không sửa file nào.

## A. Phát hiện

**[C-1] [Cao] Máy chạm chồng lead ĐÃ CÓ NGƯỜI THẬT CHĂM / đã chốt** · `…37-stepnick-patch.cjs` mốc A2: trước #37 lọc DUY NHẤT của lead loop func là `if (lead.dropped) continue;`, sau #37 thêm `lost/junk/brandAllow/roleBlockOf/commentIdOf`; AdsPower path theo `harness-selfcmt` (`tref exists → roleBlockOf → parsePost.post_id → enqueue`) + CLAUDE.md v119-35 ("bỏ dropped/junk/đã có thread/không post_id"). Không thấy điều kiện `stage`/`assignee`/`first_care_at`/`closed_at`/`outreach_replied`; script vận hành phải tự thêm `isOpen` (`_l34_regen`, `_l32_brand`) → xác nhận engine không lọc. · Kịch bản: sales đã gọi/hẹn (stage `booked`) → nick vẫn comment công khai "nhắn riêng em báo giá" + inbox mở đầu → khách nhận 2 giọng cùng brand; lead `closed` vẫn bị inbox chào bán. · Sửa: gate `!dropped && !lost && !closed_at && stage=='new' && !assignee && !first_care_at && !outreach_replied`; trigger `leads/{id}` (stats.js đã có) khi stage rời `new`/có assignee → `outreach_threads/{id}.active=false` + task queued → skipped. · **Chắc** (func) / **Suy luận** (AdsPower).

**[C-2] [Cao] Chọn lead `detected_at desc` + bỏ lead có thread → lead cũ đói, không ưu tiên nóng** · CLAUDE.md ENGINE; `harness-lenh37` stub có `orderBy/limit` nhưng limit thật chưa rõ. · Kịch bản: 1 nick ≈ 40 lead/ngày (van react + pace) trong khi scanner ra 60–140 lead/ngày → lead sáng bị lead chiều đè mãi; lead **hot** 14:00 gặp 40 suất react đã bị lead **cold** (ma trận cold = react) tiêu từ sáng → mai không còn "mới nhất" → không bao giờ chạm. Nếu không limit thì lead 30 ngày vẫn được comment lên bài cũ (spam signal). Ngoài ra mỗi lead đã có thread tốn 1 `tref.get()`/nick/tick (5 nick × 100 lead × 288 tick ≈ 144k read/ngày/brand). · Sửa: điểm ưu tiên = temp (hot 3/warm 2/cold 1) × suy giảm tuổi, trần 7 ngày; engine đóng dấu `leads.outreach.queued_at` lúc enqueue → query `where brand & queued_at==null` (lọc máy chủ, hết `tref.get()`). · **Suy luận**.

**[C-3] [Vừa] Lead ghi 2 bước (LỆNH #41) → engine bốc lead khi `temp`/`role` chưa có** · #37 A2 `const temp37 = lead.temp || 'cold'`. · Kịch bản: tick rơi đúng khe → hot bị coi cold → thread `steps:['react']`; sau đó AI chấm hot/`seller` nhưng thread đã tồn tại → không nâng phễu, không tắt (roleBlockOf chỉ chạy lúc tạo). · Sửa: bỏ qua lead thiếu `score/temp` hoặc `detected_at` < 3'; trigger lead ghi `role seller/poster_self` sau khi có thread → tắt thread + huỷ task. · **Suy luận**.

**[C-4] [Cao] Re-enqueue "nguyên fpayload, KHÔNG reserve cap lại" → VƯỢT VAN khi chạy khác ngày** · CLAUDE.md FUNNEL ("re-enqueue nguyên fpayload (không reserve cap lại)"); `…34-move.mjs` ("Không đụng cap"); `_cl_reopen.mjs`; kiểm #34: 11/12 thread hscl-01 kẹt 6 ngày. · Kịch bản: 11 phễu reserve ngày D; D+6 nick sống lại → 11 × (react+comment+friend+inbox) trong ~1,5 h mà `outreach_usage/{pid}__{D+6}` = 0 → lead mới hôm đó vẫn đủ 40/12/10/8 → nick vừa hồi chạy ~2× van; `hardCapCheck` của worker đọc cùng doc usage → cũng mù. · Sửa: thread lưu `reservedDay`; khác ngày VN → `tryConsume` lại bước chưa `doneSteps` (hết → hẹn mai); hoặc kế toán lúc THỰC HIỆN (worker `increment` khi bước done, engine kiểm `usage+queued<cap`). · **Chắc** (thiết kế) — LỆNH C-3 xác nhận code.

**[C-5] [Vừa] Refund vào doc NGÀY HIỆN TẠI + `increment(-1)` trên doc chưa có → usage ÂM** · `harness-worker-refund` kịch bản A khẳng định `usage.friend === -1`; doc worker 06 "Hạn chế đã biết". · Kịch bản: dead-letter/skip 00:30 VN cho phễu reserve hôm qua → usage hôm nay `-1` mỗi bước → `tryConsume` (`u.react >= cap`) cấp thừa; nhiều thread chết qua đêm → âm sâu. · Sửa: refund theo `reservedDay` (chỉ cùng ngày), clamp ≥0 trong transaction. · **Chắc**.

**[C-6] [Cao] Thread MỒ CÔI khi nick chết: engine không tự chuyển/đóng, worker lặp vô hạn** · kiểm #34 ("nick đã tự tắt → engine không re-enqueue 6 ngày"; nick needLogin còn active → task `paused` lặp mỗi giờ từ 5/9); `_l34_move.mjs` là thao tác tay. · Kịch bản: nick `active:false` (needLoginCount≥3/safetyPaused/challenge) rơi khỏi `fb_accounts where active==true` → thread `active:true` nằm mãi, lead không ai chạm, thẻ brand tưởng đang chạy; `tries` chỉ đếm fail thật → không có trần thời gian thread. · Sửa: sweeper trong `outreachTick`: thread active mà `pid` không sống > 2 h → round-robin nick sống cùng brand (giãn 10') hoặc `step:'orphan'` + hoàn cap + cảnh báo (C-12); thread active > 72 h → đóng. · **Chắc**.

**[C-7] [Cao] Re-enqueue khi task đang `running` → DOUBLE hành động** · CLAUDE.md FUNNEL (lease `nextAt+30′`; thread tới hạn → re-enqueue), task id tất định `{leadId}__funnel`, worker `reapStale` chỉ requeue `running` > max(15', nickTimeout+12'). · Kịch bản: VPS bận (maxConcurrent 1) → task `queued` > 30' → engine `set` đè task (status → queued?) đúng lúc worker vừa claim `running` → lượt sau claim lại → comment đã đăng nhưng `markDone` chưa ghi → comment 2 lần công khai. · Sửa: engine đọc task trước: `running` → chỉ gia hạn lease; `queued` → chỉ cập nhật `nextAt`, không đụng payload/status; dùng transaction. · **Suy luận** (chưa thấy `apEnqueueTask`/nhánh retry).

**[C-8] [Vừa] Cùng 1 NGƯỜI bị chạm nhiều lần qua nhiều lead** · thread khoá theo `leadId`; `outreach_map/{uid}` chỉ để khớp reply; không khoá theo `author_url`/uid. · Kịch bản: khách đăng cùng bài ở 3 group → 3 lead → 3 lời mời kết bạn + 3 inbox gần giống (có thể 3 nick) → report → bay nick. · Sửa: `outreach_people/{key=uid|author_url|phone}` lưu `lastTouchAt`; chạm < 30 ngày hoặc đã reply → bỏ; comment-lead cùng người → chỉ reply. · **Suy luận**.

**[C-9] [Vừa] Thiếu van theo GROUP/ngày và theo VPS/IP; burst 08:00** · CLAUDE.md v119-35 (`caps` theo nick), v120-scale (ngoài giờ 8–22 `return sớm`). · Kịch bản: 10 lead cùng group/ngày → 1 nick comment 10 bài cùng group với CTA giống nhau → admin group xoá/kick (kênh mất nick #1, `cmpFinish` không chữa được); 08:00 mọi nick `nextFreeAt<=now` → CONC 25 mở phễu đồng loạt trên cùng proxy. · Sửa: `outreach_usage` thêm khoá `g:<gid>` (≤3/group/nick/ngày, ≤8/group/brand); đầu ngày rải `nextFreeAt` 0–120'. · **Suy luận**.

**[C-10] [Vừa] Nội dung: `spam` không gate, CTA brand lặp nguyên văn, template 2–3 mẫu, template/reply không `stripForbid`** · `…34-patch.cjs` A13 (`spam` chỉ vào `out/meta`); `…30-c30.cjs` `ensureCta` (`cta = c.cta` nếu ≤140 → nối NGUYÊN VĂN; `CTA_DEF` 3, xưng "mình" trong khi AI xưng "em"); A3 (`CMP_DEF` 2, `Q_DEF` 3, `IBX_TPL` 2); A4/A5 template/reply không `stripForbid`, không lọc SĐT. · Kịch bản: brand ghi CTA cố định → mọi comment thiếu CTA kết bằng cùng chuỗi → 12 comment/ngày câu cuối y hệt = duplicate-content signal; AI trả `spam 8` vẫn đăng; `lead.reply` chứa giá/từ cấm ra công khai ở chế độ reply. · Sửa: `spam ≥ 7` → sinh lại 1 lần rồi rơi mẫu soft/cmp; pool paraphrase CTA 6–8 câu chọn theo hash leadId; Jaccard với 20 comment gần nhất của nick (`outreach_log.text`) > 0,7 → sinh lại; `stripForbid` + lọc SĐT cho cả 3 chế độ; đồng bộ ngôi qua `pronounOf`. · **Chắc**.

**[C-11] [Cao] Khách trả lời bằng BÌNH LUẬN/react → không ai bắt; `checkReplies` phủ thấp** · `lenh-2026-09-05-worker-uid.md` (chỉ đọc `messages/t/<uid>`, cần uid số, 5 thread/nick/60', `replyChecks` ≤6); LỆNH #36(f): "đã inbox 1 · replied 0"; funcWebhook chỉ nick func (không dùng). · Kịch bản: comment-lead "Giá" → nick reply "em nhắn riêng chị nhé" → khách hỏi lại dưới comment → không ai đọc, lead vẫn `new`, nick trông như bot; ~2/3 lead-bài không có `author_url` → không bao giờ có uid → inbox không được kiểm reply. · Sửa: worker cuối phiên mở `facebook.com/notifications` 1 lần → khớp "đã trả lời bình luận của bạn / đã thích" với `post_id/comment_id` trong thread → `replied` + note + stage responded + push; fallback recheck permalink comment sau 6 h/24 h. · **Chắc**.

**[C-12] [Cao] Không giám sát chủ động; log engine chưa chắc kích alert** · LỆNH #36(f) phát hiện 4/5 nick chết chỉ nhờ chạy tay; bài học LỆNH #21 ("`console.error` = severity DEFAULT, không kích policy #9") mới áp cho scanner; FE `sysAlertCard` chỉ hiện khi super mở tab Cảnh báo. · Kịch bản: VPS mất điện → task queued tích 3 ngày, web vẫn "xanh", không email/push. · Sửa: cuối `outreachTick` tính health (brand on & alive=0 · queued > 60' · dead hôm nay · nick needLogin/uiLang) → `console.log(JSON.stringify({severity:'ERROR',message:'[OUTREACH-HEALTH] …'}))` + `system_status/outreach` (banner FE như BrightData) + việc Hộp việc super. · **Suy luận** (severity) / **Chắc** (thiếu cơ chế).

**[C-13] [Vừa] Đo hiệu quả nội dung lệch** · `…36-stats-patch.cjs`: `sentNow = !!ao.inbox_at`; `isRep` gồm `stage∈{responded,booked,closed}` (sales đổi tay). · Kịch bản: phễu warm/cold (không inbox) không vào `sent` → hiệu quả kiểu bình luận/parent không đo; sales gọi chốt sau khi máy inbox → tính "phản hồi nhờ nội dung"; reply qua comment không tính. · Sửa: tách `sentCmt/repCmt` (theo `comment_at`) và `sentIbx/repIbx`; `rep` chỉ nhận `outreach_replied/replied_at`; stage tay → chỉ số riêng "chốt sau khi máy chạm". · **Chắc**.

**[C-14] [Vừa] `genForLead` 11–16 s/lead trước khi biết nick chạy được; không lọc nick ốm ở đầu tick** · CLAUDE.md LỆNH #4/#5 (16 s/lead, CONC 25 vs timeout 300 s); #34 (`_l34_regen` = LLM lần 2 cho 12 thread). · Kịch bản: nick `needLogin` nhưng `active:true` (chưa đủ 3 lần) vẫn được bốc lead mới → LLM + reserve → worker gate pause → lặp mỗi giờ; 25 nick cùng tick × 15 s = timeout. · Sửa: lọc `needLogin/challenge/uiLang≠vi/safetyPaused` ngay đầu tick; deadline mềm 240 s; soạn LƯỜI (worker gọi `genContent` nội bộ ngay trước bước comment) hoặc `LLM_CONTENT_MODEL` nhanh. · **Chắc** (thời gian) / **Suy luận** (lọc nick).

**[C-15] [Thấp] funcWebhook nhận secret qua `?key=` → lộ trong Cloud Logging `httpRequest.requestUrl`** · CLAUDE.md ENGINE ("verify qua Bearer/`?key=`"). · Func path đang không dùng nên rủi ro thấp; sửa: chỉ nhận Bearer, xoay secret (đã lỡ dán chat). · **Chắc** (thiết kế) / **Suy luận** (func gọi kiểu nào).

## B. Đã kiểm, an toàn
- Cổng vai SAU kiểm thread tồn tại, TRƯỚC enqueue; `skipLeadRole` ghi `active:false` → không xét lại (harness-selfcmt ✓); func path #37 gate ma trận/van/vai/comment-lead, `t.allow` tương thích thread cũ (14/14 ✓).
- LLM chỉ gọi khi `steps` có comment/inbox; `llmJson` thích nghi tham số GPT-5 + retry trần token; AI lỗi → fallback vẫn qua gate đối thủ + meta.
- Bình luận dưới bài người khác: 3 tầng, không rõ → coi là bài bán → `cmpFinish` gọt giá/brand/so sánh/SĐT ≤190 + CTA chỉ mời nhắn riêng (37/37 ✓); opt-out/CTA khớp xưng hô; `commentUrlOf` chuẩn hoá 2 dấu `?`.
- Task id tất định + `doneSteps` + `reapStale` transaction + `maxInstances:1` + watchdog 06d → không 2 task/lead, không chồng tick, hết zombie.
- Tenancy: `outreach_log` brand user phải lọc `brandCode` (emulator LỆNH #38: admin không lọc → TỪ CHỐI); `outreach_stats/content_stats/daily_stats` write=false; `workers/worker_config/fb_accounts/brands` super; `outreach_tasks/threads/usage/map` không block → chỉ Admin SDK; token func ở Secret Manager; SA key worker theo chốt anh.

## C. Cần xác minh bằng LỆNH chỉ đọc (`~/firebase-s13/functions`)
1. Lead loop AdsPower có lọc stage/assignee? · `awk '/async function stepNickAdspower/,/async function apEnqueueFunnel/' outreach.js | nl -ba`; `grep -n "stage\|assignee\|first_care\|closed_at\|lost\|limit(" outreach.js`.
2. Query lead có `limit`? · `grep -n -A3 "orderBy('detected_at'" outreach.js`.
3. Re-enqueue có reserve lại / kiểm task `running`? · `grep -n "fpayload\|apEnqueueTask\|status: 'queued'\|running\|reservedDay\|tryConsume(" outreach.js`; `awk '/function tryConsume/,/^}/' outreach.js`.
4. Usage âm hôm nay? · Admin SDK: `outreach_usage` doc id kết thúc `__<ngàyVN>` → in field < 0.
5. Thread mồ côi / queued lâu / dead · `outreach_threads where active==true` nhóm theo `pid` đối chiếu `fb_accounts.active/needLogin`; `outreach_tasks where status=='queued'` có `createdAt` > 60'; `status=='dead'` 7 ngày.
6. Cùng người bị chạm nhiều lần · `leads where outreach.at>0` nhóm theo `author_url` (fold) đếm > 1.
7. Mật độ comment theo group/nick/ngày · `outreach_log` 7 ngày action /Bình luận|Trả lời/ → group id từ `post_url` → max/nick/ngày.
8. Severity log engine · `grep -n "console.error\|severity" outreach.js content.js`; `gcloud logging read 'resource.labels.service_name="outreachtick" severity>=ERROR' --limit 5`.
9. Rules match-all · `grep -n "document=\*\*\|outreach_tasks\|outreach_threads\|outreach_usage" ~/firebase-s13/firestore.rules`.
10. funcWebhook key qua query? · `grep -n "query.key\|req.query" outreach.js`; `gcloud logging read 'resource.labels.service_name="funcwebhook"' --limit 3 --format='value(httpRequest.requestUrl)'` (che giá trị).
11. Thời gian tick · `gcloud logging read 'resource.labels.service_name="outreachtick" httpRequest.latency>="200s"' --limit 10`.
12. Lead thiếu temp lúc enqueue · thread `fpayload.steps==['react']` mà lead hiện hot/warm → đếm (bằng chứng C-3).

## D. Đề xuất thông minh
1. **Kế toán van lúc THỰC HIỆN + `reservedDay`** — worker cộng usage khi bước done, engine kiểm `usage+queued<cap`; khác ngày → reserve lại; refund chỉ cùng ngày, clamp ≥0. An toàn nick (đóng C-4/C-5). `tryConsume` + nhánh retry + worker `logStep`. Vừa.
2. **Hàng đợi lead ưu tiên + dấu `leads.outreach.queued_at`** — điểm = temp × suy giảm tuổi, trần 7 ngày, bỏ lead có người thật; query máy chủ thay `tref.get()`. Tỉ lệ chốt (hot trước) + read ↓ ~90%. `stepNickAdspower` + index `leads(brand, outreach.queued_at, score)`. Vừa.
3. **Khoá theo NGƯỜI (`outreach_people`)** — uid/author_url/phone, chạm < 30 ngày hoặc đã reply → bỏ; cùng người → chỉ reply. An toàn nick + trải nghiệm khách. `apEnqueueFunnel` + worker `ensureUid`. Nhỏ.
4. **Van theo group + rải giờ mở cửa** — ≤3 comment/group/nick/ngày, ≤8/group/brand; 08:00 rải `nextFreeAt` 0–120'. Né admin group, hết burst 1 proxy. `tryConsume` khoá `g:<gid>`, đầu ngày trong tick. Nhỏ.
5. **Quality gate nội dung** — `spam ≥ 7` sinh lại; Jaccard với 20 comment gần nhất > 0,7 → sinh lại; CTA paraphrase pool; `stripForbid`/lọc SĐT 3 chế độ; đồng bộ ngôi. An toàn nick + tỉ lệ phản hồi. content.js hậu xử lý. Nhỏ–Vừa.
6. **Bắt reply qua trang Thông báo** — cuối phiên mở `facebook.com/notifications`, khớp `post_id/comment_id` → `replied` + stage + push; fallback recheck permalink 6 h/24 h. Tỉ lệ chốt, phủ lead không uid. worker `checkReplies`. Vừa.
7. **Follow-up inbox lần 2 sau 24–48 h** — bước `followup` 1 lần, chỉ khi chưa reply, AI 1 câu hỏi khác lần 1, van ≤4/nick/ngày. Tỉ lệ phản hồi +30–50% ở tin lạnh. `apEnqueueFunnel` + prompt + worker inbox cùng thread. Vừa.
8. **Health check trong tick + `system_status/outreach`** — brand on & 0 nick sống · queued > 60' · dead hôm nay · nick ốm → log JSON severity ERROR (email qua policy #9) + banner FE + Hộp việc super. Vận hành. Nhỏ.
9. **Sweeper thread mồ côi** — nick không sống > 2 h → round-robin nick sống cùng brand hoặc `orphan` + hoàn cap; > 72 h → đóng. Hết `_l34_move` tay. 1 query/tick. Nhỏ.
10. **Dừng máy khi người thật vào** — mở rộng trigger `statsOnLead`: stage rời `new` / có `assignee`/`first_care_at`/`closed_at`/`lost` → thread `active:false` + task queued → skipped + hoàn cap. Đóng C-1, không cần Rules. Nhỏ.
