# LỆNH #48 (12/09/2026) — LỆNH A "AI hardening" (PB-5 · PB-3 · PB-4 · PB-2 a/b/d · PB-1 · 2 vá scraper) — CHỜ ANH CHẠY

> Bản lưu; **khối lệnh thật dán trong chat** (anh copy lên Cloud Shell). File: `lenh-2026-09-12-48.sh` (KHỐI 1 = `_l48_patch.cjs` + `_l48_after.mjs` heredoc quoted, 0 dấu `!` ngoài heredoc) · `lenh-2026-09-12-48-patch.cjs` (patch, 54 mốc) · `lenh-2026-09-12-48-after.mjs` (KHỐI 2 chỉ đọc) · `lenh-2026-09-12-48-fixture/` (mã ĐANG CHẠY dựng nguyên văn từ dump #47/#47d: index.js 148–908 · lib/scorer.js · lib/scraper.js · lib/config.js + `stub48.mjs` Firestore giả) · `harness-lenh48-2026-09-12.mjs` (**58/58 PASS**).
> Theo báo cáo rà soát quét→lọc→chấm điểm 11/09 (`docs/rasoat-scan-2026-09-11/`) + 8 chốt của anh 12/09 + LỆNH #47/#47b/#47c/#47d + KHỐI 0 LỆNH B (record thô BrightData).

## 1. Sửa gì (4 file, marker `LENH #48`, fail-closed NGUYÊN TỬ: đủ 54 mốc cả 4 file mới ghi)

| Mã | File | Thay đổi |
|---|---|---|
| **PB-3** | `lib/scorer.js` `llmChat46` | model `gpt-5*`/`o*` gửi `reasoning_effort` (`LLM_REASONING`, mặc định `low`) + `max_completion_tokens` (`LLM_MAX_TOKENS` 2000); API từ chối → gửi lại không kèm; `finish=length` → nới gấp đôi 1 lần; JSON hỏng → thử lại ĐÚNG 1 lần; 400 bất kỳ → thử 1 lần KHÔNG json mode rồi mới `badreq`; timeout mặc định **45 s** (`LLM_TIMEOUT_MS`, #46 là 60 s); **ngân sách 90 s/bài** (`LLM_POST_BUDGET_MS`) — chờ kế vượt ngân sách thì dừng thử lại; `usage.reasoning` đọc từ `completion_tokens_details`; `prefilterLead(post, brandAi, {tries})` cho breaker |
| **PB-3e** | `lib/scorer.js` `scoreLead.replyOnly48` | model quên `reply` → gọi RIÊNG 1 lượt, prompt = nhu cầu ĐÃ cấu trúc (không text thô), `maxTokens` 600 — thay cho "chấm lại toàn bài tối đa 2 lần" |
| **PB-5a/b** | `lib/scorer.js` `heuristic(text, source, brandAi)` | dự phòng biết **Hồ sơ AI brand** (`brandKw48`: bigram từ nganh/dichvu/khach, bỏ từ dừng, ≤80 cụm, +25/+15) thay vì thuần từ khoá agency; có Hồ sơ AI → KHÔNG gán `service:'Performance Marketing'`; **thiếu LLM_API_KEY → điểm tạm KẸP ≤59 + `_nokey`** (trước: 90 điểm NÓNG) |
| **Scraper** | `lib/scraper.js` `normalizePost` | `profile_id` (uid số 16 ký tự, KHỐI 0) → `user_url = profile.php?id=<uid>` + `author_uid`; `user_url` có sẵn → cắt tham số tracking + lấy uid từ `/people/<tên>/<uid>/`, `profile.php?id=`, `/user/<uid>`; map thêm `num_comments`, `gid` (`group_id`) — cho LỆNH B |
| **Scraper** | `lib/scraper.js` `normalizeComment` | map **`commentator_profile_url`** (cắt `?__cft__…`) → `user_url` + `author_uid`; không có → `user_id` (pfbid) làm link dự phòng `profile.php?id=pfbid…` (author_uid rỗng); `comment_id` base64 `comment:<post>_<cid>` → số. ⇒ kết bạn/inbox mở cho ~100 % lead-bài + phần lớn comment-lead (trước: ~31 % / 0 %) |
| **PB-1a** | `index.js` Pha 3a | **lease ứng viên** `score_retry/R_<post_id>` `{kind:'lease', tries 0, nextAt +30′, leaseBy runId}` NGAY sau khi dựng ứng viên (1 batch); gỡ (`settle48`) CHỈ khi bài đi hết đường: ghi lead xong / không phải lead / prefiltered_out; lỗi giữa chừng (eKYC, commit, lượt bị cắt) → doc còn → lượt lịch sau nạp chấm lại (không cần BrightData lấy lại) |
| **PB-2d** | `index.js` Pha 2 | `seen` ghi bằng **`create()`** — 2 lượt chồng cùng bài → ALREADY_EXISTS → đọc lại, bỏ bài lượt kia đã nhận, ghi lại phần còn lại (`scans.seenRace`); trước: `set()` trước quyết định → lượt lỗi mất bài im lặng |
| **PB-2b** | `index.js` nạp `score_retry` | nạp bài chờ bằng **`runTransaction`** (lease 15′ `leaseBy`) → 2 lượt chồng chỉ 1 lượt chấm (hết chấm đôi/lead trùng) |
| **PB-2a** | `index.js` | `sow46 = trigger==='scheduled'` — "Quét ngay"/backfill không nạp hàng chờ, không chạy sweeper (không chồng việc lượt lịch) |
| **PB-1c** | `index.js` | **trần mềm** `SCAN_SOFT_DEADLINE_S` 1200 s: quá → bài còn lại KHÔNG chấm (giữ lease, `scans.softStop`), sweeper bỏ qua, `system_status/scan` |
| **PB-1d** | `index.js` `mapPool` | bọc trọn 1 bài `try/catch` → lỗi eKYC/commit/… chỉ rớt bài đó (`[pipe]`, `scans.pipeErrors`, `scanned_posts` decision `error`, lease giữ), không rớt cả lượt |
| **PB-1e** | `index.js` wrapper | `scanAll` ném lỗi → `scans {status:'aborted', error}` + `system_status/scan {phase:'aborted'}` + console ERROR `[SCAN-ABORTED]` (alert #9) rồi ném tiếp; `system_status/scan` phase starting/done mỗi lượt |
| **PB-3d** | `index.js` | **circuit-breaker** trong lượt: 3 bài chấm hỏng liên tiếp & 0 OK → bài còn lại chỉ thử 1 lần (`[LLM-CB]`, `scans.cbOpen`) — lượt không đốt 300 s vào retry |
| **PB-4c** | `index.js` | 400 lặp: bài 1 dự phòng ≤59, từ bài 2 (`badreq48 ≥ 2 & 0 OK`) = lỗi hệ thống → xếp hàng (`kind badreq`), `[LLM-DOWN] badreq` — không đẻ lead heuristic mỗi bài |
| **PB-4a** | `index.js` giám sát | **cửa sổ trượt 15′** (`system_status/llm.win` ≤5 lượt): DOWN khi Σfail ≥ 2 & Σok = 0 (kích cả khi mỗi lượt 1 bài — ngưỡng cũ "≥2 hỏng TRONG lượt" không bao giờ kích); **hysteresis**: đang DOWN cần ≥2 OK mới `[LLM-UP]`; cửa sổ lưu cả khi doc chưa có |
| **PB-4b** | `index.js` giám sát | tầng 1: Σ preFail ≥ 3 & 0 preOk → `llm.pre=false` + WARNING `[LLM-PRE-DOWN]` (vẫn fail-open), hồi → `[LLM-PRE-UP]`; `scans.llmPreOk` |
| **PB-5** | `index.js` sweeper | chỉ chạy khi có key & breaker chưa mở & chưa quá trần mềm; lease `rescore_lease` 10′ (2 lượt không chấm cùng lead); bỏ lead `rescore_err` badreq/4xx < 24 h; **sales đang chăm + AI nói không phải lead/vai → GIỮ `temp` ≥ cold + `ai_flag` (`not_lead` / `role_<vai>`) + `base_score`** — không ghi `junk` (web không ẩn lead đang chăm); AI nói là lead → gỡ `ai_flag`; lead gộp ≥2 nhóm → chấm trên MỌI lần chạm (`touches[].text` ghép `---`) |
| **PB-3g** | `index.js` eKYC | lead **LẠNH** không kiểm eKYC lúc quét (`zalo_defer:true`, `ZALO_CHECK_COLD=false`) — tiết kiệm ~40–50 % lượt eKYC; quét-vét eKYC bỏ lead `zalo_defer` chưa ai chăm (có assignee/first_care/last_touch → kiểm) |
| **PB-1b** | `index.js` nạp `score_retry` | nguồn tạm tắt → GIỮ bài chờ + hoãn 6 h (trước: xoá ngay = mất bài); quá 7 ngày mới bỏ |
| — | `index.js` lead | thêm `author_uid`, `zalo_defer`; `scans` thêm `llmPreOk pipeErrors softStop seenRace leased cbOpen badreq model tokensReasoning runId status` |
| — | `lib/config.js` | `LLM_TRIES` · `LLM_TIMEOUT_MS` 45000 · `LLM_POST_BUDGET_MS` 90000 · `LLM_REASONING` low · `LLM_MAX_TOKENS` 2000 · `SCAN_SOFT_DEADLINE_S` 1200 · `ZALO_CHECK_COLD` false (đều tuỳ chọn trong `.env`, không cần thêm gì) |

**Không nằm trong #48 (cố ý)**: khoá lượt TTL transaction + lead id tất định + gieo/gặt theo group + nhịp thích ứng → LỆNH B; PB-10 push gate / PB-6 stats recount / Rules whitelist / `tagLeadBrand brand_hint` → LỆNH C; PB-9/PB-12/PA-11 → sau. Không cần Rules/index mới (`score_retry` chỉ Admin SDK; query `leads where detected_at >=` single-field).

## 2. Kiểm trước khi gửi
- **Fixture = mã đang chạy** (sha8 4 file khớp #47/#47d: index.js `14a3a823` · scorer `12223e1c` · scraper `66714197` · config `81d91f79`) → **54 mốc mỗi mốc đúng 1 lần trong VĂN BẢN DUMP** (harness đối chiếu tự động).
- Harness `harness-lenh48-2026-09-12.mjs` **58/58**: patch áp/idempotent/`node --check`/fail-closed nguyên tử (thiếu 1 mốc scraper → exit 1, không ghi file nào)/LỆCH · scorer 13 ca (reasoning, từ chối, finish=length, JSON hỏng 1 lần, 400 → không json, ngân sách, prefilter tries, bigram brand, service, nokey ≤59, replyOnly48) · scraper 7 ca (profile_id, tracking, 3 dạng URL, pfbid) · **index.js chạy TRỌN `scanAll` trên Firestore giả 18 kịch bản** (C1 lượt thường · C2 500 + breaker 14 lượt gọi + [LLM-DOWN] · C3 nạp lại + [LLM-UP] · C4 manual không nạp · C5 2 lượt chồng → 1 lead · C6 seen ALREADY_EXISTS · C7 trần mềm giữ lease rồi 31′ sau chấm · C8 eKYC ném ở bài mới + bài chờ → lease/doc chờ GIỮ · C9 badreq hệ thống · C10 cửa sổ 1 bài/lượt ×2 → DOWN, 2 OK → UP · C11 [LLM-PRE-DOWN/UP] · C12 sweeper human/lease/badreq/touches/ai_flag · C13 nokey · C14 eKYC lạnh + quét-vét · C15 replyOnly48 · C16 aborted · C17 nguồn tắt 6 h/7 ngày · C18 backfill).
- Harness đã bắt 2 lỗi patch trước khi gửi: (1) `await unlease48(x)` nối SAU dấu `//` của comment cũ → thành comment, lease không bao giờ gỡ; (2) cửa sổ 15′ chỉ lưu khi doc `system_status/llm` đã tồn tại → doc mới không bao giờ tích luỹ 2 lượt hỏng. Cả hai đã sửa (lease/doc chờ giờ gỡ ở CUỐI qua `settle48`).
- Dry-run `lenh-2026-09-12-48.sh` với fake `firebase`/`gcloud` + fixture UNPATCHED: lần 1 PATCH OK → import OK → LIVE LỖI net (không chặn) → deploy giả → exit 0; lần 2 idempotent, exit 0. `_l48_after.mjs` chạy trên shim firebase-admin: in đủ 5 mục, exit 0. Quét secret: sạch.

## 3. KHỐI 1 (dán vào Cloud Shell) — xem file `lenh-2026-09-12-48.sh` (khối trong chat = `cat > /tmp/l48.sh <<'EOF_L48'` … `EOF_L48` rồi `bash /tmp/l48.sh`)
Trình tự: backup 4 file `.bak-<TS>` → `_l48_patch.cjs` (fail-closed) → `node --check` ×4 → import test `.env` (in `cfg46 tries/timeout/budget/reasoning`, `SCAN_SOFT_DEADLINE_S`, `ZALO_CHECK_COLD`) → (c) gọi thử `scoreLead` THẬT 1 lần với Hồ sơ AI mẫu (in model · giây · hotness · token + reasoning) — lỗi KHÔNG chặn → deploy `scheduledScan,manualScan` xích `&&` → describe. Lỗi ở (a)/(b) tự khôi phục từ `.bak`.

## 4. KHỐI 2 (sau ≥ 15′, tốt nhất 1–2 giờ có bài mới; chỉ đọc)
```bash
cd ~/firebase-s13/functions && node _l48_after.mjs
```
Kỳ vọng: `system_status/llm` có `win` (cửa sổ) + `pre=true` + `cbOpen=false` · `system_status/scan phase=done` · `score_retry` = 0 hoặc vài `lease` của lượt đang chạy (lease >2 h = lượt bị cắt, lượt sau tự nạp) · 12 lượt: `softStop 0`, `pipeErrors ≈ 0`, `seenRace 0`, `status done`, `rt` (reasoning token) > 0 với gpt-5 · **lead 24h: author_uid số bài ≈100 %, bình luận cao**, không còn link mang tracking, `zalo_defer` ≈ số lead lạnh có SĐT · log: `[LLM-CB]/[LLM-PRE-DOWN]/[SCAN-ABORTED]/[pipe]` = 0 hoặc rất ít.

## 5. Sau #48
LỆNH B (hướng B 1 group nhiều brand + tốc độ + PC-4; cần 3 số BrightData dashboard: trần snapshot đồng thời/request-phút · đơn giá posts/comments USD/1k record + record lỗi có tính tiền · gói có `notify` webhook) → LỆNH C (PB-10 push "brand xuất hiện", PB-6 recount SAU khi #48 chạy ổn, Rules whitelist `dropped_reason/ai_feedback`, `tagLeadBrand brand_hint` dòng 75 codebase2) → zip FE (PA-4, PC-6, PA-11, `no_text`, B UI, `llm.pre`).

## KẾT QUẢ
### KHỐI 1 — ĐÃ CHẠY OK 12/09 15:51–15:53 VN (08:51–08:53 UTC), exit=0 (anh gửi 2 ảnh Cloud Shell)
- sha256 `/tmp/l48.sh: OK` (khối dán trong chat khớp file byte-identical) · backup `lib/config.js · lib/scorer.js · lib/scraper.js · index.js` `.bak-20260912-085129`.
- (a) `PATCH OK 4 file (LENH #48)` · marker `LENH #48`: config.js **1** · scorer.js **12** · scraper.js **6** · index.js **45** (đúng số dự kiến từ fixture).
- (b) `IMPORT OK · scheduledScan = function · manualScan = function · harvestComments = function · replyOnly48 = function · cfg46 tries 4 timeout 45000 ms budget 90000 ms reasoning "low" maxTokens 2000 · CFG soft 1200 s · ZALO_CHECK_COLD false · model gpt-5.6-sol / gpt-5-nano`.
- (c) gọi thật: `LIVE OK · gpt-5.6-sol · 4,4 s · hotness 95 · is_real_lead true · role buyer · reply 140 ký tự · token 999 + 165 (reasoning 0) · lượt gọi 1` → `reasoning_effort` + `max_completion_tokens` được API nhận ở lượt đầu (không 400, không gửi lại); reasoning 0 token = effort `low` gần như không tốn token suy nghĩ với bài ngắn (KHỐI 2 cột `rt` sẽ cho số thật trên lượt quét).
- (d) `✔ Deploy complete!` → `scheduledScan ACTIVE 2026-09-12T08:53:10Z` · `manualScan ACTIVE 2026-09-12T08:53:03Z`.
### KHỐI 2 — NGHIỆM THU PASS 12/09 16:07 VN (anh chạy `node _l48_after.mjs` ~14′ sau deploy, 1 ảnh Cloud Shell)
- **1. `system_status/llm`: `ok=true` · at 16:06 · hồi lúc 11/09 17:14 (từ #46) · model gpt-5.6-sol / tầng 1 gpt-5-nano.** #48: **cửa sổ 15′ = 5 lượt** `[15:54 ok2 fail0 preOk3 · 15:57 ok2 fail0 preOk5 · 16:00 ok0 fail0 preOk2 · 16:03 ok1 fail0 preOk5 · 16:06 ok1 fail0 preOk3]` (preFail 0 hết) · tầng 1 `pre=true` · breaker lượt gần nhất `cbOpen=false`; có field `win` → PB-4 ghi cửa sổ ngay từ lượt đầu (đúng bài học "lưu cả lần đầu").
- **1b. `system_status/scan`: `phase=done`** · runId `scheduled_mty5uell_kdx7b1` · trigger scheduled · lượt xong 16:06 14 s · softStop 0 · pipeErrors 0 → PB-1 ghi pha bắt đầu/kết thúc đúng, không lượt nào `aborted`/`starting` treo.
- **2. `score_retry`: 0** → không lease ứng viên >2 h, không bài chờ (OpenAI khoẻ suốt 5 lượt; `settle48` gỡ lease đúng ở cuối pipeline).
- **3. 5 lượt sau deploy** (giờ · giây · bài/lead · leased · reasoning tok): `16:06 14 s 0/0 leased 3 rt 59 · 16:03 15 s 5/1 leased 5 rt 111 · 16:00 8 s 0/0 leased 2 rt 0 · 15:57 16 s 4/0 leased 5 rt 269 · 15:54 17 s 7/1 leased 3 rt 130` — `cb false`, `status done` hết; **tổng: soft 0 · pipe 0 · seenRace 0 · aborted 0** (đúng kỳ vọng). Lượt vẫn 8–17 s (không chậm đi so với 15 s trung vị #47). `rt` 59–269 token/lượt = `reasoning_effort low` có tốn token suy nghĩ thật trên bài dài (khác test (c) bài ngắn rt 0) — chi phí nhỏ, giữ `low`. Lượt trước deploy in `(lượt TRƯỚC deploy #48)` như thiết kế.
- **4. Lead 24h: 92 (bài 83 · bình luận 9).** `author_uid` số: **bài 2/83 = đúng 2 lead-bài tạo SAU deploy (15:54 + 16:03) → 100 % lead-bài mới có uid số** (81 lead cũ trước #48 không có là bình thường); bình luận 0/9 — cả 9 đều trước deploy → **mapping `commentator_profile_url` CHƯA được kiểm trên dữ liệu thật** (theo dõi: comment-lead đầu tiên sau 15:53 VN phải có `author_uid`/`author_url` dạng `/people/…` hoặc pfbid). `author_url` có: dạng `{trống 65, username 15, profile.php?id=số 8, pfbid 4}` · **0 link mang tracking** (cắt `?` chạy đúng). `zalo_defer` 0 / lạnh 27 / có SĐT 0 → đúng (chỉ hoãn eKYC khi lead lạnh CÓ SĐT; 27 lead lạnh 24h không có SĐT). `ai_flag` 0 (sweeper chưa gặp lead sales đang chăm bị AI nói không phải lead) · **`ai_scored:false` = 0 → backlog 252 lead điểm tạm của #46 đã xử lý hết.**
- **5. Log 24h: 52 dòng `{LLM-RESCORE 26, khác 26}`**, dòng mới nhất 11/09 11:48 UTC (= 18:48 VN hôm qua) → **0 dòng `[LLM-CB]/[LLM-PRE-DOWN]/[LLM-DOWN]/[SCAN-ABORTED]/[pipe]/[seen]/[lease]` kể từ deploy** (5 lượt sạch). Nếu sau này thấy `[LLM-CB]` → OpenAI đang lỗi đợt, lượt sau tự nạp `score_retry`.
- **Kết luận: LỆNH #48 (LỆNH A) HOÀN TẤT — KHỐI 1 15:53 + KHỐI 2 PASS 16:07 VN.** Còn theo dõi 2 điểm (không chặn): (1) comment-lead đầu tiên sau deploy có `author_uid` (mapping `commentator_profile_url` + pfbid dự phòng); (2) 1 lượt thấy `rt` cao bất thường (>1.000) → cân nhắc `LLM_REASONING=minimal` nếu API hỗ trợ. Có thể chạy lại `node _l48_after.mjs` bất kỳ lúc nào (chỉ đọc).
