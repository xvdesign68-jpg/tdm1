# LỆNH B · KHỐI 0 (12/09/2026) — CHỈ ĐỌC, chuẩn bị cho LỆNH B (hướng B "1 group nhiều brand" + tốc độ)

Khối lệnh thật = `docs/lenh-2026-09-12-b0.sh` (dán vào chat dạng `cat > /tmp/b0.sh <<'B0EOF' … B0EOF` rồi `bash /tmp/b0.sh`). Output: màn hình + `~/scan-dump-0912e.txt` → `cloudshell download` rồi upload vào chat.

## Vì sao cần
Sau #47d (`docs/lenh-2026-09-12-47.md` mục "#47d — KẾT QUẢ") còn 3 mốc B chưa có mã/dữ liệu thật:
- (a) **`tagLeadBrand` đang deploy từ `~/codebase2/tagLeadBrand`** (lưới dự phòng gán brand theo nguồn cho lead `brand_pending:true`) — chưa dump bao giờ; B fan-out theo brand nên phải biết nó tra brand theo gì (tên nguồn? URL?) để ghi `brand_hint` và vá ưu tiên (§9.15 thiết kế B). Kèm trạng thái deploy (region/runtime/trigger/updateTime) của tagLeadBrand · notifyBrandZalo · sendZaloZBS + mtime 3 bản index.js để biết bản nào đang chạy.
- (b) zalo-fn `getConfig()` — doc cấu hình Zalo (chỉ cần khi LỆNH C vá `notifyBrandZalo` bỏ qua lead `dropped/ai_scored:false`, §9.16).
- (c) **Tên field thô record BrightData** (bài + bình luận): `normalizePost` không map số bình luận (#47d mục 1) → §9.4 "gieo comment chỉ cho bài có bình luận" cần biết dataset có `num_comments`/`comments` lồng/`group_id` không; §9.3 chuẩn hoá gid slug→số cần biết record có id group số không.

## Cách (c) lấy record mà không tốn tiền
`_b0_bd.mjs` (đặt trong `~/firebase-s13/functions`, Admin SDK chỉ đọc): đọc `pending_snapshots` (gieo mỗi ~10′/nguồn, gặt 3′ sau) → hỏi `progress` từng snapshot (≤12 bài + ≤6 bình luận/vòng, 8 vòng × 15 s) → snapshot nào `ready` thì GET json (idempotent, không ảnh hưởng lượt gặt) → in **tên field + kiểu + giá trị số/ngày/url/id**; **che giá trị** nội dung/danh tính (`HIDE` = text/content/message/name/user/profile/author/image/email/phone…). Có `comments` lồng → in thêm field của phần tử đầu. Dòng `TÓM:` gom field liên quan BÌNH LUẬN / GROUP / id-url.
Ngoại lệ duy nhất: sau 8 vòng không snapshot bài nào chín → **GIEO 1 snapshot 1 bài** (`num_of_posts:1`, không `posts_to_not_include`) ở nguồn có nhiều bài gần nhất (theo `scans.bySource` 6 lượt) ≈ 1 record ≈ 0,001–0,003 USD, chờ chín ≤ 5′. Tắt bằng `B0_NO_TRIGGER=1 bash /tmp/b0.sh`.

## Kiểm trước khi gửi
- `bash -n` OK; 0 dấu `!` ngoài heredoc (bài học Cloud Shell history-expansion); `node --check` phần mjs OK.
- Dry-run cây giả (stub firebase-admin + stub `fetch` BrightData + fake gcloud, scratchpad `l47/`): **A** snapshot chín → in đủ 18 field bài + 11 field bình luận, che text/name/user_url · **B** không chín → gieo 1 bài → chín → in field · **C** `B0_NO_TRIGGER=1` → "KHÔNG lấy được record bài (đã tắt gieo)" · **D** thiếu `BRIGHTDATA_TOKEN` → thoát mã 2 có thông báo. Quét output: 0 token/IP/nội dung bài/tên người lộ.
- Bug bắt được lúc dry-run: `QuerySnapshot.forEach` không có trong stub → script dùng `.docs.forEach` (SDK thật có cả 2); `[ "$rc" != 0 ]` có `!` ngoài heredoc → `-ne`.

## KẾT QUẢ (anh chạy 12/09 07:44Z = 14:44 VN; 5 ảnh + file text `scan-dump-0912e.txt` anh upload ~15:10 VN → lưu `docs/lenh-2026-09-12-47-dump/scan-dump-0912e.txt`, quét secret sạch — mọi mốc vá `tagLeadBrand` neo được bằng text)

### (a) `~/codebase2/tagLeadBrand/index.js` — 94 dòng, ĐÚNG là lưới dự phòng gán brand THEO TÊN NGUỒN
- `onDocumentCreated('leads/{leadId}')` (gen2, `setGlobalOptions({region:'asia-southeast1', maxInstances:10})` trong code nhưng describe cho thấy `maxInstanceCount 100`, timeout 60 s, runtime nodejs22; deploy 2026-08-11T09:10Z; `package.json` name `smartlead-tag-lead-brand`, node 22, deps firebase-admin + firebase-functions).
- Logic: lead lúc **tạo** có `brand` khác rỗng → `[skip]` return · không có `source` → warn return · `getBrandForSource(lead.source)` = `sources.where('name','==',sourceName).limit(1)` (cache map name→brand 60 s) → không ra brand → `set({brand_pending:true, brand_tagged_at})` merge · ra brand → `set({brand, brand_pending: delete, brand_tagged_at, brand_tagged_by:'tagLeadBrand'})` merge.
- ⇒ **Race với birthstamp inline của s13** (`commitLeadNow` `ref.set(lead)` không brand → `ref.update({brand: brandBySource[lead.source]…})` ~50 ms sau): cả 2 cùng ghi `brand` sau create; hiện nay cùng giá trị nên vô hại. **Với hướng B (1 group → N brand, các source doc có thể TRÙNG TÊN)**: `limit(1)` theo tên → có thể gán brand SAI đè lên brand fan-out → **B bắt buộc**: (1) lead create mang `brand_hint` (= brand fan-out) + `source_id`; (2) inline stamp s13 dùng `lead.brand_hint` thay `brandBySource[lead.source]`; (3) **vá tagLeadBrand** (codebase2, deploy `--only functions:tagLeadBrand`): `const hint = String(lead.brand_hint||'').trim(); const brand = hint || await getBrandForSource(sourceName);` — 1 dòng, fail-safe. Dòng mốc: `const brand = await getBrandForSource(sourceName);` (dòng 75).
- 3 file codebase2 có chữ `brand_pending/tagLeadBrand`: `notifyBrandZalo/index.js` · `tagLeadBrand/index.js` · `sendZaloZBS/index.js` (2 bản đầu = snapshot cũ; bản deploy `notifyBrandZalo` 2026-08-12 từ zalo-fn — mtime zalo-fn index.js 2026-06-30 10:09:44 = mtime codebase2/notifyBrandZalo → cùng nội dung gốc). Deploy: `sendZaloZBS` 2026-08-06 · `tagLeadBrand` 2026-08-11 · `notifyBrandZalo` 2026-08-12 (đều GEN_2 asia-southeast1, `document.v1.created` / `document.v1.updated` `leads/{leadId}`).

### (b) zalo-fn `getConfig()` (dòng 70–73) = `CONFIG_DOC.get()` (doc ref khai phía trên, ngoài 30 dòng; token ở `_system/zalo_tokens` theo thông báo lỗi dòng 85 → config nhiều khả năng cùng nhánh `_system/…`). Đủ cho LỆNH C nếu cần vá ZBS bỏ qua `dropped/ai_scored:false`.

### (c) Field thô BrightData — 2 record thật (snapshot đang chín, KHÔNG phải gieo thêm → 0 chi phí)
- Vòng 1: pending bài 3 / bình luận 1 → `sd_…9pkp6t` running · `sd_…v58mk` ready **records 0 · errors 1** (record đầu = `dead_page`, input `num_of_posts 5 + posts_to_not_include[…]` = probe) · `sd_…dnj2y` ready records 2 → dùng 2 · bình luận `sd_…nyzlqs` ready **records 0 · errors 3** (3 record đều lỗi `"Thi…"` = bài không có bình luận). Vòng 2: bình luận `sd_…x2j3p` ready records 3 · errors 1 → dùng 3.
- **RECORD BÀI — 37 field**: `attachments avatar_image_url content date_posted delegate_page_id following group_category group_created_at group_external_website group_id group_intro group_logo group_members group_name group_url hashtags header_image input is_sponsored location num_comments num_reaction_type num_shares other_posts_url post_external_image post_id post_type posts_to_not_include price profile_id publisher_image_url shortcode timestamp url user_is_verified user_to_not_include user_username_raw`. Giá trị mẫu: `post_id 2182424042305431` · `url https://www.facebook.com/groups/vieclamtotchosv/posts/2182424042305431/` (URL bài dùng **slug** group) · **`group_id 1189400231607822`** (gid SỐ ngay trong record dù nguồn khai slug) · `group_url …/groups/vieclamtotchosv/` · `group_members 24300` · `group_category "Public group"` · **`num_comments 0`** · `num_shares 0` · `num_reaction_type array(0)` · `post_type "Post"` · `date_posted 2026-09-12T07:44:07Z` · `timestamp 07:44:38Z` (lag BrightData ~31 s) · **`profile_id` string 16 ký tự (che theo regex; 16 ký tự = nhiều khả năng uid SỐ của người đăng)** · `user_username_raw` (che) · **KHÔNG có `user_url`/`profile_url`/`user_profile_url`** · `posts_to_not_include array(200)` + `input {end_date,num_of_posts,posts_to_not_include,start_date,url}` (BrightData echo).
- **RECORD BÌNH LUẬN — 21 field**: `attached_images comment_id comment_link comment_text commentator_profile commentator_profile_url date_created input num_likes num_replies parent_comment_id post_id post_url reply source_type subtype timestamp type url user_id user_name`. Mẫu: `comment_id` base64 (`Y29tbWVudDoy…` = `comment:<post>_<cid>`) · `comment_link …/posts/2182418518972650/?locale=en_US&comment_id=21824186356393…` · `post_id 2182418518972650` · `post_url …/posts/2182418518972650/?locale=en_US` · `parent_comment_id null` · `reply false` · `num_replies 0` · `type Comment` · **`commentator_profile_url` string 454 ký tự (che)** · `commentator_profile null` · **`user_id` string 72 ký tự (che — không phải uid số trần; thử base64)** · `user_name` (che) · KHÔNG có `user_url`/`commenter_url`/`commenter_profile_url`.

### File text (bổ sung sau ảnh) — mốc chính xác + chi tiết ảnh không thấy
- **`tagLeadBrand/index.js` 94 dòng nguyên văn** (dòng 17 `require('firebase-functions/v2/firestore')` bị mask vì ≥28 ký tự — không phải secret). Mốc vá LỆNH C (content-anchored, mỗi mốc đúng 1 lần): dòng 32 `async function getBrandForSource(sourceName) {` · dòng 69 `const sourceName = lead.source || '';` · **dòng 75 `  const brand = await getBrandForSource(sourceName);`** → thay bằng `const hint = String(lead.brand_hint || '').trim();` + `const brand = hint || await getBrandForSource(sourceName);` · dòng 86–91 `snap.ref.set({ brand: brand, brand_pending: FieldValue.delete(), brand_tagged_at, brand_tagged_by: 'tagLeadBrand' }, { merge: true })` → thêm `brand_tagged_by: hint ? 'brand_hint' : 'tagLeadBrand'` để phân biệt trong log. `initializeApp()` top-level (dòng 22) hợp lệ vì codebase2 mỗi function 1 thư mục (không đụng bài học #10 của s13). Code 2026-06-30, deploy 08-11.
- **Record bài**: `input {end_date, num_of_posts, posts_to_not_include, start_date, url}` — engine đang gửi cả `start_date/end_date` (đường sow) · `posts_to_not_include` echo đủ 200 phần tử trong MỖI record (nặng payload, không tính tiền) · `shortcode` 16 ký tự (mã bài dạng chữ, không cần) · `user_username_raw = null` (username không có → chỉ còn `profile_id`) · `avatar_image_url` 449 ký tự = CDN có token (không dùng) · lag `date_posted 07:44:07` → `timestamp 07:44:38` = BrightData thấy bài sau ~31 s.
- **Record bình luận**: `comment_id` giải mã = `comment:2182418518972650_2182418635639305` (đúng công thức worker `cmtFeedbackId/cmtParentFbid`) · `comment_link …/posts/<pid>/?locale=en_US&comment_id=<cid>` (cid số) · `post_url`/`url` mang `?locale=en_US` → mọi khoá bài cha phải `urlKey` bỏ query · `commentator_profile_url` **454 ký tự** = URL có tracking (`?__cft__[0]=…&__tn__=`) → cắt tại `?` rồi lấy uid: dạng `/groups/<gid>/user/<uid>/` (bình luận trong group) hoặc `/profile.php?id=` hoặc `/people/<tên>/<uid>/` — regex `/\/user\/(\d{5,})|[?&]id=(\d{5,})|\/people\/[^/]+\/(\d{5,})/`; không ra số → giữ nguyên URL đã cắt query làm `author_url` (worker DOM mở được) · `user_id` **72 ký tự** = độ dài **pfbid** (66–72), KHÔNG phải base64 `user:<uid>` (29 ký tự) → không giải mã; chỉ dùng khi `commentator_profile_url` trống: `author_url = https://www.facebook.com/profile.php?id=<pfbid>` (DOM mở được, không có uid số).
- **Vòng 2** thấy `pending: bài 7` (tăng từ 3) = lượt gieo kế của engine chạy giữa 2 vòng — chu kỳ sow đang sống; script không gieo thêm (không tốn record).

### Kết luận mới (đưa vào thiết kế B §9.18–9.21 + LỆNH A)
1. **`num_comments` CÓ trong record bài** → §9.4 làm được: gieo snapshot bình luận CHỈ khi `num_comments > 0`; bài mới thường 0 → bình luận đến sau → dùng watch_posts (câu 6) re-fetch record bài (1 record) 2 lần/ngày × 2 ngày → `num_comments` tăng mới gieo comment. Hết 84 % record comment rỗng (3/3 lỗi "bài không có bình luận" ở mẫu này).
2. **`group_id` số có sẵn trong record bài** → §9.3 chuẩn hoá gid slug→số học ngay lúc gặt (`group_state.gidNum`) — không cần gọi thêm gì.
3. **⭐ `profile_id` (16 ký tự) = uid số người đăng nhưng `normalizePost` KHÔNG map** (chỉ tìm `user_url/profile_url/user_profile_url` — dataset này không có) → đây là lý do chỉ ~31 % lead có `author_url` và "0/12 lead có uid số" → kết bạn/inbox chỉ chạy được cho ~1/3 lead. Vá 2 dòng (`user_url = 'https://www.facebook.com/profile.php?id=' + profile_id` khi `/^\d{5,}$/` + `author_uid`) → **kết bạn/inbox mở cho ~100 % lead-bài, `gqlFriend` (API nội bộ) dùng được thay DOM**. Đề xuất gộp vào LỆNH A (độc lập với B, giá trị cao) — kiểm `profile_id` là số ngay trong KHỐI kiểm của A.
4. **`commentator_profile_url` KHÔNG được `normalizeComment` map** (code tìm `commenter_url/commenter_profile_url`) → comment-lead **luôn** trống `author_url` → chưa bao giờ kết bạn/inbox comment-lead. Vá: thêm `c.commentator_profile_url` (cắt `?…` tracking 454 ký tự) + rút uid số theo 3 dạng URL; `user_id` 72 ký tự = pfbid → chỉ làm `author_url` dự phòng, không giải mã. Gộp LỆNH A.
5. `comment_link` có `?locale=en_US&comment_id=` → worker `findCommentEl` regex `[?&]comment_id=` OK; `post_url` bình luận mang `?locale=en_US` → `urlKey` bỏ query khớp bài cha ✓ (57 mồ côi là slug↔số, không phải locale).
6. Record lỗi snapshot bình luận (3/3) và `dead_page` bài = record `error` không `url` → `good()` loại đúng; có tính tiền hay không vẫn chờ 3 số dashboard BrightData.
