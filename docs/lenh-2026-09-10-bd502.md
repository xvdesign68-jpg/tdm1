# Cảnh báo `[BRIGHTDATA-DOWN]` 10/09/2026 17:28 VN — BrightData trả 502 Bad Gateway (chỉ đọc)

Mail alert policy LỆNH #9 → log ERROR `[BRIGHTDATA-DOWN] 9/23 nguồn lỗi BrightData, 0 nguồn OK (từ 2026-09-10 17:28 VN): trigger 502: <html>…502 Bad Gateway…nginx` (rev `scheduledscan-00070-waj`). Đây là bộ dò bdwatch của LỆNH #21 kích đúng thiết kế: lượt quét gieo 9 nguồn đến hạn → cả 9 nhận HTTP 502 từ nginx của BrightData = máy chủ BrightData lỗi (khác sự cố 04/09 `400 Customer is not active` = tài khoản). Engine tự thử lại mỗi 3′; khi BrightData hồi, lượt kế ghi `[BRIGHTDATA-UP]` (WARNING, không kích alert) + `system_status/brightdata.ok=true` → banner web tắt. Không mất lead: nguồn chưa gieo được sẽ gieo lại lượt sau.

Khối kiểm (chỉ đọc; curl BrightData chỉ in mã HTTP, không in token):

```bash
cat > /tmp/bd502.sh <<'BD'
set -u
cd ~/firebase-s13/functions || { echo 'KHONG VAO DUOC ~/firebase-s13/functions'; exit 1; }
cat > _bd502.mjs <<'EOF_H'
import admin from 'firebase-admin';
if (!admin.apps.length) admin.initializeApp();
const db = admin.firestore();
const toMs = v => v && v.toMillis ? v.toMillis() : (v && v._seconds ? v._seconds * 1000 : (typeof v === 'number' ? v : 0));
const vn = ms => new Date(ms + 7 * 3600e3).toISOString().slice(5, 19).replace('T', ' ');
const ss = await db.doc('system_status/brightdata').get(); const s = ss.exists ? ss.data() : {};
console.log('system_status/brightdata:', ss.exists ? JSON.stringify({ ok: s.ok, since: s.since ? vn(toMs(s.since)) : null, recoveredAt: s.recoveredAt ? vn(toMs(s.recoveredAt)) : null, runs: s.runs, errors: s.errors, sample: String(s.sample || '').replace(/<[^>]+>/g, ' ').replace(/\s+/g, ' ').slice(0, 120) }) : '(chưa có)');
const sc = await db.collection('scans').orderBy('at', 'desc').limit(10).get();
sc.forEach(d => { const x = d.data(); const bs = Array.isArray(x.bySource) ? x.bySource : []; let ok = 0, skip = 0, err = 0; bs.forEach(r => { if (r.bd === 'ok') ok++; else if (r.bd === 'skip') skip++; else if (r.error) err++; });
  const e1 = (bs.find(r => r.error) || {}).error; console.log(vn(toMs(x.at)), '| dur', Math.round((x.durationMs || 0) / 1000) + 's', '| nguồn', x.sourcesCount, '| bd ok/skip/err', ok + '/' + skip + '/' + err, '| bài', x.postsFetched, '| bdRec', x.bdRecords, '| lead', x.leadsCreated, e1 ? '| lỗi mẫu: ' + String(e1).replace(/<[^>]+>/g, ' ').replace(/\s+/g, ' ').slice(0, 70) : ''); });
EOF_H
node _bd502.mjs; rm -f _bd502.mjs
echo "=== log DOWN/UP 4 giờ qua ==="
gcloud logging read 'resource.type="cloud_run_revision" AND resource.labels.service_name="scheduledscan" AND (textPayload:"BRIGHTDATA" OR jsonPayload.message:"BRIGHTDATA")' --freshness=4h --order=desc --limit=12 --format='value(timestamp,severity,jsonPayload.message,textPayload)' 2>/dev/null | sed -E 's/<[^>]+>/ /g; s/[[:space:]]+/ /g' | cut -c1-200
echo "=== BrightData API từ Cloud Shell (chỉ mã HTTP; 404/400 = BrightData sống, 502/503 = BrightData lỗi) ==="
set -a; . ./.env 2>/dev/null; set +a
curl -s -o /dev/null -w 'progress endpoint: HTTP %{http_code} (%{time_total}s)\n' --max-time 25 -H "Authorization: Bearer ${BRIGHTDATA_TOKEN:-}" "https://api.brightdata.com/datasets/v3/progress/khong-ton-tai"
curl -s -o /dev/null -w 'trang chủ api: HTTP %{http_code}\n' --max-time 25 "https://api.brightdata.com/"
BD
bash /tmp/bd502.sh
```

Đọc: `system_status.ok` false + lượt gần nhất `bd ok 0` + curl 502 → BrightData còn lỗi, chỉ chờ (kiểm lại sau 15–30′; quá 2 giờ → mở ticket BrightData / xem status page của họ). `ok true` hoặc curl 404/400 + lượt mới có `bd ok > 0` → đã hồi, bấm "Close alert" trong mail/console (không bấm cũng tự đóng sau 24 h).

## ★ KẾT QUẢ (anh chạy 10/09 ~17:40 VN) — SỰ CỐ ĐÃ TỰ HỒI SAU 1 LƯỢT (3 phút)
- `system_status/brightdata`: `ok:true`, since 17:28:07 → recoveredAt **17:31:09** (errors 9, sample "trigger 502 … nginx").
- 10 lượt quét: 17:03→17:25 bình thường (bd ok 7–18/lượt, có bài/lead); 17:25 có 1 nguồn "snapshot fetch 500" (dấu hiệu sớm); **17:28 lượt lỗi: bd ok/skip/err 0/14/9, 2 s, 0 bài**; **17:31 hồi: ok 21/2/0, 33 s, 5 bài, 5 bdRec**.
- Log: 10:28:07Z ERROR `[BRIGHTDATA-DOWN]` → 10:31:09Z WARNING `[BRIGHTDATA-UP] BrightData hoạt động lại`.
- curl từ Cloud Shell: progress endpoint HTTP 404 (= BrightData sống, id không tồn tại), trang chủ api 404.
- Kết luận: BrightData chập ~3 phút (502 phía nginx của họ), engine tự hồi lượt kế, không mất lead (nguồn lỗi gieo lại ở lượt 17:31). Alert vẫn "open" vì log hồi là WARNING (đúng thiết kế) → anh bấm **Close alert**, hoặc để tự đóng sau 24 h.
- Tuỳ chọn (chưa làm, chờ anh chốt): bdwatch chỉ ghi ERROR khi **2 lượt liên tiếp** lỗi (chập 1 lượt = 3′ chỉ ghi WARNING) để bớt mail cho sự cố thoáng qua; hoặc giữ như hiện nay để biết mọi lần chập.
