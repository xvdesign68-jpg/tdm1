# LỆNH #49 — `commentator_profile_url` là ẢNH ĐẠI DIỆN, không phải link hồ sơ (bug LỆNH #48) — 13/09/2026

## 1. Phát hiện (13/09 10:18 VN, `_ld_after.mjs` bản 2 mục 6)
- Lead SAU #48 (từ 12/09 15:53 VN): 38 · **bài 35/35 uid số (100 %)** ✓ (`profile_id` → `profile.php?id=<uid>` đúng như #48 dự kiến) · **bình luận 3/3 có `author_url` nhưng cả 3 đều là `https://scontent-*.xx.fbcdn.net/v/t39.30808-1/…`** = URL **ảnh đại diện** trên CDN, không phải trang cá nhân.
- Gốc: LỆNH B KHỐI 0 dump record bình luận **che mọi chuỗi ≥28 ký tự** → em thấy `commentator_profile_url` dài 454 ký tự và ĐOÁN là link hồ sơ kèm tracking `?__cft__` (thật ra là ảnh `…jpg?stp=…&_nc_…`, cùng dạng `avatar_image_url` 449 ký tự của record bài). #48 map thẳng field đó vào `author_url`.
- Hệ quả nếu để nguyên: worker mở URL ảnh thay trang cá nhân → bước kết bạn/inbox comment-lead thất bại 5 lượt (`uiFail`, không trừ Safety nhờ R-9 nhưng đốt lượt), thẻ lead link "trang cá nhân" sai; `author_uid` KHÔNG bị ảnh hưởng (uid số chỉ lấy từ `user_id` khi là số — vd "Anonymous participant" 224550447624797).
- Phạm vi: chỉ comment-lead tạo từ 12/09 15:53 VN (deploy #48) tới lúc deploy #49 (Chủ nhật ít bài → 3 lead lúc phát hiện). Lead-bài KHÔNG bị (dataset bài không có field ảnh trong đường `user_url`).

## 2. Vá (`docs/lenh-2026-09-13-49-patch.cjs`, 1 file `lib/scraper.js`, 4 mốc, marker `LENH #49`, fail-closed NGUYÊN TỬ, idempotent; mốc = mã sau D)
- **M1** helper `fbUrl49(...ứng viên)` = ứng viên đầu tiên là URL host `facebook.com/fb.com/fb.me` và KHÔNG phải CDN (`fbcdn.net`, `/v/t39.`); `profileOf48(raw,id)` chỉ nhận link đó → ảnh bị bỏ → rơi về `user_id` (uid số → `profile.php?id=<uid>`; pfbid → `profile.php?id=<pfbid>` như #48 đã định).
- **M2** `authorUidOf48` chỉ rút uid từ URL facebook (không đọc `&id=` lạc trong query CDN).
- **M3/M4** `normalizeComment`: `user_url`/`author_uid` = `fbUrl49(user_url, commenter_url, commenter_profile_url, commentator_profile, commentator_profile_url)` — `commentator_profile` (null ở mẫu B0, có thể là link thật ở record khác) được ưu tiên trước field ảnh.
- Hành vi bài (`normalizePost`) không đổi; thêm lưới: `user_url` là ảnh → bỏ, rơi về `profile_id`.

## 3. Dọn dữ liệu (`docs/lenh-2026-09-13-49-fix.mjs` = `_l49_fix.mjs`, DRY mặc định, `--apply` mới ghi)
- Lead `detected_at ≥ 12/09 08:53Z` có `author_url` khớp `/fbcdn\.net|\/v\/t\d{2}\./` → `author_uid` số → `author_url = profile.php?id=<uid>`; không → `author_url = ''` (worker bỏ qua kết bạn/inbox, react/comment vẫn chạy) + dấu `author_url_fix49 {old, at, uid}`. Kèm `outreach_threads/{leadId}.fpayload.profile_url` nếu cũng là ảnh. Đọc theo trang ≤300 + `select()` kèm field orderBy.

## 4. Kiểm trước khi gửi
- Harness `docs/harness-lenh49-2026-09-13.mjs` **22/22**: dựng mã đang chạy (fixture B + patch B + push #46 + patch C + patch D qua `LD_EXPORT_DIR` của harness D) → patch OK 4 mốc · marker ×5 · idempotent · fail-closed (thiếu marker #48 → exit 1 không ghi; M3 lệch → exit 1 không ghi dù M1/M2/M4 khớp) · `--check` · import 7 ca (ảnh+pfbid → pfbid link, uid rỗng dù CDN có `&id=`; ảnh+uid số; `commentator_profile` link thật kèm tracking thắng ảnh + uid `/user/<uid>/`; link thật ở `commentator_profile_url` giữ như #48; ảnh không user_id → rỗng; bài profile_id; bài user_url ảnh → profile_id) · `_l49_fix.mjs` trên Firestore giả NGHIÊM (select thu hẹp, startAfter kiểm) với 324 lead (qua trang 300): DRY 0 ghi → APPLY 2 lead + 1 thread đúng giá trị, lead link thật/lead trước #48 không đụng → APPLY lần 2 = 0.
- `.sh` KHỐI 1 (`docs/lenh-2026-09-13-49.sh`, sha256 `c286f28b3c0671b67c1b76f5618ca1a3190538c2b6be9d5ce5a55e5000df965e`, 13.788 byte / 103 dòng, 0 `!` ngoài heredoc): dry-run fake HOME + fake firebase/gcloud ×2 exit 0 idempotent (1 `.bak`); kiểm âm deploy lỗi → `DỪNG (c)` rõ ràng (code vá tại chỗ, chưa lên máy chủ, in lệnh deploy lại).

## 5. KHỐI 1 (anh dán Cloud Shell) → KHỐI 2
- KHỐI 1: `cat > /tmp/l49.sh <<'EOF' … EOF; bash /tmp/l49.sh` = backup `lib/scraper.js.bak-<TS>` → patch → `--check` → import test 4 ca → deploy `scheduledScan,manualScan` (gate `Deploy complete`) → describe → **DRY** `_l49_fix.mjs`.
- KHỐI 2: đọc DRY ở (d) → `node _l49_fix.mjs --apply` → nghiệm thu comment-lead mới sau deploy: `node _ld_after.mjs` (mục 6: bình luận phải có `author_url` dạng facebook/pfbid, không còn `scontent`).

## 6. BÀI HỌC
- Dump che chuỗi dài làm mất HOST của URL → mọi kết luận "field X là link gì" phải đọc được ít nhất host + 40 ký tự đầu (che phần đuôi/tracking thôi). Lần sau dump: che theo mẫu token/IP, không che theo độ dài.
- Sau mỗi LỆNH đổi cách map dữ liệu thô → nghiệm thu bằng doc THẬT đầu tiên (mục 6 bản 2 làm việc này) chứ không chỉ harness với record giả do em dựng theo đúng giả định sai.

## 7. KẾT QUẢ (điền sau khi anh chạy)
- **KHỐI 1 ĐÃ CHẠY OK 13/09 10:31–10:33 VN (TS `20260913-033128` UTC; exit=0)**: backup `lib/scraper.js.bak-20260913-033128` · **PATCH OK 4 mốc**, marker `LENH #49` 5 dòng · `--check` OK · **(b) IMPORT OK 4/4 ca** (c1 ảnh+pfbid → `profile.php?id=pfbid…`, uid rỗng · c2 ảnh+uid số → `profile.php?id=224550447624797` · c3 link hồ sơ thật+ảnh → `/groups/…/user/100012345678901` + uid · p1 bài profile_id → `profile.php?id=…`) · **(c) deploy `manualScan` + `scheduledScan` Successful → Deploy complete → ACTIVE `2026-09-13T03:32:46Z` / `03:32:52Z`** · **(d) DRY**: lead từ 12/09 15:53 VN **39** · author_url là ảnh/CDN **3** (đã sửa trước đó 0): `cmt 09-12 17:34 z15mrc-tts-1 Anonymous participant → profile.php?id=224550447624797` · `cmt 09-13 00:34 z15mrc-tts-1 Hoàng Thuỳ Hà → xoá author_url (không uid số)` · `cmt 09-13 06:07 hscl-01 Lee Phương Anh → xoá author_url` · outreach_threads liên quan 0/0 → đúng dự kiến (3 lead phát hiện ở mục 6 bản 2, không thread nào bị ảnh hưởng). Lưu ý nhỏ: "Anonymous participant" là người tham gia ẩn danh của group → uid 224550447624797 là id giả danh, kết bạn/inbox sẽ không tới (vô hại: 1 lần fail UI, không trừ Safety); có thể loại tên ẩn danh khỏi cổng kết bạn/inbox ở đợt worker sau.
- **KHỐI 2 ĐÃ CHẠY OK 13/09 10:40 VN**: `_l49_fix.mjs --apply` → lead từ 12/09 15:53 VN **40** · author_url ảnh/CDN **3** → **ĐÃ GHI: lead 3 · thread 0** (Anonymous participant → `profile.php?id=224550447624797`; Hoàng Thuỳ Hà + Lee Phương Anh → `author_url=''`) · `_ld_after.mjs` mục 6 ngay sau đó: **BÀI 37/37 uid số (100 %)** · **BÌNH LUẬN 3: author_url 1 (Anonymous, `profile.php?id=…`) + 2 `TRỐNG`** — **hết `scontent` ✓**. Chưa có comment-lead MỚI sau deploy #49 (10:32 VN) để kiểm đường mã mới trên dữ liệu thật → theo dõi: lần `node _ld_after.mjs` kế (thứ Hai) dòng `cmt` có `detected_at` > 10:32 phải có `author_url` dạng facebook/pfbid. **LỆNH #49 KHÉP LẠI** (còn 1 nghiệm thu mềm).
