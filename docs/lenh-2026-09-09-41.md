# LỆNH #41 (09/09/2026) — bộ đếm "lead mới" (`daily_stats.new/hot/warm/cold/junk`) không tăng từ sau 04/09: chẩn đoán + vá + đếm lại

**Phát hiện** (từ output LỆNH #39 (f) và #40 (g), 09/09 06:40 VN): mọi doc `daily_stats` ngày 07–09/09 có `new 0 · hot 0` trong khi cùng doc có `slaN 2 / careLe60 1`
(tức ngày đó CÓ lead hợp lệ được chăm). `slaN` (LỆNH #40, tính lại tuyệt đối từ leads) và `careLe60` (trigger, sự kiện chăm) đúng → **nhánh "tạo lead mới" của trigger
`statsOnLead` không chạy** từ khi backfill LỆNH #17 (04/09). Hệ quả: Bảng brand "Lead 7 ngày/30 ngày" và (từ v119-80) KPI Bảng điều khiển "Lead hợp lệ / Lead nóng 14 ngày"
đọc bộ đếm → **thiếu số** từ 05/09; SLA đạt chia cho `new` = 0 → "—".

**Nguyên nhân khả dĩ**: trigger chỉ đếm "lead mới" khi `before == null` (đúng 1 write tạo doc). Nếu scanner ghi lead 2 bước (tạo doc rồi mới gán brand /
chấm điểm), write tạo bị bỏ (chưa có brand → return sớm, hoặc chưa có điểm → tính nhầm junk), write sau có `before` → không bao giờ vào `new`.
**Vá** (marker `LENH #41`): "lead mới" = lần ĐẦU doc đủ điều kiện đếm (có brand + có temp/score) — tạo 1 bước vẫn đếm đúng 1 lần, tạo 2 bước đếm ở bước đủ dữ liệu,
doc chưa có điểm không bị tính junk oan, đổi điểm sau đó không đếm đôi. Kèm `_l41_recount.mjs`: (1) in kiểu dữ liệu 6 lead mới nhất + so doc 4 ngày gần nhất với số
đếm lại (bằng chứng), (2) dựng lại **tuyệt đối** `new/hot/warm/cold/junk` 60 ngày từ leads (set merge, field khác giữ nguyên). Script còn in log lỗi statsOnLead 3 ngày
+ phân bố HTTP status 24h để loại trừ trường hợp trigger lỗi. Harness `docs/harness-lenh41-2026-09-09.mjs` PASS; dry-run `.sh` fake ×2 idempotent.

## KHỐI 0 — dán 2 file vào `~/firebase-s13/functions`
```bash
cd ~/firebase-s13/functions
cat > _l41_stats.cjs <<'EOF_L41S'
<dán nguyên nội dung docs/lenh-2026-09-09-41-stats-patch.cjs>
EOF_L41S
cat > _l41_recount.mjs <<'EOF_L41R'
<dán nguyên nội dung docs/lenh-2026-09-09-41-recount.mjs>
EOF_L41R
node --check _l41_stats.cjs && node --check _l41_recount.mjs && echo OK
```

## KHỐI 1 — chạy
```bash
cat > /tmp/l41.sh <<'EOF_L41'
<dán nguyên nội dung docs/lenh-2026-09-09-41.sh>
EOF_L41
cd ~/firebase-s13/functions && bash /tmp/l41.sh 2>&1 | tee ~/l41-$(date +%Y%m%d-%H%M).log
```
**Mong đợi**: (b) log lỗi rỗng, phân bố status toàn `200` · `PATCH OK stats.js (LENH #41…)` · `SYNTAX OK` · `IMPORT OK · statsOnLead = object · new đếm đúng…✓` ·
deploy `Successful update operation` · (e) bảng "6 lead mới nhất" (kiểu dữ liệu) + bảng "4 ngày gần nhất: doc → đếm lại" với dấu `✗ LỆCH` ở các ngày 05–09/09 ·
(f) `ĐÃ GHI N doc daily_stats` · (g) 12 doc mới nhất `new/hot` > 0 ở ngày có lead. Anh dán output cho em check (không có secret).

## Sau khi chạy
F5 web (v119-80 đã deploy): Bảng điều khiển "Lead hợp lệ / Lead nóng" (14 ngày) và Bảng brand "Lead 7 ngày" lên số đúng; SLA đạt hết "—". Từ giờ lead mới tự cộng ngay khi
scanner ghi. Nếu (b) lộ lỗi trigger hoặc bảng (e) không lệch mà số vẫn 0 → nguyên nhân khác, em soạn tiếp từ output.

## ★ ĐÃ CHẠY OK (09/09/2026 07:09–07:11 VN = 00:09 UTC, anh dán 4 ảnh output) — CHẨN ĐOÁN ĐÚNG, ĐÃ VÁ + ĐẾM LẠI
- (a) `SYNTAX OK (script)`. (b) log lỗi statsOnLead 3 ngày **RỖNG**; 24h **341 lượt, toàn `200`** → trigger chạy bình thường, lỗi là LOGIC (nhánh `!before` + `if (!brand) return` của #17 bỏ write tạo doc chưa đủ brand/điểm), không phải trigger hỏng.
- (c) `PATCH OK stats.js (LENH #41…)` · `SYNTAX OK` · marker dòng 62 · `IMPORT OK · statsOnLead = function · new đếm đúng: tạo 1 bước ✓ · tạo rồi chấm điểm sau ✓ · tạo rồi gán brand sau ✓ · update thường không đếm ✓ · chưa có điểm không tính junk ✓`. Backup `stats.js.bak-20260909-0009xx` (TS UTC).
- (d) deploy `statsOnLead(asia-southeast1)` **Successful update operation**, describe `ACTIVE · google.cloud.firestore.document.v1.written · 2026-09-09T00:09:41Z`.
- (e) 6 lead mới nhất: `brand` string · `temp` string · `score` number · `detected_at` Timestamp · `tempOf` khớp temp → kiểu dữ liệu chuẩn (không phải lỗi kiểu). Bảng 4 ngày gần nhất: **13/13 doc `∅/∅/∅` (field `new/hot/junk` KHÔNG TỒN TẠI) → đếm lại 9/2 · 14/6 · 8/2 (hscl-01 06–08/09) · 28/13 · 43/17 · 51/20 · 2/0 (z15mrc-tts-1 06–09/09) · 21/0 · 8/1 · 16/0 · 1/0 (tuyendung1) · 2/0 · 4/0 (test-agency) — tất cả `✗ LỆCH`** = bằng chứng nhánh "lead mới" chưa từng chạy sau backfill 04/09.
- (f) `recount: quét 1998 lead (bỏ qua: không brand 0, không detected_at 0) → 90 doc daily_stats từ 2026-07-12 (60 ngày)` → **`ĐÃ GHI 90 doc`** (khớp 90 doc của #40). Theo brand: hscl-01 20 ngày · 637 hợp lệ · 140 nóng | test-agency 32 ngày · 225 · 76 | z15mrc-tts-1 19 ngày · 656 · 236 | z15mrc-tuyendung1 19 ngày · 316 · 35. **`rác 0` ở mọi brand** = scanner KHÔNG ghi lead junk vào collection `leads` (junk chỉ nằm ở `scanned_posts` decision rejected) → bộ đếm `junk` trong daily_stats luôn 0, KPI không dùng.
- (g) 12 doc mới nhất đúng số: 09/09 (07:10 VN) tuyendung1 new 1 · tts-1 new 2 · test-agency/hscl-01 0 (chưa có lead sáng) · 08/09 tuyendung1 16/0 · tts-1 **51/20** (slaN 2 slaOk 1 · scanned 880) · test-agency 4/0 (slaN 1/1) · hscl-01 8/2 · 07/09 tuyendung1 8/1 · tts-1 43/17 · test-agency 0 · hscl-01 14/6 (slaN 1 slaOk 0).
- **Hệ quả trên web (v119-80, F5)**: Bảng điều khiển "Lead hợp lệ / Lead nóng" 14 ngày và Bảng brand "Lead 7 ngày/30 ngày" lên số thật (trước 05/09 trở đi = 0); "SLA đạt" hết "—" ở brand có slaN > 0 (mẫu vẫn nhỏ 1–2/ngày vì sales ít bấm mốc chăm đầu). Từ giờ lead mới tự cộng ngay khi scanner ghi (kể cả ghi 2 bước).
- **BÀI HỌC**: trigger đếm "tạo mới" KHÔNG được dựa vào `before == null` khi doc có thể được ghi nhiều bước — phải đếm ở lần chuyển trạng thái "chưa đủ điều kiện → đủ điều kiện"; và mọi bộ đếm cộng dồn cần 1 script đếm lại tuyệt đối để đối soát (ở đây `_l41_recount.mjs`, chạy lại bao nhiêu lần cũng ra cùng số). Kiểm định kỳ: `node _l41_recount.mjs --dry --days=7` → bảng 4 ngày phải toàn `✓`.

## KHỐI KIỂM (tuỳ chọn, CHỈ ĐỌC) — số 4 ô KPI Bảng điều khiển phải bằng bao nhiêu (09/09, anh hỏi "Lead hợp lệ là thế nào")
`docs/lenh-2026-09-09-41-kpi14check.mjs` → dán thành `~/firebase-s13/functions/_l41_kpi14.mjs` (selftest 1 ca giả với stats.js đã vá #17+#36+#40+#41: OK). Chạy `node _l41_kpi14.mjs` (Super Admin, mọi brand) hoặc `--brand=hscl-01`.
In: ô Bài đã quét / Lead hợp lệ / Lead nóng / Đã chốt theo ĐÚNG công thức FE `kpi14()` (Σ `daily_stats.new/hot/closed/scanned` 14 ngày giờ VN tính cả hôm nay, delta so 14 ngày liền trước), tách theo brand,
rồi đếm trực tiếp từ kho `leads` cùng cửa sổ theo `detected_at`: có điểm = hợp lệ (nóng) + rác → `✓ khớp` / `✗ LỆCH` ô "Lead hợp lệ"; trong số hợp lệ bao nhiêu đã Loại / không thành / vai người bán-chủ bài → còn mở thật.
**Định nghĩa ô "Lead hợp lệ" (v119-80)**: lead AI chấm ≥ 40 điểm (temp cold/warm/hot, không junk) PHÁT HIỆN trong 14 ngày, mọi brand với super, đếm theo ngày phát hiện → vẫn tính lead sau đó bị Loại/không thành (kể cả 158 chủ bài LỆNH #31b dropped);
caption "tổng đã nạp N" = `D.kpi.validLeads` = lead hợp lệ KHÔNG dropped đang nạp trong cửa sổ realtime của trình duyệt (không phải kỳ 14 ngày) — 2 định nghĩa trên 1 ô, đề xuất sửa (xem chat 09/09).
