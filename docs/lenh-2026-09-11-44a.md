# LỆNH #44a — CHỈ ĐỌC: dump 5 khối engine + trigger để soạn LỆNH #44 (Đợt 1 automation) — 11/09/2026

Mục đích: LỆNH #44 sẽ vá `outreach.js` (E-16 nick chết không được xếp việc · R-6 sweeper thread mồ côi · E-17 van hiệu lực = min(brand, hardCap) · R-1 bỏ lead người thật đang chăm · E-4 reserve lại van khi re-enqueue khác ngày) và `stats.js` (R-1: lead có người thật vào → tắt thread). Dump 43 (10/09) mới có `stepNickAdspower/apEnqueueFunnel/tryConsume`; còn thiếu thân `outreachTick`/`runOne`, `CAPS`/`brandCaps`, `usageOf`, `roleBlockOf/skipLeadRole`, `addLog`, và handler `statsOnLead`. **Không ghi gì, không in secret** (chuỗi ≥ 40 ký tự trong dấu nháy bị che).

Dán nguyên khối vào Cloud Shell:

```bash
cat > /tmp/l44a.sh <<'L44A'
set -u
cd ~/firebase-s13/functions || { echo 'KHONG VAO DUOC ~/firebase-s13/functions'; exit 1; }
OUT=~/engine-dump-0911.txt; : > "$OUT"
mask() { sed -E "s/(['\"\`])[^'\"\`]{40,}\1/\1<che>\1/g"; }
blk() { # blk <file> <regex mốc đầu> <số dòng>
  local f="$1" re="$2" n="${3:-60}"; local s; s=$(grep -nE "$re" "$f" | head -1 | cut -d: -f1)
  if [ -z "$s" ]; then echo "--- $f :: KHONG THAY MOC /$re/" | tee -a "$OUT"; return; fi
  echo "--- $f :: /$re/ từ dòng $s ---" | tee -a "$OUT"; sed -n "${s},$((s+n-1))p" "$f" | nl -ba -v "$s" | mask | tee -a "$OUT"
}
echo "== wc ==" | tee -a "$OUT"; wc -l outreach.js stats.js content.js index.js | tee -a "$OUT"
echo "== outreach.js: danh sách hàm/const đầu dòng ==" | tee -a "$OUT"
grep -nE "^(export )?(async )?function [A-Za-z0-9_]+\(|^(export )?const [A-Za-z0-9_]+ ?=" outreach.js | cut -c1-110 | tee -a "$OUT"
blk outreach.js "^const CAPS" 12
blk outreach.js "function brandCaps" 14
blk outreach.js "function brandAllow" 14
blk outreach.js "function usageOf" 12
blk outreach.js "function dayKey" 6
blk outreach.js "function roleBlockOf" 22
blk outreach.js "function skipLeadRole" 14
blk outreach.js "function addLog" 30
blk outreach.js "export const outreachTick" 90
grep -nE "runOne|nextFreeAt|p-limit|pLimit|CONC" outreach.js | cut -c1-140 | tee -a "$OUT"
echo "== stats.js ==" | tee -a "$OUT"
grep -nE "^(export )?(async )?function [A-Za-z0-9_]+\(|^(export )?const [A-Za-z0-9_]+ ?=|onDocumentWritten" stats.js | cut -c1-110 | tee -a "$OUT"
blk stats.js "export const statsOnLead" 60
blk stats.js "function statsEvents" 40
echo "== index.js: export chuỗi ==" | tee -a "$OUT"; grep -nE "^export \* from" index.js | tee -a "$OUT"
echo "== deployed ==" | tee -a "$OUT"; gcloud functions list --project=smartlead-z15 --format="table(name,state,updateTime)" 2>/dev/null | grep -Ei "outreachTick|statsOnLead|genContent|funcWebhook" | tee -a "$OUT"
echo; echo "XONG → tải về: cloudshell download $OUT  (rồi upload file vào chat)"
L44A
bash /tmp/l44a.sh
```

Kết quả mong đợi: file `~/engine-dump-0911.txt` (vài trăm dòng), anh `cloudshell download` rồi upload vào chat. Em soạn LỆNH #44 từ đúng mốc trong file này (patch fail-closed, idempotent, backup `.bak`, deploy xích `&&`).
