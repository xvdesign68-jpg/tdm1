# LỆNH E (13/09/2026) — Đợt 2.2 + 2.3: CHẤM ĐIỂM v2 — prompt theo BRAND + vai reseller/proxy (PA-1) · 6 tiêu chí con × `config.weights`, hotness tính bằng code, CHẠY BÓNG (PC-2)

> Sau LỆNH D / #49 (mốc trên mã có marker `LENH C` + `LENH D` ở index.js, `LENH #48` ở lib/scorer.js). Chưa chạy D → script tự DỪNG, không ghi gì. LỆNH E KHÔNG đụng lib/scraper.js (#49 độc lập, chỉ cảnh báo nếu thiếu).
> File: `lenh-2026-09-13-e.sh` (KHỐI 1, nhúng `_le_patch.cjs` · `_le_live.mjs` · `_promptcmp.mjs` · `_pc2_shadow.mjs` · `_le_after.mjs`) + `-e-patch.cjs` (19 mốc, 3 file `lib/config.js · lib/scorer.js · index.js`, marker `LENH E`, fail-closed NGUYÊN TỬ) + `-e-live.mjs` + `-e-promptcmp.mjs` + `-e-shadow.mjs` + `-e-after.mjs` + harness `harness-lenhe-2026-09-13.mjs` **46/46**.
> **sha256 `lenh-2026-09-13-e.sh` = `df3025933acf50ad3245fafa1e0948ddb59e1a45afd7279b5151adf4e5c32d12`** (63.147 byte / 410 dòng). Dry-run fake HOME (mã sau #49 xuất từ harness) + fake firebase/gcloud ×2 exit 0 (lần 2 idempotent, giữ `.bak` gốc qua `_le_backup_ts`), 0 `!` ngoài heredoc, không shebang. Dry-run lần đầu bắt được 1 kỳ vọng sai trong import test (thiếu 5/6 tiêu chí → `null`, không phải 30) và đường `restore()` khôi phục 3 file đúng.

## 1. Vì sao (anh chốt 12/09: câu 3 + câu 7; critic: bỏ gate "vai rỗng/other")
- **PA-1 (L-2, N2-6)**: prompt chấm điểm hiện là "chuyên gia agency marketing" + khối "BRAND ĐANG PHỤC VỤ" đè lên sau → AI chấm theo bối cảnh chung rồi mới "nhớ" brand; không có vai **đại lý / mua sỉ** (reseller) và **đăng hộ** (proxy) → bài "cần nhập sỉ mực khô" bị coi là người bán (loại) dù brand có bán sỉ; bài "hỏi giúp chị mình" bị chấm thấp/vai other.
- **PC-2 (C-1, F-B1)**: `config/app.weights` (6 key trên web "Chấm điểm AI") chỉ được nhét vào prompt thành **1 câu văn** `weightsHint` — chỉnh trọng số trên web KHÔNG đổi điểm; AI trả 1 số `hotness` không giải thích được. Cần điểm tiêu chí con tính bằng code từ trọng số thật, nhưng phải **chạy bóng** trước (điểm hiển thị/KPI/automation không đổi kỳ) rồi mới bật.
- Cả 2 đều có **công tắc**: `.env PROMPT_BRAND_V2` (mặc định true) / `SCORE_V2` (mặc định `shadow`) — `config/app.scoring {promptV2, scoreV2:'off'|'shadow'|'on'}` (web, zip v119-91) ghi đè `.env`.

## 2. Thay đổi (patch `_le_patch.cjs`, fail-closed NGUYÊN TỬ 3 file, idempotent theo marker `LENH E`)
- **lib/config.js** (1 mốc): `PROMPT_BRAND_V2: bool(env, true)` · `SCORE_V2: String(env || 'shadow')` (đặt trước `PREFILTERED_TTL_DAYS`).
- **lib/scorer.js** (5 mốc): `normRole` thêm **`reseller`** (đại lý/mua sỉ/bán buôn/bán lại/wholesale/nhập hàng — xét TRƯỚC `seller` vì chứa "sell") + **`proxy`** (đăng hộ/hỏi hộ/mua hộ/thay mặt/on behalf/người thân/giúp/giùm) · `prefilterLead` dùng `preSysE(brandAi, v2)` (tầng 1 theo brand: "SÀNG LỌC NHANH lead cho một brand ngành X"; brand bán sỉ → đại lý/mua sỉ = maybe) · `scoreLead` dùng `sysE(brandAi, v2)` (**prompt brand-first**: HỒ SƠ BRAND ngành/sản phẩm (tách ≤12)/khách/giọng, CÓ/KHÔNG bán sỉ, quy tắc vai: brand không bán sỉ → reseller `is_real_lead=false`; brand chưa Hồ sơ AI → prompt **trung tính SME** không còn "agency marketing"; `promptV2=false` → SYS cũ + `brandAiCtx` 100 % như trước) + JSON contract thêm **`criteria{intent,fit,timing,industry,area,quality}` 0–3, `confidence` 0–1, `why[]`** · sau `normRole` gọi **`normalizeE(obj, weights, v2)`**: `is_real_lead` chuỗi → bool, `hotness` 0–1 → ×100, `critE` quy 0–10/0–100 → 0–3 (thiếu ≥3/6 tiêu chí → `null` giữ raw), **`hotnessV2E = Σ w·c/3 ÷ Σ w × 100`** với `w` = `config/app.weights` (trống → 30/25/15/12/8/10), ghi `hotness_raw` + **`ai_v2 {v:1, mode, raw, score, criteria, conf, why, w}`**; `mode='on'` → `hotness = score v2`; `shadow` → hotness = raw (điểm AI như cũ); `off`/promptV2 false → không `ai_v2` · helper expose qua `scoreLead.scoringModeE/hotnessV2E/normalizeE/sysE/preSysE/brandProfileE/hasProfileE/normBoolE/weightsE` (script + harness dùng).
- **index.js** (13 mốc): mỗi lượt `v2E = scoreLead.scoringModeE(config)` + `noProfileE` · `distV2E` + `roleUnknownE` · prefilter/score truyền `v2: v2E` · cổng vai: **`__resBlockE = role==='reseller' && !(brand.ai.banSi===true)`** → `roleBlock` → `recordPost(decision:'reseller')` (không lead, không đẻ thread); `role==='proxy'` → LEAD + **`contact_via_poster:true`**; vai rỗng/other mà `is_real_lead` → VẪN lead, chỉ đếm `roleUnknownE` (critic) · lead + `scanned_posts` ghi `ai_v2` · `scans` thêm `promptV2, scoreV2, distV2, roleUnknown, noProfileBrands` · **sweeper #46/#48** chấm lại cũng đi v2 (`v2: v2E`), reseller + brand không bán sỉ → `dropped:true, dropped_by:'rescore_role', dropped_reason:'AI chấm lại: đại lý/mua sỉ – brand không bán sỉ'` (sales đang chăm → giữ như PB-5), `up` ghi `ai_v2 + contact_via_poster`.
- **KHÔNG đổi**: Rules (`ai_v2`/`contact_via_poster` do Admin SDK ghi; FE ghi `config/app.scoring` + `brands.ai.banSi` = super đã có quyền), index, worker (proxy vẫn kết bạn/inbox theo `author_url` — anh chốt), outreach.js (không đọc `ai_v2`), stats.js (`temp` vẫn theo `hotness` — shadow không đổi KPI; bật `on` → điểm mới áp cho lead MỚI + lead sweeper chấm lại, lead cũ giữ).

## 3. KHỐI 1 (dán vào Cloud Shell) — khối trong chat = `cat > /tmp/le.sh <<'EOF_LE'` … `EOF_LE` rồi `bash /tmp/le.sh` (nội dung = file `lenh-2026-09-13-e.sh`, sha256 ở đầu)
Thứ tự: `--check` 5 script → guard `LENH D` (index) + `LENH #48` (scorer) có / `LENH E` chưa (chạy lại → giữ `.bak` gốc qua `_le_backup_ts`) → backup `.bak-$TS` 3 file → patch (in `PATCH OK 3 file … = 19 mốc`) → `--check` 3 file (lỗi → `restore()`) → (b) import test `.env` (in `IMPORT OK · PROMPT_BRAND_V2 true · SCORE_V2 shadow · v2(đủ 3) 100 · v2(intent 3, còn lại 0) 30 · thiếu 5/6 → null · normRole reseller/proxy/seller · sysE brand-first true`; sai → restore) → **(c) `node _le_live.mjs`**: gọi AI THẬT 1 bài `scanned_posts` decision lead gần nhất bằng prompt v2 (in raw/v2/criteria/vai/why; ≈ 0,003 USD; lỗi/không có bài KHÔNG chặn) → **(d) deploy `scheduledScan` + `manualScan`** (gated `Deploy complete`, lỗi in describe + cách chạy lại/quay lui) → describe → **(e) `node _promptcmp.mjs --n=40 --days=3`**: so prompt CŨ ↔ v2 trên 40 bài thật đã có quyết định (mỗi brand ≤ lead + ≤ loại; in từng bài quyết định/điểm/vai cũ ↔ v2 lead?/raw/điểm tiêu chí/vai/tin cậy/why + tổng kết đồng ý %, lead cũ → v2 KHÔNG, loại cũ → v2 LEAD, đổi vai, brand thiếu Hồ sơ AI, chi phí; ≈ 0,12 USD; chỉ đọc; lỗi không chặn).
**Đọc (e)**: "lead cũ → v2 KHÔNG" = bài v2 gạt (xem `why`/vai có hợp lý: người bán/đại lý/không đúng ngành brand) · "loại cũ → v2 LEAD" = bài v2 cứu (đúng ngành brand?) · brand ⚠ chưa Hồ sơ AI → v2 dùng prompt trung tính SME → khai Hồ sơ AI (Người dùng → Hồ sơ AI) là việc cần làm trước khi bật. Không ưng prompt mới → web Chấm điểm AI tắt "Prompt theo brand" (hoặc `.env PROMPT_BRAND_V2=false` + deploy lại) — điểm tiêu chí vẫn ghi bóng.

## 4. KHỐI 2 (chỉ đọc, sau ≥ 15′)
```bash
cd ~/firebase-s13/functions && node _le_after.mjs
```
Kỳ vọng: (1) `config/app.scoring` chưa đặt (theo `.env` true/shadow), `system_status/llm ok=true` · (2) mọi lượt sau deploy có `promptV2 true · scoreV2 shadow · distV2 · roleUnknown` nhỏ, `noProfileBrands` = danh sách brand chưa Hồ sơ AI (khai xong sẽ trống), `llmFail 0` · (3) lead 24 h có `ai_v2` (mode shadow), `score == ai_v2.raw` (dòng ⚠ "shadow nhưng score ≠ raw" phải 0 trừ multitouch/rescore), vai có thể xuất hiện `reseller`/`proxy`, `contact_via_poster` đếm được · (4) `scanned_posts` decision có thể thêm `reseller` · (5) brands: ✓/✗ Hồ sơ AI + cờ bán sỉ.
**Sau 3 ngày**: `node _pc2_shadow.mjs --days=3 --minAge=0` (ma trận nhiệt độ raw × v2 theo brand, raw − v2 TB, conf TB). **Sau 14 ngày** (cohort ≥ 7 ngày tuổi): `node _pc2_shadow.mjs` — dải NÓNG theo v2 có tỉ lệ phản hồi/hẹn ≥ raw và người loại ≤ raw → bật `scoreV2 = on` trên web (Chấm điểm AI → "Điểm theo tiêu chí: Áp dụng"); kém → giữ shadow, chỉnh trọng số web (đổi trọng số = đổi điểm v2 ngay ở lượt sau, không cần deploy).

## 5. Kiểm (harness `harness-lenhe-2026-09-13.mjs` 46/46)
Dựng mã đang chạy = harness D export (mã sau C) + patch D + patch #49 (sha8 index.js 816aa53a · scorer 20078309 · scraper d80a00bd · config 08000249) → patch E: PATCH OK 19 mốc / idempotent / `--check` 3 file + 4 script / fail-closed nguyên tử (mốc I9 index lệch → 0 file ghi, config/scorer đủ mốc) / LỆCH (config đã E, scorer/index chưa → báo, không ghi) / thiếu `LENH D` → DỪNG. Scorer thuần 22 ca (normRole reseller trước seller · scoringModeE env/config/giá trị lạ → shadow · hotnessV2E công thức + quy thang 0–10/0–100 + thiếu ≥3 tiêu chí → null + weights ghi đè/trống · normalizeE shadow/on/off/promptOff/"yes"/"85"/criteria thiếu · sysE brand banSi/không banSi/không hồ sơ/promptV2=false · preSysE · hasProfileE). `scanAll` trọn vòng 18 ca trên Firestore + BrightData + LLM giả: (a) shadow mặc định (score = raw 85 hot, `ai_v2` đủ, `scanned_posts` có ai_v2, `scans` promptV2/scoreV2/distV2/roleUnknown/noProfileBrands, prompt gửi AI là brand-first + tầng 1 theo brand) · (b) `config/app.scoring.scoreV2='on'` → score = 67 (warm) dù raw 90, `dist` theo v2 · (c) promptV2=false → prompt cũ 100 %, không ai_v2 · (d) reseller + brand không bán sỉ → không lead, decision `reseller`; brand `banSi` → LEAD role reseller, prompt nêu "CÓ bán sỉ" · (e) proxy → LEAD + `contact_via_poster` · (f) role other + is_real_lead → vẫn lead, `roleUnknown 1`; `is_real_lead:"false"` → scored_low · (g) brand không Hồ sơ AI → prompt trung tính, `noProfileBrands ["b1"]` · (h) sweeper: reseller → `dropped rescore_role` "đại lý/mua sỉ" + ai_v2; proxy → giữ lead, contact_via_poster, score raw 75, ai_v2.score 67. Harness bắt trước khi gửi: mock route theo cả body làm sysE (có chữ "nhập sỉ") khớp nhầm → route theo `messages.user`; 57 điểm là **cold** (ngưỡng warm 60) → chỉnh ca (b).

## 6. Rủi ro / ghi chú
- Chi phí AI tăng nhẹ (prompt dài hơn ~30 %, JSON thêm criteria/why ≈ +60 token/bài); `reasoning_effort low` #48 giữ nguyên. Theo dõi `scans.tokensScore` vài ngày.
- `hotnessV2E` trả `null` khi AI thiếu ≥3 tiêu chí → lead vẫn có điểm (raw), `ai_v2.score null` → `_pc2_shadow` đếm "thiếu criteria"; nhiều → siết prompt.
- Bật `on` đổi phân bố nhiệt độ (KPI/automation ma trận nhiệt độ theo `temp`) → chỉ bật sau 14 ngày bóng + `_pc2_shadow` PASS; tắt lại `shadow` bất cứ lúc nào (lead đã chấm giữ điểm).
- Brand chưa Hồ sơ AI → prompt trung tính SME (không tệ hơn prompt cũ) nhưng mất lợi thế brand-first — Cảnh báo hệ thống (zip v119-91) sẽ nhắc từ `scans.noProfileBrands`.
- FE đi cùng: **zip v119-91 / v120-esm-ao** — Chấm điểm AI card "Chấm điểm v2" (2 công tắc → `config/app.scoring`), trọng số lưu kiểm Σ = 100, Hồ sơ AI ô "Brand có bán sỉ / đại lý" (`ai.banSi`), chip vai "Đại lý / mua sỉ" + "Đăng hộ" (liên hệ qua người đăng), DECIS `reseller` + `too_old`, tooltip/modal phân tích điểm từ `ai_v2`, Cảnh báo hệ thống dòng brand chưa Hồ sơ AI.

## 7. KẾT QUẢ KHỐI 1 — (chờ anh chạy)
## 8. KẾT QUẢ KHỐI 2 — (chờ)
