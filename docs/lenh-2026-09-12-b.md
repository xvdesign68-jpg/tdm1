# LỆNH B (12/09/2026) — HƯỚNG B "1 GROUP NHIỀU BRAND" + TỐC ĐỘ + PC-4 — CHỜ ANH CHẠY

> Bản lưu; **khối lệnh thật dán trong chat** (anh copy lên Cloud Shell). File: `lenh-2026-09-12-b.sh` (KHỐI 1 = nhúng `_lb_patch.cjs` + `_lb_sources.js` + `_lb_rules.cjs` + `_lb_tag_patch.cjs` + `_lb_after.mjs`, heredoc quoted, 0 dấu `!` ngoài heredoc) · `lenh-2026-09-12-b-patch.cjs` (patch 7 file, **73 mốc**, marker `LENH B`) · `lenh-2026-09-12-b-sources.js` (CF mới `createSource` · `sourceOnWrite` · `bdReady`) · `lenh-2026-09-12-b-rules.cjs` (block `lead_links`) · `lenh-2026-09-12-b-tag-patch.cjs` (codebase2 `tagLeadBrand`) · `lenh-2026-09-12-b-after.mjs` (KHỐI 2 chỉ đọc) · `lenh-2026-09-12-b-fixture/` (mã ĐANG CHẠY sau #48: `index.js · lib/scraper.js · lib/config.js · lib/scorer.js` = fixture #48 + patch #48; `outreach.js` = fixture #46 + patch #46; `stats.js · scanstats.js · lib/multitouch.js · codebase2/tagLeadBrand.js` = dump #47/#47b/#47e; `stubB.mjs` Firestore giả deep-merge) · `harness-lenhb-2026-09-12.mjs` (**98/98 PASS**).
> Theo thiết kế `docs/rasoat-scan-2026-09-11/thiet-ke-group-nhieu-brand.md` §8.1–8.4 + §9.1–9.21 (anh chốt 12/09) + KHỐI 0 (`docs/lenh-2026-09-12-b0.md`) + LỆNH #47/#48.

## 0. 3 số BrightData dashboard — CHƯA CÓ → mặc định an toàn (đổi trong `.env`, KHÔNG cần chạy lại LỆNH)
| Số anh chưa cho | Mặc định LỆNH B dùng | Khi có số thì đặt |
|---|---|---|
| Trần snapshot đồng thời / request-phút | sàn nhịp gieo **5′** (`SCAN_INTERVAL_MIN_FLOOR=5`), gộp ≤ **40** group/1 trigger (`BD_MAX_TRIGGERS_PER_RUN=40`), hỏi progress chỉ với snapshot ≥ **120 s** (`BD_PROGRESS_MIN_AGE_S=120`), 429/5xx = "busy" → hoãn (không tính là ngưng) | có trần rộng → `SCAN_INTERVAL_MIN_FLOOR=3` (group sôi động gieo 3′); trần hẹp → giảm `BD_MAX_TRIGGERS_PER_RUN` |
| Đơn giá posts/comments + `dead_page` có tính tiền? | record lỗi **coi là CÓ tính tiền** (`BD_ERROR_RECORD_BILLED=true` → `bdRecords`/`bd_month` đếm theo raw) | không tính tiền → `BD_ERROR_RECORD_BILLED=false`; đơn giá vào ô benchmark ROI trên web như trước |
| Gói có `notify` webhook? | **không dùng** (`BD_NOTIFY_URL` trống) → gặt ở lượt kế sau ≥ 120 s | có → `.env`: `BD_NOTIFY_KEY=<chuỗi ngẫu nhiên>` + `BD_NOTIFY_URL=https://asia-southeast1-smartlead-z15.cloudfunctions.net/bdReady?key=<chuỗi đó>` (KHÔNG dán key vào chat) → snapshot chín được đánh dấu `ready` → gặt ngay lượt kế (−1–2′) |
Ô **"Nhịp gieo mỗi nguồn"** trên web (`config/app.scanIntervalMin`, v119-50) giờ là **SÀN** của nhịp thích ứng (không phải nhịp cố định): muốn 5′ thì để 5 (mặc định 10 của ô = sàn 10′ → chậm mục tiêu); KHỐI 2 in sàn/trần đang hiệu lực; zip FE sau đổi nhãn ô thành "Sàn nhịp gieo (phút)". Muốn tick nhanh hơn (đã có khoá lượt): `.env` `POLL_MINUTES=2` rồi `firebase deploy --only functions:scheduledScan` (Scheduler đọc từ code). Không bắt buộc.

## 1. Sửa gì (7 file + 1 file mới + Rules + codebase2; marker `LENH B`; fail-closed NGUYÊN TỬ: đủ 81 mốc cả 7 file mới ghi)
| Mã | File | Thay đổi |
|---|---|---|
| B-1/B-4 | `index.js` Pha 1 mới `pha1B` | **Nhóm nguồn theo group gid số** (`buildGroupsB`: gid từ URL/field `gid` → `g_<num>`; slug → `g_<num>` nếu đã học `group_state/s_<slug>.gidNum`, không thì `s_<slug>`; nguồn chủ = `sharedAt` nhỏ nhất/không có). `group_state/{gkey}` (migration đọc doc cũ theo URL). **Gặt** `harvestPostsB` (claim transaction, route record theo `group_id` → gkey, học gid slug→số → ghi `sources.gid` + `group_state.gidNum`) → **fan-out**: mỗi record × mỗi source doc trong group = 1 ứng viên riêng (`brandB`, `post.__brand`, `gid`, `shared`), `seen` đọc 1 lần/bài (cache). **Gieo gộp** 1 trigger/lượt cho group tới nhịp (`sowPostsB`, chunk ≤ 40, `pending_snapshots/PB_<snapshot>` kèm `groups[]`). |
| T-8 | `index.js` `ivB` | **Nhịp thích ứng** EWMA bài mới/giờ (τ 60′, chỉ cập nhật ở lượt gặt probe, không sweep/deep): ≥4/giờ → sàn 5′ · ≥0,4/giờ → 10′ · ít hơn → trần 30′; hysteresis ±20 %; group mới (<2 mẫu) = sàn; vừa có bài mới (<30′) = sàn; im ≥6 h ban ngày = trần; đêm 23–6 h ×2 (trần); super ghi đè từng nguồn `sources.scanIntervalMin`; sàn/trần = `config/app.scanIntervalMin`/`scanIntervalMaxMin` > `.env`. `group_state.iv/band` để FE hiện. |
| T-4/S8 | `index.js` sow | **Sweep** = 20 bài + `start_date = lastSweepAt − 6 h` + `end_date` + `notInclude = recentIds`; **escalate** (probe đầy bài mới) = gieo đủ 20 NGAY lượt này (`notInclude = vừa gặt ∪ recentIds`); probe 10 cho group ≥4 bài/giờ; `lastTriggerAt/lastSweepAt` ghi SAU gieo OK. |
| T-3 | `index.js` bdwatch + sow | Đang ngưng → cần **2 lượt OK liên tiếp** mới `[BRIGHTDATA-UP]` (`okStreak`); ngưng ≥ `BD_DEEP_SWEEP_AFTER_MIN` (30′) → `system_status/brightdata.deepDue` → lượt sau **gieo sâu** mọi group (`start_date = lúc ngưng − 1 h`, `numPosts = rate×giờ + 10 ≤ 200`) 1 lần (`deepDue.doneAt`, log `[BRIGHTDATA-DEEP]`). 429/5xx khi hỏi progress = busy → hoãn, **không** tính là ngưng. |
| T-1 | `index.js` `scanAll` bọc | **Khoá lượt** `system_status/scan_lock` (transaction, TTL `SCAN_LOCK_TTL_S` **1860 s** ≥ timeout 1800 — rà) cho MỌI trigger (lượt bỏ vì khoá → `system_status/scan.busyRuns`): lịch gặp khoá → bỏ lượt (`busy:true`, log `[SCAN-BUSY]`, không ghi scans); Quét ngay/backfill → `manualScan` trả **409 `{error:'busy'}`**. `runId48` = holder. Nhả ở `finally` (kể cả lượt ném lỗi). Lỗi transaction → fail-open. |
| T-6 | `index.js` | **Lượt thuần skip** (không gieo/gặt/ứng viên, không bài chờ AI tới hạn, không busy, chưa tới nhịp việc phụ) → KHÔNG ghi `scans`, chỉ `system_status/scan.skipRuns++`. **Việc phụ theo nhịp** `HOUSEKEEPING_MIN` 5′ (`lastHousekeepingAt`): hồ sơ BD collect, quét-vét eKYC, sweeper chấm lại điểm tạm. Quét ngay/backfill luôn chạy đủ. |
| B-4 | `index.js` Pha 2 | `seen/{post_id}` = `{at, run, brands:{<brand>:'pending'|'lead'|'low'|'pre'|'ex'|'self'|'seller'}}` — 1 doc/bài `create()` (gộp brand), brand thêm sau → set-merge `brands.<X>='pending'`; **doc cũ không `brands`**: brand X = ĐÃ xử lý nếu nguồn X không `sharedAt` hoặc `sharedAt < seen.at` (brand vào sau mốc mới được chấm). ALREADY_EXISTS (2 lượt chồng) → đọc lại, bỏ bài/brand lượt kia đã nhận. Quyết định ghi lại theo brand qua `recordPost → seenDecB → flushSeenB` (batch). |
| B-2 | `index.js` lease/retry | `score_retry` id **`R_<post_id>__<brand>`** (`retryRef48`/`deferPost46` theo `post.__brand`), doc `src` thêm `source_id/gid/fanout`; nạp lại tìm nguồn theo `source_id` → (url + brand) → url; `row` theo `source_id`. |
| §9.2/§9.12 | `lib/scraper.js` | `normalizePost.post_id = postIdB(p)` = `post_id ‖ id ‖ urlKey(url) ‖ h_<hash tác giả+nội dung>` — **bỏ `Math.random()`**. |
| B-4/§9.2 | `index.js` `commitLeadNow` | Lead fan-out (`brand_hint` + `post_id`) → id **`L_<post_id>__<brand>` + `create()`** (ALREADY_EXISTS → `dupLead`, không đè); lead mang `post_id/gid/source_id/brand_hint/shared_post/num_comments/textKey`; **GIỮ 2 bước ghi brand**: create không `brand` → birthstamp `update({brand: brand_hint …, brand_tagged_by:'scanner-inline'})` (ZBS `justTagged`); bỏ v-fixgrp cho lead fan-out; `lead_links/{gid}_{post_id}.brands{<brand>:leadId}` cho bài dùng chung (Rules read super, lead KHÔNG mang mã brand khác). Đường cũ (không brand_hint) giữ nguyên. |
| B-8/PC-4 | `lib/multitouch.js` | `tryMergeTouch(db, lead, {brand})`: **chỉ gộp trong cùng brand** (`brand_hint`/`brand`), limit 12; **PC-4** `textKeyB` (fold 160 ký tự đầu, bỏ link/dấu, <24 ký tự → không) → `tryMergeTextB`: cùng brand ≤14 ngày cùng `textKey` lead mở → gộp thành touch (`merged_by:'textKey'`), query `where textKey ==` single-field (không cần index ghép). *Lệch thiết kế §9.8 (simhash 64 bit) → hash prefix 160 ký tự: đủ cho "rải cùng 1 bài đi nhiều nhóm", rẻ, không cần thư viện; bài sửa vài chữ đầu không gộp — chấp nhận.* |
| §9.4/9.20/S6 | `index.js` Pha 1b | `ctxAllB`: **mọi brand dùng chung group nhận bình luận** (ứng viên riêng, gid, shared, `__brand`); gieo snapshot bình luận **CHỈ khi `num_comments > 0`** (bài watch cũng vậy) → `noCmt`; `cmt_scrape.lastAt` ghi **SAU** gieo OK (`fetchComments` trả `sownUrls`; gieo lỗi → lượt sau gieo lại). |
| §9.20 | `index.js` watch_posts | Bài ra lead nhưng 0 bình luận lúc gặt → `group_state.watch{post_id:{url,at,checks}}` (≤5/nguồn, 2 ngày, ≤4 lần): lượt **sweep** loại các bài này khỏi `notInclude` → BrightData trả lại record với `num_comments` mới → gieo bình luận; `checks+1` mỗi lần. |
| B-3 | `index.js` bySource/bd_month/scans · `scanstats.js` | `bySource` dòng có `brand/source_id/gid/bdShared` (tiền BrightData ở nguồn chủ); `bd_month.src` key `s_<url>` (chủ) / `s_<url>__<brand>` (dùng chung) + field brand/source_id/shared; `scanstats.scanEvents` lấy `r.brand` trước `normUrl` → `daily_stats.scanned` đúng brand; `scans` thêm `groups sharedGroups sown harvested escalated bdBusy deepSweep sweepRecords noCmt dupLead hvOrphan gidLearned housekeeping`. |
| §8.2 | `outreach.js` | **`acquireLocksB`** (transaction `outreach_locks/{post_<post_id>|cmt_<comment_id>|person_<uid>|person_u_<username>|person_<pfbid>}` = `{brand, leadId, pid, state:'reserved' 6 h | 'touched' 14 ngày, expireAt}`) gọi ở **CẢ `apEnqueueFunnel` lẫn `stepNick` TRƯỚC `tryConsume`/`genForLead`**; thua → `skipLeadSharedB`: thread `step:'skipped_shared'`, `active:true`, `nextAt = retryAt = hết hạn khoá` (touched → `active:false`), log ⏭ Nhường; **xét lại**: `stepNickAdspower` nhánh retry (lead vẫn mở → `apEnqueueFunnel` lại), `drive()` func-path (lấy khoá → `step:'react'`); nhả: `!steps.length`, sweep44 đóng phễu quá 72 h, `stats.stopMachine` (người thật). Lỗi transaction → fail-open. Cùng brand → dùng lại khoá. |
| B-5 | `sources.js` (MỚI) | **`createSource`** (onRequest, super hoặc admin đúng brand): URL chuẩn hoá, gkey (gid số/slug đã học), 409 khi cùng brand + cùng group / cùng brand + trùng tên bỏ dấu; id **`<gid|slug>__<brand>`**; group đã có brand khác → `sharedAt/sharedBy/sharedWith`. **`sourceOnWrite`** (onDocumentWritten `sources/{id}`, so before/after gid/brand/active/url/sharedAt/name): group ≥2 brand → `system_status/sources.shared[gkey]` `{brands, gid, name nguồn chủ, since, by, sources[], ackAt}` + **push FCM super** + WARNING `[SOURCE-SHARED]` (chỉ khi thêm brand mới); còn ≤1 brand → gỡ. **`bdReady`** webhook notify (`?key=BD_NOTIFY_KEY`) → `pending_snapshots PB_/C_ ready:true`. |
| §9.21 | codebase2 `tagLeadBrand` | dòng 75 `const brand = hint || await getBrandForSource(sourceName)` (`hint = lead.brand_hint`), `brand_tagged_by: hint ? 'brand_hint' : 'tagLeadBrand'` → 2 source doc trùng TÊN khác brand không còn gán sai. Deploy `--only functions:tagLeadBrand` từ `~/codebase2` (lỗi = cảnh báo, không chặn KHỐI 1). |
| — | Rules | `match /lead_links/{lid}` read super / write false (bản sao block `workers`). `outreach_locks`, `group_state` gkey, `pending_snapshots PB_` = Admin SDK (không cần block). |
| — | `lib/config.js` | 10 khoá tuỳ chọn (mục 0 + `SCAN_INTERVAL_MAX_MIN 30`, `SCAN_LOCK_TTL_S 1860`, `BD_DEEP_SWEEP_AFTER_MIN 30`, `HOUSEKEEPING_MIN 5`, `BD_NOTIFY_KEY`). |

**"Quét ngay" (manual, không ngày, không sourceUrl) giờ đi ĐƯỜNG B** (rà đối kháng #1): gieo NGAY mọi group (snapshot đang chín thì chờ) + gặt cái đã chín, trả về sau vài giây — không còn chờ 90 s/nguồn giữ khoá lượt ~12′ và không tạo `P_<url>` mồ côi; bài về ở lượt lịch kế (≤3′). **Đường cũ chỉ còn cho backfill / "Quét thử nguồn này" (sourceUrl) / nguồn nick / URL không phải group** (`legacyPha1B`, 1 lần/group + `fanoutLegacyB`); snapshot `P_<url>` mà đường cũ (hoặc lượt lịch cuối trước bản B) để lại cho URL group → `harvestPostsB` gặt như 1 group (không mồ côi, không mua lại). **Không đổi worker VPS** — khoá `touched` do ENGINE tự nâng (rà): khoá `reserved` của brand khác hết hạn mà phễu họ đã có bước (`outreach_threads.doneSteps` worker ghi, hoặc phễu func đã qua react) → `touched` 14 ngày; sweep44 đóng phễu đã có bước → touched; người thật tiếp quản (`stats.stopMachine`) → touched; TTL `outreach_locks.expireTs` + sweep44 dọn khoá hết hạn > 24 h. **Không có FE trong LỆNH này** (zip FE sau: chip "Dùng chung", form tạo nguồn qua `createSource`, toast 409 busy, `sourceId` cho "Quét thử nguồn này").

## 2. Kiểm trước khi gửi
- **Fixture = mã đang chạy**: harness tự dẫn xuất (fixture #48 + patch #48 → byte-identical với fixture B 4 file; fixture #46 + patch #46 → outreach.js; stats/scanstats/multitouch == VĂN BẢN DUMP #47/#47b) rồi đối chiếu **81 mốc mỗi mốc đúng 1 lần**; tagLeadBrand 2 mốc (dump #47e).
- Harness `harness-lenhb-2026-09-12.mjs` **136/136** (98 gốc + 38 ca rà đối kháng, mục 6): patch áp/idempotent/`node --check` 8 file/fail-closed nguyên tử (thiếu 1 mốc scanstats → exit 1, không ghi file nào)/LỆCH · tag patch + rules script idempotent · **scraper 10 ca** (postIdB tất định, gid, sowPostsB 2 group/1 trigger + chunk 40, harvestPostsB chờ ≥120 s / claim / route group_id / học gid / dead_page tính tiền / 429 busy / failed) · **multitouch 5 ca** (brand khác không gộp, cùng brand gộp, PC-4 gộp/không gộp chéo brand, văn bản ngắn) · **index.js chạy TRỌN `scanAll` 13 kịch bản** (C1 lượt đầu chỉ gieo 2 group 1 trigger · C2 gặt → 3 bài × 2 brand + 1 × 1 = 7 lead id tất định, brand đúng nguồn, 2 bước brand, AI 7+7, seen.brands, lead_links, group_state/học gid/watch, bySource bdShared, bd_month key __brand · C3 thuần skip · C4 khoá lượt: 2 lượt chồng → 1 busy, Quét ngay 409, TTL · C5 seen cũ + sharedAt · C6 dupLead · C7 nhịp: probe 10, escalate 20, EWMA, idle 30′, sweep start_date · C8 DOWN → 2 OK → UP + deepDue → deep sweep · C9 score_retry theo brand → nạp lại · C10 bình luận: num_comments gate, lastAt sau gieo, fan-out 2 brand, gieo lỗi không ghi lastAt · C11 Quét ngay 1 lần/group + fan-out · C12 watch loại khỏi notInclude · C13 busy) · **outreach 11 ca** (lockKeysB 4 dạng, first-come, thua khoá không đốt van/AI, người khác cùng bài OK, cùng người group khác bị khoá person, retry khi hết hạn, touched nhường vĩnh viễn, stopMachine nhả, sweep44 nhả, func stepNick thua/lấy lại) · **scanstats/sources/tag 15 ca** (createSource 6 đường, sourceOnWrite 3, bdReady 2, notify gặt ngay, tagLeadBrand 2).
- Dry-run `lenh-2026-09-12-b.sh` với fake HOME (fixture UNPATCHED + node_modules giả) + fake `firebase`/`gcloud`: lần 1 PATCH OK 7 file → sources.js → Rules chèn → IMPORT OK 13 hàm + CFG B → deploy giả 8 function + rules → tagLeadBrand patch + deploy giả → exit 0; lần 2 idempotent (7 file/Rules/tag đều "ĐÃ CÓ") exit 0. `_lb_after.mjs` chạy trên shim firebase-admin: in đủ 8 mục, exit 0. `bash -n` OK; 0 dấu `!` ngoài heredoc; quét secret sạch.
- **Rà đối kháng (workflow 3 lens → refuter)**: xem mục 6 — mọi phát hiện xác nhận ĐÃ SỬA trong patch/harness/.sh, dry-run ×2 lại exit 0.

## 3. KHỐI 1 (dán vào Cloud Shell) — xem file `lenh-2026-09-12-b.sh` (khối trong chat = `cat > /tmp/lb.sh <<'EOF_LB'` … `EOF_LB` rồi `bash /tmp/lb.sh`)
Trình tự: backup 7 file + `firestore.rules` `.bak-<TS>` → `_lb_patch.cjs` (fail-closed) → `sources.js` → `node --check` ×8 → Rules `lead_links` → import test `.env` (13 hàm + CFG B + `postIdB/gidNumOfB/lockKeysB`) → deploy **`firestore:rules` + 8 function** `scheduledScan manualScan outreachTick statsOnLead scanStatsOnRun createSource sourceOnWrite bdReady` (xích `&&`) → describe → codebase2 `tagLeadBrand` patch + deploy (lỗi = cảnh báo). Lỗi ở (a)/(b)/(c) tự khôi phục từ `.bak`. KHÔNG đụng `.env`.
**Bước (e) bổ sung** (`lenh-2026-09-12-b-e.sh`, 12/09 sau KHỐI 1): `~/codebase2` KHÔNG có `firebase.json` (tagLeadBrand vốn deploy bằng gcloud từ `~/codebase2/tagLeadBrand`) → script kiểm marker + `node --check` → có `firebase.json` trong thư mục function thì `firebase deploy --only functions:tagLeadBrand`, không thì `gcloud functions deploy tagLeadBrand --gen2 --region=asia-southeast1 --runtime=nodejs22 --entry-point=tagLeadBrand --source=~/codebase2/tagLeadBrand --quiet` (cập nhật function có sẵn: trigger/env/SA/maxInstances giữ nguyên) → describe SAU. Dry-run fake gcloud/firebase 2 nhánh exit 0.

## 4. KHỐI 2 (sau ≥ 15′, tốt nhất 1–2 giờ ban ngày; chỉ đọc)
```bash
cd ~/firebase-s13/functions && node _lb_after.mjs
```
Kỳ vọng: `system_status/scan` có `skipRuns` tăng (≈ 30–60 % lượt), `scan_lock` holder trống · `brightdata ok=true` · `group_state` 20+ doc `g_<gid>` với `rate/band/iv` (nguồn slug học gid → `s_<slug>.gidNum`) · `pending_snapshots PB_` 0–3 (tuổi < 5′) · 12 lượt: `sown` vài group/lượt, `harvested` 1 ở lượt gặt, `busy 0`, `dup 0`, `hvOrphan 0`, `hk ✓` mỗi ~5′ · seen mới 100 % có `brands` · lead 24 h: id `L_<post_id>__<brand>`, `brand == brand_hint`, `brand_tagged_by scanner-inline` (hoặc `brand_hint` từ tagLeadBrand) · **LAG trung vị 5–7′ / p95 ≤ 20′** (mốc gốc 11′/36′) · `lead_links` = 0 tới khi có group 2 brand · log `[B]` mỗi lượt gieo/gặt, `[SCAN-BUSY]` hiếm, `[SCAN-ABORTED]` 0.
**Nghiệm thu "1 group nhiều brand"** (khi anh muốn): thêm nguồn thứ 2 cùng group cho brand khác qua CF (FE chưa có nút → tạm dùng curl với idToken super, hoặc chờ zip FE): sau 2–3 lượt → `system_status/sources.shared` có group, push super "Group dùng chung", lead mới của group đó có 2 doc `L_<post_id>__<brandA>` / `__<brandB>` + `lead_links`; automation: brand tới trước giữ khoá, brand kia thread `skipped_shared` xét lại sau 6 h.

## 5. Rủi ro & quay lui
- Quay lui code: `cd ~/firebase-s13/functions; for f in lib/config.js lib/scraper.js index.js outreach.js stats.js scanstats.js lib/multitouch.js; do cp "$f.bak-<TS>" "$f"; done; rm -f sources.js; cp ~/firebase-s13/firestore.rules.bak-<TS> ~/firebase-s13/firestore.rules` rồi deploy lại 8 function + rules (xoá `createSource/sourceOnWrite/bdReady` bằng `firebase functions:delete` nếu muốn sạch). Dữ liệu mới (`group_state/g_*`, `seen.brands`, `lead_links`, `outreach_locks`, `pending_snapshots/PB_*`) không phá mã cũ: mã cũ đọc `group_state` theo URL (doc cũ còn), `seen` chỉ xem `exists`, pending `PB_` bị bỏ qua (mã cũ chỉ đọc `P_<url>`/`C_`).
- Lượt đầu sau deploy: mọi group chưa có `group_state/g_*` → lấy từ doc URL cũ (lastTriggerAt/lastSweepAt/recentIds) → không gieo dồn; group_state mới ghi `iv/band` từ lượt đầu (rate 0, mẫu 0 → sàn 5′ tới khi có ≥2 mẫu).
- Lead id đổi dạng (`L_<post_id>__<brand>`) — FE không phụ thuộc dạng id; `notes` subcollection theo id lead như cũ.
- `dupLead` > 0 ở lượt đầu là bình thường nếu 2 lượt chồng trước khi khoá có hiệu lực; sau đó phải = 0.

## 6. Rà đối kháng (12/09 tối — Workflow 3 lens tìm lỗi: pipeline · firestore · automation → refuter từng phát hiện; + em tự kiểm từng mục trên mã đã vá)
36 phát hiện thô (14 + 11 + 11; refuter kiểm 17 mục ≥ vừa: 6 xác nhận / 11 bác vì đã vá song song) → **đã sửa 25 nhóm** (mỗi nhóm có ca harness "(rà)" kiểm âm/dương), 3 mục bác hoặc đã có sẵn, phần còn lại ghi nhận cho LỆNH C. Refuter chạy sau khi patch đã sửa nên nhiều verdict ghi "đã vá trong working tree".

**Đã sửa (patch/harness/.sh):**
1. **"Quét ngay" gieo P_<url> rồi không ai gặt + giữ khoá lượt ~12′** (CAO) → Quét ngay đi đường B (gieo ngay mọi group + gặt cái đã chín, trả về sau vài giây; `forceDue`); `harvestPostsB` gặt cả `P_<url>` cũ của URL group (Quét thử/backfill/lượt cuối trước B) như 1 group. Harness C11/C11b/C11c.
2. `num_comments` thiếu field bị coi là 0 → không gieo bình luận → `normalizePost` → `null` (= chưa biết, vẫn gieo); watch_posts mở cho cả bài KHÔNG phải lead — kể cả bài RỚT TẦNG 1 (đa số là bài chào bán của người khác; 0 bình luận → comment-lead 44 %) — có ưu tiên `pri` (lead 2 > AI chấm không lead 1 > rớt tầng 1 0), trần 5/group áp lúc ghi theo pri. C14/C15.
3. PC-4 gộp 2 NGƯỜI khác nhau cùng văn bản → `tryMergeTextB` không gộp khi `identityKey`/`author_uid` khác; lead gộp chưa có định danh nhận `identityKey/author_url/author_uid/name` của bản mới. B.
4. bdwatch bị che: progress không phản hồi (mạng) → `unknownKeys` → row `bd=skip` + lỗi + `scrapeErrors` → DOWN đúng lượt; trigger gộp lỗi sau khi vừa gặt (bd ok) cũng = DOWN (`sowFailB`, row `bd=err`). C16/C8b.
5. Nguồn slug học gid trong lượt + escalate/deep cùng lượt → snapshot mang khoá `g_<num>` + alias `s_<slug>`↔`g_<num>` khi gặt (không mồ côi); **di trú group_state** (URL/s_ → g_) ghi đủ `lastTriggerAt/lastSweepAt/recentIds/rate/watch` ngay lượt đầu (không gieo sớm/sweep notInclude rỗng 1 lần). C17/C24.
6. Lượt thuần skip nuốt lỗi trigger → điều kiện thêm `!scrapeErrors`; skip vẫn cộng `bd_month.runs/skipRuns`. C21.
7. 2 snapshot cùng group 1 lượt → `gs` gộp `gsWrite`, khử trùng ứng viên `(post_id, brand)`, lease batch 1 doc/ứng viên. C18.
8. `posts/scanned` chỉ đếm bài MỚI (như `leadingNew` cũ; `bdPosts` = record tính tiền). C2/C18.
9. watch_posts kiểm ở probe mỗi ≥12 h (không chỉ 4 sweep liên tiếp) + dọn entry hết hạn cả khi không có bài mới. C22.
10. `commitLeadNow` `create()`/`set()` lỗi không phải trùng → ném → Pha 3b giữ lease (không mất bài). C19.
11. Gặt bình luận: meta mang `gkey` → nguồn brand gieo đã tắt vẫn vào brand khác cùng group (orphan 0). C20.
12. `bdShared` đặt ở `buildGroupsB` (Quét ngay/backfill cũng tách key `bd_month`); 2 doc nguồn CÙNG brand cùng group → bỏ nguồn dư (`row.error`, `scans.dupSources`, WARNING `[SOURCE-DUP]`, KHÔNG rơi xuống đường cũ gieo riêng). C23.
13. `deepDue.doneAt` giữ qua set-merge → lần ngưng 2 không gieo sâu → xoá `doneAt/groups` khi UP. C8b.
14. Khoá `touched` không ai ghi (worker chưa làm) → engine tự nâng: `acquireLocksB` đọc `outreach_threads` của holder hết hạn (doneSteps / phễu func đã qua react) → touched 14 ngày; holder hết hạn nhưng phễu còn ĐANG chạy (chưa bước, re-enqueue/needLogin) → không chen, gia hạn khoá 6 h (`aliveByOther`); sweep44 đóng phễu có bước → touched; `stats.stopMachine` (người thật) → touched (`touchLocksStB`, KHÔNG export trùng tên); cùng brand còn hạn không đè `leadId`. D ×6.
15. Thread `skipped_shared` xét lại nhưng không bước nào (hết van/ma trận) → hẹn sáng mai / `skipped_matrix` (không chiếm `limit(1)` mỗi tick); có `createdAt`; sweep44: nick cờ → chuyển nick sống, > 14 ngày → đóng. D ×3.
16. `lockKeysB` khoá người theo ALLOWLIST (uid số · pfbid · `people/<tên>/<uid>` · `facebook.com/<uid số>` · username hợp lệ, %-decode); `l.php/permalink.php/events/…` không khoá người. D.
17. `drive()` (func) lấy được khoá → đọc lại lead + ghi đủ `allow/reply/uid/name`, `taskStatus queued`, xoá `skipReason`. D.
18. `outreach_locks` có `expireTs` (Timestamp) → .sh bật TTL policy (lỗi = cảnh báo) + sweep44 dọn khoá hết hạn > 24 h. D.
19. `sourceOnWrite` đọc-ghi `system_status/sources` trong transaction (2 trigger cùng group không push/WARNING đôi).
20. `SCAN_LOCK_TTL_S` 1860 ≥ timeout 1800; lượt bỏ vì khoá ghi `busyRuns/lastBusyAt`; fail-open ghi JSON WARNING. C4.
21. `harvestPostsB`: snapshot READY gặt bất kể tuổi; running/unknown/busy quá 2 h (probe) / 6 h (sweep/deep/legacy) mới bỏ (không treo vô hạn khi 429); `bdProgressB` 5xx (vd 502 nginx 10/09) = `unknown` → bdwatch bắt DOWN (không phải busy); đánh dấu `harvestedAt` trước khi xoá (xoá hụt → không gặt/tính tiền lại); `minAgeS` giữ 120 s cả khi có notify (doc `ready:true` vẫn xét ngay).
22. `.sh`: chạy lại KHÔNG tạo `.bak` mới (giữ bản gốc, `_lb_backup_ts`; có marker mà thiếu `_lb_backup_ts` → DỪNG, không backup bản đã vá); deploy lỗi → nói rõ "có thể MỘT PHẦN đã lên" + bảng describe + hướng dẫn chạy lại/quay lui TOÀN BỘ; `_lb_after.mjs` in sàn/trần nhịp, busyRuns, P_ đường cũ.

**Bác / đã đúng:** transaction đọc→ghi, batch không trùng doc, race seen (lens firestore #11); `commitLeadNow` (đã ném lỗi từ vòng sửa trước); alias gid (đã có, chỉ còn di trú → mục 5).

**Ghi nhận, để LỆNH C (không chặn):** B-9 `backfill_done` khoá theo URL+khoảng (brand vào sau không "Quét thử" cùng group; fan-out backfill toàn bộ cho mọi brand — cần `opts.brand` từ FE); `group_state` doc khoá URL cũ nằm lại (không dùng, vô hại); worker chưa ghi `touched` trực tiếp (engine đã bù); FE: nhãn ô "Sàn nhịp gieo", chip Dùng chung, `createSource`, toast 409.

## 7. KẾT QUẢ KHỐI 1 (anh chạy 12/09 12:11Z = 19:11 VN + bước (e) bổ sung 19:28 VN; exit 0) — **PASS TRỌN: 9 function + Rules + TTL đã lên**
- sha256 khối chat khớp `4332f154…ff2f`, 902 dòng · backup `TS=20260912-121124` (7 file + `firestore.rules` `.bak-20260912-121124`, `_lb_backup_ts` ghi).
- **PATCH OK 7 file** (marker `LENH B`: `lib/config.js` 1 · `lib/scraper.js` 10 · `index.js` 75 · `outreach.js` 20 · `stats.js` 2 · `scanstats.js` 1 · `lib/multitouch.js` 5) · `sources.js` tạo · `node --check` ×8 OK · Rules block `lead_links` chèn + compiled.
- **IMPORT OK** (13 hàm; `scheduledScan/manualScan/outreachTick/statsOnLead/scanStatsOnRun/createSource/sourceOnWrite/bdReady = function`; `releaseLocksB = undefined` từ `stats.js` là **ĐÚNG THIẾT KẾ** — stats.js dùng `touchLocksStB` nội bộ, không export; dòng in này chỉ là vết cũ của import test, không phải lỗi) · CFG B: `SCAN_LOCK_TTL_S 1860 · POLL_MINUTES 3` + 7 khoá B mặc định.
- **Deploy complete**: `firestore:rules` released · 8 function **ACTIVE** asia-southeast1 (updateTime 2026-09-12T12:12:54–12:13:18Z): `scheduledScan · manualScan · outreachTick · statsOnLead · scanStatsOnRun · createSource · sourceOnWrite · bdReady`; `createSource` URL `https://asia-southeast1-smartlead-z15.cloudfunctions.net/createSource`.
- (d2) TTL `outreach_locks.expireTs` OK.
- **(e) codebase2 `tagLeadBrand`: PATCH OK (marker LENH B, `brand_hint` ưu tiên) NHƯNG deploy LỖI** `Error: Not in a Firebase app directory (could not locate firebase.json)` tại `~/codebase2` → cảnh báo, không chặn (nguồn trùng TÊN khác brand hiện = 0 nên tra theo tên vẫn đúng). Nguyên nhân: function này vốn deploy bằng **gcloud** từ `~/codebase2/tagLeadBrand` (describe: `maxInstanceCount 100` dù code `setGlobalOptions({maxInstances:10})`; trigger không có filter `namespace` như bản Firebase CLI `notifyBrandZalo`) → không có `firebase.json` là bình thường. **Sửa: chạy `lenh-2026-09-12-b-e.sh`** (mục 3, đường gcloud cập nhật function có sẵn). Phải xong TRƯỚC khi thêm nguồn thứ 2 cùng group cho brand khác.
- **★ Bước (e) bổ sung ĐÃ CHẠY OK 12/09 12:28Z = 19:28 VN (`lenh-2026-09-12-b-e.sh`, exit 0)**: `~/codebase2` đúng là KHÔNG có `firebase.json` (11 thư mục function + zip; `tagLeadBrand/` mtime 12:19 = đã vá ở KHỐI 1) · marker `LENH B` 2 (dòng 75 `hint`, dòng 91 `brand_tagged_by`) · SYNTAX OK · describe TRƯỚC `ACTIVE · 2026-08-11T09:10:27Z · nodejs22 · created · asia-southeast1 · maxInstances 100 · cli-gcloud` → nhánh (2b) `gcloud functions deploy` từ source → **rev `tagleadbrand-00003-sic`, ACTIVE, updateTime `2026-09-12T12:28:58Z`**, describe SAU giữ nguyên trigger/region/maxInstances 100/`cli-gcloud` (chỉ đổi mã nguồn), URL `https://asia-southeast1-smartlead-z15.cloudfunctions.net/tagLeadBrand`. ⇒ **KHỐI 1 trọn vẹn: 9 function + Rules + TTL đã lên.** Lead mới tạo từ giờ: `brand_tagged_by` = `brand_hint` (fan-out) hoặc `scanner-inline`.
- Kế tiếp: KHỐI 2 đã chạy → xem mục 8.

## 8. KẾT QUẢ KHỐI 2 (anh chạy 12/09 19:34 VN, ~23′ sau deploy; ảnh Cloud Shell) — **PASS về cơ chế; LAG chưa đo được (mới 1 lead sau B); mục 5 báo 0 % là LỖI SCRIPT KIỂM (đã sửa)**
- **Khoá lượt + tick**: `system_status/scan phase=done`, lượt gần nhất 19:34 (14 s), `scan_lock` holder trống (nhả bởi `scheduled_…`), **busyRuns 0**, `[SCAN-BUSY]/[SCAN-ABORTED]` 0. Nhịp gieo hiệu lực: **sàn 5′** (config/app.scanIntervalMin trống) · trần 30′. `brightdata ok=true` (okStreak 0 = đúng: chỉ đếm khi đang hồi từ ngưng), chưa cần deep sweep. `sources.shared` 0 (chưa có group 2 brand — đúng, chưa tạo nguồn dùng chung).
- **group_state theo gid**: 27 doc gkey (`g_<gid số>` 18 · `s_<slug>` 9, **đã học gid 4** ở lượt đầu) + 26 doc URL cũ giữ lại không dùng. Bậc nhịp **fast 11 / idle 16** (idle = `n≥2 && rate<MID && không bài mới >6 h` — tối 19 h nhiều group im là hợp lý; group có bài mới 19:34 đều fast 5′). Ví dụ hscl-01 `g_307578305113311` rate 7,6 bài/giờ (n 3) fast 5′ gieo 19:34 · gặt 19:34 · sweep 18:40 · watch 5.
- **Chu trình gieo gộp → gặt**: 19:13 (lượt B đầu) gặt 6 snapshot cũ + **gieo 17 group + sweep 1** (start_date −6 h) → 19:16 gặt 2 · gieo 6 → 19:22 gặt 2 (13/32 record) · **gieo 23 group (2 escalate)** → 19:28 gặt 1 gộp (10/13) · gieo 7 → 19:31 hk ✓ (0/0, việc phụ) → 19:34 gặt 1 gộp (10/13 record, 7 group) · gieo 7 · ứng viên 10. ⇒ **1 trigger/lượt cho mọi group tới nhịp** (gộp ≤40) đúng thiết kế; group fast gieo lại mỗi ~6′ (2 tick), snapshot chín sau 3–6′ → gặt ở tick kế. `pending_snapshots` PB_ 1 (1′, 7 group) · C_ 0 · P_ đường cũ 0. `skipRuns` 1 (19:19 "lượt việc phụ không có gì để chăm — không ghi scans") · `dupLead 0 · orphan 0 · busy 0 · deep 0`. `gidLearned` +4/+2/+1 ở 3 lượt đầu.
- **Lead 24 h**: 89 · **sau B: 1 lead** (19:13–19:34, tối) — có đủ `id L_<post_id>__<brand>` · `shared_post` · `textKey` · `brand_hint` = brand · `brand_tagged_by scanner-inline` (inline thắng race với tagLeadBrand — bình thường). Gộp textKey 0, brand ≠ brand_hint 0, brand_hint chưa có brand 0. `lead_links` 0 · `outreach_locks` 0 · `skipped_shared` 0 (chưa có group chung + nick automation đều tắt) — đúng.
- **LAG phát hiện** (detected_at − time bài, lead-bài n=79 trong 24 h): trung vị **10′** · p95 **22′** · <15′ 82 % (mốc gốc #47: 11′ / 36′ / 69,6 %). **Chưa kết luận được** vì 78/79 mẫu là lead TRƯỚC deploy B; cần chạy lại `node _lb_after.mjs` sáng 13/09 (~9–10 h VN, giờ có bài) — kỳ vọng trung vị 5–7′ / p95 ≤ 20′.
- **Mục 5 "seen 6 h: 300 doc · có brands map 0 (0 %)" = LỖI SCRIPT KIỂM, không phải lỗi patch**: query `where('at','>=',6 h).limit(300)` không `orderBy` → Firestore trả **300 doc CŨ NHẤT** trong 6 h (13:34→…) = toàn bài trước deploy B → 0 % là oan (lead sau B có `brand_hint/textKey` chứng tỏ đường B đã chạy). Đã sửa `docs/lenh-2026-09-12-b-after.mjs` (`orderBy('at','desc')` + in mốc mới nhất); Cloud Shell: vá `_lb_after.mjs` bằng khối mục 4b rồi chạy lại → kỳ vọng ≥ 95 % doc mới nhất có `brands` (`pending` chỉ ở bài đang chờ AI).
- **Log 24 h**: 12 dòng — 6 `[B]` (mỗi lượt gieo/gặt 1 dòng) + 6 khác; 0 `[SCAN-BUSY]`, 0 `[SCAN-ABORTED]`, 0 `[SOURCE-SHARED]` (chưa tạo nguồn chung). `hk ✓` 3 lượt (19:13/19:25/19:31 — nhịp việc phụ 5′ ≈ mỗi 2 tick).
- **Tuỳ chọn tinh chỉnh (không bắt buộc)**: group im >6 h chờ tối đa 30′ mới gieo lại (trần `config/app.scanIntervalMaxMin`, mặc định `SCAN_INTERVAL_MAX_MIN` 30) — sáng 8 h bài đầu tiên của group im qua đêm có thể chậm tới 30′; muốn gắt hơn đặt `scanIntervalMaxMin: 15` (tốn thêm ~1 trigger gộp/15′ lúc đêm, record gần 0 vì probe 5 bài + notInclude).
- **Kết luận**: KHỐI 2 PASS về cơ chế (khoá lượt, gid, gieo gộp, gặt claim, nhịp thích ứng, lead id tất định, brand_hint, không busy/abort). **Còn 2 việc nghiệm thu**: (a) chạy khối 4b NGAY để xác nhận mục 5; (b) chạy lại `node _lb_after.mjs` sáng 13/09 để chốt LAG + số lead/giờ ban ngày. Sau đó → LỆNH C → zip FE (nhãn "Sàn nhịp gieo", chip Dùng chung, form nguồn qua `createSource`, toast 409).

### 4b. Vá `_lb_after.mjs` trên Cloud Shell (mục 5 orderBy desc) + chạy lại — chỉ đọc, idempotent
```bash
cd ~/firebase-s13/functions && cat > /tmp/lb_p4b.js <<'EOF_P4B'
const fs=require('fs');const f='_lb_after.mjs';let s=fs.readFileSync(f,'utf8');
if(/3600e3\)+\.orderBy\('at', 'desc'\)\.limit\(300\)/.test(s)){console.log('đã vá (mục 5 seen đã orderBy desc)');}
else{const n=(s.match(/3600e3\)+\.limit\(300\)/g)||[]).length;if(n!==2){console.error('KHONG THAY MOC (đếm '+n+', cần 2)');process.exit(1);}
s=s.replace(/(3600e3\)+)\.limit\(300\)/g,"$1.orderBy('at', 'desc').limit(300)");fs.writeFileSync(f,s);console.log('PATCH OK _lb_after.mjs mục 5 (orderBy desc, 2 mốc)');}
EOF_P4B
node /tmp/lb_p4b.js && node --check _lb_after.mjs && node _lb_after.mjs
```

## 9. KẾT QUẢ KHỐI 2 LẦN 2 (anh chạy khối 4b + `_lb_after.mjs` 13/09 04:18 VN, ~9 h sau deploy; ảnh Cloud Shell) — **PASS: mục 5 đã đúng (100 %), engine chạy đêm ổn định, 18 lead sau B đủ dấu; LAG vẫn chưa kết luận (mẫu đêm) → khối 4c `_lb_lag.mjs`**
- **Khối 4b**: `PATCH OK _lb_after.mjs mục 5 (orderBy desc, 2 mốc)` → `node --check` OK → chạy lại. Chạy lần nữa sẽ in "đã vá" (idempotent).
- **Khoá lượt + tick**: `scan_lock` trống, **busyRuns 0**, 12 lượt gần nhất **2–6 s** đều đặn 3′ (sown 0–9 · harvested 0–2 · dup 0 · orphan 0 · busy 0 · deep 0), `hk ✓` mỗi ~6′ (2 tick), `[SCAN-BUSY]/[SCAN-ABORTED]/[SOURCE-SHARED]` 0. Log 24 h 352 dòng (`[B]` 176 = 1 dòng/lượt gieo/gặt). `skipRuns` 11 (lần cuối 22:49) — ban đêm hầu như tick nào cũng có group tới nhịp nên ít lượt thuần skip; đúng thiết kế (lượt có việc mới ghi `scans`).
- **group_state**: 29 doc gkey (`g_` 20 · `s_` 9, **đã học gid 6**) · bậc nhịp **slow 16 / mid 6 / fast 7** với **đêm ×2** (mid 20′, fast 10′, slow trần 30′). Ví dụ hscl-01 rate 2,1 bài/giờ (n 50) → **mid 20′** vì đã rớt dưới ngưỡng RA fast (hysteresis: đang fast phải ≥3,2; vào fast cần ≥4,8); tuyendung rate 1,78 → **fast 10′** nhờ quy tắc "bài mới <30′ → fast". Khớp `ivB`, KHÔNG phải lỗi. pending PB_ 1 / C_ 0 / P_ 0.
- **Mục 5 seen (đã vá)**: 6 h → **156/156 doc MỚI NHẤT có `brands` map (100 %)**, quyết định theo brand {pre 94, low 13, lead 10, seller 11, ex 12, self 16}, bài ≥2 brand 0 (chưa có group chung) ⇒ **0 % ở lần 1 là lỗi script, đường B ghi `seen.brands` đúng**.
- **Lead 24 h**: 66 · **sau B 18** — 18/18 id `L_<post_id>__<brand>` · shared_post · `brand_hint` = brand (0 lệch, 0 thiếu brand) · textKey 16/18 (2 thiếu = comment-lead, `textKeyB` chỉ gắn cho bài — đúng) · `brand_tagged_by scanner-inline` 66/66 (inline thắng race với `tagLeadBrand` → hint chỉ là lưới dự phòng). `lead_links` 0 · `outreach_locks` 0 · `skipped_shared` 0 (chưa có group chung + nick automation tắt).
- **LAG (mục 6, n=60 lead-bài 24 h)**: trung vị **10′** · p95 **35′** · <15′ 73 % (mốc gốc 11′/36′/69,6 %) — **chưa kết luận**: mẫu TRỘN 42 lead trước B + 18 sau B, và 18 lead sau B đều rơi vào **19 h–04 h** (đêm: nhịp ×2 = 10–30′ theo thiết kế, không phải mốc so). Cần đo riêng lead sau B ban ngày → **khối 4c**.
- **Kết luận**: LỆNH B PASS về cơ chế + dữ liệu (seen/lead/lock/gid). **Việc còn lại duy nhất để đóng B**: chạy khối 4c sau vài giờ có bài (9–17 h VN 13/09) → kỳ vọng ban ngày trung vị 5–7′ · p95 ≤ 20–25′ (script tự kết luận PASS/CHƯA ĐẠT khi ≥20 mẫu). Chưa đạt → nhìn mục 5/6 của script: lead ở group slow/idle 30′ → hạ `config/app.scanIntervalMaxMin: 15`; lead ở group fast 5′ mà vẫn >15′ → độ trễ nằm ở BrightData (không phải nhịp). Sau đó → LỆNH C → zip FE.

### 4c. Đo LAG chỉ lead SAU B, tách ngày/đêm, theo brand/bậc nhịp/group — chỉ đọc (`lenh-2026-09-12-b-lag.mjs`; chạy lúc 9–17 h VN)
```bash
cd ~/firebase-s13/functions && cat > _lb_lag.mjs <<'EOF_LAG'
/* LỆNH B — ĐO LAG PHÁT HIỆN LEAD (13/09/2026) — CHỈ ĐỌC. Chốt mục tiêu tốc độ của hướng B (mốc gốc #47: trung vị 11′ · p95 36′ · <15′ 69,6 %).
   Khác `_lb_after.mjs` mục 6 (đo TRỘN mọi lead 24 h): script này (1) CHỈ lấy lead sinh SAU deploy B (có `brand_hint`), (2) tách BAN NGÀY 08–22 VN ↔ ĐÊM (đêm nhịp ×2 theo thiết kế, không so với mốc),
   (3) theo brand · theo bậc nhịp của group (group_state g_<gid>) · theo group · theo giờ, (4) phân bố bucket <5/5–10/10–15/15–30/30–60/≥60′, (5) kết luận tự động khi đủ ≥20 mẫu ban ngày.
   LAG = detected_at (lúc SmartLead ghi lead) − time (giờ đăng bài do BrightData trả) → gồm cả độ trễ BrightData lập chỉ mục + chờ tới nhịp gieo + chờ snapshot chín + tick 3′.
   Chạy: `node _lb_lag.mjs [số giờ, mặc định 24]`. Đọc theo TRANG ≤300 + select() (bài học #36/#37/#47). Đặt trong ~/firebase-s13/functions. Không ghi gì. */
import { initializeApp, applicationDefault, getApps } from 'firebase-admin/app';
import { getFirestore } from 'firebase-admin/firestore';
if (!getApps().length) initializeApp({ credential: applicationDefault(), projectId: 'smartlead-z15' }); const db = getFirestore();
const OFF = 7 * 3600e3, now = Date.now(), H = Math.min(168, Math.max(1, Number(process.argv[2]) || 24));
const ms = v => !v ? 0 : (typeof v === 'number' ? v : (v.toMillis ? v.toMillis() : (v.seconds ? v.seconds * 1000 : (Date.parse(v) || 0))));
const hm = v => ms(v) ? new Date(ms(v) + OFF).toISOString().slice(5, 16).replace('T', ' ') : '—';
const hourVN = v => new Date(ms(v) + OFF).getUTCHours();
const pct = (a, b) => b ? Math.round(100 * a / b) + ' %' : '—';
const srt = arr => arr.slice().sort((x, y) => x - y); const med = arr => { const a = srt(arr); return a.length ? a[Math.floor(a.length / 2)] : 0; }; const pq = (arr, p) => { const a = srt(arr); return a.length ? a[Math.min(a.length - 1, Math.floor(a.length * p))] : 0; };
const stat = arr => arr.length ? 'n ' + arr.length + ' · trung vị ' + Math.round(med(arr)) + '′ · p75 ' + Math.round(pq(arr, 0.75)) + '′ · p95 ' + Math.round(pq(arr, 0.95)) + '′ · <15′ ' + pct(arr.filter(x => x < 15).length, arr.length) : 'n 0';
const BK = [[0, 5, '<5′'], [5, 10, '5–10′'], [10, 15, '10–15′'], [15, 30, '15–30′'], [30, 60, '30–60′'], [60, 1e9, '≥60′']];
const buckets = arr => BK.map(([a, b, l]) => l + ' ' + arr.filter(x => x >= a && x < b).length).join(' · ');
async function pageAll(q, orderField, sel, max) { const out = []; let last = null; while (out.length < (max || 3000)) { let qq = q.orderBy(orderField).limit(300); if (sel && sel.length) qq = qq.select(...sel); if (last) qq = qq.startAfter(last); const s = await qq.get(); s.docs.forEach(d => out.push(Object.assign({ id: d.id }, d.data()))); if (s.size < 300) break; last = s.docs[s.docs.length - 1]; } return out; }
console.log('== LỆNH B — LAG PHÁT HIỆN ' + H + ' h gần nhất — ' + new Date(now + OFF).toISOString().slice(0, 16).replace('T', ' ') + ' VN ==');
const L = await pageAll(db.collection('leads').where('detected_at', '>=', new Date(now - H * 3600e3)), 'detected_at', ['kind', 'brand', 'brand_hint', 'gid', 'source', 'post_id', 'detected_at', 'time', 'ai_scored'], 6000);
const gs = (await db.collection('group_state').get().catch(() => ({ docs: [] }))).docs.map(d => Object.assign({ id: d.id }, d.data()));
const gById = {}; gs.forEach(g => { gById[g.id] = g; }); const bandOf = l => { const g = l.gid ? gById['g_' + String(l.gid)] : null; return g ? (g.band || '?') + ' ' + (g.iv || '?') + '′' : '(không rõ)'; };
const rows = L.filter(l => l.kind !== 'comment' && l.time && ms(l.detected_at)).map(l => Object.assign({ lag: (ms(l.detected_at) - ms(l.time)) / 60000, day: hourVN(l.detected_at) >= 8 && hourVN(l.detected_at) < 22 }, l)).filter(r => r.lag >= 0 && r.lag < 24 * 60);
const cmt = L.filter(l => l.kind === 'comment').length, bad = L.length - cmt - rows.length;
const postB = rows.filter(r => r.brand_hint), preB = rows.filter(r => !r.brand_hint);
console.log('1. Lead ' + H + ' h: ' + L.length + ' (lead-bài đo được ' + rows.length + ' · comment-lead ' + cmt + ' bỏ qua · thiếu time/lệch ' + bad + ')  →  SAU B (có brand_hint) ' + postB.length + ' · TRƯỚC B ' + preB.length + (postB.length ? ' · lead sau B đầu tiên ' + hm(postB[0].detected_at) : ''));
console.log('   mốc gốc #47 (trước B, 30 ngày): trung vị 11′ · p95 36′ · <15′ 69,6 %');
console.log('2. TRƯỚC B (chỉ để so):  ' + stat(preB.map(r => r.lag)) + (preB.length ? '  |  ' + buckets(preB.map(r => r.lag)) : ''));
const dayB = postB.filter(r => r.day), nightB = postB.filter(r => !r.day);
console.log('3. SAU B — BAN NGÀY 08–22 VN (so với mốc):  ' + stat(dayB.map(r => r.lag)) + (dayB.length ? '  |  ' + buckets(dayB.map(r => r.lag)) : ''));
console.log('   SAU B — ĐÊM 22–08 VN (nhịp ×2 theo thiết kế, không so mốc):  ' + stat(nightB.map(r => r.lag)) + (nightB.length ? '  |  ' + buckets(nightB.map(r => r.lag)) : ''));
{ const by = {}; dayB.forEach(r => { const k = r.brand_hint || '?'; (by[k] = by[k] || []).push(r.lag); }); const ks = Object.keys(by).sort((a, b) => by[b].length - by[a].length);
  console.log('4. SAU B ban ngày theo BRAND: ' + (ks.length ? '' : '(chưa có)')); ks.forEach(k => console.log('   ' + k + ': ' + stat(by[k]))); }
{ const by = {}; postB.forEach(r => { const k = bandOf(r); (by[k] = by[k] || []).push(r.lag); }); const ks = Object.keys(by).sort((a, b) => by[b].length - by[a].length);
  console.log('5. SAU B (ngày+đêm) theo BẬC NHỊP group lúc đo (band hiện tại của group_state, có thể đã đổi): ' + (ks.length ? '' : '(chưa có)')); ks.forEach(k => console.log('   ' + k + ': ' + stat(by[k]))); }
{ const by = {}; postB.forEach(r => { const k = (r.gid ? 'g_' + r.gid : '(không gid)') + ' ' + String(r.source || '').slice(0, 28); (by[k] = by[k] || []).push(r.lag); }); const ks = Object.keys(by).sort((a, b) => by[b].length - by[a].length).slice(0, 12);
  console.log('6. SAU B theo GROUP (top 12 theo số lead): ' + (ks.length ? '' : '(chưa có)')); ks.forEach(k => console.log('   ' + k + ': ' + stat(by[k]))); }
{ const by = {}; postB.forEach(r => { const k = hourVN(r.detected_at); (by[k] = by[k] || []).push(r.lag); }); const ks = Object.keys(by).map(Number).sort((a, b) => a - b);
  console.log('7. SAU B theo GIỜ phát hiện (VN): ' + (ks.length ? ks.map(k => String(k).padStart(2, '0') + 'h n' + by[k].length + '/' + Math.round(med(by[k])) + '′').join(' · ') : '(chưa có)')); }
{ const n = dayB.length, m = med(dayB.map(r => r.lag)), p = pq(dayB.map(r => r.lag), 0.95), slow = dayB.filter(r => r.lag >= 30).length;
  if (n < 20) console.log('8. KẾT LUẬN: CHƯA ĐỦ MẪU ban ngày sau B (n ' + n + ' < 20) → chạy lại sau vài giờ có bài (9–17 h VN): node _lb_lag.mjs 24');
  else if (m <= 8 && p <= 25) console.log('8. KẾT LUẬN: ✓ PASS tốc độ — ban ngày sau B trung vị ' + Math.round(m) + '′ (mốc 11′, kỳ vọng 5–7′) · p95 ' + Math.round(p) + '′ (mốc 36′, kỳ vọng ≤ 20–25′)');
  else console.log('8. KẾT LUẬN: ⚠ CHƯA ĐẠT — ban ngày sau B trung vị ' + Math.round(m) + '′ · p95 ' + Math.round(p) + '′ · ≥30′ ' + slow + '/' + n + ' → xem mục 5/6: group nào chậm & đang ở bậc nào (slow/idle 30′ = group ít bài, cân nhắc config/app.scanIntervalMaxMin: 15) hay lag do BrightData (lead ở group fast 5′ mà vẫn >15′)'); }
EOF_LAG
node --check _lb_lag.mjs && node _lb_lag.mjs 24
```
