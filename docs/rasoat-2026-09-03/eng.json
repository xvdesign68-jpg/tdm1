[
 {
  "id": "P08",
  "exists": "partial",
  "exists_evidence": "Đã có nguyên liệu nhưng CHƯA có nút deep-link: `zaloDial()` là code chết tại src/app/20-feed.js:675 (0 nơi gọi, CLAUDE.md v119-17 liệt kê); `normPhoneVN`/`phoneLocal` 20-feed.js:680-681; `contactPills()` 20-feed.js:765-770 chỉ gọi `pillHtml()` (758) = pill chép số qua `__slCopy`; modal dùng lại contactPills tại 65-charts-lead-modal.js:64; `kCardV2` 30-pipeline.js:304-320 KHÔNG render SĐT. grep `tel:|sms:|zalo.me|pointer: coarse` trong src/app + app.css = 0 kết quả. `escUrl` (50-config-views.js:131) chỉ cho phép http(s) → `tel:`/`sms:` sẽ bị đổi thành '#', cần helper riêng. CSP (netlify.toml:24) KHÔNG có form-action/navigate-to → link tel:/sms:/zalo.me không bị chặn. ⚠ `l.touches` ĐÃ TỒN TẠI với nghĩa khác: mảng lần xuất hiện đa nhóm do scraper ghi (20-feed.js:689, 793, 803 render timeline) → không được ghi `touches{channel,by,at}` đè lên.",
  "feasible": "yes",
  "realistic_effort": "S",
  "days_estimate": "1.5–2 ngày (FE) + 0.5 ngày nếu ghi nhật ký chạm cần Rules",
  "backend_needs": [
   "rules"
  ],
  "risks": "(1) Trùng tên field: proposal đặt `touches` nhưng lead đã có `touches[]` của scraper → phải dùng `contact_log[]` hoặc subcollection `leads/{id}/contacts` (giống notes) — nếu dùng arrayUnion vào field mới trên doc lead thì Rules `leads` update chưa được kiểm whitelist (CLAUDE.md:399 vẫn là mục tồn) → kiểm trước khi ship. (2) Desktop: `tel:`/`sms:` không có handler → nút vô dụng, chỉ nên hiện trên `pointer:coarse` hoặc ẩn khi không phải mobile; zalo.me trên desktop mở Zalo Web (OK). (3) `phone_has_zalo===undefined` (đa số lead chưa eKYC) → vẫn hiện nút Zalo, đúng như proposal chấp nhận. (4) Realtime: feed diff thay thẻ theo lead.id (v119-23 giữ focus ô ghi chú) — thêm HTML tĩnh vào thẻ không phá diff; kanban pvRepaint vẽ lại cả bảng nên không vấn đề. (5) Pháp lý: gọi/nhắn lead quét từ group chưa có đồng ý (NĐ13) — thao tác do NGƯỜI bấm nên chấp nhận được, nhưng mẫu SMS phải có opt-out.",
  "implementation_sketch": "- 20-feed.js: viết `contactBar(l)` thay `pillHtml()` trong `contactPills()` (765): nút Gọi `href=\"tel:\"+digits` · Zalo dùng lại `zaloDial()` (675, hồi sinh) ẩn khi `phone_has_zalo===false` · SMS `sms:<số>?body=`+encodeURIComponent(mẫu brand) · Chép giữ `__slCopy`. Helper mới `escTelUrl(p)` chỉ giữ `[0-9+]` rồi esc() (không qua `escUrl` vì nó chặn scheme ngoài http).\n- 30-pipeline.js `kCardV2` (304): chèn `${contactBar(l,{compact:true})}` vào `.pv2-foot` (thẻ kanban hiện chưa có SĐT); modal tự có vì dùng contactPills (65:64).\n- app.css: `.ct-bar .btn{min-height:44px}` trong `@media (pointer:coarse)` (chưa có media này) + ẩn Gọi/SMS ở `pointer:fine`.\n- Nhật ký chạm (nền P07): `SL_FB.logContact(leadId,channel)` = `updateDoc(leads/{id},{contact_log:arrayUnion({channel,by:uid,at:Date.now()})})` tại live.js cạnh `updateLead` (423) — TÊN KHÁC `touches`; nếu Rules leads có whitelist thì thêm `contact_log`; hoặc ghi subcollection `leads/{id}/contacts` (Rules pattern như notes).\n- smoke.js: check thẻ feed demo có `a[href^=tel:]`; verify bản min bằng string 'zalo.me'.",
  "risks_note": ""
 },
 {
  "id": "P09",
  "exists": "partial",
  "exists_evidence": "Zalo OA/ZBS ĐÃ có ở tầng HỆ THỐNG nhưng chỉ để BÁO SALES, không phải nhắn LEAD: codebase `~/smartlead-zalo-fn` (bootstrapZaloTokens, sendZaloZBS, refreshZaloToken, notifyBrandZalo — CLAUDE.md:76); FE danh sách 'Sales nhận thông báo theo brand' qua Zalo ZBS OA chính chủ (85-users-admin.js:200, live.js:726); lead có field `zalo_notified` (CLAUDE.md mục dữ liệu BrightData); card kênh 'Zalo OA' trong Tích hợp chỉ là toggle cấu hình (live.js:44, 50-config-views.js:345 'cần token trong functions/.env'). Zalo cá nhân qua func: CHỈ research (CLAUDE.md mục 'func.vn' — FIND_USER→REQUEST_FRIEND→USER_SEND_MESSAGE đã đọc docs, chưa test, chưa code); engine outreach.js chỉ xử lý FB (stepNick/stepNickAdspower), grep `platform.*zalo|'zalo'` trong 45-outreach.js/live.js = 0 (chỉ có `platform` ghi 'facebook' khi saveFbAccount). Ma trận ⚙️ brand (45-outreach.js) chỉ 4 hành động FB.",
  "feasible": "yes-with-backend",
  "realistic_effort": "XL",
  "days_estimate": "Lớp OA/ZBS cho lead: 8–10 ngày (nếu dùng 1 OA Z15 chung: 4–5 ngày); lớp Zalo cá nhân qua func: 10–12 ngày; tổng 4–6 tuần (chưa tính thời gian duyệt mẫu ZBS + xác thực OA)",
  "backend_needs": [
   "cf",
   "secret",
   "external-api",
   "rules",
   "index",
   "data-migration"
  ],
  "risks": "(1) ToS Zalo: kết bạn/nhắn người lạ bằng công cụ = khoá số vĩnh viễn (market.md:57), tin lạ vào mục 'Tin nhắn từ người lạ' nhiều người tắt → hiệu quả có trần; cần điều khoản 'nick của brand, brand chịu rủi ro' + van ≤20 mời/ngày (thấp hơn ngưỡng 50). (2) func Zalo chưa test thật (khác FB đã test) — session QR rớt như FB (needLogin) → phải tái dùng cơ chế needLogin/challenge. (3) ZBS: chỉ OA xác thực gói Tăng trưởng 2,5tr/năm mới gửi template; mọi mẫu phải duyệt; quota ngày + 1 tin hậu mãi/user/ngày (market.md:53-54) → OA theo TỪNG brand = mỗi khách hàng phải tự mua gói + duyệt mẫu (onboarding nặng); OA chung Z15 thì tin mang thương hiệu Z15, không phải brand. (4) OAuth Zalo OA v4: refresh token hết hạn 3 tháng → cần cron refresh/brand (đã có refreshZaloToken cho 1 OA, phải tham số hoá theo brand). (5) Pháp lý NĐ13: lead quét chưa có đồng ý — ZBS Giao dịch chỉ hợp lệ khi lead ĐÃ tương tác (responded/booked) đúng như proposal giới hạn. (6) Firestore: query lead `brand+stage in[responded,booked]+phone!=''` cần composite index mới; scheduler thêm 1 tick nữa → dùng chung outreachTick để không tick chồng (maxInstances:1).",
  "implementation_sketch": "- Lớp 1 (ưu tiên, hợp lệ): CF `zaloOaConnect` (OAuth code→token, lưu Secret Manager `zalo-oa-<brand>`) + `zaloOaRefresh` onSchedule; mở rộng `sendZaloZBS` nhận `{brand, phone, templateId, params}` đọc token theo brand (fallback OA Z15). Trigger: onDocumentUpdated `leads/{id}` khi stage→responded/booked & có `phone` → gửi mẫu; webhook OA (`zaloOaWebhook`, verify secret) → ghi note hệ thống + stage như funcWebhook. FE: 50-config-views.js `views.integrations` thêm card 'Zalo OA' theo brand (nút Kết nối → URL OAuth), 65-charts-lead-modal nút 'Gửi ZBS' (chọn mẫu) → `SL_FB.sendZbs(leadId,tpl)` = HTTP CF Bearer idToken (pattern saveFbAccount, region asia-southeast1). Rules: `zalo_oa/{brand}` chỉ metadata (không token), read admin brand.\n- Lớp 2 (van thấp): `fb_accounts.platform='zalo'` (đổi tên hiển thị 'Tài khoản nick'), outreach.js thêm `stepNickZalo`: lọc lead brand where temp in[hot,warm] & phone_has_zalo==true → FIND_USER→REQUEST_FRIEND (ghi `outreach_threads.step='zalo_friend_sent'`) → tick sau `GET_REQUEST_FRIEND_STATUS` → USER_SEND_MESSAGE(lead.reply + opt-out); CAPS thêm `zalo_friend:20, zalo_msg:20`, giờ 8–21h; tái dùng tryConsume/outreach_usage/outreach_log/expireAt/needLogin.\n- FE 45-outreach.js: ma trận ⚙️ thêm hàng '📱 Zalo' (friend/msg) → `brands.outreach.matrix.*.zalo_*`; bảng nick thêm cột Kênh (FB/Zalo); modal Thêm nick chọn platform; live.js `saveFbAccount` truyền `platform`.\n- Backend: index `leads(brand, temp, phone_has_zalo, detected_at)`; Rules không đổi (engine Admin SDK); funcWebhook mở rộng event Zalo (uid Zalo→lead qua outreach_map).\n- Đề nghị cắt: ship Lớp 1 với OA chung Z15 trước (4–5 ngày) để đo phản hồi, Lớp 2 chỉ bật cho brand ký điều khoản rủi ro."
 },
 {
  "id": "P10",
  "exists": "partial",
  "exists_evidence": "Không có view 'today'/part 25-today (grep `views.today|25-today` = 0). Nguyên liệu đã có nhưng rải rác: rail feed 'Lead của tôi' (20-feed.js:610, `isMine()` 292) + 'HÔM NAY CẦN CHĂM' (614-618 lọc `fu_at<=eod`) — rail bị ẩn hoàn toàn ở ≤1140px (`.feed-rail{display:none}` pipeline-v2.css:483); `markFirstCare` set first_care_at (20-feed.js:298-301); `crmCfg()` slaWarn/slaBad theo brand (305-313); `slaChip`/`fuChip` (317, 331). KPI delta cứng '+12,4%/+8,1%/+15,2%/+0,6đ' tại 20-feed.js:116-119 và `kpi.scanned = valid*14` bịa tại live.js:129 — xác nhận đúng như proposal. Router `go()` 10-core-overview.js:49-60 (`views[name]`), registry `views.X=function` (20-feed.js:108 overview).",
  "feasible": "yes",
  "realistic_effort": "M",
  "days_estimate": "Card đầu Overview (quick win): 1.5–2 ngày; view `today` riêng + mặc định mobile + CSS: thêm 3 ngày → tổng 4–5 ngày",
  "backend_needs": [
   "none"
  ],
  "risks": "(1) Số liệu tính trên cửa sổ lead đang nạp (500 realtime + leadsExtra/auto-fill ≤4000, cap `leadsCapped`) — với 'hôm nay' là đủ nhưng 'Đã chốt tuần này' phụ thuộc `stage_at` chỉ có từ v167 (lead cũ không có → undercount, phải fallback first_care_at). (2) Lead 'chưa ai chăm' dùng `first_care_at` — mốc ghi bằng đồng hồ client (CLAUDE.md:404) → lệch nhẹ. (3) `SLApp.reload` (90-boot): view mới đi đường repaint mềm + hoãn khi input focus; card có nút 'Xong' đổi D.leads → phải gọi renderer cục bộ (như pvRepaint) chứ không đợi snapshot, tránh cảm giác 'bấm không ăn'. (4) Đổi view mặc định trên mobile đụng `go()` fallback 'overview' và deep-link hash — cần giữ hash `#today` không nằm trong SUPER_ONLY_VIEWS, nav item mới trong app.html + Cmd-K palette. (5) Bỏ KPI 'Bài đã quét' bịa: D.scans có số bài/lượt quét nhưng scoped theo brand không rõ → với brand user nên ẩn (đúng đề xuất) thay vì tính sai kiểu khác.",
  "implementation_sketch": "- Bước 1 (S): 20-feed.js `views.overview` (108): với role≠super hoặc viewport ≤639 chèn `todayCard()` trước hàng KPI; `kpiCard(...)` bỏ tham số delta cứng (166-170 đã hỗ trợ delta rỗng) và ẩn ô 'Bài đã quét' cho brand user.\n- `todayCard()` tính từ `D.leads`: quaHen=`fu_at<=now && stage!=='closed'`; henHomNay=lọc theo eod như rail 614; nongChuaCham=`temp==='hot' && !first_care_at && stage==='new' && ageMin>crmCfg(l).warn` (tái dùng logic slaChip 317-325); vuaGiao=`assigned_at>now-24h && isMine(l)`; chotTuan=`stage==='closed' && (stage_at||first_care_at)>=đầu tuần`. Top-3 'nên gọi ngay' = sort `score*(1+ageMin/slaBad)`.\n- Hành động dòng: 'Xong' → `markFirstCare(l)` (298) + re-render card; '+Hẹn kế' → mở modal `openLead(id)` (fuBox có sẵn); Gọi/Zalo dùng `contactBar` từ P08.\n- Bước 2 (M): part mới `src/app/25-today.js` (`views.today`, cùng IIFE scope; tools/gen-globals tự quét) + nav item `data-view=\"today\"` trong app.html + CSS `.td-*` mobile-first; 90-boot: nếu `matchMedia('(max-width:639px)')` và !roleIsSuper() và không có hash → `go('today')`.\n- smoke.js thêm check card Hôm nay trong demo (bơm lead có fu_at quá hạn)."
 },
 {
  "id": "P11",
  "exists": "partial",
  "exists_evidence": "`pvMoveNext(id)` là code chết tại 30-pipeline.js:197-202 (gọi `pvApplyStage(id, D.stages[i+1].key)`), `pvApplyStage` 176-196 đã optimistic + hoàn tác qua `pvToastUndo` (62) + ghi stage_at/first_care_at; kéo-thả HTML5 `dragstart` (30-pipeline.js:257) không chạy touch; mobile ≤639px kanban đổi `flex-direction:column` (pipeline-v2.css:358 trong @media 339) → đúng 'xếp dọc 7 cột'. Modal: thanh segment `<i class=on>` không bấm được (65-charts-lead-modal.js:109-111); nút 'Đánh dấu đã xử lý' set thẳng `{stage:'closed'}` (65:136,153) và `updateLead()` luôn `closeModal()` sau khi ghi (65:148). Không có bottom-sheet/tab stage nào (grep `sheet|data-stage-tab` = 0).",
  "feasible": "yes",
  "realistic_effort": "S",
  "days_estimate": "2 ngày (tab mobile + sheet + nút Bước kế + chip modal + smoke)",
  "backend_needs": [
   "none"
  ],
  "risks": "(1) Modal giữ closure `l` và `updateLead` đóng modal — chip stage phải gọi `pvApplyStage` rồi re-render riêng khối `.ld-stage` (không gọi updateLead của modal) để modal không đóng; `pvApplyStage` chỉ `pvRepaint()` khi có `.kanban` trong #view nên từ modal/feed không vẽ lại kanban — OK. (2) Chống bấm đúp: pvApplyStage đã `return` khi `lead.stage===stage`, nhưng 2 tap nhanh '→ Bước kế' sẽ nhảy 2 bước → disable nút 600ms sau tap. (3) Tab ngang mobile = nhánh render mới trong `pipelineV2()` (204) và `pvColMore` cap 50/cột phải giữ; pvRepaint đang vẽ full innerHTML → khi đổi tab phải nhớ tab đang chọn qua biến module (như feedFilter). (4) Đổi 'Đánh dấu đã xử lý' → 'Đã chốt'/'Không thành' đụng ngữ nghĩa KPI closeRate (live.js:129) và cần stage 'lost' (D.stages hiện không có) → gói chung P26/P34, không làm ở đây. (5) Chuyển lùi >1 bước confirm nhỏ: dùng `confirm()` native trên mobile là đủ, tránh modal chồng modal (modalBg dùng chung).",
  "implementation_sketch": "- 30-pipeline.js: `kCardV2` (304) thêm nút `<button data-pv-next>` → hồi sinh `pvMoveNext` (197) + guard chống đúp; tap tên giai đoạn/`data-pv-stage` mở `pvSheet(id)`: bottom-sheet 7 lựa chọn (D.stages) + 'Đặt hẹn' (openLead) + Gọi/Zalo (contactBar P08) → `pvApplyStage(id,stage)`.\n- `pipelineV2()` (204): nếu `matchMedia('(max-width:639px)')` render `.pv-tabs` (7 tab + đếm, biến `pvTab` giữ tab) + danh sách compact cột đó thay `.kanban`; giữ `pvColMore` và bộ lọc.\n- 65-charts-lead-modal.js:109-111: `.segs i` → `<button data-stage=\"key\">` có nhãn; handler gọi `pvApplyStage(l.id,key)` rồi re-render riêng `.ld-stage` từ `D.leads.find(...)` (không closeModal); chuyển lùi >1 bậc → confirm.\n- CSS pipeline-v2.css @media ≤639: `.pv-tabs` cuộn ngang, `.pv-sheet` fixed bottom, tap ≥44px.\n- smoke.js: viewport 390px → có `.pv-tabs` + bấm '→ Bước kế' đổi stage demo."
 },
 {
  "id": "P12",
  "exists": "none",
  "exists_evidence": "Mô hình hiện tại là 1 user = 1 brand string: live.js:927 `brand = isSuper ? '' : (data.brand||'')`, fail-closed 'nobrand' 950-955, `dataKey = role|brand|khung` 970-972, `startData(role,brand)` 743-748 `byBrand`; MỌI truy vấn tenancy đều `where('brand','==',byBrand)` (live.js:221 leads, 364-365 notes collectionGroup, 762 sources, 846 scanned, 871 brand doc, 512 users) — grep `where(...'in'` = 0. Xem nhiều brand chỉ qua `SUPER_EMAIL` hard-code (live.js:19, 906). Khối brand sidebar tĩnh (80-rbac-auth.js:251-290, hiện tier từ `MB.plan.tier`). Rules `users/{uid}` chặn user tự sửa `brand` (CLAUDE.md:79 affectedKeys hasAny(['role','active','brand',...])) → user KHÔNG thể tự đổi brand đang chọn bằng client write. Không có `users.brands[]`/`brandRoles`/role `owner` ở đâu (grep = 0).",
  "feasible": "yes-with-backend",
  "realistic_effort": "L",
  "days_estimate": "Phase A brand switcher (users.brands[] + đổi brand qua CF + Rules): 6–8 ngày; Phase B view gộp owner (listener nhiều brand + overview gộp + PDF): thêm 8–10 ngày → 3–4 tuần",
  "backend_needs": [
   "rules",
   "cf",
   "index",
   "data-migration"
  ],
  "risks": "(1) Tenancy: Rules hiện fail-closed theo 1 string; đổi sang `resource.data.brand in get(users).data.brands` ở leads/sources/notes/outreach_log/outreach_stats/scanned_posts/brands = sửa ~8 match block + 1 lượt `get()` thêm/đọc (chi phí Rules) → PHẢI test đối kháng rò chéo brand (P49) trước khi mở. (2) User không được tự ghi `brand` (Rules guard) → switcher phải qua CF `switchBrand` (verify uid có brand trong `brands[]`) hoặc nới Rules cho phép đổi `brand` CHỈ khi giá trị mới ∈ `resource.data.brands` — cách 2 rẻ hơn nhưng phải viết cẩn thận (affectedKeys chỉ ['brand','updatedAt']). (3) View gộp = N listener leads × 500 doc (hoặc `where brand in [...]` limit 500 chung → brand lớn nuốt cửa sổ) → read cost và `absorbDropped`/`leadsExtra` (v119-14/19) hiện thiết kế cho 1 khung — chỉ nên gộp ở mức KPI (đọc `outreach_stats` + count aggregate `getCountFromServer` per brand) thay vì nạp lead nhiều brand. (4) `in` ≤30 giá trị; collectionGroup notes với `in` cần index COLLECTION_GROUP mới (bài học sự cố notes 27/08). (5) Đổi brand → `dataKey` đổi → `stopData()` + nạp lại toàn bộ (cache persistent giúp) — chấp nhận được. (6) Nhiều nơi giả định `CURRENT_BRAND` đơn (crmCfg, brandBlock, users admin lọc `where brand==b` 512).",
  "implementation_sketch": "- Data: `users/{uid}.brands:[code]`, `brandRoles:{code:'owner|admin|sales'}`, giữ `brand` = brand đang chọn (migration 1 lần: brands=[brand], brandRoles={brand:role}). Role `owner` ánh xạ quyền như admin trong FE (`roleCanEditConfig`) + thêm view gộp.\n- Rules: helper `myBrands()` = get(users/{uid}).data.brands; các match leads/sources/notes/scanned_posts/outreach_log/outreach_stats/brands: `resource.data.brand in myBrands()` (giữ isSuperAdmin); users update: cho phép đổi `brand` khi `request.resource.data.brand in resource.data.brands` và affectedKeys chỉ ['brand','updatedAt'].\n- live.js: SL_FB `switchBrand(code)` = updateDoc users/{uid}{brand:code} → onSnapshot user (924) tự đổi dataKey → startData nạp lại (cơ chế sẵn). Profile thêm `brands`, `brandRoles`.\n- FE 80-rbac-auth.js `brandBlock` (251): nếu `profile.brands.length>1` render `<select>` đổi brand; 20-feed.js `views.overview` cho role owner: card 'Tổng quan các brand' đọc `getCountFromServer` (leads where brand==x & temp==hot…) + `D.outreachStats`/`brands.roi` → bảng brand; xuất PDF tái dùng bộ ROI (40-roi.js).\n- Index: leads(brand,detected_at) đã có (dùng được với `in`); notes COLLECTION_GROUP (brand,vis) đã có; thêm index nếu dùng `in`+orderBy trên outreach_log.\n- P13 gói Agency tính theo `users.brands.length`."
 },
 {
  "id": "P13",
  "exists": "partial",
  "exists_evidence": "Đã có 'Gói giải pháp' thủ công: `brands.plan = {tier,price,cycle(thang/quy/nam/tron),from,to,note,receipts[]}` (85-users-admin.js:181-183, 618; `SL_FB.setBrandPlan` live.js:703) + biên lai `plRcpList/plRcpAdd` (85:191-192, 573-587); hồ sơ hoá đơn `brands.billing` (live.js:699); tier hiện ở khối brand sidebar (80-rbac-auth.js:278). `roi.fee` tách riêng mặc định 15tr (40-roi.js:9 ROI_DEF.fee, `setBrandRoi` live.js:695) — đúng là 2 nguồn phí. Hạn mức chi tiêu chỉ có cho Bright Data toàn hệ thống (`bdBudget`, 10-core-overview.js:515-551). KHÔNG có: `plan.status/trial/nextDueAt/nicks/sources/users` (grep `trialEnds|nextDue|past_due|entitle` = 0), không CF enforce, không banner hết hạn, không lọc 'sắp hết hạn' ở trang Người dùng.",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "5–7 ngày (FE 2.5 + CF enforcePlan 1.5 + Rules/engine gate 1 + migrate fee 0.5 + test 1)",
  "backend_needs": [
   "cf",
   "rules",
   "data-migration",
   "external-api"
  ],
  "risks": "(1) Tự tạm dừng nhầm brand đang trả tiền → cần `graceDays` + cờ `pausedByPlan` để khi CF tắt `sources.active`/`outreach.on` thì bật lại đúng cái đã tắt, không đè lựa chọn tay của Super Admin; log `plan_events`. (2) Rules KHÔNG đếm được doc → 'chặn ghi vượt số nick' bằng Rules là không khả thi trực tiếp; gate ở engine `outreachTick` (skip nick vượt `plan.nicks`) + FE chặn nút Thêm nick là đủ vì fb_accounts write=isSuperAdmin (chỉ anh thao tác). (3) Migrate `roi.fee`→`plan.price`: ROI view đang đọc `R.fee` nhiều chỗ (40-roi.js:70-112) — giữ `roi.fee` làm alias đọc, ghi 1 nguồn để không vỡ ROI cũ. (4) Nhắc hạn qua Zalo ZBS tốn tiền/mẫu phải duyệt → dùng email (đã có kênh) cho brand, ZBS chỉ cho AM. (5) Bảng giá đề xuất (490k/990k/2,5tr) là quyết định kinh doanh — giá phẳng có thể lỗ với brand quét nhiều (Bright Data theo lượng) → cần trần ngân sách theo brand (P23) đi kèm. (6) Realtime: brand user đã subscribe `brands/{code}` (live.js:871-879 myBrandInfo) → banner 'hết hạn' đọc `D.myBrand.plan.status` không tốn thêm listener.",
  "implementation_sketch": "- Data: mở rộng `brands.plan` thêm `{status:'trial|active|past_due|paused', trialEndsAt, nextDueAt, limits:{nicks,sources,users}, features:{outreach,zalo}}`; migration Cloud Shell: `plan.price = plan.price || roi.fee`, `status='active'` cho brand có price>0.\n- FE 85-users-admin.js: form Gói (181-183) thêm select tier/status + limits + ngày; bảng brand cột 'Gói/Hạn' + lọc 'sắp hết hạn ≤7 ngày'; `plRcp` tự tính kỳ kế từ cycle; nút 'Xuất công nợ CSV' (client-side từ D.brands). 70-shell-tools.js `views.account` (181) thêm khối 'Gói đang dùng/hạn mức' đọc `D.myBrand.plan`; 90-boot banner khi `plan.status in [past_due,paused]`.\n- CF `enforcePlan` onSchedule('every 24 hours', asia-southeast1, maxInstances:1): quét brands → nhắc 7/3/1 ngày (email qua kênh sẵn + notifyBrandZalo cho AM) → quá hạn+grace: set `sources.active=false` + `outreach.on=false` + `pausedByPlan=true` + `plan.status='paused'`; khi thanh toán (Super Admin đổi status active) → CF/FE khôi phục.\n- Engine outreach.js: `brands` filter thêm `plan.status in [trial,active]` và số nick active ≤ `plan.limits.nicks` (patch nhỏ, deploy outreachTick).\n- Rules: brand user read `brands/{code}` đã có; `plan` chỉ super ghi (đã vậy). Không cần index."
 },
 {
  "id": "P14",
  "exists": "partial",
  "exists_evidence": "Có nền SLA nhưng hoàn toàn CLIENT-SIDE & thụ động: `crmCfg` slaWarnMin/slaBadMin theo brand (20-feed.js:305-313; cấu hình `setBrandCrm` 85-users-admin.js:786-808); `slaChip` (317) chỉ vẽ chip 'Chưa chăm X phút', `fuChip` (331) chip hẹn/quá hẹn; rail 'HÔM NAY CẦN CHĂM' (614-618); đặt hẹn `fu_at/fu_note` ở modal (65:157). Chuông app = lead score≥60 (live.js:124 `alerts`), không tham chiếu fu_at. Không có `fu_notified|sla_escalated|sla_events` (grep = 0). Kênh gửi: view Cảnh báo chỉ toggle `cfg.channels` (50-config-views.js:345 'cần token trong functions/.env'); backend có Telegram token (.env, CLAUDE.md:39) + cụm Zalo ZBS báo sales theo brand (`notifyBrandZalo`, danh sách người nhận 85:200/live.js:726) — tái dùng được cho leo thang. Báo cáo KPI 'TG phản hồi TB 2,4 phút −18%' cứng tại 50-config-views.js:387. `users.phone` có sẵn (live.js:934) để map sales→SĐT Zalo; không có map Telegram chat_id/sales.",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "6–8 ngày (CF slaTick 2 + index/Rules 0.5 + kênh gửi 1.5 + FE KPI thật/cấu hình SLA 2 + test 1)",
  "backend_needs": [
   "cf",
   "index",
   "rules",
   "secret",
   "external-api"
  ],
  "risks": "(1) Truy vấn 'first_care_at null' KHÔNG index được (field thiếu không vào index) → CF phải quét `leads where brand==X & temp=='hot' & detected_at > now-24h` rồi lọc trong code (read ~vài chục doc/brand/5' — chấp nhận) hoặc pipeline ghi `first_care_at:null` tường minh (đụng backend scan). (2) `fu_at`, `assigned_at`, `first_care_at` là ms từ đồng hồ client (CLAUDE.md:404) → SLA lệch khi máy sai giờ; đề nghị CF tính bằng server time + dung sai 5' thay vì migrate serverTimestamp (migrate = đụng mọi sink đọc số ms). (3) ZBS: quota 1 tin hậu mãi/user/ngày + phí/tin (market.md:54) → nhắc sales qua Telegram (miễn phí, cần map chat_id/sales) hoặc Zalo OA tin tư vấn; ZBS chỉ cho leo thang tới chủ brand. (4) Chống lặp: mỗi nhắc ghi cờ `fu_notified_at`/`sla_escalated` lên lead (Admin SDK) → snapshot leads bắn về client (thêm 1 rebuild/lead, nhỏ). (5) Tick 5' thêm 1 scheduler — gộp vào `outreachTick` hoặc CF riêng `slaTick` với maxInstances:1 để tránh chồng; brand nhiều → giới hạn query limit + index `leads(brand, fu_at)`. (6) Người nhận: sales chưa khai SĐT/Telegram → fallback chuông app (cần nối `alerts` từ collection `sla_events` thay vì score≥60).",
  "implementation_sketch": "- CF `slaTick` onSchedule('every 5 minutes', asia-southeast1, maxInstances:1): (a) `leads where brand==X & fu_at between [now, now+15'] & !fu_notified_at` → nhắc assignee (deep-link `#feed?lead=<id>`) → set `fu_notified_at`; (b) hot & chưa chăm quá slaWarn → nhắc sales; quá slaBad → nhắc admin/owner brand + AM kèm gợi ý sales rảnh nhất (đếm `assignee_uid` mở, như openAssignDialog) → set `sla_escalated`; ghi `sla_events/{id}` {brand, leadId, kind, t, ok}. Kênh: Telegram (token .env, map `users.telegramChatId` mới) / Zalo ZBS via `sendZaloZBS` (SĐT `users.phone`) / email.\n- Index: composite `leads(brand ASC, fu_at ASC)`; dùng lại `leads(brand, detected_at)` cho quét hot chưa chăm.\n- Rules: `sla_events` read = super || brandCode==myBrand (pattern outreach_stats), write=false. `users.telegramChatId` nằm ngoài guard field nên user tự khai được ở Cài đặt tài khoản.\n- FE: 85-users-admin.js khối CRM brand (786-808) thêm SLA theo giai đoạn (`slaRespondMin`, `slaStuckDays`) → `setBrandCrm`; live.js `lazy` subscribe `sla_events` (brand user where brand==) → `D.slaEvents`; 50-config-views.js `views.reports` (387) thay '2,4 phút' bằng TB `first_care_at - detected_at` từ D.leads + 'SLA đạt %' từ sla_events; chuông (70-shell-tools.js) gộp sla_events; card Hôm nay (P10) hiện 'tỷ lệ chăm trong 15 phút' theo sales.\n- 70-shell-tools.js `views.account`: ô 'Telegram chat id' + nút test."
 },
 {
  "id": "P01",
  "exists": "partial",
  "exists_evidence": "grep -ri 'do_not_contact|suppression|opt.out|retention|erase|consent' src/app + live.js + worker.mjs → 0 kết quả về cờ/collection; chỉ có text UI '45-outreach.js:421 \"Có opt-out & ghi log mọi thao tác\"' (lời hứa, chưa có cơ chế). Mảnh có sẵn: (a) TTL pattern `expireAt` đã chạy production cho outreach_log (worker.mjs:57,173,541,604 + CLAUDE.md LỆNH S3 policy ACTIVE); (b) `lead.dropped` (30-pipeline.js:204) loại lead khỏi pipeline nhưng feed/engine KHÔNG tôn trọng; (c) `logLeadView` (65-charts-lead-modal.js:51-55, live.js:429) = audit xem lead sơ khai; (d) funcWebhook đã set `outreach_threads.step=replied` + stage responded (CLAUDE.md:176) nhưng KHÔNG phân loại 'từ chối'; (e) engine inbox 'luôn có opt-out' chỉ ở func path (CLAUDE.md:175), worker AdsPower dùng nguyên `lead.reply` làm inbox_msg (worker.mjs:697-700) → KHÔNG có câu opt-out; (f) privacy.html tĩnh.",
  "feasible": "yes-with-backend",
  "realistic_effort": "L",
  "days_estimate": "12–18 ngày (FE 4, engine+worker gate 3, CF eraseSubject+retention 4, Rules/index/TTL 1, test tenancy 2–3)",
  "backend_needs": [
   "rules",
   "index",
   "cf",
   "worker",
   "data-migration"
  ],
  "risks": "(1) Xoá cascade sai tenant: khớp SĐT xuyên brand → key suppression PHẢI có prefix brand + eraseSubject chỉ chạy trong brand của caller, super phải chọn brand tường minh + confirm gõ lại SĐT. (2) AI phân loại 'từ_chối' sai → mất lead: chỉ auto-set cờ khi confidence ≥0.85 + từ khoá cứng ('đừng nhắn', 'spam', 'không quan tâm'), còn lại chỉ đề xuất cho sales; cờ gỡ được (ghi consent_log ai gỡ). (3) Retention TTL xoá nhầm: TTL Firestore xoá doc lead nhưng subcollection notes KHÔNG tự xoá (orphan collectionGroup notes vẫn hiện ở index brand) → CF đêm phải xoá notes trước rồi mới đóng dấu expireAt lead; loại trừ closed/booked/có deal/assignee còn hoạt động. (4) Regression realtime: thêm field `do_not_contact` vào lead là patch updateLead → snapshot diff feed OK (diff theo lead.id), nhưng buildData whitelist (live.js buildData) phải cho field đi qua — bug 'field rơi khỏi rebuild' đã dính 2 lần (CLAUDE.md). (5) Read cost: tra suppression mỗi bước worker = 1 read/bước (~4 read/lead) — không đáng kể; engine tra ở stepNickAdspower dùng get theo doc id deterministic (không query). (6) Pháp lý: Luật 91/2025 không có ngoại lệ dữ liệu công khai (market.md:64) → gói này là giảm thiểu, KHÔNG hợp pháp hoá scraping; cần luật sư review + hợp đồng xử lý dữ liệu với brand. (7) Rules leads update hiện chưa xác minh whitelist field (CLAUDE.md 'Việc còn tồn') → sales có thể tự gỡ `do_not_contact`; nên ghi cờ qua CF hoặc thêm guard `affectedKeys().hasOnly`.",
  "implementation_sketch": "- FE 20-feed.js (thẻ) + 65-charts-lead-modal.js: nút 🚫 → `SL_FB.setDoNotContact(leadId,{on,reason})` (live.js: updateDoc leads/{id}{do_not_contact:true,dnc_at,dnc_by} + setDoc `suppression/{brand}__{sha256(normPhoneVN(phone)||uid||author_url)}` — sha256 qua crypto.subtle); chip 🚫, ẩn pill SĐT copy; rail 'Hôm nay cần chăm' + 30-pipeline lọc `!l.do_not_contact` (giống `!l.dropped`).\n- Engine outreach.js `stepNickAdspower`/`apEnqueueFunnel`: skip lead `do_not_contact` + get `suppression/{brand}__{hash}` (doc id deterministic, 1 read) trước khi enqueue; `stepNick` (func) tương tự.\n- Worker runFunnel: trước MỖI bước đọc lại `outreach_threads/{leadId}.active` + suppression doc → false thì throw fail-closed (không retry, set thread active:false). Nối `brands.content.optOutLine` vào inbox_msg (đóng lỗ hứa-mà-không-có).\n- CF funcWebhook (+ reader inbox P03 sau này): OpenAI gpt-4o-mini phân loại reply {từ_chối|quan_tâm|hỏi_giá|hẹn|khác, confidence}; từ_chối ≥0.85 → set cờ + suppression + `consent_log/{auto}` {brand,leadId,source:'reply',snippet,at}; thấp hơn → note gợi ý cho sales.\n- CF `retentionTick` onSchedule 02:00 VN (pattern outreachTick, asia-southeast1, maxInstances:1): query leads where brand==code & stage in [new,checked] & detected_at < now-coldMonths & !assignee → xoá notes subcollection (batch 500) rồi set `expireAt`; TTL policy `leads.expireAt` + `scanned_posts.expireAt` (gcloud firestore fields ttls update — pattern S3). Index: leads(brand, stage, detected_at).\n- CF `eraseSubject` onRequest (verify superadmin hoặc admin đúng brand qua verifyIdToken như fbaccounts.js): tìm theo phone/uid/author_url TRONG brand → xoá leads+notes+outreach_threads/map/log liên quan, ghi `erasure_log/{auto}` {brand,requestedBy,keysHash,counts,at} (KHÔNG lưu SĐT thô, chỉ hash).\n- Rules: `suppression` read=super||admin cùng brand, write=false (qua CF); `consent_log`/`erasure_log` read=super||admin brand, write=false; leads update guard `affectedKeys()` không cho sales đụng `do_not_contact`.\n- FE part mới `48-compliance.js` view 'Tuân thủ' (nav Vận hành, KHÔNG đưa vào SUPER_ONLY_VIEWS để admin brand dùng; live.js lazy subscribe suppression/erasure_log theo brand khi hash #compliance, giống lazyOutreach); import CSV DNC = batch setDoc suppression client-side (≤500/lần)."
 },
 {
  "id": "P02",
  "exists": "partial",
  "exists_evidence": "grep -ri 'daily_brief|dailyBrief|notify_log' src/app live.js → 0. Có sẵn: (a) card 'Thông báo cá nhân' toàn DEMO — 70-shell-tools.js:323-325 toggle Telegram/'Email tổng hợp hằng ngày 8:00'/âm thanh + handler dòng 507 chỉ `toast('...(demo)')`, không ghi users; (b) bảng 'Sales nhận thông báo theo brand' THẬT (85-users-admin.js:200-211, live.js:725-726 collection brand_sales với phone Zalo/email) do backend Zalo ZBS `notifyBrandZalo`/`sendZaloZBS` dùng khi có lead chất lượng (CLAUDE.md:76); (c) Telegram alert lead nóng ở backend (live.js:43 kênh telegram, 50-config-views.js:345 'cần token trong functions/.env'); (d) Overview KPI 4 card delta GIẢ (brief shell-overview lỗi high) — chưa có card 'Hôm nay'; (e) dữ liệu tính brief đã có trong lead: stage_at/fu_at/first_care_at/assignee/detected_at (20-feed.js:298-318, 30-pipeline.js:130) + outreach_stats/{brand}__{day} (CLAUDE.md v119-34) + fb_accounts.needLogin.",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "5–7 ngày (CF dailyBrief 2, kênh Telegram/email 1, nối Zalo ZBS qua cụm smartlead-zalo-fn 1–2 (chờ mẫu duyệt), FE card Hôm nay + notify settings 1.5, Rules/index 0.5)",
  "backend_needs": [
   "cf",
   "external-api",
   "rules",
   "index",
   "secret"
  ],
  "risks": "(1) ZBS: từ 1/1/2026 thay ZNS, cần OA gói Tăng trưởng 2,5tr/năm + mẫu duyệt + quota 1 tin hậu mãi/user/ngày (market.md:53-54) → 2 bản sáng/tối = vượt quota → gộp 1 tin hoặc tối đi Telegram/email. Cụm `~/smartlead-zalo-fn` là codebase riêng thứ 3 (CLAUDE.md:76), deploy lệch tên có thể đè function (bài học trùng tên s13/codebase2) → chỉ `--only functions:dailyBrief`. (2) PII: tuyệt đối không gửi SĐT/nội dung bài khách qua Telegram/Zalo (tên+điểm+deep-link thôi) — NĐ 356/2025. (3) Chi phí đọc: brand ~10k lead, quét toàn bộ mỗi sáng = đắt; dùng `getCountFromServer` với where(brand, detected_at>=hômqua) + outreach_stats sẵn (counter, 1 read) + query top-3 lead limit 3 orderBy score → cần composite index leads(brand, detected_at) (đã có theo CLAUDE.md E7) + leads(brand, fu_at) MỚI. (4) OpenAI chỉ diễn giải số → nếu key hết credit thì fallback template thuần (đã có pattern aiText fallback trong outreach.js). (5) Card 'Hôm nay' render ở Overview: Overview đang re-render full innerHTML mỗi snapshot (brief low/perf) — subscribe thêm `daily_brief/{brand}__{day}` 1 doc là rẻ, nhưng phải đưa vào `d.dailyBrief` qua buildData pass-through kẻo 'field rơi'. (6) Múi giờ: ngày VN phải tính `Date.now()+7h` như outreach_stats, không dùng UTC.",
  "implementation_sketch": "- CF `dailyBrief` (~/firebase-s13/functions/brief.js, ESM v2 onSchedule('30 7 * * *', tz Asia/Ho_Chi_Minh, region asia-southeast1, maxInstances:1)): với mỗi brand active → count(leads brand & detected_at ≥ hôm qua), count(hot chưa first_care_at >1h), count(fu_at trong hôm nay / fu_at < now & stage∉closed), closed hôm qua (stage_at), đọc `outreach_stats/{brand}__{hômqua}` (react/comment/inbox/replied), fb_accounts where brand & needLogin, top-3 = query leads brand & stage in [new,checked,responded] orderBy score desc limit 3 → ghi `daily_brief/{brand}__{day}` {nums, top3:[{id,name,score,why}], text} (OpenAI gpt-4o-mini diễn giải 5 dòng, fallback template).\n- Gửi: đọc `brand_sales` (đã có phone/email theo brand) → Zalo ZBS qua `sendZaloZBS` (mẫu 'daily_brief' duyệt trước, 1 tin/ngày gộp) / Telegram bot sendMessage (token đã trong .env) / email fallback; ghi `notify_log/{auto}` {brand,channel,to-hash,ok,err,at,expireAt 30 ngày}.\n- FE 10-core-overview.js `views.overview`: card 'Hôm nay' đầu trang render từ `D.dailyBrief` (live.js: onSnapshot doc `daily_brief/{byBrand}__{todayVN}`; super: where day==today limit 50 → bảng brand) — deep-link `#feed&lead=<id>` cần router go() parse tham số lead (hiện go() chỉ đọc hash view; thêm vài dòng mở modal openLead sau render).\n- FE 70-shell-tools.js card Thông báo cá nhân: toggle → `SL_FB.setMyNotify({brief:true,channel:'telegram'|'zalo'|'email',hour:7})` = setDoc users/{uid}.notify (Rules users: guard affectedKeys đã có — chỉ cần cho phép field `notify` tự sửa).\n- Rules: `daily_brief` read = super || (isActiveUser && resource.data.brandCode==myBrand), write=false; `notify_log` read=super. Index: leads(brand ASC, fu_at ASC), leads(brand ASC, stage ASC, score DESC)."
 },
 {
  "id": "P03",
  "exists": "partial",
  "exists_evidence": "Worker KHÔNG đọc inbox: grep 'conversations|unread|chưa đọc|GET_THREADS' worker.mjs → 0; worker chỉ GỬI (domInbox 475, domInboxProfile 489) và ghi `outreach_map` khi inbox có uid (worker.mjs:549). Bắt phản hồi mới có ở kênh func: funcWebhook → note '💬 Khách đã phản hồi' + stage responded + thread replied + addLog reply (CLAUDE.md:169-176) — CLAUDE.md Phase 2 'CÒN LÀM (6) reply-detection cho nick adspower chưa có'. FE: grep 'conversation|hội thoại|hộp thư' src/app → chỉ có log hội thoại Trợ lý AI (85-users-admin.js:333) — KHÔNG có view Hộp thư/tab hội thoại trong lead modal; stage 'responded' đã có trong STAGES (live.js:40) và ROI (40-roi.js:26). Lead modal 65-charts-lead-modal.js:131 chỉ hiện `l.reply` 1 chiều.",
  "feasible": "yes-with-backend",
  "realistic_effort": "L",
  "days_estimate": "15–22 ngày (worker đọc messenger/notifications fail-closed 5–6, CF onWrite AI 2, FE part Hộp thư + tab hội thoại 4–5, gửi tay qua outreach_tasks send_message + van 2, Rules/index/TTL 1, nghiệm thu thật trên nick 3)",
  "backend_needs": [
   "worker",
   "cf",
   "rules",
   "index",
   "external-api"
  ],
  "risks": "(1) Nick ban: đọc messenger.com là hành vi thêm mỗi phiên (~20–40s), giống người hơn là spam; nhưng mở thread = FB đánh dấu 'đã xem' → khách chờ, sales phải trả lời <15' — nếu không thì tệ hơn không đọc. Gửi tin TAY từ Hộp thư đi qua worker = hành động inbox thêm ngoài van → cần van riêng `reply≤20/nick/ngày` và chỉ trên thread đã có (không cold). (2) DOM messenger đổi thường xuyên (đã xác nhận không có graphql cho inbox — CLAUDE.md 'INBOX xác nhận KHÔNG có graphql') → selector fail-closed như comment-lead; false-positive nguy hiểm nhất là ghi nhầm tin của thread KHÁC vào lead → scope STRICT: chỉ mở thread có `outreach_threads.uid/profile_url` khớp, khớp bằng uid/permalink chứ không bằng tên. (3) PII: hội thoại là dữ liệu cá nhân → `conversations` Rules theo brandCode + TTL expireAt 90 ngày + không lưu ảnh/file, chỉ text; OpenAI nhận đoạn trích ≤1k ký tự. (4) Realtime path: set stage responded từ worker phải ghi kèm `stage_at` (brief pipeline lỗi medium: funcWebhook không ghi stage_at → 'Kẹt N ngày' sai ngay) — sửa cả funcWebhook. (5) Read cost thấp (per-lead doc), nhưng snapshot `conversations` where brandCode==X orderBy lastInboundAt → composite index mới. (6) Thread nhóm 2 người (CREATE_THREAD func) vs thread 1-1 (AdsPower) → cấu trúc DOM khác; chỉ làm AdsPower 1-1 trước.",
  "implementation_sketch": "- Worker runNick: sau khi hết task của nick (trước adspowerStop) thêm `readInbox(page, nickPid)`: mở messenger.com, lấy danh sách thread có badge chưa đọc (≤10), với mỗi thread lấy uid/permalink từ href → tra `outreach_map/{uid}` HOẶC `outreach_threads where pid==nick & uid==` → khớp mới mở, đọc ≤5 tin cuối không phải của mình → ghi `conversations/{leadId}` {brandCode,pid,uid,messages:arrayUnion(...), lastInboundAt}, note hệ thống vào leads/{id}/notes (shape đúng: {leadId,brand,vis:'team',text,by_uid:'engine',by_name:'Tiếp cận (tự động)',at:ms}), updateLead {stage:'responded',stage_at:now,outreach_replied:true}, thread step 'replied' active:false, addLog status 'reply' (+bumpStat replied). Thông báo comment: mở facebook.com/notifications lọc 'đã trả lời bình luận của bạn' khớp post_id trong outreach_threads → note reply comment. Mọi bước try/catch fail-closed + detectChallengeStrict.\n- CF `onConversationWrite` (onDocumentWritten conversations/{leadId}, asia-southeast1): OpenAI gpt-4o-mini → {intent, summary3, suggestions[2]} theo `brands.ai.{giong,dichvu,khach}` → merge vào doc `ai`.\n- FE part mới `47-inbox.js` view 'Hộp thư' (nav Vận hành; nhóm/brand/nick/lead, SLA 15' chip đỏ, bàn giao = updateLead assignee) + 65-charts-lead-modal.js tab 'Hội thoại'; live.js `lazyInbox()` subscribe `conversations` where brandCode==byBrand orderBy lastInboundAt desc limit 100 (super: orderBy limit 200) → `d.conversations`; nút Gửi → `SL_FB.sendReply(leadId,text)` = addDoc `outreach_tasks/{leadId}__reply_<ts>` {action:'send_message', pid, workerId (từ fb_accounts), payload:{uid|profile_url,text}} — worker `execOnPage` thêm case send_message dùng domInbox/domInboxProfile + verify sẵn có. Chuông (70-shell-tools.js:76-95 bell) thêm mục 'Khách vừa trả lời' từ D.conversations.\n- Rules: `conversations` read = super || (isActiveUser && brandCode==myBrand), write=false; `outreach_tasks` hiện Admin-SDK only → đi qua CF `enqueueReply` verifyIdToken để giữ Rules đóng. Index: conversations(brandCode, lastInboundAt desc); outreach_threads(pid, uid). TTL conversations.expireAt."
 },
 {
  "id": "P04",
  "exists": "partial",
  "exists_evidence": "Không có wizard/checklist: grep -i 'wizard|onboarding|checklist|quét thử' src/app → 0. Mảnh có sẵn: thêm brand + Hồ sơ AI (nganh/dichvu/khach/giong) + Logo + Gói ở bảng Brand (85-users-admin.js:149, 505-513); `openSourceForm` (50-config-views.js:68) và gán brand nguồn với toast 'Chạy script dán nhãn để cập nhật lead cũ' (50-config-views.js:31) = xác nhận đúng lỗi; funnel chia 0 → 'NaN%' xác nhận tại 20-feed.js:183-188 (`max=D.funnel[0].value`, `f.value/max`); quét ngay gọi MANUAL_SCAN_URL với body tự dựng (65-charts-lead-modal.js:239-250, 70-shell-tools.js:680,804-808 backfill có tham số khoảng ngày + method) — CHƯA có tham số nguồn/limit; ma trận Tiếp cận preset OA_PRESETS đã có (45-outreach.js:57,190-213); crmCfg SLA/assignMode đã có (20-feed.js:305-325).",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "5–7 ngày (wizard modal 3, checklist Overview + fix NaN 1, backend manualScan nhận {sourceIds,limit,brand} + rate-limit 1, thư viện group gợi ý (config doc) 0.5, smoke 0.5)",
  "backend_needs": [
   "cf",
   "rules"
  ],
  "risks": "(1) Chi phí BrightData/AI cho 'Quét thử': manualScan hiện quét toàn bộ nguồn → backend PHẢI nhận `sourceIds[]` + `limit:15` + khoá `brands/{code}.trialScanAt` ≤1 lần/giờ; nút Quét ngay hiện không chống bấm đúp (brief reliability medium) → wizard phải disable nút tới khi scan_jobs báo xong. (2) Wizard gán brand cho nguồn: lead CŨ của nguồn không đổi brand (đúng như toast 'script dán nhãn') → với brand mới thì không sao, nhưng gán nguồn ĐANG thuộc brand khác = rò lead chéo brand → chỉ cho chọn nguồn chưa gán hoặc tạo mới. (3) Tenancy: wizard là super-only (Rules brands/sources write=isSuperAdmin) — agency muốn tự tạo brand cần mở Rules → để sau. (4) Overview checklist tick tự động cần biết 'lượt quét đầu' → đọc `scans` where brand (đã subscribe) và `brand_sales` (brand user có thể không có quyền đọc → tick 'nhận báo Zalo' chỉ hiện cho super). (5) 85-users-admin.js đã 902 dòng → tách part `87-brand-wizard.js`.",
  "implementation_sketch": "- FE part mới `87-brand-wizard.js`: nút '+ Brand mới' ở 85-users-admin.js bảng Brand → modal stepper 6 bước; bước 1 setDoc brands/{code} (reuse hàm thêm brand hiện có); bước 2 preset ngành = hằng `BRAND_PRESETS{spa,fnb,bds,edu,mebe}` → setDoc brands/{code}.ai; bước 3 đọc `config/group_library` (super soạn, mảng {industry,url,name}) + `SL_FB.setSource(id,{brand,url,keywords,exclude})` — sửa openSourceForm nhận `brand` sẵn trong form (bỏ toast script); bước 4 setDoc brands/{code}.crm{slaWarnMin,slaBadMin,assignMode} + `setOutreach(code,{matrix:OA_PRESETS['cân bằng'],caps})`; bước 5 link mời (flow users pending có sẵn); bước 6 POST MANUAL_SCAN_URL body {brand, sourceIds:[...], limit:15, trial:true} → theo dõi `scan_jobs` realtime (pattern backfill 70-shell-tools.js:627) → render 3 lead đầu từ D.leads (brand mới).\n- Backend manualScan (s13, bản 22/08 verifyCaller): thêm parse `sourceIds/limit/trial`, guard trial ≤1/giờ/brand (transaction trên brands doc), limit 15 bài.\n- Overview 20-feed.js `views.overview`: nếu D.leads.length==0 && có brand → render `onboardingChecklist()` thay KPI/chart: tick từ D.sources (brand gán), D.scans (có scan brand), D.users (có sales brand), brand_sales (nếu super). Fix funnel: `const max=D.funnel[0].value||0; pct = max? Math.round(v/max*100) : 0`.\n- Rules: `config/group_library` read mọi active user, write super. Không cần index mới (sources where brand đã có)."
 },
 {
  "id": "P05",
  "exists": "none",
  "exists_evidence": "ls smartlead → không có manifest.webmanifest/sw.js; grep -i 'manifest|serviceWorker|Notification|fcm|messaging|navigator.onLine|waitForPendingWrites' app.html src/app live.js netlify.toml → 0 (chỉ live.js:174 bật persistentLocalCache). CSP netlify.toml:24 connect-src chưa có fcm.googleapis.com, không có worker-src. Mobile: app.css có @media 639px (1284,1624) nhưng nav là drawer hamburger + floating-hotline + FAB mascot (brief shell lỗi medium). Không có hashchange listener (grep 0 → nút Back không đổi màn). Backend chưa có `notifyTick`/`users.fcm` (CLAUDE.md không nhắc).",
  "feasible": "yes-with-backend",
  "realistic_effort": "L",
  "days_estimate": "12–16 ngày (manifest+SW+build hash list 3, bottom tab + dọn floating mobile 3, offline chip 1, FCM FE+CF notifyTick+funcWebhook push 4, CSP/Netlify/Rules/index 1, test iOS/Android thật 2–3)",
  "backend_needs": [
   "cf",
   "rules",
   "index",
   "external-api"
  ],
  "risks": "(1) SW cache sai → user kẹt bản cũ: assets đã cache immutable 1 năm theo hash ?v= (tools/build.mjs) nên precache list PHẢI do build.mjs sinh từ cùng hash; HTML network-first; `skipWaiting`+`clients.claim` + banner 'có bản mới' — smoke.js thêm check SW đăng ký. Nhắc Cmd+Shift+R sẽ KHÔNG đủ khi có SW → cần nút 'Tải bản mới' trong app. (2) Firebase SDK ES module CDN gstatic: SW không cache module Firebase (để network) tránh lệch version. (3) iOS: Web Push chỉ khi đã 'Thêm vào màn hình chính' (≥16.4) → onboarding riêng; Safari không có beforeinstallprompt. (4) FCM cần `firebase-messaging-sw.js` ở root + VAPID key (public, không phải secret) + Netlify header `/sw.js` Cache-Control max-age=0; CSP thêm `connect-src https://fcm.googleapis.com https://fcmregistrations.googleapis.com`, `worker-src 'self'`. (5) Firestore cost: notifyTick 5' quét `leads where fu_at in [now, now+15']` toàn hệ (single-field index có sẵn) rồi lọc `fu_notified_at` client (tránh inequality kép); ghi `fu_notified_at` chống lặp. (6) Rules users: guard affectedKeys hiện chặn role/active/brand… → thêm cho phép user tự ghi `fcm[]`/`quietHours`. (7) Regression realtime: online/offline chip đọc `waitForPendingWrites` không đụng snapshot path; bottom tab đổi layout ≤639px → kiểm giữ focus/caret ô ghi chú khi repaint (v119-23) vì sheet mobile.",
  "implementation_sketch": "- tools/build.mjs: sinh `sw.js` từ template + danh sách assets đã hash (`assets/min/js/*.min.js?v=`, css, icons, logo); `manifest.webmanifest` (name SmartLead, theme #1B2DCC, icons 192/512, start_url app.html, display standalone). app.html: `<link rel=manifest>`, `navigator.serviceWorker.register('/sw.js')` trong 90-boot.js + listen `updatefound` → banner (reuse fbDown banner kiểu 'net').\n- 80-rbac-auth.js/app.html: bottom tab ≤639px (Hôm nay=overview · Lead=feed · Pipeline · Tìm=Cmd-K palette mobile · Tôi=account) theo role; CSS ẩn `.floating-hotline` + FAB trợ lý trên ≤639px, mục 'Hỏi AI' vào tab Tôi. Thêm `window.addEventListener('hashchange',()=>go(...))` (fix Back).\n- 90-boot.js: chip Ngoại tuyến từ `online/offline` + `SL_FB.pendingWrites()` (live.js wrap `waitForPendingWrites(db)`).\n- Push: live.js `enablePush()` = getMessaging(app)+getToken({vapidKey, serviceWorkerRegistration}) → arrayUnion users/{uid}.fcm; `firebase-messaging-sw.js` root. CF `notifyTick` onSchedule('every 5 minutes', asia-southeast1, maxInstances:1): leads where fu_at ≥ now & fu_at ≤ now+15' → lọc !fu_notified_at & assignee → users fcm → sendEachForMulticast; cũng quét `assigned_at ≥ lastTick`; funcWebhook/worker reply → push assignee; nick needLogin/challenge → push super. Ghi `fu_notified_at`. Rules: users update cho phép self ghi `fcm`,`notify`. Netlify: header /sw.js + /firebase-messaging-sw.js Cache-Control no-cache; CSP mở fcm."
 },
 {
  "id": "P06",
  "exists": "partial",
  "exists_evidence": "1 field dùng chung xác nhận: `lead.reply` render tại 65-charts-lead-modal.js:131 và engine/worker dùng NGUYÊN reply cho cả comment công khai lẫn inbox (CLAUDE.md LỆNH I; worker.mjs:658,665,697 comment_msg=inbox_msg=lead.reply). grep -i 'templates|persona|variant|spam-score' src/app live.js → 0; `brands.ai{nganh,dichvu,khach,giong}` đã có (85-users-admin.js:149,505-513) = persona sơ khai; engine `aiText()` OpenAI có fallback mẫu xoay (CLAUDE.md v119-26) nhưng KHÔNG có key nên rơi mẫu; assistantChat CF có (live.js:645) chưa có mode draft. Bug 'Sao chép gợi ý' ghi undefined: 65-charts-lead-modal.js:143 `writeText(l.reply)` không guard → lead thiếu reply chép 'undefined' (xác nhận). Không có spam-score/variantId trên outreach_threads (worker không có 'variant').",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "6–9 ngày (CF genVariants + spam-score 3, engine chọn biến thể + variantId 1, FE modal (✨ Viết lại/Lưu bản gửi/mẫu của tôi) 2, mục Nội dung trong ⚙️ brand 1.5, Rules 0.5, test)",
  "backend_needs": [
   "cf",
   "secret",
   "rules",
   "worker"
  ],
  "risks": "(1) Token cost ×3/lead: chỉ sinh cho hot/warm lúc chấm điểm (junk/cold bỏ), cache trong `lead.replies`, 'Viết lại' ≤5 lượt/lead/ngày enforce ở CF (counter users/{uid}.draftCount_day) — assistantChat hiện không giới hạn tần suất client (brief low/security). (2) Comment công khai vẫn là spam signal theo TẦN SUẤT/nick dù nội dung khác → Content Studio không thay van; giữ cap comment 12/nick. (3) Persona lệch: bản 'người duyệt' (`lead.reply_final`) thắng AI; brand mới bật 'duyệt trước' = worker chỉ chạy comment/inbox khi có reply_final HOẶC brand.content.autoApprove=true — thay đổi hành vi engine → cờ mặc định giữ hành vi cũ (autoApprove:true) kẻo dừng phễu đang chạy. (4) Spam-score so trùng với 30–50 comment gần nhất của brand cần đọc outreach_log (index brandCode+at desc CÓ sẵn CICAgNjpgYIK) — rẻ. (5) Regression: giữ nguyên field `comment_msg/inbox_msg` trong payload (engine chọn biến thể rồi vẫn ghi vào 2 field này) → worker KHÔNG cần đổi ngoài ghi `variantId` vào logStep. (6) Rules: `users/{uid}.templates` tự ghi (guard affectedKeys), `brands.content` write super (đúng Rules brands hiện có).",
  "implementation_sketch": "- Data: `brands/{code}.content={persona:{xungho,giong,nickName},templates:{comment[],inbox[],zalo[],sms[]},optOutLine,autoApprove:true}`; lead: `replies:{comment:[3],inbox:[3],zalo:[3],sms:[1]}`, `reply_final:{comment,inbox}`, `replies_at`.\n- CF `genVariants` (s13, onRequest verifyIdToken + gọi nội bộ từ pipeline chấm điểm sau khi có temp∈hot/warm): prompt từ bài + need/intent/service + brands.ai + brands.content + 3 note gần nhất (leads/{id}/notes orderBy at desc limit 3) → JSON 3 biến thể/kênh; `spamScore(text, recent[])` = từ khoá bán hàng + link + Jaccard trigram vs 50 outreach_log text gần nhất của brand → gắn `spam` mỗi biến thể; loại >0.7.\n- Engine outreach.js `apEnqueueFunnel`: `pickVariant(lead,'comment')` = reply_final ưu tiên, không thì biến thể có `used` thấp nhất (brands.content.usage increment) → payload.comment_msg/inbox_msg + `variantId` lên thread/task; inbox nối `optOutLine`. Worker logStep ghi `variantId` vào outreach_log (1 dòng) → về sau tính reply-rate theo biến thể.\n- FE 65-charts-lead-modal.js: khối gợi ý đưa lên dưới bài gốc, tabs kênh, nút '✨ Viết lại' → `SL_FB.assistantChat(msgs,{mode:'draft',leadId,channel})` (CF assistantChat thêm mode), 'Lưu làm bản gửi' → updateLead {reply_final.<kênh>}, 'Lưu làm mẫu của tôi' → updateDoc users/{uid}.templates arrayUnion; fix copyReply: `writeText(l.reply||'')` + disable nút khi rỗng. 45-outreach.js `oaOpenBrandCfg` thêm tab 'Nội dung' (persona/mẫu/optOut/autoApprove) → `SL_FB.setBrandContent(code,patch)` setDoc merge brands.content; 'Gửi thử vào nick mình' = enqueue outreach_tasks send_message tới uid super (cần P03 send_message).\n- Rules: users update tự ghi `templates`; brands write super (đã có). Không index mới. Migration: không bắt buộc (lead cũ không có replies → fallback lead.reply)."
 },
 {
  "id": "P07",
  "exists": "none",
  "exists_evidence": "grep -i 'tel:|zalo.me|sms:|pendingTouch|visibilitychange|MediaRecorder|transcribe|không nghe máy|lost_reason' src/app live.js → chỉ `zaloDial` (20-feed.js:675) là CODE CHẾT (comment dòng 679 'đã bỏ nút Gọi/Nhắn Zalo'); `l.touches` ở 20-feed.js:689,793 là touches của BrightData (đa nhóm), KHÔNG phải call log. Có sẵn: `addLeadNote` (live.js:521-530, shape {leadId,brand,text,vis,by_uid,by_name,at}), ghi `first_care_at` (20-feed.js:298-301), `fu_at/fu_note` modal (65-charts-lead-modal.js:114-131,155-165), `dropped` (30-pipeline.js:204). Không có subcollection `touches`, không có CF transcribe (CLAUDE.md không nhắc). Phụ thuộc P08 (nút Gọi/Zalo) chưa tồn tại.",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "4–6 ngày (nút Gọi/Zalo + pendingTouch + bottom-sheet 5 chip + ghi note/fu_at 2, subcollection touches + Rules + rail thống kê 1, tuỳ chọn 🎤 CF transcribeNote 1.5, test iOS/Android 1)",
  "backend_needs": [
   "rules",
   "cf",
   "secret",
   "index"
  ],
  "risks": "(1) Sheet gây phiền: chỉ mở khi lần rời app xuất phát từ nút Gọi/Zalo (`pendingTouch` sessionStorage, TTL 10') + rời ≥20s + nút Bỏ qua; iOS Safari `tel:` đôi khi không bắn visibilitychange → fallback hiện chip 'Kết quả cuộc gọi?' trên thẻ khi mở lại lead. (2) Realtime/diff: bottom-sheet gắn ngoài #view (như panel trợ lý AI) để repaint feed không xoá sheet; ghi note qua addLeadNote đã có latency-compensation → nhớ `D.notes=SL_FB.notesSnapshot()` trước renderFeed (bài học v119-20). (3) fu_at ghi từ đồng hồ client (đã là nợ kỹ thuật) — giữ nhất quán, không trộn serverTimestamp. (4) 'Không nhu cầu' → dropped:true = lead ẩn khỏi pipeline nhưng feed không đánh dấu (brief lỗi high) → thêm chip 'Đã loại' ở thẻ cùng đợt; lost_reason là field mới cần Rules leads update cho phép. (5) 🎤 audio: MediaRecorder iOS ra audio/mp4 → CF phải nhận mp4/webm; không lưu file (PII giọng nói), chỉ transcript; key OpenAI đã ở backend; cost ~0.006$/phút. (6) Rules `leads/{id}/touches` create cho active user cùng brand; admin dashboard đếm theo sales cần collectionGroup touches where brand → index COLLECTION_GROUP mới — bài học notes failed-precondition: PHẢI tạo index trước khi ship.",
  "implementation_sketch": "- 20-feed.js + 30-pipeline.js: pill SĐT → sheet 'Chăm khách 1 chạm' (Gọi `tel:`, Zalo `zaloDial()` hồi sinh, SMS `sms:`, Chép) — set `sessionStorage.pendingTouch={leadId,channel,at}`.\n- 90-boot.js: listener `visibilitychange`/`focus` → nếu pendingTouch && Date.now()-at ≥20s → mở `touchSheet(leadId)` (DOM ngoài #view) 5 chip; chọn → (a) `SL_FB.addLeadNote(leadId,brand,'📞 Gọi 14:02 — Không nghe máy','team')`, (b) `updateLead({fu_at, fu_note, first_care_at/by nếu chưa, ...})` theo nhịp (không nghe +2h; hẹn lại datetime; quan tâm +1 ngày; không nhu cầu → {dropped:true,dropped_at,dropped_by,lost_reason}; sai số → {phone_invalid:true}), (c) `SL_FB.addTouch(leadId,{channel,result,by_uid,by_name,at})` = addDoc leads/{id}/touches (live.js mới), (d) gợi ý stage → pvApplyStage.\n- Rail feed / ROI 40-roi.js / Overview admin: đếm touches hôm nay theo sales từ `D.touches` (live.js: collectionGroup('touches') where brand==byBrand & at≥7 ngày, lazy khi mở roi/overview).\n- Tuỳ chọn 🎤: MediaRecorder ≤90s → POST CF `transcribeNote` (s13, verifyIdToken, gpt-4o-mini-transcribe 'vi' → gpt-4o-mini trích JSON {ket_qua,gia_tri,hen_luc,tom_tat}) → sheet điền sẵn, sales xác nhận.\n- Rules: `match /leads/{id}/touches/{t}` create = isActiveUser && request.resource.data.brand==myBrand && by_uid==uid, read = super || brand==myBrand; leads update cho phép `lost_reason`,`phone_invalid`. Index COLLECTION_GROUP `touches(brand ASC, at DESC)` tạo TRƯỚC khi ship (LỆNH Cloud Shell)."
 },
 {
  "id": "P15",
  "exists": "partial",
  "exists_evidence": "Nguyên liệu có, báo cáo chưa: `grep -n 'window.print\\|pdf\\|@media print\\|brandReport\\|roi_stats' src/app/*.js live.js` = 0 kết quả. Có sẵn: brands.logo (85-users-admin.js:149, v124), brands.plan {tier,price,from,to,cycle,note,updatedBy} (70-shell-tools.js:263-300), brands.billing (live.js:699), ROI client-side 40-roi.js (roiParamsFor/roiCompute, rolling 30 ngày trên cửa sổ 500 lead), ticker thông báo toàn hệ (70-shell-tools.js:513-575, demo lưu localStorage 'sl_ann_ticker'), cụm Zalo `~/smartlead-zalo-fn` notifyBrandZalo/sendZaloZBS (CLAUDE.md:76). Không có email provider nào (grep Resend/sendEmail = 0). Không có deal_value/closed_at (40-roi.js:64 realized = closedN×AOV).",
  "feasible": "yes-with-backend",
  "realistic_effort": "L",
  "days_estimate": "12-18 ngày (FE 4 · CF render+gửi 6 · Zalo/email 2 · counter+Rules+index 2 · nghiệm thu 2)",
  "backend_needs": [
   "cf",
   "external-api",
   "rules",
   "index",
   "secret",
   "worker"
  ],
  "risks": "(1) Puppeteer trong CF gen2 cần ~1GB RAM + cold start 10-20s, region asia-southeast1; deploy trùng tên/region đã từng gây 'Failed to fetch' (CLAUDE.md v119-25). (2) Số liệu: ROI hiện là 'tiết kiệm giả định' theo CPL benchmark, và tính từ cửa sổ 500 lead → báo cáo tháng gửi khách sẽ SAI/thổi phồng nếu không có counter riêng; doanh thu thật phụ thuộc P34 (deal_value) — chưa có thì phải ghi rõ 'ước tính' trên PDF, không được in như số thật. (3) Zalo OA: ZBS/ZNS có phí+quota+template phải duyệt; gửi PDF qua Zalo cá nhân = KHÔNG (func chỉ Zalo User API, không phù hợp gửi báo cáo chính thức). (4) Email: chưa có provider → thêm Resend/SendGrid = thêm secret + DNS SPF/DKIM domain agency. (5) Link ký hạn Storage 30 ngày = ai có link đọc được số liệu brand → cần token ngẫu nhiên dài + expiry. (6) Rules: brands write=isSuperAdmin, `brands.notices[]` cần ghi từ CF (Admin SDK) — không mở write client. (7) FE: route `#report` mới phải thêm vào meta/SUPER_ONLY_VIEWS đúng (10-core-overview.js:36-48) và không đụng đường diff feed.",
  "implementation_sketch": "- Counter: CF trigger `onDocumentWritten leads/{id}` (lọc ngay đầu hàm: chỉ khi stage/temp/brand đổi) → `roi_stats/{brand}__{YYYY-MM}` FieldValue.increment (pattern y hệt outreach_stats v119-34) → hết phụ thuộc cửa sổ 500 lead.\n- CF `brandReport` (asia-southeast1, memory 1GiB, timeoutSeconds 120) onSchedule '0 8 * * 1' + '0 8 1 * *' + check `plan.to - 7d`; + onRequest `brandReportNow` (verify idToken super/admin brand) gọi tay từ web. Dựng HTML từ roi_stats + outreach_stats + brands.{logo,name,plan,roi} → Puppeteer PDF → Storage `reports/{brand}/{kỳ}.pdf` → getSignedUrl 30 ngày → ghi `brand_reports/{brand}__{kỳ}` {url, sentAt, openedAt}.\n- Gửi: gọi notifyBrandZalo (cụm smartlead-zalo-fn) với link; email qua Resend (secret RESEND_API_KEY ở .env/Secret Manager) tới `brands.billing.email`.\n- Notice gia hạn: CF ghi `brands/{code}.notices[]` (Admin SDK); FE 70-shell-tools renderTicker đọc thêm `MB.notices` theo brand (giữ ticker toàn hệ).\n- FE: part mới `src/app/42-report.js` (nav 'Báo cáo' nhóm Hiệu quả) — 'Xem trước' render HTML cùng template + @media print trong app.css; 'Lịch gửi' + lịch sử từ `brand_reports` (subscribe lazy khi hash #report). SL_FB thêm `sendBrandReport(code)` (HTTP CF, Bearer idToken) + `reportsFor(code)`.\n- Rules: `brand_reports` read = super || brandCode==myBrand, write=false; `roi_stats` read tương tự (copy block outreach_stats LỆNH Q). Index: `brand_reports(brandCode asc, sentAt desc)`."
 },
 {
  "id": "P16",
  "exists": "partial",
  "exists_evidence": "20-feed.js:455 `l.__q=fold([l.name,l.text,l.need,l.intent,l.industry,l.source,l.service].join(' '))` — KHÔNG có phone/email/id. 70-shell-tools.js:22 palette tìm `l.name+industry+need+source` — cũng không có phone. Helper sẵn: `normPhoneVN` (20-feed.js:680, chuẩn '+84xxxxxxxxx'), `phoneLocal` (:681), `fmtPhoneVN`; lead.phone đã lưu chuẩn '+84…' (20-feed.js:749 so sánh `normPhoneVN(m)===l.phone`). `ckOpen` (70-shell-tools.js:58) chỉ gọi từ Ctrl/Cmd+K (:68) và `#kbdHint` (:71, app.html:108 — ẩn mobile); mobile-bar (app.html:92-95) chỉ có menu + logo, không có nút tìm. Tra ngoài RAM: live.js chỉ có `loadOlder`/`leadsExtra` (:180-242) theo thời gian, không có query theo phone.",
  "feasible": "yes",
  "realistic_effort": "S",
  "days_estimate": "1.5-2 ngày (FE 1 · live.js tra kho 0.5 · smoke 0.5)",
  "backend_needs": [
   "none"
  ],
  "risks": "(1) `l.__q` được cache trên object lead (object mới mỗi snapshot) → thêm field vào chuỗi là an toàn, nhưng nhánh 'toàn số → endsWith' phải tách riêng khỏi includes để không khớp nhầm số trong text bài (vd giá '3456'); giữ ngưỡng ≥4 số. (2) Query toàn kho `where('brand','==',byBrand), where('phone','==',norm)`: Rules leads read đã brand-scope, composite index leads(brand, phone) CẦN tạo (2 where equality = index composite, Firestore báo failed-precondition → banner channel v119-18 sẽ hiện nếu quên). Super Admin không lọc brand → chỉ 1 where phone (single-field tự có). (3) Lead tìm ngoài RAM nhét vào `leadsExtra` phải qua `dataEpoch` + dedupe seen (live.js:226-229) để không nhân đôi / tiêm lead brand cũ. (4) Số cuối endsWith trên kho lớn = không query được server-side (Firestore không có endsWith) → tra kho chỉ cho SĐT đầy đủ; 4 số cuối chỉ tìm trong RAM — nói rõ trên UI. (5) Palette hiện `slice(0,9)` và không có nút hành động — thêm Gọi/Zalo dùng lại `tel:`/`zaloDial` (zaloDial đang là code chết, CLAUDE.md v119-17).",
  "implementation_sketch": "- 20-feed.js:455: `__q` += `phoneLocal(l.phone)`, `l.phone`, `l.email`, `l.id`; trước `includes` thêm nhánh `/^\\d{4,}$/.test(q)` → so `digitsOnly(phoneLocal(l.phone)).endsWith(q)`.\n- 70-shell-tools.js:22: cùng logic; dòng kết quả `sub` = `${stage} · ${assignee||'chưa giao'} · ghi chú cuối` (leadNotes(l.id)[0]); thêm 2 nút nhỏ `tel:` + Zalo (`https://zalo.me/<số>`).\n- app.html mobile-bar: thêm `<button id=\"mSearch\">` → 70-shell-tools gắn `ckOpen`; CSS hiện chỉ <1024px.\n- live.js SL_FB thêm `findLeadByPhone(norm)`: `getDocs(query(leads, [byBrand? where brand==byBrand], where phone==norm, limit 5))` → push vào leadsExtra (dedupe id, kiểm dataEpoch) → `rebuild()`; feed chip 'ngoài cửa sổ'.\n- Index: composite `leads(brand ASC, phone ASC)` (LỆNH gcloud cho anh). Không cần Rules."
 },
 {
  "id": "P17",
  "exists": "partial",
  "exists_evidence": "Bell panel 70-shell-tools.js:76-95: `(D.alerts||[]).slice(0,4)`, D.alerts dựng ở live.js:124 = `leads.filter(l=>l.score>=60).slice(0,12)` kênh giả 'Đa kênh' — đúng như mô tả; `#bpAll` → `go('alerts')` mà `alerts` ∈ SUPER_ONLY_VIEWS (10-core-overview.js:48) → user thường bị đẩy về overview. Nguyên liệu suy việc đã có đủ trong D.leads: `isMine` (20-feed.js:292), `first_care_at`/`markFirstCare` (:298-301), `slaWarn` theo brand (:312), `fu_at` + rail 'Hẹn hôm nay' (:614-618), `stage=='responded'`/`outreach_replied` do funcWebhook set (CLAUDE.md:176), `assignee_uid` (live.js:493). Chưa có: danh sách việc, trạng thái đã xem, badge theo tôi.",
  "feasible": "yes",
  "realistic_effort": "S",
  "days_estimate": "2 ngày (FE 1.5 · smoke/test Playwright 0.5)",
  "backend_needs": [
   "none"
  ],
  "risks": "(1) Badge tính lại mỗi `SLApp.reload` — rẻ (1 vòng D.leads), nhưng phải đặt ở đường repaint chung, KHÔNG trong diff feed để tránh regression v119-23. (2) 'Xong' → ghi note hệ thống qua `addLeadNote` — note realtime v119-20/21: KHÔNG push tay vào notesCache, chỉ gọi SL_FB rồi `D.notes=notesSnapshot()`. (3) localStorage 'đã xem' theo thiết bị: sales dùng 2 máy sẽ thấy lại việc; chấp nhận bước đầu, nêu rõ. (4) Điều kiện (d) 'lead nóng brand chưa ai chăm > slaWarn' với admin brand có thể rất nhiều dòng → cap 20 + sort ưu tiên (quá hẹn > phản hồi > mới giao > SLA). (5) Với brand user thì D.leads đã brand-scope, nhưng `outreach_replied` là field engine ghi — Rules leads read cho brand user đọc doc → OK, không rò.",
  "implementation_sketch": "- 70-shell-tools.js: thay khối BELL bằng `myTasks()` từ D.leads: 4 nhóm theo mô tả, mỗi item {id:leadId+':'+kind+':'+at, kind, lead}; sort ưu tiên; cap 20.\n- Seen: localStorage `sl_bell_seen_v1` = {itemId: ts}, prune khi điều kiện hết; `bellBadge` = count chưa seen; 0 → ẩn badge (hiện set text).\n- Row: Gọi (`tel:`) / Zalo / Xong (→ `SL_FB.addLeadNote(id,'✅ Đã xử lý việc: …',vis team)` + với kind 'mới giao' gọi `markFirstCare(l)`), click tên → openLead.\n- Footer: super → `go('alerts')`; admin/sales → `go('feed')` + set feedFilter='mine' (rail 'Lead của tôi' đã có) — bỏ dẫn tới trang super-only.\n- Hook: gọi `renderBellBadge()` trong `SLApp.reload` (90-boot) sau rebuild; không đụng live.js (D.alerts giữ nguyên cho trang alerts super)."
 },
 {
  "id": "P18",
  "exists": "partial",
  "exists_evidence": "Modal `openLead(id)` 65-charts-lead-modal.js:45 = snapshot tĩnh từ D.leads (find) + notes; không có timeline. `outreach_log` chỉ subscribe ở `lazyOutreach` theo brandCode/limit100 khi ở tab #outreach (live.js:803-805), không có query theo leadId; `grep stage_history\\|outreach_threads` src/app + live.js = 0. `pvApplyStage` (30-pipeline.js:176-190) ghi `{stage,stage_at}` không append lịch sử; `assignLead` (live.js:492-494) ghi assigned_at/assigned_by (mốc mới đè cũ). Rules: outreach_log read cho brand user đã có (LỆNH C); outreach_threads/outreach_tasks chỉ Admin SDK, KHÔNG có Rules read (CLAUDE.md:177). Worker ghi `doneSteps` lên outreach_threads (worker.mjs:589-598) + log từng bước có leadId/brandCode/status (logStep). funcWebhook đã ghi note '💬 Khách đã phản hồi' + stage responded (CLAUDE.md:176).",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "5-7 ngày (FE modal+chip 3 · live.js subscribe vòng đời 1 · CF stop + Rules + index 1 · test 1)",
  "backend_needs": [
   "index",
   "rules",
   "cf"
  ],
  "risks": "(1) Modal hiện là innerHTML tĩnh; subscribe outreach_log theo leadId phải unsub khi đóng modal (closeModal/route change) — nếu quên → listener rò, mỗi lần mở lead thêm 1 listener = tốn read + repaint. Mỗi lần mở = 1 query 30 doc (rẻ). (2) Index composite `outreach_log(leadId ASC, at DESC)` bắt buộc; thiếu → failed-precondition → banner channel (bài học notes 27/08: kiểm index COLLECTION scope trước khi ship). (3) Mở Rules read `outreach_threads` theo brand: doc thread chứa `fpayload` (comment_msg/inbox_msg = lead.reply, author_url) — không có secret, nhưng brand user thấy nội dung máy gửi — chấp nhận; write vẫn false. (4) `stage_history[]` arrayUnion trên doc lead: mỗi kéo thẻ +1 phần tử; cap 50 ở client (hiếm vượt); pvApplyStage optimistic+revert v119-19 phải giữ (revert không cần xoá phần tử). (5) Nút 'Dừng máy': ghi `outreach_threads.active=false` qua CF (không mở write) — engine func path retry thread tới hạn theo active, worker funnel đang chạy giữa phiên KHÔNG dừng ngay (by-design, tối đa 1 lead). (6) func-path log thiếu name/temp (CLAUDE.md v119-38 tồn b) chỉ ảnh hưởng nhãn, không chặn. (7) Chip '🤖 đã chào' trên thẻ feed: phải là 1 field trên lead (vd `outreach_last` do worker/engine ghi) chứ KHÔNG join outreach_threads trong renderFeed (diff feed theo lead → join bên ngoài phá diff + tốn subscribe 1 collection nữa).",
  "implementation_sketch": "- Rules: `match /outreach_threads/{id}` read = super || (active && resource.data.brandCode==myBrand) — kiểm engine có ghi brandCode lên thread (task có brandCode; thread cần bổ sung nếu thiếu); write=false. Index `outreach_log(leadId, at desc)`.\n- live.js: SL_FB `watchLeadTrail(leadId, cb)` = onSnapshot(query(outreach_log, where leadId==id, orderBy at desc, limit 30)) + getDoc(outreach_threads/{id}); trả unsub. `stopOutreach(leadId)` = HTTP CF `outreachStop` (Bearer idToken; verify super/admin brand khớp lead.brand) → set active:false + taskStatus 'stopped' + addLeadNote hệ thống.\n- Engine/worker: khi bước done, ghi thêm `leads/{id}.outreach_last={step,at,pid}` (1 field, Admin SDK) → feed/kanban chip đọc thẳng `l.outreach_last` (diff feed vẫn theo lead object).\n- 30-pipeline.js pvApplyStage + live.js assignLead: patch thêm `stage_history: arrayUnion({stage,by:uid,at:Date.now()})` (import arrayUnion; Rules leads update whitelist — cần kiểm LỆNH 'Rules leads update có whitelist field không' đang tồn).\n- 65-charts-lead-modal.js: khối `<div id=\"ldTrail\">` + hàm `renderTrail(items)` gộp notes (💬/👤) + log (🤖) + stage_history (🔀) sort at desc; badge đầu modal từ thread.doneSteps; nút 'Dừng máy, tôi tiếp quản' (ẩn khi thread không active); unsub trong closeModal + hashchange."
 },
 {
  "id": "P19",
  "exists": "none",
  "exists_evidence": "`grep -n 'phoneIdx\\|authorIdx\\|contacts\\|merged_into\\|suspect\\|Khách cũ' src/app/*.js live.js` = 0 kết quả liên quan (chỉ có chống trùng BÀI khi quét: 60-scan-views.js:55, 70-shell-tools.js:639/704 và `brand_sales` id `${brand}__${phone}` live.js:727 — khác mục đích). Engine: `outreach_map/{uid}` (worker.mjs:549,701) chỉ dùng để khớp reply, KHÔNG dùng để chặn inbox lại trong 30 ngày. Có sẵn normPhoneVN (20-feed.js:680), lead.phone chuẩn +84, lead.author_url (~31% lead, CLAUDE.md phần GATE), lead.temp/score do pipeline chấm.",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "6-8 ngày (client idx+chip+cảnh báo giao 2 · CF contacts+Rules+index 2 · engine skip 0.5 · gộp 1 · tầng nghi ngờ rule+LLM 1.5 · test 1)",
  "backend_needs": [
   "cf",
   "rules",
   "index",
   "worker",
   "data-migration"
  ],
  "risks": "(1) Gộp nhầm: 'Ẩn danh' là name phổ biến (BrightData), author_url username có thể đổi → CHỈ tự gộp theo phone chuẩn hoặc uid số; username/pfbid chỉ gợi ý — như đề xuất; cần enforce ở code. (2) Phủ thấp: phone chỉ ở phần lead có SĐT, author_url ~31% → 'khách cũ' bắt được thiểu số; nói rõ với anh trước để không kỳ vọng. (3) CF trigger onCreate leads: volume 7-12k doc lịch sử; trigger chỉ chạy doc mới → cần backfill 1 lần (script Admin SDK) — data-migration. (4) Rules `contacts` read theo brand (doc id có prefix brand → dùng field brandCode để so, không parse id). (5) `merged_into`: pipeline/feed lọc thêm `!l.merged_into` — đụng renderFeed/pv filter, đã có pattern `!l.dropped` (v119-16) → thêm cùng chỗ, ít regression. (6) Engine skip 'uid/phone đã inbox 30 ngày': engine func+worker đều đọc `outreach_map/{uid}.updatedAt` trước khi enqueue inbox — 1 read/lead, rẻ; nhưng uid số hiếm (CLAUDE.md: 0/12 lead có uid số) → skip theo author_url chuẩn hoá thay vì uid. (7) Tầng 'nghi người bán' bằng LLM: chi phí OpenAI/lead (đã có pipeline chấm điểm — nên nhét thêm 1 field `seller_signals` vào prompt chấm hiện có thay vì gọi LLM lần 2); hạ điểm tự động = rủi ro false-positive giết lead thật → chỉ gắn chip + hạ tối đa 10 điểm, không đẩy xuống junk. (8) Client Map dựng mỗi snapshot: O(n) trên ≤4000 lead — rẻ, nhưng đặt trong buildData memo (live.js) chứ không trong renderFeed.",
  "implementation_sketch": "- live.js buildData: `phoneIdx`/`authorIdx` (Map) dựng theo reference mảng leads (memo như buildScansMemo) → `d.dupIdx`. Key phone = l.phone (đã +84); key author = uid số nếu có, else username lowercase từ author_url.\n- 20-feed.js leadCard + 65 modal: chip 'Khách cũ · N lead · #stage cuối (assignee)' từ dupIdx (chỉ phone/uid = chắc; username = 'có thể là'), nút mở lead cũ (openLead). `showAssignPop`: nếu lead cũ cùng số đang do người khác chăm → confirm.\n- CF `onDocumentCreated leads/{id}` → `contacts/{brand}__{key}` merge {brandCode, leadIds arrayUnion, lastStage, lastAssignee, postCount30d, industries arrayUnion}; script backfill 1 lần. Rules contacts read = super || brandCode==myBrand, write=false. Index contacts(brandCode, updatedAt desc).\n- Gộp: SL_FB `mergeLead(fromId,intoId)` = updateDoc from {merged_into, dropped:true}; feed/pipeline/ROI lọc `!l.merged_into` cạnh `!l.dropped`.\n- Engine (outreach.js stepNickAdspower + apEnqueueFunnel): trước bước inbox check `contacts`/`outreach_map` theo author key có inboxAt trong 30 ngày → bỏ bước inbox (steps[] không có inbox, không tiêu cap). Worker không đổi.\n- 'Nghi người bán': rule client (regex 'nhận làm|inbox em|bảng giá|zalo em' + ≥3 industry/tuần từ contacts) → chip '⚠ nghi người bán' + field `suspect` do CF contacts ghi lên lead; LLM để pipeline chấm điểm sẵn có (backend s13) thêm 1 field, không gọi thêm."
 },
 {
  "id": "P20",
  "exists": "none",
  "exists_evidence": "`grep -n 'audit\\|updatedBy\\|changedBy' src/app live.js`: chỉ có `updatedBy` email trong `setBrandBilling`/`setBrandPlan` (live.js:699,703) và hiển thị 'Cập nhật lần cuối bởi' (70-shell-tools.js:248,300; 85-users-admin.js:552,602). `setUserRole`/`setUserActive`/`setUserBrand` (live.js:678-692), `updateLead`/`deleteLead` (:423-425), `assignLead` (:492 có assigned_by email), setSource/setConfig/setOutreach KHÔNG gắn actor. Re-auth chỉ có ở đổi mật khẩu (live.js:599 reauthenticateWithCredential) — không có khi đổi role. Không có collection audit_log, không có tab Nhật ký trong 85-users-admin. CLAUDE.md không có CF audit nào (chỉ Rules guard field-level users, dòng ~80).",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "5-7 ngày (CF trigger 6 collection 2 · live.js gắn updatedBy 12 method + Rules ép uid 1 · FE tab Nhật ký+CSV 1.5 · confirm/re-auth role 0.5 · test 1)",
  "backend_needs": [
   "cf",
   "rules",
   "index",
   "data-migration"
  ],
  "risks": "(1) Trigger `leads/{id}` volume lớn: mỗi update lead (first_care_at, fu_at, stage_at, engine ghi outreach_last/replied…) = 1 invocation dù return sớm → ở 3000 nick + engine ghi liên tục có thể vài chục nghìn invocation/ngày (~$0.4/1M, chấp nhận được) nhưng KHÔNG dùng onDocumentWritten cho leads nếu sau này có counter khác — gộp chung 1 trigger cho P15 roi_stats + P19 contacts + audit để trả 1 lần invocation. (2) Rules ép `request.resource.data.updatedBy.uid == request.auth.uid` trên users/brands/sources/config/fb_accounts: MỌI đường ghi client hiện có (12+ method) phải gắn field, quên 1 method = permission-denied → banner channel → hồi quy diện rộng; cần smoke Playwright từng method + deploy Rules SAU zip. Admin SDK (engine/worker/CF) không qua Rules → actor 'system/engine' fallback đúng. (3) TTL 12 tháng bằng `expireAt` (pattern outreach_log ACTIVE) — cần bật policy; audit_log 'bất biến' chỉ đúng nếu write=false cho client và CF không có đường xoá. (4) diff before→after với leads chứa text bài/phone → audit_log chứa dữ liệu cá nhân: giới hạn diff vào field nhạy đã lọc, không chép text bài. (5) Re-auth khi nâng quyền: user MFA (80-rbac-auth) → reauthenticate cần flow MFA lại — dùng `reauthenticateWithCredential` password rồi nếu `auth/multi-factor-auth-required` thì resolver; chỉ áp cho super (người duy nhất đổi role theo Rules). (6) FE tab Nhật ký query theo brandCode+at desc = composite index bắt buộc.",
  "implementation_sketch": "- live.js: helper `actor()` = {uid, email} từ auth.currentUser; gắn `updatedBy:actor(), updatedAt:serverTimestamp()` vào setUserRole/setUserBrand/setUserActive/setBrandLeadWindow/setSource/deleteSource(soft: ghi trước xoá)/setConfig/setOutreach/setFbAccount*/assignLead/updateLead(patch merge); deleteLead → gọi CF `deleteLeadAudited` thay deleteDoc trực tiếp (trigger onDelete không có actor).\n- CF `auditTrail` (asia-southeast1) onDocumentWritten cho users, brands, sources, config, fb_accounts, leads — leads: `if(!SENSITIVE.some(k=>before[k]!==after[k])) return;` ngay đầu; ghi `audit_log/{auto}` {actor:after.updatedBy||'system', action, target:{col,id}, brandCode, diff (chỉ field nhạy), at, expireAt:+12 tháng}.\n- Rules: `audit_log` read = super || (isActiveUser && role admin && brandCode==myBrand), write=false; các collection nhạy thêm `&& request.resource.data.updatedBy.uid == request.auth.uid` (đặt trong cùng đợt deploy với zip).\n- Index `audit_log(brandCode ASC, at DESC)`; TTL policy `expireAt`.\n- 85-users-admin.js: tab 'Nhật ký' (lazy subscribe `audit_log` limit 200, lọc actor/loại/ngày client) + nút CSV (Blob download — lưu ý trong artifact sandbox không tải được nhưng app Netlify thì OK).\n- Đổi role → admin/superadmin: modal confirm + ô mật khẩu → `SL_FB.reauth(pw)` (tách từ đường đổi mật khẩu live.js:599) → setUserRole kèm `changedBy`."
 },
 {
  "id": "P21",
  "exists": "none",
  "exists_evidence": "`grep -n 'exp_group\\|holdout\\|variant\\|Thompson\\|lift' src/app/45-outreach.js live.js worker/worker.mjs CLAUDE.md` = 0 kết quả. Engine chỉ có `REACT_MIX` xoay cảm xúc và `lead.reply` làm nội dung (CLAUDE.md LỆNH I) — không có biến thể nội dung có nhãn. Có sẵn: `outreach_stats/{brand}__{day}` counter (v119-34), `outreach_threads.doneSteps` (worker.mjs:589-598), lead.stage/stage_at (30-pipeline.js:186), `outreach_replied` (funcWebhook), bd_month chi phí BrightData (10-core-overview.js:374-404), ROI_DEF.cplAds (40-roi.js:9). KHÔNG có deal_value (40-roi.js:64), không có chi phí VPS/proxy/nick khai báo.",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "7-9 ngày (engine exp_group+variant 1.5 · worker ghi variant 0.5 · CF job đêm attribution 2 · FE khối Hiệu quả 2 · brand.plan chi phí 0.5 · test 1.5) — Thompson sampling + PDF/Zalo để đợt sau",
  "backend_needs": [
   "cf",
   "worker",
   "rules",
   "index",
   "data-migration"
  ],
  "risks": "(1) Holdout 10% = brand mất 10% lead không được chạm — khách SME trả tiền để automation chạy; phải là opt-in per brand + tự tắt khi đủ mẫu (vd ≥200 lead/nhóm) và mặc định TẮT với brand nhỏ (5-10 nick, vài chục lead/ngày → cần nhiều tuần mới có ý nghĩa thống kê). (2) Không có deal_value → 'doanh thu do automation' vẫn là AOV giả định — hiển thị phải ghi 'ước tính' hoặc chờ P34; chỉ reply-rate/booked-rate là số thật. (3) Reply-detection chỉ có ở nick func (funcWebhook); nick AdsPower CHƯA bắt phản hồi khách (CLAUDE.md Phase 2 còn làm mục 6) → 'replied' cho AdsPower = 0 dù có reply → lift sai lệch nghiêm trọng cho kênh anh đang dùng; PHẢI làm reply-detection AdsPower (worker quét messenger inbox) trước hoặc song song. (4) Nhiều biến thể nội dung = comment công khai đa dạng hơn (giảm spam) nhưng inbox biến thể do AI xoay có thể lệch giọng brand — giữ lead.reply làm 'variant A' mặc định. (5) CF job đêm join threads×leads: ~số thread/tháng (vài nghìn) read — rẻ; ghi `outreach_attr/{brand}__{tháng}` 1 doc/brand. (6) Attribution 'chạm cuối' vs 'có tham gia' hiện 2 con số → cần chú thích rõ trên UI, dễ tranh cãi với khách. (7) Ban-rate/nick từ challengeCount (fb_accounts) là số thật, tốt.",
  "implementation_sketch": "- Engine outreach.js `stepNickAdspower`/`stepNick`: khi nạp lead mới, `exp_group = brand.outreach.holdoutPct>0 && hash(leadId)%100 < pct ? 'holdout':'auto'` → updateDoc lead {exp_group}; holdout → không enqueue (không tiêu cap); thread lưu `variant` (id mẫu comment/inbox, mặc định 'reply'); FE modal ⚙️ brand (v119-35) thêm ô 'Holdout %' + công tắc.\n- Worker: khi funnel done ghi `variant` + `doneSteps` sẵn có lên thread (đã có), log step giữ nguyên.\n- CF `outreachAttr` onSchedule '30 1 * * *' (VN): quét `outreach_threads` cập nhật trong 45 ngày + leads tương ứng (stage, stage_at, outreach_replied, deal_value nếu có) → `outreach_attr/{brand}__{YYYY-MM}` {byVariant, byNick, byTemp, byStep: {touched,replied,booked,closed,revenueEst}, holdout:{n,replied,booked,closed}, cost:{vps,proxy,nick từ brand.plan.autoCost, bd từ bd_month, openai}} → cpl = cost/touched.\n- Rules `outreach_attr` read = super || brandCode==myBrand, write=false. Không cần index (đọc theo doc id).\n- FE 45-outreach.js: khối 'Hiệu quả' trên thẻ brand: lift = replyRate(auto)−replyRate(holdout) + Wilson CI 95%; bảng theo variant/nick/temp; so CPL với `roiParamsFor(brand).cplAds`; nhãn 'chạm cuối'/'có tham gia'. `lazyOutreach` subscribe thêm `outreach_attr` tháng hiện tại (guard oaSignature v119-37 thêm trường).\n- Reply-detection AdsPower (tiền đề): worker mỗi phiên nick mở messenger.com kiểm thread trong outreach_map có tin mới → ghi outreach_replied + note (tái dùng logic funcWebhook)."
 },
 {
  "id": "P22",
  "exists": "partial",
  "exists_evidence": "Duyệt tay đã có: 85-users-admin.js:122-125 thẻ 'Chờ duyệt' + :425 `SL_FB.approveUser`; live.js:681-686 `approveUser` chỉ setDoc users/{uid} {role,brand,active,approvedAt}, KHÔNG gửi thông báo; live.js:178-181 `pendingUnsub` đếm user pending realtime. View users nằm trong SUPER_ONLY_VIEWS (10-core-overview.js:48, chặn 3 lớp :50 / 70-shell-tools.js:13 / 80-rbac-auth.js:205). grep 'invite|createInvite|invites/' src/app + live.js → 0 kết quả. CLAUDE.md: Rules users guard `affectedKeys().hasAny(['role','active','brand',...])` — user thường KHÔNG tự sửa quyền; approve = superadmin only.",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "5-7 ngày (FE 2, CF+Rules 2, test leo thang quyền 1-2)",
  "backend_needs": [
   "cf",
   "rules",
   "external-api"
  ],
  "risks": "(1) Mở Rules `users` update cho admin brand = điểm nhạy nhất tenancy: phải AND đủ 4 điều kiện (cùng brand hiện tại của target, affectedKeys ⊆ {role,active,leadFrom,leadTo,leadFromAt,leadToAt}, role mới ∈ {sales,viewer}, target không phải superadmin/admin) — thiếu 1 vế là admin brand A khoá được admin brand B hoặc tự nâng lên admin brand khác. (2) Token mời trong `invites/{token}` phải chỉ CF đọc (Rules read=false) — nếu FE đọc được thì lộ brand+role. (3) Người tự đăng ký Google → CF onCreate khớp token: ai có link đều vào được trong 48h → giới hạn uses=1 + ràng email nếu có. (4) Gửi Zalo cần `notifyBrandZalo`/`sendZaloZBS` (cụm ~/smartlead-zalo-fn, ZBS chỉ gửi được cho người ĐÃ follow OA) → thông báo 'bạn đã vào brand X' không chắc tới; fallback in-app. (5) refreshAdmin (live.js:389-410) đọc FULL `users`/`fb_accounts`/`brands`/`brand_sales` — mở cho admin brand thì Rules read users phải lọc theo brand → getDocs(collection users) sẽ permission-denied → cần query `where('brand','==',myBrand)` riêng cho non-super.",
  "implementation_sketch": "- Part 85-users-admin.js: nút 'Mời sales' (roleCanEditConfig) → modal chọn role sales/viewer + hạn 48h → gọi `SL_FB.createInvite({role})` → hiện link + QR (đã có lib `window.qrcode` dùng cho 2FA ở 70-shell-tools.js:376 — tái dùng).\n- live.js: `createInvite` = fetch CF `createInvite` (pattern MANUAL_SCAN_URL.replace + Bearer idToken như saveFbAccount); `refreshAdmin` nhánh non-super: getDocs(query(users, where brand==byBrand)) thay full collection; bỏ `users` khỏi SUPER_ONLY_VIEWS, ẩn cột brand/superadmin cho admin brand.\n- CF (~/firebase-s13, asia-southeast1): `createInvite` (verify idToken → role admin/superadmin → invites/{token}={brand,role,expiresAt,uses,createdBy}); HTTP `redeemInvite(token)` gọi từ màn pending (80-rbac-auth.js:179 renderPending thêm ô 'Có mã mời?') → set role/brand/active + xoá token; gọi notifyBrandZalo nếu có.\n- Rules: `invites` read/write=false (chỉ Admin SDK); `users` update thêm nhánh admin-cùng-brand với affectedKeys whitelist + role mới ∉ {admin,superadmin}; `users` read cho admin brand: resource.data.brand==myBrand().\n- Không cần index mới (query users where brand — single-field)."
 },
 {
  "id": "P23",
  "exists": "partial",
  "exists_evidence": "Config toàn cục 1 doc: live.js:764 `onSnapshot(doc(db,'config','app'))` mọi user subscribe; `setConfig` live.js:486 ghi merge (keywords/exclude 50-config-views.js:163, bdBudget/bdFx/openaiCredit 10-core-overview.js:551-572, aiMode :286, autoScanEnabled :227, roi 40-roi.js:238, promoBanner 20-feed.js:19) → ngân sách BrightData/credit OpenAI/assistantKb (live.js:155) đang nằm CHUNG doc mọi user đọc được. Per-brand đã có: `brands/{code}.roi/ai/billing/outreach/logo` (live.js:694-700, setBrandAi/setBrandRoi); sources có `brand` + `aiMode` riêng group (50-config-views.js:29-31,116) nhưng chỉ super gán. Sources subscribe theo brand (live.js:762 `where brand==byBrand`). Không có `brands.cfg`/`budgetCap`/`scanWindow`/`config/public|private` (grep 0). Tiếp cận super-only (10-core-overview.js:48 v119-36).",
  "feasible": "hard",
  "realistic_effort": "L",
  "days_estimate": "15-20 ngày (FE 6, live.js 2, Rules 2, backend scan merge 4-5, migrate + nghiệm thu 3)",
  "backend_needs": [
   "cf",
   "rules",
   "data-migration"
  ],
  "risks": "(1) Đụng lõi quét backend (`manualScan`/`scheduledScan` ở ~/firebase-s13, em chưa thấy code) — merge sai keyword brand ⊕ global = brand mất lead hoặc lead lạc brand; phải có bước 'dry-run so số lead 7 ngày trước/sau'. (2) Tách config/app → 2 doc là data-migration đụng MỌI listener (live.js:764 + 12 điểm setConfig) — rủi ro regression ở buildData whitelist (bug 'field rơi khỏi rebuild' từng dính 2 lần theo CLAUDE.md). (3) Mở Nguồn quét cho admin brand = tăng chi phí BrightData không kiểm soát → BẮT BUỘC ghép budgetCap/brand + engine dừng nguồn khi chạm 100% (backend). (4) Mở Tiếp cận cho non-super: `lazyOutreach` (live.js) đã có nhánh brand user (index outreach_log brandCode+at, Rules outreach_log/outreach_stats sẵn), nhưng `fb_accounts` Rules read=isSuperAdmin, `workers` super-only, thẻ nick hiện token/VPS → phải làm view rút gọn + Rules fb_accounts read where brand==myBrand; thao tác bật/tắt outreach.on = bay nick → cần clamp caps ở engine (`brandCaps` chỉ tin field trong clamp). (5) Rules `brands` update affectedKeys ⊆ {cfg,outreach} cho admin cùng brand: nhớ `outreach.caps` là nơi nhạy — clamp phía engine + FE.",
  "implementation_sketch": "- Giai đoạn 1 (an toàn, S): tách `config/private` (bdBudget, bdFx, openaiCredit, assistantKb, assistantLeadLimit) — live.js subscribe thêm doc private CHỈ khi isSuper (snapErr bỏ qua permission-denied); Rules `config/private` read/write=isSuperAdmin; giữ `config/app` làm public. Backend assistantChat đọc assistantKb từ config/private (1 dòng đổi path).\n- Giai đoạn 2: `brands/{code}.cfg={keywords,exclude,channels,scanWindow,budgetCap,aiMode}` — live.js `setBrandCfg(code,cfg)` (setDoc merge như setBrandAi); 50-config-views.js Keyword/Cảnh báo có tab 'Chung (super)' vs 'Brand của tôi'; Nguồn quét: form thêm group cho admin brand tự set `brand=myBrand` (setSource đã có; Rules sources create/update cho admin nếu request.resource.data.brand==myBrand()).\n- Backend: manualScan/scheduledScan khi quét source có brand → đọc brands/{brand}.cfg, keywords = union(global, cfg.keywords), exclude = union; thêm doc `brand_budget/{code}__{YYYY-MM}` cộng dồn chi phí BrightData/brand (bd_month đã có cân đối theo brand) → ≥80% set aiMode saver, 100% skip sources brand.\n- Rules: brands update cho admin cùng brand affectedKeys ⊆ {cfg,outreach,updatedAt}; fb_accounts read brand==myBrand; bỏ `sources/keywords/alerts/outreach` khỏi SUPER_ONLY_VIEWS nhưng gate nội dung bằng roleIsSuper() trong từng view.\n- Index: sources(brand) single-field có sẵn; không cần composite."
 },
 {
  "id": "P24",
  "exists": "partial",
  "exists_evidence": "`updateLead` pass-through: live.js:423 `updateDoc(doc(db,'leads',id),patch)`. Field FE ghi (grep `updateLead(`): 20-feed.js:300 {first_care_at,first_care_by}; 30-pipeline.js:24,187-189 {stage,stage_at,first_care_*}; :245,:278 {dropped,dropped_at,dropped_by}; 65-charts-lead-modal.js:153 {stage:'closed'}, :156 {fu_at,fu_note}; live.js:433 logLeadView {views,views_by.*,lastViewAt,lastViewBy}; :492-505 assign {assignee,assignee_uid,assignee_email,assigned_at,assigned_by} + unassign deleteField. Notes create: live.js:521-525 `brand` do client truyền (`l.brand||''`), không đối chiếu lead. Rules leads whitelist CHƯA kiểm (CLAUDE.md:399 'LỆNH kiểm Rules leads update có whitelist field không' còn tồn; understand.json: 'sales có thể sửa brand/score/temp/assignee_uid nếu Rules chỉ check brand'). Sửa SĐT/tên: grep `phone_edited|editPhone|phone_ai` → 0; `normPhoneVN` có sẵn 20-feed.js:680. Custom claims: grep CLAUDE.md 'custom claim' → 0.",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "4-6 ngày (Rules+emulator test 2, CF claims 1, FE sửa SĐT/tên 1-2, nghiệm thu 1)",
  "backend_needs": [
   "rules",
   "cf",
   "data-migration"
  ],
  "risks": "(1) Whitelist thiếu field → thao tác hợp lệ chết im dưới toast 'Lỗi: permission-denied' — danh sách trên là từ FE, nhưng backend engine/worker (Admin SDK) ghi `stage:'responded'`, `outreach_replied`, `stage_at` — Admin SDK bỏ qua Rules nên OK, nhưng sales KHÔNG được ghi `outreach_*`. (2) `logLeadView` dùng dotted path `views_by.<email>` → affectedKeys thấy `views_by` — whitelist phải gồm `views_by` chứ không phải khoá con; đây là fire-and-forget nên fail cũng không toast. (3) `unassignLead` dùng deleteField → affectedKeys vẫn liệt kê key → nhớ đưa `assignee_*` vào nhánh admin. (4) Custom claims: CF onWrite users → setCustomUserClaims, nhưng token client cache ~1h → sau khi đổi role/brand phải `getIdToken(true)`; Rules vẫn cần fallback get(users) cho user chưa refresh → tạm thời 2 đường. (5) Notes brand đối chiếu `get(/leads/$(leadId)).data.brand` = +1 read/ghi chú (rẻ). (6) Sửa `phone` đổi dedupe (chủ ý) + `phone_has_zalo` reset → Zalo ZBS gửi lại cần chú ý.",
  "implementation_sketch": "- Rules `leads` update: `isSuperAdmin()` full; admin cùng brand: affectedKeys ⊆ {name,phone,email,stage,stage_at,first_care_at,first_care_by,fu_at,fu_note,dropped,dropped_at,dropped_by,lost_reason,lost_at,views,views_by,lastViewAt,lastViewBy,assignee,assignee_uid,assignee_email,assigned_at,assigned_by,phone_edited_by,phone_edited_at,phone_ai,phone_has_zalo}; sales: tập trên trừ assignee_* (nhận lead qua transaction claimLead live.js:498-502 cần cho sales ghi assignee_* KHI resource.data.assignee_uid==null && request.resource.data.assignee_uid==uid). Cấm brand/score/temp/reply/detected_at bằng !hasAny.\n- Rules `notes` create: `request.resource.data.brand == get(/databases/$(db)/documents/leads/$(leadId)).data.brand`.\n- CF (~/firebase-s13) `onUserWrite` Firestore trigger users/{uid} → `auth.setCustomUserClaims(uid,{role,brand,active})`; live.js user listener (925) sau khi data đổi role/brand → `getIdToken(true)`.\n- FE 65-charts-lead-modal.js: nút ✏️ cạnh tên/pill SĐT → form 3 ô → `SL_FB.updateLead(id,{name,phone:normPhoneVN(x),email,phone_ai:l.phone_ai||l.phone,phone_edited_by,phone_edited_at,phone_has_zalo:deleteField})` (đưa `normPhoneVN` ra scope chung — đã cùng IIFE).\n- Test: emulator rules-unit-testing với 6 kịch bản (sales sửa brand, sales sửa lead người khác, admin sửa score, unassign, logLeadView, note sai brand)."
 },
 {
  "id": "P25",
  "exists": "partial",
  "exists_evidence": "Thu hồi phiên chỉ là cờ client: live.js:604-608 `revokeSession` set `users/{uid}.sessions.{sid}.revoked=true`; live.js:940-946 phiên bị revoke → signOut (client tự nguyện). Khoá tài khoản: `setUserActive` live.js:679 set active:false → listener :947 `stopData()` + màn pending — Firestore cache IndexedDB (persistentLocalCache live.js:173) KHÔNG xoá; grep `clearIndexedDb|terminate(` → 0. 2FA TOTP tự nguyện có sẵn (live.js:552-582 enroll/unenroll, 70-shell-tools.js:361-425) nhưng KHÔNG ép theo role (grep `mfaRequired` 0). Offboarding/bàn giao lead hàng loạt: grep `offboard|bàn giao` 0; `assignLead` từng lead (live.js:492). Auth Admin `disabled`/`revokeRefreshTokens`: không có CF nào theo CLAUDE.md.",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "5-7 ngày (CF adminUserAction+offboardUser 2-3, live.js purge cache 1, Rules 1, FE ép 2FA + modal 2)",
  "backend_needs": [
   "cf",
   "rules",
   "data-migration"
  ],
  "risks": "(1) `clearIndexedDbPersistence` yêu cầu `terminate(db)` trước + KHÔNG tab khác đang mở (multi-tab manager live.js:173) → ném failed-precondition → phải try/catch + fallback `location.reload()`; sau terminate, `db` chết → mọi SL_FB method phải re-init hoặc reload trang (đơn giản nhất: reload). (2) Rules `auth_time > tokensValidAfter` cần `get(users)` mỗi read lead — đã có get(users) cho role nên không thêm read, nhưng auth_time là lúc ĐĂNG NHẬP (không phải refresh token) → user đăng nhập lâu bị khoá read ngay sau revoke = đúng ý; user hợp lệ đăng nhập từ 30 ngày trước KHÔNG bị ảnh hưởng vì tokensValidAfter chỉ set khi revoke. (3) Batch offboard >500 lead chia lô; chuyển lead `assignee_uid==uid` cần index leads(assignee_uid) single-field (tự có). (4) Ép 2FA cho admin: chặn ở 80-rbac-auth.js là client-side — Rules không biết MFA → chỉ là policy mềm; Firebase Auth không có 'require MFA per role' server-side (Identity Platform tenant có nhưng đổi kiến trúc). (5) Ghi chú private của người nghỉ chuyển sang team = đổi `vis` hàng loạt trên collectionGroup notes where by_uid — index by_uid COLLECTION_GROUP đã tạo (sự cố 27/08).",
  "implementation_sketch": "- CF `adminUserAction(uid,action)` (asia-southeast1, verify super như fbaccounts.js): disable → `auth.updateUser(uid,{disabled:true})` + `auth.revokeRefreshTokens(uid)` + users/{uid}.{active:false,tokensValidAfter:now}; enable ngược lại; resetMfa → `auth.updateUser(uid,{multiFactor:{enrolledFactors:null}})`.\n- CF `offboardUser(uid,{toUid,notesMode})`: query leads where assignee_uid==uid && stage not in (closed) → batch 450/lô set assignee_* = người nhận + assigned_by + addDoc note 'Bàn giao từ A'; collectionGroup notes where by_uid==uid && vis=='private' → vis:'team' hoặc delete; brand_sales where uid → delete; ghi `audit_log`.\n- Rules `leads` read (nhánh non-super): thêm `&& request.auth.token.auth_time > (get(users).data.tokensValidAfter||0)/1000`.\n- live.js: listener users (925) khi `active===false` hoặc `tokensValidAfter > auth_time` → `stopData(); await terminate(db); try{await clearIndexedDbPersistence(db)}catch{}; signOut; location.reload()`. Thêm `SL_FB.adminUserAction`, `SL_FB.offboardUser` (fetch CF).\n- FE 85-users-admin.js: nút 'Khoá & bàn giao' → modal chọn người nhận (usersList cùng brand) + radio ghi chú riêng tư; 80-rbac-auth.js: nếu `config/app.security.mfaRequiredRoles` chứa role && !SL_FB.totpEnrolled() → gate 'Bật 2FA để tiếp tục' (tái dùng UI 70-shell-tools.js:361)."
 },
 {
  "id": "P26",
  "exists": "partial",
  "exists_evidence": "Nút 'Loại' đã có trên kanban cho MỌI lead (30-pipeline.js:273-279 `[data-pv-drop]` → {dropped,dropped_at,dropped_by}, optimistic + revert) + 'Loại ngay' hàng loạt cho junk (:244-246, chưa confirm); 'Đánh dấu đã xử lý' = stage:'closed' (65-charts-lead-modal.js:136,:153). Toast Hoàn tác 6s đã có cho chuyển stage (`pvToastUndo` 30-pipeline.js:62-72) nhưng KHÔNG dùng cho Loại. Feed filter 20-feed.js:451-457 KHÔNG lọc dropped/junk (chỉ temp/mine/query); pipeline lọc `!l.dropped` (:204,:290); tải sales loại dropped (20-feed.js:382,:589). grep `lost_reason|Không thành|Khôi phục|scan_feedback` → 0. Chip 'Đã loại' trên feed: không có.",
  "feasible": "yes",
  "realistic_effort": "S",
  "days_estimate": "2 ngày FE + 0.5 ngày Rules (nếu P24 chưa làm thì chỉ cần thêm lost_reason/lost_at vào whitelist)",
  "backend_needs": [
   "rules"
  ],
  "risks": "(1) Nếu Rules leads hiện KHÔNG whitelist thì `lost_reason/lost_at` ghi được ngay; nếu P24 làm trước phải thêm vào whitelist. (2) Feed ẩn dropped mặc định: bộ đếm Nóng/Ấm/Lạnh trên rail tính từ toàn bộ D.leads (20-feed.js:443 comment) → phải quyết đếm có hay không gồm dropped cho nhất quán, tránh 'đếm 12 nhưng thấy 9'. (3) Đường diff feed theo lead.id (90-boot reload): đổi filter dùng `_feedCapKey` reset về 200 (20-feed.js:458) — thêm khoá 'showDropped' vào capKey để không lệch cap; giữ vị trí cuộn cần lưu scrollTop trước render. (4) Khôi phục optimistic: revert `dropped=false` rồi updateDoc {dropped:false,lost_reason:deleteField()} — deleteField phải import qua SL_FB (live.js đã có deleteField). (5) 'Mất khách vì…' trên ROI dựa vào lead trong cửa sổ realtime 500 + leadsExtra — thống kê không đủ toàn bộ lịch sử (chấp nhận, ghi rõ 'trong kỳ').",
  "implementation_sketch": "- 30-pipeline.js: thay `[data-pv-drop]` → popover 6 lý do + ghi chú → `updateLead(id,{dropped:true,dropped_at,dropped_by,lost_reason,lost_note})` + `pvToastUndo('Đã đánh dấu Không thành', ()=>updateLead(id,{dropped:false,lost_reason:deleteField(),...}))`; 'Loại ngay' hàng loạt thêm `confirm()`.\n- 65-charts-lead-modal.js: `doneBtn` đổi thành 2 nút '✅ Đã chốt' (stage:'closed', hỏi deal_value nếu P34) và '✕ Không thành' (dùng chung popover).\n- 20-feed.js:451: `if(!showDropped && !roleIsSuper() && (l.dropped || l.temp==='junk')) return false;` + công tắc 'Hiện rác/đã loại (N)' cạnh filter; card dropped → chip xám 'Đã loại · bởi X · lý do' + nút 'Khôi phục'; capKey += '|'+showDropped.\n- live.js: thêm `SL_FB.restoreLead(id)` = updateDoc {dropped:false, dropped_at/by/lost_reason/lost_at: deleteField()} (để FE không cần import deleteField).\n- 40-roi.js / 10-core-overview.js: bảng 'Mất khách vì…' group by lost_reason × source từ D.leads.filter(dropped) trong kỳ.\n- Rules leads: whitelist thêm lost_reason, lost_note, lost_at (cùng P24). Feedback 'spam/sai ngành' → addDoc `scan_feedback` (Rules create cho active user, field {leadId,brand,reason,by}) — nuôi P37."
 },
 {
  "id": "P27",
  "exists": "partial",
  "exists_evidence": "ROI đã có chế độ 'Đơn giản' (40-roi.js:104,:213-219 `roiModeSegHtml`, :192-211 `roiSimpleBlock` = phí gói ÷ lead) lưu `brands.roi.mode` — nhưng vẫn hiện CPL benchmark/'tr₫'/bảng tham số, chưa có 'tiền thu về từ lead' (grep `deal_value` → 0). assistantChat: live.js:645-657 gửi `{messages}` thuần, KHÔNG có `context`; 90-boot.js:186-187 FAB gọi `assistantChat(hist.slice(-12))`; grep `Giải thích cho tôi|Hỏi AI về lead` → 0; rate-limit: understand.json ghi 'Rate-limit assistantChat' là đề xuất (chưa có); backend nạp `assistantLeadLimit` (live.js:156) lead vào prompt mỗi câu.",
  "feasible": "yes-with-backend",
  "realistic_effort": "S",
  "days_estimate": "2-3 ngày (FE 1.5, CF thêm context + rate-limit 0.5-1)",
  "backend_needs": [
   "cf"
  ],
  "risks": "(1) Không có `deal_value` → 'Tiền thu về' chỉ là ước tính `C.realized` (giá trị nền × tỉ lệ chốt) — PHẢI ghi nhãn 'ước tính' to; nếu không chủ shop hiểu là tiền thật. (2) AI diễn giải lại số → prompt phải cấm tính lại, chỉ 'đọc' JSON số đã tính (roiCompute) + luôn hiện số gốc cạnh. (3) Modal lead gửi context {bài, notes, outreach log} = dữ liệu cá nhân lead sang OpenAI — đã đang gửi lead vào assistantChat nên không mới, nhưng notes `vis=private` của người khác KHÔNG được đưa vào (lọc theo Rules đã áp ở client: D.notes chỉ chứa note mình thấy — OK). (4) Backend assistantChat nếu vẫn nạp ≤500 lead mỗi câu thì thêm context làm prompt phình → nên khi có `context.lead` thì bỏ nạp lead toàn bộ (tiết kiệm token). (5) Rate-limit 20/user/giờ ghi Firestore `ai_quota/{uid}__{giờ}` increment trong CF — 1 write/lượt, rẻ.",
  "implementation_sketch": "- 40-roi.js: mở rộng `roiSimpleBlock` thành 'Xem đơn giản' (mặc định simple khi role admin && matchMedia mobile): 3 thẻ (Thu về [ước tính] = C.realized+C.expected; Đã trả = R.fee; Lời/lỗ + x lần) + 1 câu kết luận sinh client; ẩn CPL benchmark/params; ẩn `R.fee` khi roleIsSales (đã có `roleCanEditConfig`). Nút 'Giải thích cho tôi' → `SL_FB.assistantChat([{role:'user',content:'Giải thích…'}],{kind:'roi',numbers:{...C,...R}})`.\n- 65-charts-lead-modal.js: nút 'Hỏi AI về lead này' + 3 chip → mở panel FAB (90-boot.js askAssistant) với context {lead:{text,need,intent,stage,reply}, notes:leadNotes(id).slice(0,5), outreach:D.outreachLog.filter(leadId)}; ≥5 notes tự gợi 'Tóm tắt & bước kế'.\n- live.js:645: `assistantChat(messages, context)` → body `{messages, context}`.\n- CF assistantChat (~/firebase-s13): nhận `context`; nếu có `context.lead` → không nạp danh sách lead; system prompt 'KHÔNG tính lại số, chỉ diễn giải'; rate-limit transaction `ai_quota/{uid}__{YYYYMMDDHH}` ≥20 → 429 {error:'rate_limited'}; FE toast 'Đã hết 20 lượt/giờ'.\n- Không cần Rules/index (ai_quota chỉ Admin SDK)."
 },
 {
  "id": "P28",
  "exists": "partial",
  "exists_evidence": "Đã có mảnh: worker heartbeat `workers/{workerId}` 30s (worker.mjs:70-90, goOffline :917) + FE panel VPS tính isOnline<2' + timer 60s hạ 🔴 (v119-38, live.js lazyOutreach) — nhưng chỉ HIỂN THỊ khi mở tab Tiếp cận, không báo chủ động; `system_alerts` chỉ 1 doc `ekyc_credit` (live.js:842) hiện ở Overview; `fb_accounts.needLogin/challenge` badge (45-outreach.js:246) thụ động; BrightData budget/dự phóng ở widget super (10-core-overview.js:551) client-side; `gcpUsage` CF đọc BigQuery (live.js:634); `scans` realtime 1000 doc (live.js:827) → có thể suy 'không có bản ghi mới >30'' nhưng chỉ trên client. grep `healthTick|daily_stats|uptime|Sức khoẻ hệ thống` (live.js, src/app, CLAUDE.md) → 0. Telegram/Zalo có ở backend (.env token, 50-config-views.js:345; cụm zalo-fn notifyBrandZalo) nhưng chưa có CF cảnh báo hạ tầng. Cloud Monitoring alert policy: không có ghi nhận trong CLAUDE.md.",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "5-8 ngày (Monitoring policy 0.5, healthTick 2, brand health 8h 2, FE dải sức khoẻ 1.5, cooldown/nghiệm thu 1)",
  "backend_needs": [
   "cf",
   "external-api",
   "rules",
   "index"
  ],
  "risks": "(1) Ồn: healthTick 10' × nhiều điều kiện → phải có cooldown per (loại,đối tượng) trong `system_alerts/{key}` (lastNotifiedAt ≥ 1h) + gộp 1 tin/giờ; nick needLogin có thể lặp nhiều lần/ngày. (2) Firestore read: healthTick đọc workers (ít), fb_accounts where needLogin==true|challenge!=null (single-field, có), outreach_tasks where taskStatus=='failed' (single-field có), scans limit 1 orderBy at desc — tổng <100 read/tick, 144 tick/ngày ≈ 15k read/ngày = rẻ; bản tin 8h đọc leads theo brand có thể nặng ở nhiều brand → dựa vào counter `daily_stats` (P35, CHƯA có) hoặc tạm dùng `outreach_stats` + count() aggregation query (1 read/1000 doc). (3) Telegram cho Super Admin qua bot backend — chat_id cấu hình .env, KHÔNG vào FE (đúng CLAUDE.md). (4) Ngưỡng brand 'nóng chưa chăm >60'' cần query leads where brand & temp=='hot' & first_care_at==null → composite index leads(brand, temp, first_care_at) mới. (5) Alert 'scheduler chết im' phải đặt ở Cloud Monitoring (ngoài Firestore) vì healthTick cũng là scheduler — nếu cả cụm scheduler chết thì healthTick chết theo; dùng metric `cloudfunctions.googleapis.com/function/execution_count` absent 30' cho outreachTick. (6) FE Overview thêm dải chip → repaint overview đã là full innerHTML theo config snapshot; thêm 1 onSnapshot `system_alerts` (collection thay vì 1 doc) cho super — cần Rules read system_alerts=isSuperAdmin (brand chips: doc `brand_health/{code}` read brand==myBrand).",
  "implementation_sketch": "- Cloud Monitoring (gcloud, LỆNH Cloud Shell): alert policy execution_count absent 30' cho outreachTick/scheduledScan; error ratio >5% cho funcWebhook; uptime check https://smartlead.z15miracle.com.vn; notification channel email + webhook Telegram.\n- CF `healthTick` onSchedule('every 10 minutes', asia-southeast1): checks → workers.lastSeen>5' (worker.mjs heartbeat 30s) · fb_accounts needLogin/challenge mới (challengeAt > lastRun) · outreach_tasks taskStatus=='failed'&&active==false đếm tăng · bd_month ≥80% (đã có sổ) · scans mới nhất >30' khi config.app.autoScanEnabled · gcpUsage ngưỡng → upsert `system_alerts/{kind__target}` {sev, msg, firstAt, lastAt, count, notifiedAt} + gửi Telegram (bot .env) nếu notifiedAt>1h; auto-resolve khi hết điều kiện. Cờ riêng `workers.lastError` từ worker khi adspowerStart lỗi (worker.mjs:178) để phân biệt lỗi hạ tầng ≠ lỗi task.\n- CF `brandHealthDaily` onSchedule('0 8 * * *' VN): mỗi brand tính điểm từ scans/leads (count() aggregation), sources.lastLeadAt, fb_accounts brand, users.lastLogin (users có sessions.*.at), brands.billing/plan → `brand_health/{code}` {score, color, issues[]} + 1 tin Telegram tổng cho AM.\n- live.js: super subscribe `collection system_alerts` (thay doc ekyc_credit đơn — giữ tương thích) + `brand_health/{byBrand}` cho user thường → D.sysAlerts/D.brandHealth; Rules: system_alerts read=isSuperAdmin, brand_health read = super || brand==myBrand, write=false.\n- 10-core-overview.js: dải 'Sức khoẻ hệ thống' (super, danh sách alert theo sev) / chip xanh-vàng-đỏ + 'cần làm ngay' (owner) — render trong rebuild(['overview']).\n- Index: leads(brand ASC, temp ASC, first_care_at ASC) nếu làm check 'nóng chưa chăm'."
 },
 {
  "id": "P29",
  "exists": "none",
  "exists_evidence": "grep 'brand_templates|templates|Nhân bản|clone' src/app + live.js → 0 (chỉ có comment 'clone' ở live.js:87). Keyword toàn cục vẫn thêm bằng prompt() từng từ: 50-config-views.js:166-167. Brand có sẵn field ai/roi/billing/plan/logo/leadWindow + setter riêng lẻ: live.js:693-706 (setBrand/setBrandRoi/setBrandAi/setBrandPlan/setBrandLogo/setBrandLeadWindow), setBrandCrm live.js:488. brands/{code}.outreach.matrix/caps đã có (45-outreach.js:222). Keyword theo brand CHƯA có — chỉ config/app toàn cục (live.js:121-122, 486) + keyword riêng theo nguồn (sources).",
  "feasible": "yes",
  "realistic_effort": "S",
  "days_estimate": "2 ngày (FE) — 1 ngày nếu chỉ làm textarea dán hàng loạt + Nhân bản brand, thêm 1 ngày soạn nội dung 6 bộ mẫu ngành",
  "backend_needs": [
   "rules"
  ],
  "risks": "(1) Lệch model: 'cfg.keywords/exclude' hiện là TOÀN CỤC config/app, không có brand.keywords → 'merge vào keyword brand' phải chọn 1 trong 2: ghi vào config/app (ảnh hưởng MỌI brand — agency nhiều ngành trộn keyword spa vào F&B = lead rác tăng) hoặc ghi vào keyword riêng của từng nguồn của brand (setSource merge, đúng ngữ nghĩa hơn). Nếu muốn keyword theo brand thật thì backend scan phải đọc thêm field đó = ngoài phạm vi S. (2) Clone copy nhầm outreach.matrix 'Mạnh' + caps cao sang brand nick mới → bay nick; đề xuất đúng: ép reset caps/matrix về preset Cân bằng. (3) Rules: brands write=isSuperAdmin (CLAUDE.md) → chỉ Super làm được, đúng persona đề xuất; doc template mới cần Rules read cho super (hoặc để trong config/app để khỏi thêm match). (4) Group gợi ý chuyển private / lệch vùng — chỉ là nội dung, không phải bug code. Không đụng đường realtime/diff.",
  "implementation_sketch": "- Data: 1 doc `config/industry_templates` (map ngành→{keywords[],exclude[],groups[],msgs[],ai{},roi{},sla,matrix,caps}) — dùng lại setConfig-style setDoc merge; Rules thêm match config/industry_templates read=isSuperAdmin, write=isSuperAdmin (hoặc nhét vào config/app.templates để khỏi sửa Rules).\n- 50-config-views.js: thay 2 prompt() (dòng 166-167) bằng modal textarea dán nhiều dòng/phẩy + dedupe fold() + dropdown 'Áp bộ mẫu ngành' → merge vào main/ex rồi persist() (config/app) HOẶC vào keywords riêng của nguồn trong openSourceForm (khuyến nghị cho agency đa ngành).\n- 85-users-admin.js: bảng brand thêm nút 'Nhân bản từ…' cạnh 'Hồ sơ AI' (dòng 149) → modal chọn brand nguồn / template → 1 call SL_FB.cloneBrand(code,{ai,roi,crm,outreach:{matrix,caps: PRESET_BALANCED}}) = setDoc merge (thêm method live.js cạnh setBrandCrm:488); KHÔNG copy plan/billing/logo/leadFrom/leadTo.\n- 45-outreach.js: export OA_PRESETS (Cân bằng) ra scope chung để clone tái dùng.\n- Wizard P04 gọi cùng modal; smoke thêm check mở modal + dedupe."
 },
 {
  "id": "P30",
  "exists": "partial",
  "exists_evidence": "Engine: khung giờ 8–22h VN + van/nick/ngày + brands.outreach.on đã có ở outreachTick (CLAUDE.md:175, 218). Worker: grep 'getHours|outreach.on|paused|hours' worker.mjs → 0; claimBatch (worker.mjs:860-889) chỉ lọc workerId/status/myNicks, runNick (778) mở nick ngay — KHÔNG đọc lại fb_accounts.active/brands.outreach.on/giờ. Tự tạm dừng nick khi challenge/needLogin lặp đã có (worker.mjs:168, 558). Reaper requeue running>15' (838). FE 45-outreach.js: toggle từng brand (501) + từng nick (271), grep 'Dừng tất cả|pause' → 0. system_alerts đã có 1 doc ekyc_credit (live.js:842) — pattern tái dùng được. Circuit breaker: grep → 0.",
  "feasible": "yes-with-backend",
  "realistic_effort": "S",
  "days_estimate": "3 ngày: worker gate 0.5 · FE nút Dừng tất cả + lịch giờ 1 · CF huỷ task + circuit breaker (mức cảnh báo) 1 · Rules/deploy 0.5",
  "backend_needs": [
   "worker",
   "cf",
   "rules"
  ],
  "risks": "(1) Circuit breaker ngưỡng sai → dừng oan cả hệ khi 1 nick yếu rớt liên tục; đếm outreach_log 60' = read thêm mỗi tick 5' (nhỏ, ~12 query/giờ) — bắt đầu chỉ ghi system_alerts + giảm cap, KHÔNG tự paused tuần đầu. (2) Worker gate đọc thêm 2 doc/nick/phiên (fb_accounts + brands) = rẻ; nhưng trả task về queued KHÔNG tăng tries → task nick tắt lặp vô hạn trong hàng đợi → phải kèm điều kiện: nick inactive/brand off → set task 'cancelled' (không requeue), chỉ ngoài giờ mới requeue. (3) 'Dừng tất cả' hàng loạt setOutreach nhiều brand = N setDoc; task queued đã reserve quota trong outreach_usage → huỷ phải refund (worker đã có refundQuota pattern, dead-letter) không thì nick 'cạn ngày' oan. (4) Rules: config/public.outreach.paused cần match mới; worker/CF dùng Admin SDK bỏ qua Rules. (5) Không đụng đường realtime feed; oaSignature phải thêm trường paused để repaint đúng.",
  "implementation_sketch": "- worker.mjs runNick (778): trước adspowerStart đọc `fb_accounts/{pid}` (active, needLogin) + `brands/{brandCode}` (outreach.on, outreach.hours) + `config/public` (outreach.paused); giờ VN = Date.now()+7h; không đủ điều kiện → nick tắt/brand tắt/paused ⇒ markTask 'cancelled' + refundQuota; ngoài giờ ⇒ set queued + nextAt (không tăng tries). reapStale bỏ qua task ngoài giờ.\n- outreach.js (LỆNH Cloud Shell): tick đầu đọc config/public.outreach.paused → return sớm; thêm `breaker()`: đếm outreach_log where at>=now-60' status in [fail,challenge] vs done → tỉ lệ >N% ⇒ set system_alerts/outreach_breaker {status,ratio,at} + capFactor 0.5 (tryConsume nhân); tuần 1 chỉ cảnh báo. CF callable `cancelQueuedTasks({brand?})` (verify super/admin) batch set outreach_tasks queued→cancelled + refund.\n- live.js: SL_FB.pauseAllOutreach(brands[]) = writeBatch setDoc brands.outreach.on=false + config/public.outreach.paused; subscribe system_alerts/outreach_breaker trong lazyOutreach → D.outreachBreaker; oaSignature thêm paused/breaker.\n- 45-outreach.js: nút '⏸ Dừng tất cả' ghim đầu view (super) + confirm + banner đỏ khi paused; thẻ brand hiện chip giờ (outreach.hours mặc định 8–22) + editor trong modal ⚙️ (oaOpenBrandCfg).\n- Rules: match config/public read=isActiveUser, write=isSuperAdmin; system_alerts read đã có (kiểm lại match)."
 },
 {
  "id": "P31",
  "exists": "partial",
  "exists_evidence": "Hotline cứng: app.html:70-78 (sidebar) + 133-140 (floating); <title> app.html:8; 'SmartLead by Z15 Miracle' app.html:40, 80-rbac-auth.js:98; hotline trong lỗi trợ lý 90-boot.js:195 + ROI 40-roi.js:302; mascot Cloudinary 90-boot.js:104,136; logo-mark login 80-rbac-auth.js:96,148. Đã có: brand logo (setBrandLogo live.js:701, brandBlock 80-rbac-auth.js:277 dùng MB.logo), signature-guard APP_SHOWN_SIG (80-rbac-auth.js:84-86), ticker config.announcementTicker TOÀN CỤC (70-shell-tools.js:511-530, live.js:153) — chưa có phạm vi brand. grep 'support|whitelabel|config/public' → 0.",
  "feasible": "yes",
  "realistic_effort": "S",
  "days_estimate": "2 ngày (FE + Rules 1 dòng)",
  "backend_needs": [
   "rules"
  ],
  "risks": "(1) User chưa gán brand (CURRENT_BRAND='') hoặc brand chưa cấu hình → fallback mặc định agency (config/app.whitelabel) — nếu quên fallback thì sidebar mất hotline. (2) Ticker: đổi mặc định sang TẮT cho brand chưa cấu hình = thay đổi hành vi hiện tại (thông điệp Z15 đang hiện mọi brand) — đúng ý đề xuất nhưng phải báo anh. (3) Cần đọc doc brands/{code} cho user thường: hiện Rules brands read cho isActiveUser? (CLAUDE.md chỉ nói write=isSuperAdmin; brandBlock đã đọc MB = brand mình nên read đã mở) — an toàn. (4) Regression nhỏ: <title>/hotline đổi runtime sau khi SLAuth.show, phải đặt trong show() sau khi có user.brand, và app.html vẫn giữ giá trị cứng làm fallback tới lúc JS chạy (flash 1 nhịp). (5) Mascot theo brand = URL ngoài → escUrl + CSP img-src cần cho phép domain (hiện Cloudinary) — chỉ cho phép data: hoặc Cloudinary. Không đụng realtime feed.",
  "implementation_sketch": "- Data: `brands/{code}.support {name,hotline,zalo,mascot?}` + `brands/{code}.ticker {enabled,text,theme...}`; mặc định toàn hệ ở `config/app.whitelabel` (setConfig sẵn có, không cần Rules mới) — nếu muốn doc riêng config/public thì thêm match read=isActiveUser.\n- live.js: buildData trả `whitelabel: cfg.whitelabel||null`; brand doc đã có trong MB (brandBlock) → thêm SL_FB.setBrandSupport(code,patch)/setBrandTicker cạnh setBrandLogo:701.\n- 80-rbac-auth.js SLAuth.show (sau dòng 77): hàm applyWhitelabel(MB, D.whitelabel) — set document.title, .sidebar-hotline/.floating-hotline href+number (querySelector app.html), .brand-name small, hotline trong lỗi trợ lý (90-boot.js:195 đọc biến global WL.hotline), 40-roi.js:302 cùng biến; login gate dùng logo mặc định (chưa biết brand trước login — custom domain sau).\n- 70-shell-tools.js getTicker (524): ưu tiên MB.ticker (nếu có) → config.announcementTicker chỉ khi scope==='all' → không có ⇒ TẮT; editor thêm radio 'Toàn hệ thống / Brand X'.\n- 85-users-admin.js: nút 'Hỗ trợ/White-label' cạnh Logo (dòng 149) mở modal hotline/zalo/tên/mascot.\n- Rules: brands write vẫn isSuperAdmin (OK)."
 },
 {
  "id": "P32",
  "exists": "partial",
  "exists_evidence": "View Tích hợp tĩnh: 50-config-views.js:411-436 (nút 'Cấu hình →' không handler, D.integrations luôn từ SL_DATA demo live.js:138). Kênh: cfg.channels toggle boolean 5 kênh (live.js:46-47,126; 50-config-views.js:346-363) không có ô token/Sheet URL/Gửi thử. Backend: syncLeadToSheet/backfillSheet/syncDraftToSheet ở ~/codebase2 (CLAUDE.md:75-82) — 1 Sheet toàn cục, backfillSheet khoá theo email Super; Zalo ZBS ở ~/smartlead-zalo-fn. Xuất CSV BOM đã có cho bảng tương tác (85-users-admin.js:263,343-361) — CHƯA có xuất lead theo brand/khoảng ngày; grep 'exportLeads' → 0. KiotViet/Sapo/webhook n8n: 0.",
  "feasible": "yes-with-backend",
  "realistic_effort": "L",
  "days_estimate": "15–20 ngày: Xuất CSV client + audit 2 · Sheet theo brand (gom về s13 + OAuth/Service Account + sheetId per brand) 4 · Webhook + Gửi thử + log 3 · Zalo OA (P09) 3 · KiotViet API 4–5 · CF exportLeads 20k + Storage link 2",
  "backend_needs": [
   "cf",
   "secret",
   "external-api",
   "rules",
   "index",
   "data-migration"
  ],
  "risks": "(1) PII: xuất SĐT hàng loạt là rủi ro lớn nhất (Luật BVDLCN 2026) — chỉ owner/admin, che số cho sales, ghi audit `export_log` mỗi lần; CSV client-side chỉ có tối đa cửa sổ 500+leadsExtra (live.js) nên >4k phải qua CF exportLeads Admin SDK (đọc 20k doc = ~$0.01/lần, OK) + Storage signed URL 1h. (2) Sheet sync nằm ở codebase2 snapshot CŨ trùng tên function với s13 (CLAUDE.md:77) → phải gom về s13 trước, deploy `--only functions:tên` tránh đè; mỗi brand 1 sheetId đòi CF đọc brands.integrations → thêm read/trigger. (3) KiotViet/Sapo: cần đăng ký đối tác/API key khách, rate limit, mapping SĐT trùng nhiều khách; đọc ngược trạng thái mua = cron thêm chi phí. (4) Secrets ở Secret Manager (pattern func-token đã có). (5) Firestore: query lead theo brand+khoảng ngày cần composite index leads(brand, detected_at) — CLAUDE.md nói ĐÃ có. (6) Regression: thay views.integrations không đụng feed/diff; nhưng đổi channels từ boolean sang object phải giữ tương thích buildData (live.js:126).",
  "implementation_sketch": "- Ưu tiên theo đề xuất: Xuất CSV → Sheet theo brand → Webhook → Zalo OA → KiotViet.\n- FE 50-config-views.js: views.integrations viết lại thành card có trạng thái đọc `D.integrationsStatus` (subscribe `integrations/{brand}__{kind}` trong refreshAdmin/lazy) + nút Kết nối mở modal (sheetId / webhook URL+secret / API key) → SL_FB.saveIntegration(kind,payload) = HTTP CF `saveIntegration` (pattern saveFbAccount: Bearer idToken, secret vào Secret Manager `int-<brand>-<kind>`, metadata Firestore không chứa secret) + nút 'Gửi thử' → CF `testIntegration`.\n- Tab Báo cáo (50-config-views.js views.reports): khối 'Xuất' (brand/khoảng ngày/cột) → ≤4.000 lead: dựng CSV BOM client (tái dùng 85-users-admin.js:343-361, che SĐT theo CURRENT_ROLE) ; lớn hơn: SL_FB.exportLeads(params) → CF Admin SDK query leads where brand & detected_at range limit 20k → Storage private → signed URL 1h; cả 2 nhánh ghi `export_log` {by,brand,range,n,at}.\n- Backend s13: CF onCreate/onUpdate leads → đọc brands.integrations.sheetId → append Sheet (service account share sheet) ; webhook POST JSON HMAC + lưu 20 log gần nhất `integrations/{id}/log`; KiotViet: lead stage responded/booked có phone → upsert customer + tag; cron 6h đọc đơn để set deal (nối P34).\n- Rules: integrations/{id} read/write=isSuperAdmin (hoặc admin brand mình), export_log write=isActiveUser create-only, read=isSuperAdmin."
 },
 {
  "id": "P33",
  "exists": "none",
  "exists_evidence": "grep 'Báo lỗi|impersonat|Xem như|html2canvas|tickets|supportTicket|Zalo hỗ trợ' src/app + live.js + app.html → 0. Có sẵn nguyên liệu: startData(role,brand,userWin) live.js:743 (nhận role/brand/khung riêng), CURRENT_ROLE/BRAND 80-rbac-auth.js:4-8,76-78, mọi write đi qua guard() trong SL_FB (live.js:423,486…), views_by audit xem lead live.js:435, fbDown banner 90-boot. Telegram đã có ở backend (CLAUDE.md channels). Brief 594 cũng đề xuất impersonate.",
  "feasible": "yes-with-backend",
  "realistic_effort": "S",
  "days_estimate": "3–4 ngày: Xem như 1.5 · Báo lỗi + CF supportTicket + Telegram 1.5 · link Zalo hỗ trợ/help 0.5",
  "backend_needs": [
   "cf",
   "rules",
   "secret"
  ],
  "risks": "(1) Impersonate quên chặn write → note/stage ghi với actor thật (Super) nhưng UI tưởng là user X; phải chặn ở guard() live.js (1 điểm) chứ không rải từng handler; ngoài ra 20-feed/30-pipeline có optimistic-update trước khi gọi SL_FB → cần guard ném lỗi để revert (các handler đã có nhánh revert). (2) Rules chạy theo tài khoản Super thật → chỉ mô phỏng khung/brand, KHÔNG tái hiện permission-denied của user (đúng như đề xuất thừa nhận). (3) Đổi phiên dữ liệu qua stopData/startData: dataEpoch đã có (v119-19) chống lead brand cũ tiêm phiên mới; phải reset leadsExtra/notesSubbed → dùng đúng đường stopData sẵn có, không tự viết. (4) Ticket ảnh chụp chứa SĐT khách → Storage private + TTL 30 ngày + chỉ Super đọc; html2canvas từ cdnjs phải khớp CSP script-src (hiện cho gstatic; cần thêm cdnjs vào netlify.toml CSP). (5) Chi phí: CF nhỏ.",
  "implementation_sketch": "- 85-users-admin.js bảng user (dòng 35): nút 'Xem như' (chỉ superadmin, owner agency chỉ user brand mình) → SLAuth.impersonate(u): lưu snapshot {role,brand,brandName}, set CURRENT_ROLE/BRAND/BRAND_NAME + window.IMPERSONATING={uid,email}, gọi window.SL_FB.stopData(); startData(u.role,u.brand,{from:u.leadFromAt,to:u.leadToAt}); go('overview'); banner đỏ cố định 'Đang xem như X · Thoát'.\n- live.js: biến `impersonating`; guard(p) nếu impersonating → reject Error('impersonate-readonly') trước khi tạo promise (đặt kiểm tra ở đầu mỗi method hoặc bọc guard); expose SL_FB.setImpersonate(flag). Ghi audit `audit_log` {actor,target,at,action:'impersonate'} khi vào/thoát.\n- Báo lỗi: nút ở footer sidebar/help (app.html) → modal mô tả + tự đính {brand,role,view:location.hash,version:hash ?v=, fbDown, UA} + html2canvas(document.body) → dataURL → SL_FB.sendTicket() HTTP CF `supportTicket` (verifyIdToken, ghi tickets/{id}, ảnh Storage `tickets/<id>.png` TTL lifecycle 30d, Telegram bot Z15 qua secret sẵn có). Super: tab Người dùng thêm bảng tickets (lazy get).\n- Link nhóm Zalo hỗ trợ theo gói: đọc brands.plan.tier → map URL ở config/app.supportGroups (setConfig).\n- Rules: tickets create=isActiveUser (chỉ field whitelist), read=isSuperAdmin; audit_log write=false (CF) hoặc create-only."
 },
 {
  "id": "P34",
  "exists": "none",
  "exists_evidence": "grep 'deal_value|revenue|closed_at|realized' src/app: chỉ có ROI ước tính realized = Σ roiBaseOf(l) hoặc closedN×AOV (40-roi.js:64), không có field giá trị đơn. CSS .pv2-kval đã có (pipeline-v2.css:42-43) nhưng 30-pipeline.js header cột chỉ render count (dòng 34, class .c) — không render kval. pvApplyStage 30-pipeline.js:176-197 ghi {stage,stage_at,first_care_*}; nút 'doneBtn' lead modal 65-charts-lead-modal.js:153 set stage:'closed' không hỏi giá trị. roiParamsFor/roiBudgetHint/ROI_STAGE_P sẵn (40-roi.js:18,26,27). Comment 65:144 'rules cho phép stage/assignee' → Rules leads update có whitelist field (CLAUDE.md:399 chưa kiểm chính xác).",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "4–5 ngày: bottom-sheet + ghi field 1.5 · tổng cột kanban + trọng số 1 · Overview/ROI realized 1 · Rules + kiểm whitelist 0.5 · test Playwright (kéo thẻ, revert) 1",
  "backend_needs": [
   "rules"
  ],
  "risks": "(1) Rules leads update whitelist: nếu whitelist field (rất có thể) thì updateDoc kèm deal_value/closed_at/deal_by bị permission-denied → phải LỆNH Cloud Shell mở 3 field (cho admin/sales brand mình) trước khi ship zip; test bằng tài khoản sales thật. (2) Regression pipeline realtime: pvApplyStage optimistic + revert — nếu bottom-sheet chặn trước khi ghi thì thẻ đã chuyển cột (pvRepaint) rồi user 'Nhập sau' → vẫn ghi stage; nếu user huỷ hẳn phải revert đúng như nhánh catch (stage/stage_at/first_care). Snapshot realtime giữa lúc sheet mở → SLApp.reload có thể vẽ lại kanban (deferReload chỉ hoãn khi input focus — ô số trong sheet focus thì hoãn được, OK). (3) ROI thay realized bằng Σ deal_value → lead chốt cũ không có deal_value → phải fallback roiBaseOf để không tụt số đột ngột; ROI vẫn tính trên cửa sổ 500 lead (brief 239) → tổng 'Đã thu' undercount ở brand lớn — đúng nền cho P35 counter. (4) Sales nhập ảo/nhập 0 → cho 'Nhập sau' + nhắc trên Hôm nay; deal_value ép Number trước khi vào sink HTML (bài học ${l.score}). (5) buildData whitelist (CLAUDE.md:403) — field mới phải thêm vào whitelist buildData không thì 'rơi khỏi rebuild' (bug đã dính 2 lần).",
  "implementation_sketch": "- 30-pipeline.js pvApplyStage (176): khi stage==='closed' → sau optimistic move mở `pvDealSheet(lead)` (bottom-sheet .pv2-sheet): chip AOV ngành (roiAovOf/roiParamsFor(brandDoc)), ngân sách khách (roiBudgetHint), ô nhập tay (format nghìn), 'Nhập sau' → patch thêm {deal_value:Number, closed_at:Date.now(), deal_by:CURRENT_USER.email} rồi mới updateLead; hoàn tác (pvToastUndo) xoá 3 field bằng deleteField (thêm SL_FB.updateLead hỗ trợ deleteField hoặc method clearDeal). 65-charts-lead-modal.js doneBtn (153) gọi cùng sheet.\n- Header cột kanban (30-pipeline.js:34): thêm `<span class=\"pv2-kval\">${fmtVnd(Σ)}</span>` — closed = Σ deal_value (fallback roiBaseOf), cột khác = Σ roiBaseOf×ROI_STAGE_P[key]/100 (trọng số); pvRepaint nhẹ đã giữ scroll.\n- 40-roi.js roiCompute (64): realized = Σ(l.deal_value ?? roiBaseOf(l,R)); thêm 'Đã thu từ lead SmartLead' + 'Doanh thu dự kiến tháng' (closed_at trong tháng VN) → widget Overview 10-core-overview.js (roleIsSuper false vẫn thấy) + card Hôm nay nhắc lead closed thiếu deal_value (mở sheet).\n- live.js buildData whitelist lead thêm deal_value/closed_at/deal_by; ép Number.\n- Rules (LỆNH): leads update affectedKeys hasOnly thêm ['deal_value','closed_at','deal_by'] + kiểm deal_value is number ≥0."
 },
 {
  "id": "P35",
  "exists": "partial",
  "exists_evidence": "Counter server-side pattern đã chứng minh: outreach_stats/{brand}__{ngàyVN} FieldValue.increment (CLAUDE.md v119-34; live.js:817-818 subscribe; 45-outreach.js:81) — nhưng chỉ cho automation. grep 'daily_stats' → 0. Bảng so sánh brand chỉ có ở ROI (40-roi.js:315-330 roiSuperView tính từ D.leads cửa sổ ≤500) và bd_month chi phí (live.js:828-835). Overview chỉ rẽ nhánh roleIsSuper (20-feed.js:108-112), KPI tính trên cửa sổ lead (brief 48, 311). Vai trò hiện chỉ superadmin/admin/sales — KHÔNG có role 'owner' (80-rbac-auth.js:12-15) → 'owner' = admin brand hoặc super.",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "6–8 ngày: CF trigger + logic delta 2 · backfill 30 ngày + job đối soát đêm 1.5 · Rules/index 0.5 · FE bảng brand + drill-down + subscribe 2.5 · test 1",
  "backend_needs": [
   "cf",
   "rules",
   "index",
   "data-migration"
  ],
  "risks": "(1) Trigger onWrite leads: mỗi lead update (stage, note count, views_by increment 1 lượt/30' live.js:435!) đều bắn trigger → views_by tăng liên tục = invocation dư thừa → phải lọc trong CF (chỉ đổi stage/temp/first_care_at/dropped/deal_value) và bỏ qua khi before/after cùng giá trị; ước tính chi phí CF ~vài nghìn invoc/ngày = rẻ nhưng cần đo. (2) Counter tăng dần không sửa quá khứ khi xoá lead/đổi brand lead → job đối soát đêm (đọc leads theo ngày → set lại doc) = read theo ngày, chấp nhận. (3) untouched15m/60m và Σ first-care-min là chỉ số theo THỜI GIAN chứ không theo event → phải tính ở job định kỳ (không trigger được), first_care_at là giờ client (brief 113) → SLA sai khi máy lệch giờ. (4) Backfill 30 ngày × N brand: đọc toàn bộ lead 30 ngày 1 lần (OK). (5) FE: đọc ≤31 doc/brand/tháng × 20–2000 brand — super agency lớn phải query where day>=… limit; nhiều brand → composite index daily_stats(brandCode, day). Rules read theo brandCode==myBrand hoặc super (copy block outreach_stats CLAUDE.md LỆNH Q). (6) Regression: Overview super đổi thành bảng brand = thay view hiện hữu (autoScanWidget, ngân sách BrightData…) → nên THÊM khối bảng phía trên chứ không bỏ widget cũ; oaSignature-style guard để không chớp khi 31 doc × N brand snapshot.",
  "implementation_sketch": "- CF s13 `leadStatsOnWrite` onDocumentWritten leads/{id} (asia-southeast1): tính delta before/after cho new(create theo detected_at ngày VN)/hot|warm|cold/contacted(first_care_at set)/responded/booked/closed/dropped + Σ first-care-min → `daily_stats/{brand}__{YYYY-MM-DD}` FieldValue.increment (đúng pattern addLog bump v119-34); bỏ qua write chỉ đổi views_by/views/notes.\n- CF onSchedule 02:00 VN `reconcileDailyStats`: đọc leads hôm qua theo brand → set lại doc (sửa số xoá/đổi brand) + tính untouched15m/60m; 1 lần backfill 30 ngày (LỆNH node script Admin SDK).\n- Rules: match daily_stats/{id} read = isSuperAdmin() || (isActiveUser() && resource.data.brandCode==myBrand()), write=false. Index composite daily_stats(brandCode asc, day asc).\n- live.js: subscribe `daily_stats` trong startData (super: where day>=đầu tháng trước; brand: where brandCode==byBrand) → D.dailyStats; thêm vào rebuild(['overview','reports']) với signature-guard.\n- 20-feed.js views.overview (108) nhánh roleIsSuper: thêm khối 'So sánh brand' trên cùng: bảng lead 7/30 ngày, %nóng, tỉ lệ chốt, SLA%, TG phản hồi TB, automation rep (từ outreach_stats), chi phí/lead (bd_month theo nguồn→brand), ▲▼ so kỳ trước, cờ đỏ 2 tuần tụt; click → set brand filter + go('feed'/'roi'). Admin brand: KPI Overview + Báo cáo đổi nguồn sang daily_stats (hết undercount cửa sổ 500). Nền cho P02/P28/P34 revenue (thêm Σ deal_value vào counter)."
 },
 {
  "id": "P36",
  "exists": "none",
  "exists_evidence": "worker.mjs:12-13 import firebase-admin; :38-40 `readJson('serviceAccount.json')` → `initializeApp({credential:cert(sa)})` — worker chạy Admin SDK bỏ qua Rules. grep `workerPair|workerToken|createCustomToken|custom token` trên worker.mjs + live.js + src/app + CLAUDE.md → 0 kết quả. CLAUDE.md 'LỆNH L': SA `outreach-worker@` role `roles/datastore.user` + key tải về VPS. Brief automation-backend ghi đúng lỗi [high/security] này + ý tưởng [v3/M] 'Worker gateway + pairing code' (chưa làm). Worker chỉ đụng 9 collection: fb_accounts(6 chỗ), outreach_log(4), outreach_threads(4), outreach_tasks(3), outreach_map(2), workers(2), outreach_stats/usage/worker_config(1) — KHÔNG đụng `leads` (grep `'leads'` worker.mjs = 0) → phạm vi Rules cho worker hẹp hơn đề xuất (không cần cho worker update leads).",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "5–7 ngày (CF 1 ngày, Rules 1 ngày, đổi SDK worker 2 ngày, FE 0.5, migrate + nghiệm thu đa VPS 1–2 ngày)",
  "backend_needs": [
   "cf",
   "rules",
   "worker",
   "secret"
  ],
  "risks": "(1) Client SDK Node: `runTransaction` client có ngữ nghĩa optimistic khác Admin (retry khi contention) → claimBatch/reapStale (worker.mjs:836-891) cần test lại chống chạy trùng; `FieldValue.increment/arrayUnion/serverTimestamp` đều có ở client SDK nhưng import khác (`firebase/firestore`). (2) Custom token hết hạn 1h → phải refresh TRƯỚC khi claim + onSnapshot watchMyNicks phải tự nối lại sau đổi token (client SDK tự refresh idToken nếu dùng signInWithCustomToken, nhưng custom token gốc chỉ 1h — cần CF `workerToken` cấp lại khi user record bị revoke). (3) Rules cho `worker==true` phải whitelist field-level trên fb_accounts (`diff().affectedKeys().hasOnly([needLogin,challenge,challengeSev,challengeCount,challengeAt,active,nextFreeAt,needLoginCount,updatedAt])`) — nếu để mở thì VPS bị chiếm vẫn đổi được brand/workerId nick. (4) Read cost: Rules có `get(worker_tokens/{workerId})` mỗi request → +1 read/op; worker ~vài trăm op/ngày/VPS → không đáng kể. (5) Regression: Rules `outreach_log/outreach_stats/workers` hiện read=super/brand, write=false — mở write cho worker claim phải giữ write=false cho user thường. (6) Thu hồi: xoá `worker_tokens/{workerId}` + `auth.revokeRefreshTokens(uid)` mới thật sự cắt (Rules check doc thì cắt ngay).",
  "implementation_sketch": "- CF `workerPair` (asia-southeast1, onRequest): body {code} → tra `worker_pair_codes/{code}` (TTL 10', do FE tạo) → tạo uid `worker:<workerId>` + `createCustomToken(uid,{worker:true,workerId})` + ghi `worker_tokens/{workerId}={active:true,uid,createdBy}`; CF `workerToken` cấp lại (verify token cũ còn hợp lệ + doc active).\n- FE 45-outreach.js panel VPS: nút '＋ Ghép VPS mới' → `SL_FB.createWorkerPairCode()` = setDoc `worker_pair_codes/{6 số}` {workerId, expireAt}; nút 'Thu hồi' = setDoc `worker_tokens/{workerId}.active=false`. Rules 2 collection này write=isSuperAdmin.\n- worker.mjs: thay `firebase-admin` bằng `firebase/app`+`firebase/auth`+`firebase/firestore` (Node, memoryLocalCache), boot: nếu không có `worker.token.json` thì hỏi mã ghép (readline) → `signInWithCustomToken`; wrapper `db()` tự gọi `workerToken` khi còn <10' hạn; giữ nhánh Admin SDK khi có serviceAccount.json (cờ `CFG.auth='admin'|'pair'`) 1 bản chuyển tiếp.\n- Rules: helper `isWorker()=request.auth.token.worker==true && get(/worker_tokens/$(request.auth.token.workerId)).data.active==true`; cho phép: `outreach_tasks` read + update (chỉ khi resource.workerId==token.workerId), `outreach_threads/log/stats/usage/map` write, `workers/{wid}` write khi wid==token.workerId, `fb_accounts` update field-whitelist như trên (read where workerId==mình), `worker_config/{wid}` read. Không mở `leads`, `users`, `brands`.\n- Migrate từng VPS: chép worker mới + xoá serviceAccount.json; sau khi mọi VPS ghép xong → xoá key SA + hạ role SA."
 },
 {
  "id": "P37",
  "exists": "partial",
  "exists_evidence": "Có nền: `brands.ai` (85-users-admin.js:507-530 form Hồ sơ AI 4 ô nganh/dichvu/khach/giong → `SL_FB.setBrandAi` live.js:697, backend tiêm vào prompt theo brand); trọng số toàn cục `D.scoringWeights` → `setConfig({weights})` (50-config-views.js:303-312); keyword/loại trừ TOÀN CỤC `setConfig({keywords,exclude})` (50-config-views.js:145-167) + loại trừ riêng từng nguồn (:78,114); `scanned_posts` có `decision/signals/score` (60-scan-views.js:154-260) lazy-load khi mở tab (live.js:752,840-846); `roiRates` ngưỡng ≥15 mẫu/temp (40-roi.js:41-52). CHƯA có: grep `lead_feedback|👍|👎|few-shot|compileIcp|scoring\\b` trên src/app+live.js → 0 (chỉ khớp `views.scoring` config); grep `deal_value|lost_reason|closed_at` → 0 (chỉ có `stage==='closed'` làm nhãn thắng, không có nhãn thua/lý do); không có 'Độ chính xác AI'; không có thẻ 'Đề xuất' ở keywords. Ngưỡng hot/warm là text tĩnh không chỉnh (brief config-views:20).",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "6–8 ngày (a: FE 👍/👎 + collection 1.5 ngày; CF gom few-shot + sửa prompt chấm điểm theo brand 2 ngày; b: form ICP + compileIcp + thử chấm lại 2 ngày; c: job tuần đề xuất 1.5 ngày) — CHỈ nếu backend chấm điểm (manualScan/scheduledScan trong ~/firebase-s13) sửa được để nhận tham số theo brand; hiện chỉ có brands.ai được tiêm",
  "backend_needs": [
   "cf",
   "rules",
   "index"
  ],
  "risks": "(1) Overfit/thiên lệch: 1 sales bấm 👎 hàng loạt → few-shot méo; phải gate ≥15 mẫu/brand (tái dùng ngưỡng roiRates) + Super Admin duyệt + hiện precision trước khi áp. (2) Backend chấm điểm là code em chưa dump (~/firebase-s13 manualScan/scheduledScan) — sửa prompt theo brand rủi ro regression trên pipeline đang chạy thật; phải có `brands/{code}.scoring.version` + rollback. (3) 'Thử chấm lại 20 bài' tốn token (gpt-5.5 theo brief) → giới hạn 20 bài + chỉ super; đọc `scanned_posts` lớn (3–5k doc) đã lazy nên không thêm read. (4) Nhãn 'Chốt/Thua' hiện không có `lost_reason` — cần P26/P34 trước; nếu chỉ dùng stage==='closed' thì nhãn âm = 'không chốt sau 30 ngày' (nhiễu). (5) Rules mới `lead_feedback` write cho user brand (brand==myBrand) + read super/brand; đề xuất keyword áp THEO BRAND đòi backend đọc `brands.scoring.keywords` thay `config/app.keywords` toàn cục — đổi mô hình cấu hình. (6) Regression realtime: nút 👍/👎 trên card feed nằm trong đường diff feed — phải giữ data-attr + listener delegate như `data-ndel`, không thêm listener trực tiếp lên card.",
  "implementation_sketch": "- (a) 20-feed.js card + 60-scan-views.js modal bài đã quét: 2 nút `data-fb=\"up|down\"` + popover lý do (sai ngành/người bán/đã có NCC/spam) → `SL_FB.setLeadFeedback(leadId,{expected,reason})` = setDoc `lead_feedback/{leadId}` {brand, temp, source, expected, reason, by_uid, at}. Rules: create/update khi `isActiveUser() && request.resource.data.brand==myBrand()` hoặc super; read cùng brand.\n- CF `onDocumentWritten('lead_feedback/{id}')` (asia-southeast1): gom 20–30 mẫu mới nhất/brand + precision 30 ngày theo temp/source → ghi `brands/{code}.scoring.fewshot[]` + `.accuracy{hot,warm,cold,n}`; pipeline chấm điểm đọc `brands.scoring` cạnh `brands.ai` (patch prompt theo brand, chỉ khi n≥15).\n- (b) 85-users-admin.js Hồ sơ AI thêm 2 textarea 'Khách lý tưởng' / 'KHÔNG nhận' → CF `compileIcp` (HTTP, verify super) → {weights, rules, keywords, exclude, fewshot} lưu `brands/{code}.scoring` + `scoring_versions/{code}__{ts}` (rollback); nút 'Thử chấm lại 20 bài' gọi CF `rescoreSample` đọc 20 `scanned_posts` brand gần nhất → bảng điểm cũ/mới trong modal.\n- (c) CF onSchedule tuần: đọc `scanned_posts` (decision/signals) + lead_feedback → `brands/{code}.suggestions[]`; FE 50-config-views keywords/scoring: thẻ 'Đề xuất' với nút Áp/Bỏ 1 chạm → `setBrandCrm(code,{scoring:{keywords,exclude}})` (đã có setBrandCrm merge live.js:488).\n- Feed hiện chip 'Độ chính xác AI 30 ngày: 82%' từ `D.brands[].scoring.accuracy`."
 },
 {
  "id": "P38",
  "exists": "partial",
  "exists_evidence": "Widget tồn tại khá sâu: 10-core-overview.js:317-322 CPL BrightData/lead tháng; :338-419 bảng 'Cân đối chi phí ↔ phí gói theo brand' = BrightData phân bổ theo tỉ trọng record `bd_month` (exact) hoặc cửa sổ scans, + OpenAI + eKYC + hàng 'Hạ tầng Google Cloud' (BigQuery billing) → Tổng CP, Phí gói, Lãi gộp %; tỷ giá chỉnh được. Map nguồn→brand KHỚP THEO TÊN `srcBrand[s.name]` (:335-336, fallback URL). CHƯA có: CPL lead nóng / lead phản hồi, chi phí automation (VPS/proxy/nick), bảng CPL theo NGUỒN, nút tạm dừng nguồn theo CPL, snapshot tháng (grep `cost_months|CPL nóng` → 0). `setSource(id,{active})` đã có (50-config-views.js:26, live.js:441). Lead nối nguồn theo tên `l.source` (20-feed.js:591; understand.json 'source_id' đề xuất backend ghi source_id).",
  "feasible": "yes",
  "realistic_effort": "M",
  "days_estimate": "4–6 ngày (trang mới 2 ngày tái dùng mkRow; form chi phí automation + phân bổ theo nick 1 ngày; bảng nguồn + tạm dừng 1 ngày; CF snapshot tháng 0.5–1 ngày)",
  "backend_needs": [
   "cf",
   "rules"
  ],
  "risks": "(1) Chi phí automation nhập tay (VPS/proxy/giá nick) → sai lệch; phải hiện 'nhập tay' rõ và cho phép để trống. (2) Nguồn nối theo TÊN: đổi tên nguồn → CPL nguồn rơi về 0/'chưa gán' — cần backend ghi `source_id` lên lead trước hoặc chấp nhận fallback tên+URL như hiện tại. (3) Read cost: bd_month + outreach_stats + leads đã nạp sẵn ở client → tính client không tốn read thêm; snapshot `cost_months` CF cuối tháng chỉ 1 doc/brand. (4) 'Tạm dừng nguồn khi CPL>3×TB' nếu tự động sẽ tắt nhầm nguồn mới ít lead (mẫu nhỏ) → chỉ ĐỀ XUẤT + nút bấm tay, gate ≥50 record. (5) Tenancy: trang này super/owner-only (phí gói, chi phí hệ thống); brand admin chỉ nên thấy CPL brand mình — thêm vào SUPER_ONLY_VIEWS hoặc lọc theo role. (6) Widget bd_month hiện chỉ nạp cho super (live.js) — brand-scope cần Rules mở đọc `bd_month` field theo brand (không có, doc gộp toàn hệ thống) → khó cho brand admin, dễ cho super.",
  "implementation_sketch": "- Part mới `src/app/42-costs.js` (view `costs`, nav nhóm Quản trị, SUPER_ONLY_VIEWS) tách `mkRow` khỏi 10-core-overview thành helper dùng chung; thêm cột: lead nóng, lead phản hồi (`outreach_replied`/stage responded+), CPL nóng, CPL rep, chi phí automation.\n- Chi phí automation: `config/app.autoCost{vpsMonthVnd, proxyMonthVnd, nickPriceVnd}` (setConfig sẵn) → phân bổ theo số nick `D.fbAccounts` gán brand.\n- Bảng nguồn: join `D.sources` × `D.leads` theo name (+URL fallback như :335) × `D.bdMonth.src[]` (record tính tiền/nguồn) → CPL/nguồn, lead→chốt/nguồn; nút 'Tạm dừng nguồn' gọi `SL_FB.setSource(id,{active:false})` khi CPL > 3× trung vị và ≥50 record (chỉ gợi ý, không tự tắt).\n- CF `onSchedule('0 1 1 * *')` chốt tháng: ghi `cost_months/{brand}__{YYYY-MM}` từ bd_month + outreach_stats + leads; Rules read=super; FE chart xu hướng 6 tháng đọc collection này (làm đầu vào billing P13).\n- Không đụng đường diff feed/pipeline; view mới là full-render như ROI."
 },
 {
  "id": "P39",
  "exists": "partial",
  "exists_evidence": "30-pipeline.js:138-154 `pvOf()` = chuỗi if tĩnh: quá hẹn (:148) > hẹn hôm nay (:149) > junk (:150) > kẹt N ngày (:151) > `score>=80 → 'Điểm cao - inbox trong hôm nay'` (:152, không xét stage → lead Đã hẹn/Đã phản hồi vẫn bị gợi inbox) > mặc định. Không có nút hành động trong hint (:316 chỉ text). Lead modal có nhắc hẹn fu_at + nút +3/+7/+14 (65-charts-lead-modal.js:115-127) và `lead.reply` (:131), nhưng grep `tel:|zalo.me|Soạn tin` trong 65/30 → 0 (zaloDial là code chết ở 20-feed.js:675 theo CLAUDE.md v119-17). `outreach_replied` được funcWebhook ghi lên lead (CLAUDE.md:176) nhưng grep src/app → 0 nơi đọc; `outreach_threads` FE không subscribe (Rules chỉ Admin SDK). `phone_has_zalo` có (20-feed.js:682). assistantChat chỉ nhận messages (live.js:645-659). grep `nba_` → 0.",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "5–7 ngày (ma trận rule + nút 2 ngày; nối outreach_replied/doneSteps 1 ngày cần Rules; 'Tóm tắt & bước kế' qua assistantChat 1 ngày; log nba 0.5; test diff pipeline 1 ngày)",
  "backend_needs": [
   "rules",
   "index",
   "cf"
  ],
  "risks": "(1) Hint sai làm mất niềm tin → chỉ rule tường minh cho NBA, LLM chỉ dùng cho tóm tắt/soạn tin (như đề xuất). (2) Ngữ cảnh 'bot đã inbox chưa rep 48h' nằm ở `outreach_threads.doneSteps` (Admin SDK-only) → phải mở Rules read `outreach_threads` cho brand user (`resource.data.brandCode==myBrand()`) + composite index (brandCode, active) và subscribe thêm 1 kênh onSnapshot → thêm read (≈ số lead đang chạy phễu/brand, nhỏ) — HOẶC rẻ hơn: engine denormalize `outreach_last{step,at,pid}` lên chính doc lead (worker đã ghi lead? KHÔNG — worker không đụng leads; engine outreach.js có Admin SDK → patch `apEnqueueFunnel`/funcWebhook ghi lên lead). (3) Regression: pipeline chưa diff theo id (full render + cap 50/cột) — thêm nút vào thẻ kanban đi qua delegate listener; ghi `nba_shown` mỗi render sẽ bắn updateDoc ồ ạt → chỉ log `nba_clicked` (write) và `nba_shown` gộp theo phiên/localStorage. (4) `assistantChat` chưa rate-limit (brief shell:48) → nút 'Tóm tắt' nên chỉ cho lead ≥5 ghi chú + cache kết quả trên lead (`ai_summary{at,text}`). (5) `outreach_replied` viết bởi funcWebhook chỉ cho nick func — nick AdsPower không có (gap [M] brief) → nhánh 'khách rep chưa ai trả lời' chỉ đúng khi P-hộp-thư-2-chiều làm xong.",
  "implementation_sketch": "- 30-pipeline.js: thay `pvOf` hint bằng `nbaOf(l)` = bảng ưu tiên: overdue → khách rep chưa ai trả lời (`l.outreach_replied && !note của người sau at`) → fu hôm nay → bot inbox ≥48h chưa rep (`l.outreach_last.step==='inbox'`) → kẹt ≥2 ngày theo stage (stage≠responded/booked mới gợi inbox) → có SĐT+phone_has_zalo chưa nhắn (chưa có note/touch) → mặc định. Mỗi NBA trả {hint, htype, action:{kind:'tel'|'zalo'|'draft'|'fu', arg}}.\n- Thẻ kanban :305-316 render 1 nút `data-nba=\"kind:leadId\"`; delegate handler: tel → `location.href='tel:'`; zalo → mở `https://zalo.me/<sđt>`; draft → mở lead modal + prefill `lead.reply` (biến thể theo bước từ P06 nếu có); fu → `SL_FB.updateLead(id,{fu_at})`. Click → `SL_FB.updateLead(id,{nba_clicked:arrayUnion({kind,at})})` (Rules leads update đã cho brand user).\n- Engine (LỆNH Cloud Shell): `apEnqueueFunnel`/worker-done → không đụng; thay vào đó outreachTick khi thread `done` ghi `leads/{id}.outreach_last={step,at,pid}` (Admin SDK, 1 write/lead).\n- 65-charts-lead-modal.js: nút 'Tóm tắt & bước kế' (khi ≥5 ghi chú) → `SL_FB.assistantChat([{role:'user',content: notes+stage}])` → 3 dòng, cache `ai_summary` lên lead.\n- Không cần index mới nếu đi đường denormalize lên lead; cần Rules whitelist field `nba_clicked/ai_summary` nếu Rules leads có whitelist (CLAUDE.md:399 chưa xác minh)."
 },
 {
  "id": "P40",
  "exists": "partial",
  "exists_evidence": "live.js:645-659 `assistantChat(messages)` POST `{messages}` → `{reply}` (chỉ text); 90-boot.js:100-200 widget chat: 3 chip tĩnh (:120-123), `send()` :171-197 chỉ render markdown, không parse actions, không biết view/leadId. Cmd-K palette 70-shell-tools.js:4-71 tìm trang/lead/lệnh bằng fold(), không parse câu tự nhiên (:23 chỉ khớp tên lead). Backend assistantChat nạp ≤500 lead (`assistantLeadLimit` live.js:156, brief shell:22) nhưng không hành động (brief shell:37). `assignLead/claimLead/updateLead(fu_at)/addLeadNote` đã có ở SL_FB (live.js:492-530). Chưa có rate-limit (brief shell:48). grep `actions\\[\\]|function-calling|tool_call` → 0.",
  "feasible": "yes-with-backend",
  "realistic_effort": "L",
  "days_estimate": "10–14 ngày (CF tool-schema + context 3 ngày; FE render action + xác nhận + Hoàn tác 3 ngày; chip theo view 1 ngày; Cmd-K regex 5 lệnh 2 ngày; rate-limit + log 1 ngày; test 2 ngày)",
  "backend_needs": [
   "cf",
   "rules"
  ],
  "risks": "(1) Hành động sai đích (giao nhầm lead trùng tên, hẹn sai ngày) → BẮT BUỘC thẻ xác nhận + Hoàn tác 6s (Hoàn tác = ghi ngược qua SL_FB: unassignLead/xoá note/clear fu_at — cần giữ giá trị cũ). (2) `runScan` từ chat = super-only, phải check role ở CF (verifyIdToken → role) không tin FE. (3) Prompt-injection: nội dung lead (bài FB người lạ) nằm trong context 500 lead → model có thể bị lái 'giao lead cho X' — FE chỉ thực thi action sau xác nhận người dùng, CF chỉ trả action KHÔNG ghi. (4) Chi phí/rate-limit: mỗi câu nạp 500 lead (token lớn); thêm `assistant_rate/{uid}__{giờ}` increment trong CF, quota 30/giờ/user. (5) Regression: widget nằm ngoài #view (90-boot.js:97) nên không đụng repaint; nhưng action `filterFeed` phải gọi đúng API feed hiện có (reset cap 200 card, giữ debounce) thay vì set DOM tay. (6) Cmd-K parse SĐT '0938…' → feed search hiện KHÔNG index phone (understand.json 'theo tên' evidence 20-feed.js:455) → lệnh 'giao lead 0938 cho Ngọc' chỉ đúng khi bổ sung phone vào `__q`.",
  "implementation_sketch": "- CF `assistantChat` (~/firebase-s13, LỆNH Cloud Shell): body thêm `{view, leadId, brand}`; OpenAI tools schema 7 action (openLead, filterFeed{temp,source,days}, assignLead{leadId,uid}, setFollowUp{leadId,at}, addNote{leadId,text}, draftMessage{leadId,channel}, runScan) → trả `{reply, actions:[{type,args,label}]}`; role gate runScan; rate-limit doc `assistant_rate`.\n- live.js `assistantChat(messages, ctx)` truyền ctx; 90-boot.js `send()` render `.asst-actions` nút dưới reply; click → thẻ xác nhận (tên lead + việc) → gọi đúng SL_FB (`assignLead`, `updateLead({fu_at})`, `addLeadNote`, `openLead`, hàm lọc feed sẵn có) → toast có nút 'Hoàn tác' 6s (ghi ngược). Chip gợi ý đổi theo `location.hash` + lead modal đang mở.\n- 70-shell-tools.js palette: regex 5 mẫu ('giao lead <x> cho <tên>', 'hẹn <mai|ngày> <giờ> #<n>', 'ghi chú #<n>: …', 'lọc nóng <n> ngày', 'mở #<n>') chạy local; không khớp → gửi CF cùng tool-schema.\n- Rules: không đổi (mọi ghi qua SL_FB như tay); chỉ thêm `assistant_rate` write=false (CF Admin SDK)."
 },
 {
  "id": "P41",
  "exists": "partial",
  "exists_evidence": "40-roi.js:26 `ROI_STAGE_P={new:5,checked:10,commented:15,inbox:25,responded:40,booked:60,closed:100}` cứng; :82 dùng cho giá trị pipeline theo stage. Đã có cơ chế 'tự học' 1 chiều: `roiRates` :41-52 = tỉ lệ chốt thật theo temp (cohort ≥15 mẫu >30 ngày, tag TỰ HỌC :163) → `roiEv` :55. Chưa có: grep `calibrated|model\\b|Dự kiến|run-rate|mục tiêu` 40-roi/10-core → 0; grep `closed_at|lost_reason|deal_value` → 0 (chỉ có `stage==='closed'` + `stage_at`); `brands.roi` lưu tham số (setBrandRoi live.js:695). Overview card 'Dự kiến tháng này' chưa có.",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "5–7 ngày (CF job đêm + hồi quy 2–3 ngày; FE calibratedStageP + card dự kiến + mục tiêu 2 ngày; test 1 ngày). Phụ thuộc P26/P34 để có nhãn thua/giá trị deal — không có thì chỉ dùng closed vs không-closed-sau-30-ngày",
  "backend_needs": [
   "cf",
   "rules"
  ],
  "risks": "(1) Dữ liệu: brand nhỏ vài chục lead/tháng → hiếm khi đạt 30 outcome; nếu dùng 'không chốt sau 30 ngày' làm nhãn âm thì lead đang chăm bị tính thua → bias. Gate n≥30 + AUC≥0.65 mới bật, còn lại fallback ROI_STAGE_P + tag 'CẤU HÌNH'. (2) Feature 'phút phản hồi đầu' cần `first_care_at` (có), 'số lần chạm bot' cần outreach_threads.doneSteps (Admin SDK có) — CF job đọc được. (3) Read cost job đêm: leads 90 ngày/brand (query `brand==code & detected_at>=t0`, index (brand,detected_at) ĐÃ có theo CLAUDE.md E7) — vài nghìn read/đêm, rẻ. (4) Minh bạch: công thức ROI hiện là điểm mạnh — thay xác suất bằng hệ số hồi quy khó giải thích; phải hiện 'xác suất chốt học từ N lead đã đóng' + nút 'dùng công thức mặc định'. (5) Regression: `roiCompute` dùng chung cho Overview KPI + báo cáo + PDF → đổi `ROI_STAGE_P` phải qua 1 hàm `stageP(brand,key)` để mọi nơi nhất quán; brands doc thêm field `model` đi qua `setBrandRoi`? — KHÔNG, CF Admin ghi `brands/{code}.model`; Rules brands write=super vẫn giữ.",
  "implementation_sketch": "- CF `onSchedule('0 19 * * *')` (2h VN) `trainCloseModel`: mỗi brand đọc lead 90 ngày có outcome (stage closed | lost_reason | >30 ngày không closed) → logistic regression thuần JS (gradient descent, features one-hot temp/source/industry + có SĐT/Zalo + phút phản hồi đầu + tuổi stage + số bước bot từ outreach_threads.doneSteps) → `brands/{code}.model={coef,calibratedStageP,n,auc,trainedAt}`; n<30 → xoá model.\n- 40-roi.js: `stageP(R,key)= (R.model&&R.model.n>=30&&R.model.auc>=0.65)? R.model.calibratedStageP[key] : ROI_STAGE_P[key]`; thay 2 chỗ :82 + roiEv; tag 'TỰ HỌC' như :163. Thẻ kanban 30-pipeline hiện 'chốt ~62%' từ `pLead(l)=sigmoid(coef·x)` khi có model.\n- 10-core-overview.js Overview: card 'Dự kiến tháng này' = Σ p×roiBaseOf (tái dùng roiBaseOf) + run-rate lead nóng (lead nóng MTD ÷ ngày đã qua × ngày tháng); ô mục tiêu `brands/{code}.roi.targetVnd` (setBrandRoi sẵn) → 'cần thêm ~N lead nóng' = (target−dự kiến)/(p_hot×AOV). AI diễn giải 1 câu qua assistantChat (tuỳ chọn, số đưa sẵn).\n- Rules: không đổi (brands read đã có cho brand user; model là field trong doc)."
 },
 {
  "id": "P42",
  "exists": "none",
  "exists_evidence": "grep `inboundWebhook|psid|conversations|Page API|Messenger` trên 50-config-views.js + live.js → 0. Zalo hiện chỉ OUTBOUND thông báo: cụm `~/smartlead-zalo-fn` (bootstrapZaloTokens, sendZaloZBS, refreshZaloToken, notifyBrandZalo — CLAUDE.md:76) gửi ZBS tới `brand_sales` (live.js:725-740 setSales) + chip `phone_has_zalo` (20-feed.js:682). View Tích hợp = 4 card tĩnh, nút 'Cấu hình →' không hành động, `D.integrations` lấy từ demo SL_DATA (live.js:138; brief config-views:23). `funcWebhook` chỉ bắt reply của nick func FB cá nhân (CLAUDE.md:176), không phải Page/OA. `brands.ai` + `assistantKb` (live.js:155) có sẵn làm KB. Không có FAQ brand, không có `conversations`, không có phân công 'sales rảnh nhất' (assignMode có ở setBrandCrm live.js:488 — chưa xem chi tiết).",
  "feasible": "yes-with-backend",
  "realistic_effort": "L",
  "days_estimate": "15–20 ngày (CF inboundWebhook 2 kênh + verify chữ ký 3 ngày; khớp/tạo lead theo SĐT/psid 2 ngày; LLM trả lời + guardrail + chấm ý định 3 ngày; bàn giao + chuông + stage 2 ngày; FE bật/tắt + giờ trực + FAQ + hộp hội thoại 4 ngày; Secret Manager/OA token refresh 1 ngày; test 2 ngày). Ngoài code: đăng ký Zalo OA gói Tiêu chuẩn + Meta App Review (pages_messaging) mất 1–3 tuần",
  "backend_needs": [
   "cf",
   "rules",
   "index",
   "secret",
   "external-api"
  ],
  "risks": "(1) Bot trả lời sai/hứa giá → khiếu nại; bắt buộc chế độ 'gợi ý cho người' mặc định, 'tự trả lời' chỉ khi brand admin bật + guardrail (không giá ngoài `brands.ai.dichvu`, không y tế) + log đầy đủ. (2) Ngoại vi: Zalo OA OpenAPI chỉ gói Tiêu chuẩn 1tr/năm trở lên (market.md:53), tin ngoài 48h tính phí; Meta Page cần App Review + 24h window — không kiểm soát được thời gian duyệt. (3) Token OA/Page = secret → Secret Manager + CF refresh (đã có pattern refreshZaloToken ở ~/smartlead-zalo-fn), tuyệt đối không FE/zip. (4) Pháp lý: hội thoại inbound là dữ liệu cá nhân có đồng ý (khách chủ động nhắn) → tốt hơn lead quét; vẫn cần lưu mục đích + xoá theo yêu cầu; không lưu OTP/CCCD. (5) Firestore: `conversations/{id}/messages` realtime cho sales → thêm 1 kênh onSnapshot brand-scope + composite index (brand, updatedAt desc) + Rules brand; volume tin nhắn có thể lớn hơn leads → cap 50 hội thoại gần nhất + lazy như scanned_posts. (6) Tạo lead từ inbound: khớp SĐT trùng lead quét (dedupe theo SĐT chưa có — brief lead-modal:50) → dễ tạo lead đôi; cần chuẩn hoá SĐT + query `phone==` (index single-field có sẵn). (7) Regression: chuông 🔔 dùng D.alerts (suy từ lead ≥60) — thêm nguồn 'khách nhắn' phải qua cùng D.alerts để không vẽ lại topbar sai.",
  "implementation_sketch": "- CF `inboundWebhook` (asia-southeast1, onRequest public, verify: Zalo `X-ZEvent-Signature` HMAC với OA secret; Meta `X-Hub-Signature-256` + verify_token GET) → chuẩn hoá {brand(theo oa_id/page_id map `brands/{code}.inbound{oaId,pageId}`), channel, sender(psid|zalo uid), phone?, text} → `conversations/{channel}__{sender}` {brand, leadId, status:'bot'|'human', updatedAt} + subcollection `messages`.\n- Khớp lead: query leads `brand==code & phone==sđt` (nếu có) hoặc `psid`; không có → tạo lead mới `{source:'Inbound Zalo OA|Messenger', temp từ chấm ý định, stage:'responded'}`.\n- LLM: prompt = `brands.ai` + `assistantKb` + `brands/{code}.faq[]` (form mới ở 85-users-admin cạnh Hồ sơ AI, setBrandCrm merge) + guardrail; mỗi lượt chấm intent → 'nóng' (hỏi giá/hẹn/để SĐT) → status='human', stage responded, `assignLead` theo `assignMode` (round-robin sales active brand), ghi note hệ thống vào lead (shape khớp funcWebhook) + D.alerts chuông. Gửi trả lời qua Zalo OA API `/message/cs` (token Secret Manager, refresh như refreshZaloToken) / Graph `me/messages`.\n- FE: 50-config-views `views.integrations` thay card tĩnh bằng 2 card thật (Zalo OA / Facebook Page): trạng thái kết nối, công tắc 'gợi ý'/'tự trả lời', giờ trực bot, nút 'Sao chép URL webhook + verify token' → `setBrandCrm(code,{inbound:{mode,hours}})`; part mới `48-inbox.js` (view `inbox`, mọi vai trò theo brand): danh sách hội thoại + khung chat, sales gõ trả lời → CF `inboundReply` (không cho FE gọi API Zalo trực tiếp). live.js `lazyInbox()` subscribe `conversations where brand==byBrand orderBy updatedAt desc limit 50` (index composite) → `D.conversations`.\n- Rules: `conversations` + `messages` read/write brand user (brand==myBrand), CF ghi Admin; `brands.inbound` write=super/admin brand."
 },
 {
  "id": "P43",
  "exists": "partial",
  "exists_evidence": "live.js:429-437 `logLeadView` updateDoc lead {views, 'views_by.<email>': increment, lastViewBy: em} — ghi EMAIL lên MỌI doc lead; 65-charts-lead-modal.js:51-55 chặn 1 lượt/30' qua localStorage; 85-users-admin.js:113-118 đọc `views_by` để tổng hợp 'Tương tác lead'. grep `pii_log|lead_views|revealPhone|maskPhone` trên src/app + live.js = 0. SĐT hiện đủ ở 20-feed.js:749/758 (`ph-pill`/`ph-hl` bấm chép qua `__slCopy` 20-feed.js:771) và trong modal (bài gốc bôi nổi SĐT) cho mọi vai trò.",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "4–6 ngày (bước 1: 2–3 ngày FE+live+Rules; đổi logLeadView + tab Tuân thủ: 2 ngày; bước 2 tách private/contact: +1 tuần riêng vì phải migrate 7–12k lead + sửa buildData/feed/search/ROI)",
  "backend_needs": [
   "rules",
   "index",
   "data-migration"
  ],
  "risks": "(1) Bước 1 chỉ che UI: SĐT vẫn trong snapshot `leads` client — Rules không lọc field, DevTools thấy được → không được quảng bá là 'che thật'. (2) `phoneLocal(l.phone)` được nhét vào `onclick` qua `escJsAttr` — chuỗi che phải đi qua đúng sink này, dễ tái sinh XSS nếu tự nối chuỗi mới. (3) Feed diff v78 so sánh HTML thẻ: trạng thái 'đã hiện số' phải cache theo leadId (Set) không thì repaint realtime che lại số vừa hiện. (4) Bỏ `views_by` → tab 'Tương tác lead' 85-users-admin:113-118 mất số liệu cũ nếu không đọc song song 2 nguồn. (5) `pii_log` create-only cho user active: cần Rules chặn giả mạo uid (`request.resource.data.uid==request.auth.uid`) + query super/admin cần composite (brand, at desc). (6) Bước 2: mọi query search theo SĐT (fold/__q), Sheet sync, Zalo notify, funcWebhook khớp phone đều đọc `lead.phone` → phải rà cả 3 codebase backend.",
  "implementation_sketch": "- 20-feed.js: helper `maskPhone(p)` → `0938***456`; `phonePill(l)`/`ph-hl` rẽ nhánh theo `roleIsSuper||CURRENT_ROLE==='admin'||l.assignee_uid===CURRENT_USER.uid||revealed.has(l.id)`; nút 'Hiện số' → `SL_FB.revealPhone(id,'reveal')` rồi `revealed.add(id)` + repaint thẻ; `__slCopy` gọi `SL_FB.revealPhone(id,'copy')` fire-and-forget.\n- 65-charts-lead-modal.js: cùng helper cho khối bài gốc + thay `logLeadView(id)` → `SL_FB.logLeadView` ghi `lead_views/{leadId}__{uid}` {uid, leadId, brand, n:increment, lastAt} (bỏ email + bỏ update doc lead).\n- live.js: `revealPhone:(id,reason)=>addDoc(collection(db,'pii_log'),{uid,leadId,brand,at:serverTimestamp(),reason})`; `logLeadView` viết lại setDoc merge `lead_views`; 85-users-admin đọc `lead_views` (where brand==) thay `views_by` (giữ fallback đọc views_by cũ 1 đợt).\n- Rules: `pii_log` create if isActiveUser && data.uid==auth.uid && data.brand==myBrand; read isSuperAdmin || (admin && brand==myBrand); `lead_views` tương tự; siết `leads` update để client KHÔNG ghi `views_by`/`lastViewBy` nữa.\n- Index: `pii_log(brand asc, at desc)`; `lead_views(brand, lastAt)`.\n- FE tab 'Tuân thủ' (part mới `55-compliance.js` hoặc trong 85-users-admin): bảng ai/lead/giờ/lý do + xuất CSV client-side (Blob); tải-khi-mở-tab kiểu lazyOutreach.\n- Bước 2 (đợt riêng): CF migrate `leads/{id}.phone/email` → `leads/{id}/private/contact`; Rules subcollection theo assignee/admin; buildData ghép khi có quyền."
 },
 {
  "id": "P44",
  "exists": "none",
  "exists_evidence": "grep -i 'backup|PITR|export|emulator' /home/user/tdm1/CLAUDE.md → chỉ khớp 'export * from' (ESM) và HANDOFF.md:72 (grep export index.js); không có mục backup/PITR/bucket/export brand nào. live.js/src/app không có `exportBrand`/tải JSON brand. Chỉ có `backfillSheet` (CLAUDE.md audit) = đổ lead sang Google Sheet theo brand — gần với 'bản sao dữ liệu' nhưng không phải backup.",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "3–5 ngày: PITR + bucket + CF `dailyExport` (scheduler) 1 ngày lệnh Cloud Shell; rclone/Storage Transfer sang S3 VN 1 ngày (phụ thuộc anh mở tài khoản Viettel/VNG); RESTORE.md + test restore vào project staging 1–2 ngày; CF `exportBrand` + nút FE 1 ngày",
  "backend_needs": [
   "cf",
   "secret",
   "external-api",
   "rules"
  ],
  "risks": "(1) Bucket backup = bản sao TOÀN BỘ PII mọi brand → IAM sai = rò rỉ lớn hơn app; phải SA riêng, không cấp cho compute SA mặc định (đang có `secretmanager.admin`). (2) Firestore export tính phí đọc theo doc (7–12k lead + 3–5k scanned_posts + notes ≈ 30–50k read/ngày ≈ vài nghìn đồng, chấp nhận được) nhưng export CÓ THỂ không kịp nếu dùng `firestore.export` với collection lớn nhiều lần/ngày. (3) Restore về cùng project ghi đè realtime → chỉ restore vào project staging rồi copy chọn lọc; `import` KHÔNG kích TTL/index → cần tạo lại 5+ composite index đã ghi trong CLAUDE.md. (4) rclone chạy ở đâu? Cloud Shell không chạy nền — cần Cloud Run job hoặc VPS anh; key S3 VN = secret mới. (5) `exportBrand` phải lọc đúng tenancy (leads where brand==, collectionGroup notes where brand==, outreach_log brandCode==) — nếu quên `outreach_map/outreach_threads` thì thiếu, nếu export nhầm super scope thì lộ chéo brand. (6) NĐ 53/2022 'bản sao tại VN' cần luật sư xác nhận object storage S3 VN là đủ.",
  "implementation_sketch": "- LỆNH Cloud Shell: `gcloud firestore databases update --enable-pitr`; `gsutil mb -l asia-southeast1 gs://smartlead-z15-backup` + retention 90d + lifecycle; SA `firestore-export@` role `datastore.importExportAdmin` + `storage.objectCreator` trên bucket.\n- CF `dailyExport` (onSchedule 02:00 VN, `~/firebase-s13/functions/backup.js`, region asia-southeast1) gọi Firestore Admin REST `exportDocuments` với `collectionIds` tường minh (leads, notes, users, brands, fb_accounts, outreach_*) → prefix theo ngày; ghi `system_backups/{day}` để FE super thấy 'Backup gần nhất'.\n- Sang VN: Cloud Run job `rclone sync gs://... s3-vn://...` hằng ngày (key S3 VN ở Secret Manager) HOẶC chạy trên VPS worker anh (đã online 24/7) bằng rclone task.\n- `RESTORE.md` trong ~/firebase-s13: import vào project `smartlead-z15-staging`, kiểm tra tenancy bằng script đếm lead/brand, danh sách index/TTL phải tạo lại, RTO/RPO.\n- CF `exportBrand` onRequest (verify idToken, super hoặc admin đúng brand) → gom leads+notes(collectionGroup where brand)+outreach_log(brandCode) → JSON/CSV zip → Cloud Storage signed URL 15'; FE 85-users-admin nút 'Xuất dữ liệu brand' gọi qua `SL_FB` pattern `MANUAL_SCAN_URL.replace('manualScan','exportBrand')` (giống saveFbAccount)."
 },
 {
  "id": "P45",
  "exists": "none",
  "exists_evidence": "live.js:14 import KHÔNG có `getCountFromServer`/`getAggregateFromServer` (grep = 0). Notes: live.js:350-372 collectionGroup('notes') onSnapshot TOÀN BỘ theo vai trò (super = cả hệ thống, không limit). Auto-fill: live.js:235-250 vòng 8 lượt `loadOlderLeads()` getDocs 500/lượt tới trần 4000. Avatar: live.js:935 chấp nhận data-URL tới 200KB trong `users/{uid}`; refreshAdmin live.js:395-398 getDocs trọn users/fb_accounts/brands/brand_sales. grep `docChanges|usage_client` = 0. Overview KPI tính trên cửa sổ lead nạp (brief shell-overview).",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "5–7 ngày tách 5 mục: (1) aggregation KPI 1,5 ngày; (2) notes theo lead mở + rail 30 ngày 2 ngày + regression Playwright notes 3/3 + v119-23 test 4/4; (3) avatar → Storage 1 ngày (+ Rules Storage + migrate); (4) auto-fill sang onSnapshot phân trang 1 ngày (vùng absorbDropped/dataEpoch rất nhạy); (5) usage_client 0,5 ngày",
  "backend_needs": [
   "rules",
   "index",
   "data-migration",
   "cf"
  ],
  "risks": "(1) Notes là vùng đã qua 4 vòng fix (v119-20/21/23 + index failed-precondition) — đổi từ 'toàn bộ' sang 'per-lead + 30 ngày' phá ngầm: rail 'Ghi chú' (20-feed.js:596-599 duyệt D.leads×leadNotes), `notesIdx` dedupe nid, cache resume token; ghi chú >30 ngày biến mất khỏi rail = user tưởng mất dữ liệu. (2) Aggregation không realtime + mỗi filter/khung là 1 query; poll 60s × N KPI × user online = read mới; count trên brand có khung `leadFrom/leadTo` cần where kép → index composite (brand, detected_at) đã có, nhưng theo stage/temp cần thêm. (3) Auto-fill sang onSnapshot phân trang: 8 listener/khung × mỗi listener 500 doc giữ trong RAM + `absorbDropped`/`leadsExtra` logic hiện dựa getDocs 1 lượt → regression 'lead bốc hơi' (v119-14) rất dễ tái phát. (4) Avatar ra Storage: cần Storage Rules + CORS + migrate data-URL cũ; SPA hiện KHÔNG import firebase-storage (thêm ~40KB CDN). (5) Với 7–12k lead hiện tại, chi phí Firestore chưa phải nỗi đau — lợi ích chỉ hiện ở 50k+; ưu tiên thấp hơn P43/P47.",
  "implementation_sketch": "- live.js: import `getCountFromServer`; hàm `kpiCounts(ctx)` chạy 4–6 count (tổng/hot/warm/cold/chưa chăm) theo `leadsCtx` (brand + khung) → `d.kpiServer`; gọi sau mỗi snapshot leads (debounce 30s) thay poll cứng. 10-core-overview/20-feed đọc `D.kpiServer` cho thẻ KPI + đếm Nóng/Ấm/Lạnh, fallback tính client khi thiếu.\n- Notes: `loadNotes()` giữ collectionGroup nhưng thêm `where('at','>=',now-30d)` + `orderBy at desc limit 500` (cần index notes(brand,vis,at) + (by_uid,at) COLLECTION_GROUP — đọc bài học CLAUDE.md 'notes failed-precondition'); thêm `subLeadNotes(leadId)` onSnapshot `leads/{id}/notes` khi mở modal/expand thẻ, merge vào notesCache theo nid (notesIdx đã dedupe).\n- Avatar: 80-rbac-auth/85-users-admin upload → `uploadBytes(ref(storage,'avatars/'+uid))` → lưu URL; live.js:935 chấp nhận https URL; CF một lần migrate data-URL → Storage.\n- Auto-fill: giữ getDocs (an toàn) nhưng chỉ chạy khi user mở feed/pipeline (lazy), bỏ 8 lượt mặc định ở Overview; realtime lead cũ để đợt sau.\n- usage_client: live.js đếm `snap.docChanges().length` mỗi snapshot → setDoc merge `usage_client/{brand}__{day}` increment (Rules create/update own brand); 40-roi.js hiện 'read/ngày'."
 },
 {
  "id": "P46",
  "exists": "partial",
  "exists_evidence": "Có `privacy.html` + `terms.html` tĩnh (ls smartlead/*.html) nhưng grep `privacy|terms|Điều khoản|Chính sách` trong 80-rbac-auth.js/app.html = 0 → màn đăng nhập KHÔNG có link. Hồ sơ HKD/MST có sẵn: 85-users-admin.js:542-561 `b.billing` {type hkd, ten, mst, diachi, email, sdt}. grep `tosAccepted` src/app + live.js = 0; toggle brand 45-outreach `setOutreach(code,{on})` không có bước xác nhận. grep -i 'DPIA|Mẫu 09|A05|DPO' CLAUDE.md = 0. market.md:62-67,107 đã xác định SmartLead không được miễn DPIA + Firebase SG = xuyên biên giới.",
  "feasible": "yes",
  "realistic_effort": "M",
  "days_estimate": "Phần code: 2–3 ngày (link privacy ở login 0,5; banner ToS + `tosAcceptedBy/At` 1; privacy theo brand sinh động 1–1,5). Phần hồ sơ (DPIA, Mẫu 09, DPA, DPO, runbook 72h): 1–2 tuần soạn thảo + luật sư review — ngoài code, phụ thuộc anh",
  "backend_needs": [
   "rules",
   "none"
  ],
  "risks": "(1) Cơ sở pháp lý cho quét bài công khai + cold-inbox không có ngoại lệ trong Luật 91/2025 — hồ sơ tốt giảm mức phạt (NĐ 330/2026) nhưng không hợp pháp hoá; phải nói rõ với anh, không hứa 'đã tuân thủ'. (2) privacy sinh theo brand: brand chưa khai `billing` → trang trống/tên Z15 → cần fallback rõ ràng; trang là tĩnh Netlify nên 'theo brand' phải đọc Firestore `brands/{code}` public-read hoặc query param + doc `brand_public` (mở Rules read công khai = cân nhắc). (3) Tick ToS bằng Rules `brands` write=isSuperAdmin → chỉ super tick được; ghi `tosAcceptedBy` = email super chứ không phải chủ brand → giá trị pháp lý yếu; cần chữ ký DPA offline. (4) Banner ToS chặn toggle cũ → smoke test toggle (smoke.js:105) phải cập nhật.",
  "implementation_sketch": "- 80-rbac-auth.js màn login: thêm footer link `privacy.html` / `terms.html` (+ dòng 'Đăng nhập = đồng ý…'); tokens privacy/terms v17 đã có.\n- 45-outreach.js: trong handler toggle brand `on` lần đầu (`!b.outreach.tosAcceptedAt`) mở modal ToS (nội dung: automation FB/Zalo cá nhân ngoài ToS nền tảng, brand chịu rủi ro nick, SmartLead cung cấp van) → tick → `SL_FB.setOutreach(code,{on:true,tosAcceptedBy:CURRENT_USER.email,tosAcceptedAt:Date.now()})`; thẻ brand hiện chip '✓ ToS ngày…'. Rules brands không đổi (super).\n- privacy theo brand: `privacy.html?brand=<code>` đọc `brand_public/{code}` (doc do CF/super ghi từ `billing`) hoặc đơn giản hơn: 85-users-admin nút 'Sinh trang chính sách' → tạo text từ template + billing → super copy dán (không cần Rules public).\n- Tài liệu (ngoài code, em soạn draft): DPIA (dùng bản đồ collection + TTL + Rules + Secret Manager + region làm phụ lục), Mẫu 09 cho Firebase SG (+ danh sách sub-processor: Google, BrightData, OpenAI, func.vn), DPA mẫu Z15↔brand, quyết định bổ nhiệm DPO, runbook 72h (nguồn sự kiện: `system_alerts`, needLogin/challenge, audit)."
 },
 {
  "id": "P47",
  "exists": "partial",
  "exists_evidence": "Đã có tín hiệu thô, CHƯA có điểm: worker.mjs:128 RATE_LIMIT trong taxonomy, :166-167 `challengeCount` (≥3 → active:false), :556-557 `needLoginCount`, :550/727 reset cờ khi thành công; 45-outreach.js:247-250 badge ⛔ challenge / ⚠️ needLogin; live.js:474 `clearNickFlag`. grep `apiOkRate|score|trust|ramp|okRate|lastRunAt` trong worker.mjs → chỉ có `score` của LEAD (t.score), không có score nick. `outreach_usage/{pid}__day` chỉ engine đọc (CLAUDE.md v119-26), FE không đọc (grep outreach_usage 45-outreach/live = 0). Cap hiện là hằng brand `caps{}` (v119-35), không phụ thuộc sức khoẻ nick; không có ramp-up nick mới.",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "5–7 ngày: worker ghi `stats7d/lastRunAt/apiOkRate` 1 ngày; CF nightly `nickHealth` tính score + ramp/penalty → `fb_accounts.health{score,level,reasons,capMul,until}` 1,5 ngày; engine `tryConsume` nhân `capMul` 0,5 ngày; FE vòng màu + lý do + 'Mức rủi ro' thẻ brand 1,5 ngày; calibrate trên log thật 1–2 tuần chạy nền TRƯỚC khi hiện cho khách",
  "backend_needs": [
   "cf",
   "rules",
   "worker"
  ],
  "risks": "(1) False-red: `apiOkRate` thấp do FB đổi doc_id (CLAUDE.md capture) hay lead cũ không tìm thấy bài → điểm rơi dù nick khoẻ → tự hạ cap oan; phải tách 'lỗi nick' (challenge/needLogin/RATE_LIMIT) khỏi 'lỗi dữ liệu' (findCommentEl throw, feedback_id null). (2) Ramp-up theo `createdAt` fb_accounts = ngày THÊM vào SmartLead, không phải tuổi nick FB thật → nick 2016 vừa thêm cũng bị 20% cap tuần 1 (anh sẽ khó chịu); cần ô 'năm tạo nick' nhập tay. (3) Hạ cap 50% khi RATE_LIMIT: engine reserve quota qua `tryConsume(pid,kind,cap)` — capMul phải áp ở engine (Admin SDK) chứ không phải FE; sai chỗ = FE hiện 1 số, engine chạy số khác. (4) Read: nightly CF quét outreach_log 7 ngày × 3000 nick — dùng `outreach_stats` theo nick (chưa có, hiện theo brand) hoặc worker cộng dồn counter lên `fb_accounts.stats7d` để tránh quét log. (5) oaSignature (v119-37) phải thêm `health.score/level` không thì thay đổi điểm không repaint.",
  "implementation_sketch": "- worker.mjs: sau mỗi runNick ghi `fb_accounts/{pid}` merge {lastRunAt, runs:increment, okSteps:increment(n), failSteps:increment(m), lastErrKind:'auth|challenge|data|dom'} (phân loại lỗi để không phạt lỗi dữ liệu); giữ counters challenge/needLogin hiện có.\n- Engine `outreach.js`: `brandCaps(brand)` × `acct.health?.capMul||1` trong `apEnqueueFunnel`/`tryConsume`; RATE_LIMIT flag (worker đã ghi `challenge:'RATE_LIMIT'`) → CF set `health.capMul=0.5, until=+3d`.\n- CF `nickHealth` onSchedule nightly asia-southeast1: score = 100 − 25·min(challengeCount,2) − 15·needLoginCount − 30·(1−okRate_nick) − 10·(usage>90%cap) − ramp(nickAgeDays<28 → capMul 0.2→1.0 tuyến tính); ghi `fb_accounts.health{score,level:'green|amber|red',reasons[],capMul,computedAt}`; level red 3 ngày liên tiếp → 'nick nên nghỉ' (không tự tắt — human-in-the-loop theo báo cáo checkpoint).\n- FE 45-outreach.js: oaAcctRow thêm vòng SVG màu + tooltip `reasons`; thẻ brand 'Mức rủi ro: Thấp/Vừa/Cao' = f(preset matrix + avg score nick); ô 'Năm tạo nick' trong modal Thêm nick → `fb_accounts.fbCreatedYear`; live.js `setFbAccountMeta(pid,{fbCreatedYear})`; thêm health vào `oaSignature`.\n- Rules: fb_accounts write vẫn super (ok). Index: không cần (CF Admin SDK)."
 },
 {
  "id": "P48",
  "exists": "partial",
  "exists_evidence": "Modal `openLead` 65-charts-lead-modal.js:45 KHÔNG có khối ghi chú (grep leadNotes trong part 65 = 0; chỉ có `fuNote` hẹn chăm dòng 119); mọi thao tác `updateLead(...)` → `closeModal()` (65:148,152,178). Modal là div `#modalBg/#modal` (app.html:128-129, css app.css:1020-1059), không `<dialog>`, không breakpoint riêng cho modal ở @media 639px; không `modalLeadId` → `SLApp.reload` không vẽ lại modal. Khối ghi chú chỉ ở feed: 20-feed.js:461-525 (saveNote/data-ndel/data-nvis), 836-851 render, `notesIdx` 250-267. 90-boot.js:55 chỉ miễn hoãn reload khi `cur0==='feed' && noteTyping`. Bong bóng 'Hỏi em nè!' 90-boot.js:133 luôn hiện.",
  "feasible": "yes",
  "realistic_effort": "S",
  "days_estimate": "2 ngày (tách `notesBlock(l)` + `bindNotes(root,id)` 0,5; chip KQ + Enter/Shift+Enter 0,25; không văng + `modalLeadId` re-render 0,5; bottom-sheet CSS + ẩn bubble 0,25; regression notes Playwright 3/3 + v119-23 4/4 + smoke 0,5). Bỏ `<dialog>` native — giữ div, thêm Escape + focus trap tay (tránh polyfill Safari <15.4)",
  "backend_needs": [
   "none"
  ],
  "risks": "(1) Re-render modal khi lead đổi qua `SLApp.reload`: phải giữ focus/caret/chữ đang gõ ô ghi chú như renderFeed v119-23, và KHÔNG re-render khi user đang chọn `fuAt`/`fuNote` → cần cùng cơ chế 'diff mềm' (chỉ thay khối notes + progress) thay vì innerHTML cả modal. (2) Handler `data-nsave/data-ndel/data-nvis` ở feed bind trên `list` (20-feed:462) — tái dùng phải tham số hoá root (list vs modal) không thì double-bind khi modal mở trên feed (cùng `data-ninput=id` xuất hiện 2 nơi → `querySelector` bắt nhầm ô ở feed). (3) Chip `[KQ:…]` prefix là text thuần → engine funcWebhook/AI không hiểu; ok cho v1 nhưng nên đồng thời ghi `l.last_outcome`. (4) Mở modal từ Pipeline/Chuông: `D.notes` có sẵn toàn bộ (collectionGroup) nên thấy ngay; nếu P45 sau này cắt notes 30 ngày thì modal cần `subLeadNotes` riêng. (5) Bottom-sheet vuốt đóng chạm `touchmove` với cuộn nội dung → chỉ bắt vuốt trên header.",
  "implementation_sketch": "- 20-feed.js: tách `notesBlock(l)` (HTML list + ô nhập + nút Team/Riêng tư + hàng chip KQ) và `bindNotes(root,id)` (saveNote/xoá/toggle vis/keydown Enter=lưu, Shift+Enter=xuống dòng) từ 461-525/836-851; feed gọi lại y nguyên (đường diff v78 giữ nguyên chuỗi HTML → không regression).\n- 65-charts-lead-modal.js: `openLead` chèn `notesBlock(l)` dưới 'Gợi ý phản hồi'; `updateLead` bỏ `closeModal()` → `patchModal(l)` cập nhật tại chỗ (progress bar, chip SLA, hẹn); đặt `modalLeadId=id`, `closeModal` reset; hàm `refreshModal()` được 90-boot gọi cuối `SLApp.reload` nếu `modalLeadId` và lead đó đổi (so sánh `stage/assignee/fu_at/notes.length`), chỉ thay khối notes/progress, giữ focus/caret như renderFeed.\n- 90-boot.js:55: nới điều kiện `noteTyping && (feed || modal đang mở)` không hoãn reload.\n- app.css: @media ≤639px `.modal-bg{align-items:flex-end;padding:0}.modal{max-height:92vh;border-radius:18px 18px 0 0}` + header sticky co; Escape đóng + focus trap trong `openLead`; `#asstHi` ẩn khi `modalBg.show` (toggle class body).\n- tools/smoke.js thêm check: mở modal lead demo → có `[data-ninput]` + chip KQ; lưu ghi chú → không đóng modal."
 },
 {
  "id": "P49",
  "exists": "partial",
  "exists_evidence": "Có: tools/smoke.js (feed cap/search/pipeline/outreach 19 check, `pageerror` bắt lỗi JS dòng 40, chỉ hash feed/pipeline/outreach dòng 56/82/88, không viewport mobile, không CURRENT_ROLE), eslint trong build (CLAUDE.md v119-17), Playwright test notes ad-hoc. KHÔNG có: `ls tools/` = build.mjs, gen-globals.mjs, smoke.js — không có `tools/unit`; grep -i 'rules-unit|emulator|rules-test' CLAUDE.md = 0 (mọi patch Rules đều node-script chèn block rồi deploy thẳng); worker.mjs chỉ có `--capture` (dòng 946-949), không `--selftest`; không screenshot diff.",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "5–7 ngày: (1) rules-test ~30 case + `deploy-rules.sh` 2 ngày (cần anh dump `firestore.rules` đầy đủ — em chỉ có trích đoạn trong CLAUDE.md); (2) smoke mở rộng view/mobile/role/CSP 1 ngày; (3) unit helper node:test 1 ngày (cần expose helper ra `window.SL_TEST` hoặc tách file vì part nằm trong IIFE); (4) worker `--selftest` + fixture 1 ngày; (5) screenshot diff 0,5–1 ngày (optional)",
  "backend_needs": [
   "rules",
   "worker"
  ],
  "risks": "(1) Rules test chạy trên emulator ở Cloud Shell (Java + firebase-tools) — chậm/nặng, anh không quen; nếu chỉ chạy được ở máy em thì gate 'PASS mới deploy' không thực thi được trong quy trình copy-dán hiện tại → cần script 1 lệnh `bash deploy-rules.sh`. (2) Helper trong IIFE (normPhoneVN 20-feed:680, parseTS, fold, oaMatrixOf…) không export → unit test phải hoặc expose `window.SL_TEST={...}` trong build dev, hoặc esbuild extract bằng regex — rủi ro test không đúng bản chạy thật. (3) Fixture HTML FB lỗi thời → test xanh giả; ghi ngày capture, coi là regression-guard cho parser chứ không phải chứng minh FB hiện tại. (4) smoke với `CURRENT_ROLE=sales` ở MODE demo: gating dựa `roleIsSuper`/SUPER_ONLY_VIEWS (10-core-overview:48-50) đọc từ CURRENT_USER → cần hook demo set role trước boot. (5) Thêm case mới mỗi khi thêm collection — kỷ luật; nếu quên thì test không bắt được lỗ mới (chấp nhận, giống eslint).",
  "implementation_sketch": "- Backend `~/firebase-s13/rules-test/`: `package.json` (@firebase/rules-unit-testing, node:test), `tenancy.test.mjs` với 30 case: sales A đọc lead/note/outreach_log/outreach_stats B → deny; update `brand/score/assignee_uid` bởi sales → deny; users self-escalate role/active/brand/leadFrom → deny (đối chiếu affectedKeys CLAUDE.md:79); note create sai brand/vis → deny; fb_accounts/workers/worker_config non-super → deny; pii_log giả uid → deny (khi P43 có). `deploy-rules.sh`: `firebase emulators:exec --only firestore 'npm test' && firebase deploy --only firestore:rules`. Giao bằng LỆNH Cloud Shell.\n- tools/smoke.js: vòng `for hash of ['overview','feed','pipeline','roi','history','scanned','users','outreach','alerts']` check `#view` không rỗng + 0 pageerror; thêm `page.on('console')` bắt 'Content Security Policy' + `window.onunhandledrejection`; viewport 390×844 cho feed/pipeline/outreach; chạy lần 2 với `window.SL_CONFIG={MODE:'demo',ROLE:'sales'}` → 80-rbac-auth đọc ROLE demo → kiểm nav item super-only ẩn, `location.hash='users'` bị ép về overview.\n- tools/unit/*.test.mjs: build.mjs thêm cờ `--expose-test` sinh `assets/js/app.test.js` gắn `window.SL_TEST={normPhoneVN,fmtPhoneVN,parseTS,fold,oaMatrixOf,oaCapsOf,pvAgeMs,escJsAttr}`; test node:test chạy qua Playwright evaluate.\n- worker.mjs: `--selftest` nạp `fixtures/{comment_permalink.html, challenge_dialog.html, graphql_react_ok.json, graphql_soft_block.json}` (ghi ngày capture) → chạy `findCommentEl` (regex ranh giới comment_id), `detectChallengeStrict`, `deepHasTruthy` → in PASS/FAIL, exit code; thêm vào run.bat trước khi start.\n- Screenshot diff: smoke chụp 17 view vào `tools/shots/`, so pixelmatch với baseline (tuỳ chọn, cảnh báo không chặn build)."
 },
 {
  "id": "P50",
  "exists": "none",
  "exists_evidence": "live.js:539 `signInGoogle:()=>signInWithPopup(auth,new GoogleAuthProvider()).catch(mfaCatch)` — chỉ popup; grep `getRedirectResult|signInWithRedirect|FBAN|FBAV|webview` trong live.js + src/app = 0. Ô login `.ag-in` font-size **14px** (80-rbac-auth.js:35) → iOS tự zoom. navigator.userAgent chỉ dùng ghi session (live.js:276), chưa dò in-app browser.",
  "feasible": "yes",
  "realistic_effort": "S",
  "days_estimate": "1–1.5 ngày (0.5 code + 0.5–1 test thật trên Zalo/FB iOS+Android, không giả lập được bằng Playwright)",
  "backend_needs": [
   "none"
  ],
  "risks": "(1) `signInWithRedirect` trên Safari/iOS 16.1+ bị ITP chặn cookie bên thứ 3 khi authDomain ≠ domain app — hiện `connect-src`/`frame-src` netlify.toml:24 đang cho `smartlead-z15.firebaseapp.com` ⇒ authDomain là firebaseapp.com, KHÔNG phải domain riêng như mô tả đề xuất → redirect có thể lặp/không trả user; phải làm 1 trong 2: proxy `/__/auth/*` qua Netlify (_redirects) hoặc đổi authDomain sang smartlead.z15miracle.com.vn (thêm Authorized domain trong Firebase Console — việc backend nhỏ, anh tự bấm). (2) MFA: `mfaCatch` (live.js:264) bắt lỗi từ promise popup; với redirect lỗi `auth/multi-factor-auth-required` trả về ở `getRedirectResult` lúc boot → phải nối cùng resolver để không mất bước OTP. (3) Zalo in-app webview Android nhiều bản CHẶN luôn Google sign-in (disallowed_useragent 403 từ Google) — nên nút 'Mở bằng trình duyệt' là đường chính, redirect chỉ là nỗ lực phụ. (4) Regression: getRedirectResult chạy trước onAuthStateChanged → watchdog 15s (90-boot) có thể hiện banner 'net' nhầm nếu redirect tốn thời gian; cần gọi trước khi bật watchdog.",
  "implementation_sketch": "- live.js: thêm `isInApp()` = /FBAN|FBAV|Zalo|Line\\//.test(UA) hoặc iOS Safari standalone; import `signInWithRedirect,getRedirectResult`; `signInGoogle` = isInApp()?signInWithRedirect:signInWithPopup (popup lỗi `auth/popup-blocked` → fallback redirect); trong boot() TRƯỚC onAuthStateChanged gọi `getRedirectResult(auth).catch(mfaCatch)`.\n- 80-rbac-auth.js (SLAuth.show): nếu isInApp() → banner nhỏ 'Đang mở trong Zalo/Facebook — bấm Mở bằng Safari/Chrome' + nút chép link (navigator.clipboard, fallback prompt); Android thêm intent:// URL `intent://smartlead.z15miracle.com.vn/app.html#Intent;scheme=https;package=com.android.chrome;end`.\n- CSS inline 80-rbac-auth.js:35: `.ag-in` font-size 14px → **16px** (+ ô OTP 2FA nếu có) — hết auto-zoom iOS.\n- Backend (anh tự bấm, không cần lệnh): Firebase Console → Auth → Authorized domains đảm bảo có domain Netlify; nếu đi đường proxy thì `_redirects` thêm `/__/auth/* https://smartlead-z15.firebaseapp.com/__/auth/:splat 200` + đổi `authDomain` trong firebase-config.\n- smoke.js: thêm check ô login font-size ≥16px + tồn tại nút 'Mở bằng trình duyệt' khi giả UA Zalo."
 },
 {
  "id": "P51",
  "exists": "none",
  "exists_evidence": "CLAUDE.md:240/291 — `stepNickAdspower` nạp lead MỚI theo `leads where brand==code orderBy detected_at desc` (chỉ lọc junk + matrix). grep `do_not_contact|outreach_queue|queue_preview|first_care` trong worker.mjs, live.js, 45-outreach.js = 0 (45-outreach.js chỉ có chữ 'opt-out' ở dòng 421 là text tĩnh). Không có field opt-out/DNC trên lead.",
  "feasible": "yes-with-backend",
  "realistic_effort": "S",
  "days_estimate": "2 ngày (1 ngày engine + lệnh Cloud Shell, 0.5 FE preview, 0.5 nghiệm thu)",
  "backend_needs": [
   "cf",
   "index",
   "rules"
  ],
  "risks": "(1) Đọc 30 lead/brand/nick-tick thay vì 1–vài → Firestore read tăng; với tick 5' × ~10 brand đang bật thì nhỏ (<100k read/ngày), nhưng nếu tính 'đã chạm cùng uid/phone 30 ngày' bằng query `outreach_map`/`outreach_threads` per-lead thì ×30 — nên tra bằng 1 batch `getAll` theo uid (outreach_map keyed uid đã có). (2) Đói lead lạnh: đúng như đề xuất, cần giữ tỉ lệ (vd cứ 5 lead thì 1 lead warm/cold nếu matrix cho phép) — không thì brand chỉ toàn hot chạm, ma trận Bảo thủ bị 'vô hiệu' ngầm. (3) Không rủi ro nick thêm — van/pace không đổi, chỉ đổi THỨ TỰ. (4) `outreach_queue_preview` ghi mỗi tick = thêm 1 write/brand/tick + 1 onSnapshot ở FE: phải đưa qua `oaSignature()` (v119-37) nếu không sẽ tái phát 'giật giật' tab Tiếp cận. (5) Cần thêm Rules read cho collection mới (brandCode==myBrand) — tuy Tiếp cận hiện là SUPER_ONLY nên chỉ cần isSuperAdmin. (6) `do_not_contact` mới là field lead → Rules `leads` update phải cho admin ghi (kiểm whitelist field).",
  "implementation_sketch": "- outreach.js (LỆNH Cloud Shell, patch line-based như LỆNH R): trong `stepNickAdspower` thay vòng lặp lead-đầu-hợp-lệ bằng `pickNextLead(brand)`: query `leads where brand==code orderBy detected_at desc limit 30` → lọc junk/dropped/do_not_contact/đã có `outreach_threads/{leadId}` → điểm = score + (temp==='hot'?20:0) + (author_url?15:0) + (!first_care_at?10:0) − (ageH>72?15:0) − (outreach_map có uid trong 30 ngày?30:0) → sort desc; quota fairness: đếm `outreach_stats/{brand}__day` đã phân bổ, cứ mỗi 5 lead ưu tiên chèn 1 lead warm/cold đứng đầu nhóm đó.\n- Cùng chỗ: sau khi chọn, ghi `outreach_queue_preview/{brandCode}` = {brandCode, at, items:[{leadId,name,temp,score,steps:[…]}] top 5 còn lại} (set merge, 1 write/tick).\n- Rules: `match /outreach_queue_preview/{code}` read=isSuperAdmin, write=false. Index: dùng lại `leads(brand,detected_at)` đã có (CLAUDE.md E7); không cần index mới.\n- live.js `lazyOutreach`: thêm onSnapshot `outreach_queue_preview` → `D.outreachQueue`; đưa id+leadId list vào `oaSignature()`.\n- 45-outreach.js thẻ brand: khối 'Hàng đợi kế tiếp' 5 dòng (tên · chip nhiệt độ · icon bước ❤️💬🤝📩), bấm → openLead(id).\n- FE lead modal (65-charts-lead-modal.js): checkbox 'Không tiếp cận tự động' → `SL_FB.setLeadFlag(id,{do_not_contact:true})` (setDoc merge); Rules leads cho admin/super ghi field này."
 },
 {
  "id": "P52",
  "exists": "none",
  "exists_evidence": "Ngành là chuỗi tự do: 50-config-views.js:112 default `'Đa ngành'`; live.js:106 gom `l.industry||'Khác'` thô; 40-roi.js:54 `R.industries[l.industry]` AOV key = chuỗi thô; nguồn nối lead bằng TÊN `bySrc.get(s.name)` live.js:112 (không có source_id — grep `source_id|sourceId` = 0). grep `taxonomy|INDUSTRIES` trong src/app + live.js = 0. Prompt chấm điểm nằm ở backend (~/firebase-s13 scheduledScan/manualScan) — CLAUDE.md không ghi enum ngành.",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "4–5 ngày (1 taxonomy+prompt structured output, 1 CF backfill dry-run + bảng đối chiếu, 1.5 FE chip/lọc/ROI/chart, 0.5–1 nghiệm thu với dữ liệu thật)",
  "backend_needs": [
   "cf",
   "data-migration",
   "rules"
  ],
  "risks": "(1) Backfill đổi `industry` hàng nghìn lead → trigger onSnapshot leads ở mọi client đang mở (cửa sổ 500) → 1 đợt rebuild lớn; chạy ngoài giờ + batch 400/lượt. (2) AOV theo ngành ở `roi_settings` (40-roi.js) keyed theo chuỗi cũ → sau map phải migrate luôn key `industries{}` của từng brand, không thì AOV riêng mất tác dụng âm thầm. (3) Đề xuất 'chuẩn hoá nguồn theo id' là RỘNG hơn tiêu đề: lead hiện chỉ có `source` (tên) — thêm `source_id` cần sửa pipeline ghi lead ở backend + backfill match theo tên (tên trùng/đổi tên → mất khớp) → tách thành việc riêng, không nhét chung 'S'. (4) Ép enum bằng structured output: model có thể chọn sai ngành gần nhau (Spa/Clinic vs Thẩm mỹ) → giữ `industry_raw` + `industry_conf`, chỉ đổi chip khi conf ≥0.6. (5) 'Lead ngoài ngành → gợi ý chuyển brand' cần đọc hồ sơ ngành các brand khác — super-only, và lead cross-brand đụng tenancy Rules (chuyển brand = superadmin thao tác tay). (6) Hiệu năng FE: bộ lọc ngành ở feed phải đi qua đường diff feed + `l.__q` cache, không thêm rebuild.",
  "implementation_sketch": "- Taxonomy: doc `config/public` thêm `taxonomy:{industries:[{id:'spa',label:'Spa/Clinic',services:[...]},…×12]}` (Rules config/public read all đã có, write super).\n- Backend (lệnh Cloud Shell, ~/firebase-s13): prompt scoring thêm `response_format` JSON schema enum industry_id/service_id + `industry_conf`; ghi `industry` = label chuẩn, `industry_id`, `industry_raw`. CF `backfillIndustry` HTTP (super-only, `?dry=1`) : quét leads theo brand, map bằng bảng alias (lowercase/bỏ dấu fold) + fallback OpenAI batch 50 lead/lượt, xuất CSV đối chiếu; chạy thật theo batch 400 writes. Migrate `roi_settings.industries` key theo cùng bảng.\n- FE: 10-core-overview thêm `TAX = D.taxonomy` (live.js buildData đọc config/public); chip ngành dùng label chuẩn + màu theo id (20-feed.js:825, 30-pipeline.js:38/315, 65 modal:71); feed rail/filter thêm `<select>` ngành (lọc trong renderFeed, reset cap 200); 40-roi.js: bảng AOV lặp theo `TAX.industries` (hết 'top 12 bị cắt'), `roiAovOf` tra theo `industry_id`; chart 'Lead theo nguồn' (65-charts:34 dùng label industry của source — sửa label = `s.name`).\n- Chip 'ngoài ngành': nếu `l.industry_id ∉ brand.industries[]` (brand đã có hồ sơ AI ở 85-users-admin) → chip xám + tooltip; nút 'Chuyển brand' chỉ super (dùng lại setDoc leads.brand).\n- Nguồn theo id: tách việc riêng — pipeline ghi `source_id` khi tạo lead, live.js:112 `bySrc` ưu tiên `source_id` fallback name."
 },
 {
  "id": "P53",
  "exists": "partial",
  "exists_evidence": "CSP đã có nhưng còn `script-src 'self' 'unsafe-inline'` (netlify.toml:24, có sẵn ghi chú rollback Report-Only). 19 sink `onclick=` inline (20-feed 11, 60-scan 4, 70-shell 2, 50-config 1, 65-modal 1) + `escJsAttr` 7 chỗ; pattern delegate `data-ndel` đã tồn tại (20-feed). live.js: 6 khối `MANUAL_SCAN_URL` lặp (dòng 446/455/625/635/646/661), 6 `fetch(` không AbortController (grep `signal|timeout` = 0 ở fetch); `guard()` live.js:413 chỉ bắt permission-denied → SLAuth.denied, không kèm tên thao tác. `providerData` đã dùng 1 chỗ (live.js:597 hasPw). grep `AppCheck|sendEmailVerification|emailVerified` = 0. Backend: CLAUDE.md không ghi rate-limit assistantChat/idempotency manualScan (grep `rate.limit|idempot` chỉ ra kết quả không liên quan).",
  "feasible": "yes-with-backend",
  "realistic_effort": "M",
  "days_estimate": "5–7 ngày (CSP+delegate 2, helper cf()+guard 1, email verify+provider badge 1, App Check 1 + 1 tuần monitoring chờ, rate-limit/idempotency CF 1 qua lệnh Cloud Shell)",
  "backend_needs": [
   "rules",
   "cf",
   "external-api"
  ],
  "risks": "(1) CSP bỏ unsafe-inline: ngoài `onclick=` còn `style=\"...\"` inline khắp template (đề xuất chỉ nói script) — chỉ siết `script-src`, giữ `style-src 'unsafe-inline'`; Firebase SDK gstatic + reCAPTCHA cần thêm `https://www.google.com https://www.recaptcha.net` frame/script khi bật App Check. Netlify tĩnh KHÔNG sinh nonce per-request → nonce cố định = vô nghĩa; thay bằng chuyển toàn bộ script inline app.html sang file + `'sha256-…'` hash do build.mjs tính. (2) App Check enforce chặn client cũ cache 1 năm (`?v=` immutable) + Playwright smoke (headless không qua reCAPTCHA) → cần debug token cho smoke + ít nhất 1 tuần monitoring; worker VPS dùng Admin SDK không ảnh hưởng. (3) Rules yêu cầu `email_verified` cho provider password: Rules chỉ thấy `request.auth.token.email_verified` — user Google có sẵn true; user email cũ CHƯA verify sẽ bị khoá ngay khi deploy Rules → phải gửi verify trước + grace period. (4) Timeout 20s cho `manualScan`: bước nhận jobId thôi (watchJob nền đã có) — nếu CF cold start >20s (gen2 asia-southeast1 thường 3–8s) sẽ báo lỗi giả → 30s an toàn hơn. (5) Regression: chuyển 19 onclick sang delegate đụng đường diff feed (renderFeed thay thẻ giữ focus) — listener gắn ở #view (1 lần) nên an toàn, nhưng modal lead (65) render ngoài #view → cần delegate ở document.body. Smoke phải bấm thử từng nút copy.",
  "implementation_sketch": "- FE (1 zip): 20-feed/60-scan/70-shell/50-config/65-modal: `onclick=\"__slCopy('..')\"` → `data-act=\"copy\" data-v=\"${esc(v)}\"`; 1 listener `document.body.addEventListener('click', e=>{const b=e.target.closest('[data-act]');…})` ở 90-boot; xoá `escJsAttr`. build.mjs: gom script inline app.html ra file hoặc tính sha256 → ghi vào netlify.toml `script-src 'self' 'sha256-…' https://www.gstatic.com https://apis.google.com`; thêm header `Content-Security-Policy-Report-Only` với `require-trusted-types-for 'script'` 2 tuần; smoke.js: bắt `securitypolicyviolation` event → fail nếu có.\n- live.js: helper `cf(name, body, {timeoutMs:30000, retries:1})` = AbortController + Bearer idToken + map lỗi {401:'Phiên hết hạn, đăng nhập lại',403:'Không có quyền',5xx:'Máy chủ bận',AbortError:'Mạng chậm'} → thay 6 khối MANUAL_SCAN_URL (manualScan/saveFbAccount/deleteFbAccount/assistantChat/…); `guard(p, label)` thêm tham số label → toast `Không thể ${label}: …`; SLAuth signup gọi `sendEmailVerification`; `providerData[0].providerId` + `emailVerified` đưa vào `users/{uid}` (setMyProfile) để thẻ Chờ duyệt (85-users-admin) hiện badge.\n- App Check: `initializeAppCheck(app,{provider:new ReCaptchaEnterpriseProvider(SITE_KEY),isTokenAutoRefreshEnabled:true})` trong live.js boot (SITE_KEY không secret, vào firebase-config); `self.FIREBASE_APPCHECK_DEBUG_TOKEN` khi MODE demo/smoke.\n- Backend (lệnh Cloud Shell): Rules `users` create/read gate `request.auth.token.firebase.sign_in_provider!='password' || request.auth.token.email_verified==true` (sau grace); App Check monitoring → enforce Firestore + CF (`enforceAppCheck:true` trong onRequest options); `assistantChat` thêm counter `rate/{uid}__{hour}` transaction ≤20; `manualScan` nhận header `Idempotency-Key`, cache 60s trong doc `scan_idem/{key}`."
 },
 {
  "id": "P54",
  "exists": "none",
  "exists_evidence": "grep -i `stringee|callbot|tel:|call_log|callWebhook|DNC` trong src/app, live.js, CLAUDE.md = 0. Feed chỉ có 'chép SĐT' (20-feed.js, `__slCopy`), `zaloDial` là code chết (CLAUDE.md v119-17). Kết nối (Tích hợp) là 4 card tĩnh (brief config-views). market.md:62-65 xác nhận NĐ 91/2020 + NĐ 330/2026 (phạt từ 19/8/2026, DNC 80–100tr).",
  "feasible": "hard",
  "realistic_effort": "L",
  "days_estimate": "GĐ1 (click-to-call + webhook tóm tắt): 12–15 ngày; GĐ2 (callbot ≤5 phút): +15–20 ngày + pilot 4–6 tuần; tổng L→XL nếu tính cả pháp lý/DNC",
  "backend_needs": [
   "cf",
   "secret",
   "external-api",
   "rules",
   "index",
   "data-migration"
  ],
  "risks": "(1) PHÁP LÝ là rủi ro lớn nhất: callbot gọi lead quét từ group = cuộc gọi quảng cáo chưa có đồng ý (NĐ 91/2020, phạt 10–40tr; gọi số trong DNC 80–100tr từ 19/8/2026); tra DNC 5656 cần đăng ký tên định danh với Cục ATTT — SmartLead hiện KHÔNG có bảng opt-out/DNC toàn hệ thống (grep 0) → GĐ2 chỉ khả thi khi có consent thật (lead đã phản hồi inbox/comment: stage `responded`) chứ không phải 'score ≥85'. (2) Chi phí: StringeeX/callbot tính phút + phí kích hoạt; brand nhập API key StringeeX → key phải vào Secret Manager qua CF (pattern saveFbAccount đã có) — không để trong `brands` doc. (3) Bản ghi/transcript cuộc gọi = dữ liệu cá nhân nhạy cảm (giọng nói) → lưu ở đâu, TTL, quyền nghe (sales chỉ nghe lead của mình) — Rules mới + Storage; xung đột Luật BVDLCN 2026 nếu không có thông báo ghi âm đầu cuộc gọi. (4) WebRTC widget Stringee = script bên thứ 3 → phải nới CSP (`script-src`, `connect-src wss://*.stringee.com`, `media-src`) — mâu thuẫn với P53 đang siết. (5) Realtime: note hệ thống + `fu_at` từ webhook đi đường `leads/{id}/notes` đã có (funcWebhook pattern) — an toàn với diff feed; nhưng `tel:` fallback trên mobile không đo được kết quả → cần bước hỏi kết quả sau visibilitychange (đề xuất v5/M trong brief feed:126). (6) Callbot tiếng Việt hay bị cúp máy → tỷ lệ chuyển đổi thấp làm xấu thương hiệu brand; pilot phải có nhóm đối chứng.",
  "implementation_sketch": "- GĐ1 FE: 50-config-views 'Tích hợp' → card 'Tổng đài (StringeeX)' (super/admin brand): nhập API key → `SL_FB.saveIntegration('stringee',{key})` = HTTP CF `saveIntegration` (pattern saveFbAccount: verify admin → Secret Manager `stringee-<brand>` → metadata `integrations/{brand}__stringee`). 20-feed/30-pipeline/65-modal: nút 📞 cạnh pill SĐT → nếu brand có stringee → mở widget WebRTC (script Stringee tải lazy, CSP nới); không thì `tel:`.\n- CF `callWebhook` (onRequest asia-southeast1, verify secret Bearer): nhận event kết thúc cuộc gọi + URL ghi âm/transcript → OpenAI tóm tắt 3 dòng + phân loại {no_answer|callback|interested|not_interested} → addDoc `leads/{id}/notes` (shape v119-26: vis:'team', by_uid:'engine', at ms) + `calls/{callId}` {brand, leadId, by, duration, result, recordingUrl} + set `fu_at` (+1h/+3d) → feed/pipeline realtime tự hiện. Rules `calls` read brand-scoped (giống outreach_log), write=false. Index `calls(brand, at desc)`.\n- GĐ2 (chỉ khi có consent): CF `callbotTick` onSchedule 1' quét lead `stage=='responded' && phone && !dnc && brand.callbot.on` trong 8–20h, van `callbot_usage/{brand}__day` ≤N; gọi Stringee/FPT.AI API kịch bản tư vấn; kết quả → cùng đường callWebhook → pipeline stage + giao sales (dùng thuật toán 'ĐANG RẢNH NHẤT' 20-feed).\n- Tiền đề bắt buộc trước GĐ2: collection `dnc/{phoneHash}` opt-out toàn hệ thống (áp cả outreach) + import DNC 5656 + đăng ký tên định danh; thẻ brand toggle callbot chỉ super."
 }
]