# LỆNH F (13/09/2026) — Đợt 2.4 PA-5 "SĐT gọi được ngay": gom SĐT/bình luận CÙNG TÁC GIẢ vào lead + CF `zaloCheckLead` ("Dùng số này" · "Kiểm Zalo")

> Sau LỆNH E (mốc trên `index.js` có marker `LENH E`). Chưa chạy E → script tự DỪNG, không ghi gì. Đi cùng zip FE **v119-92 / v120-esm-ap**.
> File: `lenh-2026-09-13-f.sh` (KHỐI 1, nhúng `_lf_patch.cjs` · `_lf_contactcf.js` → `contactcf.js` · `_lf_after.mjs`) + `-f-patch.cjs` (9 mốc, 1 file `index.js`, marker `LENH F`, fail-closed NGUYÊN TỬ) + `-f-contactcf.js` (CF mới) + `-f-after.mjs` + harness `harness-lenhf-2026-09-13.mjs` **29/29**.
> **sha256 `lenh-2026-09-13-f.sh` = `39e1472659d529282cb30d889abe72fc64002d1bbc7bb201445b5496e628614e`** (28.672 byte / 201 dòng). Dry-run fake HOME (mã sau E xuất từ harness, có `lib/zaloCheck.js` stub) + fake firebase/gcloud ×2 exit 0 (lần 2 idempotent, giữ `.bak` gốc qua `_lf_backup_ts`), 0 `!` ngoài heredoc, không shebang. Bài học lần dry-run đầu: `node --check file.js.new` → Node coi đuôi lạ là CommonJS → `import` báo lỗi cú pháp → file tạm phải giữ đúng đuôi (`_lf_contactcf.js` rồi `mv` sang `contactcf.js`).

## 1. Vì sao (D-1 / D-3 / F-E1 / F-E2 báo cáo rà soát 11/09)
- SĐT chỉ trích từ CHÍNH text bài/bình luận (`enrichPhoneFromText(x.post.text)`), trong khi người mua VN hay viết "ib mình" rồi để số ở **bình luận kế** (chủ bài tự bình luận dưới bài mình — LỆNH #31 đã nhận diện `self_comment` nhưng chỉ để LOẠI), hoặc khách X bình luận 2 lần (lần 2 mới có số) → lead có nhu cầu mà **không có số để gọi**; "Nên gọi tiếp theo" +8 khi có SĐT → số càng đủ, hàng đợi gọi càng đúng.
- Web chỉ bôi số khớp `l.phone` → bài có 2 số hoặc máy chủ sót thì sales không thấy; không có cách đặt số khác làm SĐT lead ngoài sửa tay.
- eKYC: lead lạnh đã hoãn từ #48 (`zalo_defer`) — nhưng sales muốn kiểm ngay khi bắt đầu chăm thì chưa có nút.

## 2. Thay đổi
### 2.1 `index.js` (patch `_lf_patch.cjs`, 9 mốc, marker `LENH F`)
- **F1** ctx bài cha (`cB`) thêm `parentPostId`, `parentX` (ứng viên còn trong lượt). **F2** bộ đếm `cmtExtraF`, `phoneCmtF`.
- **F3** trước vòng bình luận: `authKeyF(cm)` = profile key (LỆNH #31 `__slProfileKey31`) hoặc `n:<tên bỏ dấu>` khi KHÔNG ẩn danh (`__SL_ANON31`); `byAuthF` (bài|tác giả → bình luận); `extraF(cm, kk)` = ≤3 bình luận khác cùng tác giả (mỗi ≤220, tổng ≤600); `selfExtraF` (bài → bình luận của chủ bài).
- **F4** trong vòng (sau `self_comment`): chủ bài → `selfExtraF`; người khác → `comment.text_extra = extraF(...)` (đếm `cmtExtraF`). Bình luận chủ bài vẫn đi `scanned_posts` `self_comment` (không thành lead) như cũ.
- **F5** sau vòng: `selfExtraF` → ứng viên còn trong lượt (quét tay/backfill/Quét ngay) → `x.post.text_extra`; **bài đã thành lead ở lượt trước** (đường gieo/gặt theo lịch: bình luận về sau 1–2 lượt) → `leads.where('post_url','==',parentUrl).limit(6)` (mọi brand dùng chung; bỏ comment-lead) → `text_extra` (nối, không nhân đôi) + nếu lead chưa có SĐT: `enrichPhoneFromText(text_extra)` → `phone`, `contact_source:'comment'`, eKYC chỉ khi lead nóng/ấm (`phone_has_zalo`, lạnh → `zalo_defer:true`, `phone_has_zalo:null`), `email` nếu trống; đếm `phoneCmtF`.
- **F6** Pha 3b: `enrichPhoneFromText(text + '\n' + text_extra)`; `contact_source` = `'post'` nếu số nằm trong bài, `'comment'` nếu từ bình luận. **F7** lead ghi `text_extra`, `contact_source`. **F8** `scans.cmtExtra`, `scans.phoneFromCmt`. **F9** `export * from './contactcf.js'`.
- Fail-closed: không khoá tác giả (ẩn danh không link) → không gom; người lạ có SĐT dưới bài → KHÔNG gán vào bài (thành comment-lead riêng với số của họ).
### 2.2 `contactcf.js` (MỚI) — CF `zaloCheckLead` (onRequest, cors, region asia-southeast1)
- Bearer idToken → `verifyCaller` (như `sources.js`): Super Admin (SUPER_EMAIL hoặc `users.role superadmin`), hoặc admin/sales **active cùng brand lead** (`brand || brand_hint`); khác → 403.
- Body `{leadId, phone?, force?}`: `phone` → `normPhoneF` (E.164 `+84…`, nhận di động 10 số `[35789]\d{8}` và **số bàn** `2\d{9}`) → ghi `phone` (+ `phone_prev`, `contact_source:'manual'`, `phone_set_by/at`, `phone_has_zalo:null`); rồi kiểm Zalo qua `checkZalo` (lib/zaloCheck.js, cache `zalo_cache` 90 ngày) khi SĐT di động chưa có kết quả hoặc `force` → `phone_has_zalo`, `zalo_defer:false`, `zalo_checked_at`; số bàn → `source:'landline'`, không kiểm. Trả `{ok, phone, registered, source, mobile, changed}`. 400 `bad_phone`/`bad_request`, 404, 401/403.
- Ghi bằng Admin SDK → KHÔNG cần Rules; `phone` vẫn trong whitelist (LỆNH C) nên web có đường dự phòng.
### 2.3 KHÔNG đổi: Rules, index, worker, outreach.js, stats.js, `lib/zaloCheck.js` (di động như cũ — số bàn chỉ nhận ở CF/web).

## 3. KHỐI 1 (dán vào Cloud Shell) — khối trong chat = `cat > /tmp/lf.sh <<'EOF_LF'` … `EOF_LF` rồi `bash /tmp/lf.sh` (nội dung = file `lenh-2026-09-13-f.sh`, sha256 ở đầu)
Thứ tự: `--check` 3 script → guard `LENH E` có / `lib/zaloCheck.js` có `checkZalo` / `LENH F` chưa (chạy lại → giữ `.bak` gốc qua `_lf_backup_ts`) → backup `index.js.bak-$TS` (+ `contactcf.js.bak` nếu đã có) → `contactcf.js` → patch (in `PATCH OK index.js (LENH F): 9 mốc`) → `--check` (lỗi → `restore()`) → (b) import test `.env` (in `IMPORT OK · scheduledScan function · manualScan function · zaloCheckLead function · normPhoneF … +84912345678 +842838221234 "" · isMobileF true false`) → (c) deploy `scheduledScan` + `manualScan` + `zaloCheckLead` (gated `Deploy complete`; lỗi in describe + cách chạy lại/quay lui) → describe 3 function.

## 4. KHỐI 2 (chỉ đọc, sau ≥ 15′ — tốt nhất 1–2 giờ ban ngày khi có bình luận)
```bash
cd ~/firebase-s13/functions && node _lf_after.mjs
```
Kỳ vọng: (1) mọi lượt sau deploy có `cmtExtra`/`phoneFromCmt` (số > 0 khi có bình luận cùng tác giả) · (2) lead 24 h: `contact_source` post/comment/manual, vài lead `text_extra`, dòng "N lead có SĐT NHỜ bình luận" · (3) tỉ lệ có SĐT bản F ↔ trước F (đo lại sau vài ngày) · (4) "Dùng số này" 7 ngày (sau khi deploy zip v119-92). Web: `describe zaloCheckLead` ACTIVE → nút Dùng/Kiểm Zalo hoạt động (URL suy từ `MANUAL_SCAN_URL` đổi tên function).

## 5. Kiểm (harness `harness-lenhf-2026-09-13.mjs` 29/29)
Dựng mã đang chạy = harness E export (mã sau #49) + patch E = mã sau E → patch F + `contactcf.js`: PATCH OK 9 mốc / idempotent / `--check` index + contactcf + after / fail-closed nguyên tử (mốc F7 lệch → không ghi) / thiếu `LENH E` → DỪNG. `scanAll` trọn vòng trên Firestore + BrightData (bài + bình luận) + LLM giả: (1) quét tay: chủ bài tự bình luận có SĐT → lead bài `phone` từ bình luận (`contact_source comment`, `text_extra`, nóng → kiểm Zalo); người lạ có SĐT → KHÔNG gán vào bài, thành comment-lead riêng (`contact_source post`); `scans.cmtExtra 1 · phoneFromCmt 1`; bình luận chủ bài vẫn `self_comment` · (2) X bình luận 2 lần → comment-lead "cần mua 5 kg" nhận SĐT từ bình luận kia (text_extra chéo, không tự chứa; cmtExtra 2) · (3) chỉ người lạ/ẩn danh → bài không phone/text_extra; 2 bình luận ẩn danh không gom · (4) gieo/gặt theo lịch: lead tạo lượt 2 (chưa SĐT) → bình luận chủ bài gặt lượt 3 → lead cập nhật SĐT + text_extra + kiểm Zalo (nóng), `phoneFromCmt 1`; lượt sau không nhân đôi; `self_comment` ghi 1 lần · (4b) lead LẠNH nhận SĐT nhưng hoãn kiểm Zalo (`zalo_defer`, 0 lượt eKYC) · (5) CF: 401 · 403 brand khác · 404 · super "Dùng số này" (ghi phone/contact_source manual/phone_set_by + kiểm ngay) · cùng số không gọi lại · sales cùng brand kiểm lead lạnh · force gọi lại · số bàn landline + `phone_prev` · 400 bad_phone · leadId có "/" → 400 · `normPhoneF/isMobileF`.

## 6. Rủi ro / ghi chú
- Gom theo tên bỏ dấu (khi không có link hồ sơ) có thể gộp 2 người TRÙNG TÊN dưới cùng 1 bài → chỉ áp trong PHẠM VI 1 BÀI + tên không ẩn danh; số gán nhầm vẫn có thể xảy ra khi 2 người cùng tên cùng bình luận 1 bài (hiếm) — web hiện rõ "SĐT lấy từ bình luận" + khối bình luận để sales đối chiếu, nút Dùng đổi số 1 chạm.
- Đường gieo/gặt thêm 1 truy vấn `leads.where(post_url)` cho mỗi bài có bình luận chủ bài (ít); `text_extra` ≤600 ký tự vào prompt AI (chỉ với ứng viên còn trong lượt).
- eKYC: SĐT từ bình luận của lead nóng/ấm → kiểm ngay (1 lượt eKYC); lạnh → chờ sales chăm (v89 quét vét) hoặc bấm "Kiểm Zalo".
- FE đi cùng: **zip v119-92 / v120-esm-ap** — bôi MỌI số (kể cả số bàn) trong bài/bình luận, số khác có nút **Dùng** (→ CF), dòng/khối **"Chủ bài bình luận thêm / Khách còn bình luận · SĐT lấy từ đây"** (`text_extra`), nút **Kiểm Zalo** (SĐT chưa có kết quả), pill SĐT nói rõ nguồn; `SL_FB.zaloCheckLead`.

## 7. KẾT QUẢ KHỐI 1 — ĐÃ CHẠY OK 13/09 13:04–13:06 VN (anh dán ảnh Cloud Shell, `exit=0`)
- sha256 khối chat khớp `39e14726…614e` · backup TS `20260913-060439` (UTC) → `index.js.bak-20260913-060439` (contactcf.js chưa tồn tại nên không có .bak).
- **PATCH OK index.js (LENH F): 9 mốc** — ctx parentPostId/parentX · bộ đếm · khoá tác giả + gom bình luận (chủ bài → text_extra bài · X nhiều bình luận → text_extra comment-lead) · cập nhật lead đã có theo post_url · Pha 3b bắt SĐT text+text_extra · lead text_extra/contact_source · scans cmtExtra/phoneFromCmt · export contactcf.js. Marker `LENH F` index.js 12 dòng · contactcf.js 52 dòng — **khớp 100 % bản dựng lại ở máy em** (harness export + patch F: 12 / 52).
- **IMPORT OK** (.env): `scheduledScan · manualScan · zaloCheckLead` = function · `normPhoneF` di động `+84912345678` ✓ · số bàn `+842838221234` ✓ · sai → `""` ✓ · `isMobileF` true/false ✓.
- **Deploy `scheduledScan` + `manualScan` + `zaloCheckLead` → Deploy complete, ACTIVE 2026-09-13T06:06Z** (URL `zaloCheckLead` = `https://asia-southeast1-smartlead-z15.cloudfunctions.net/zaloCheckLead`, cùng gốc `MANUAL_SCAN_URL` → live.js `cf('zaloCheckLead')` gọi đúng; manualScan giữ URL Cloud Run cũ).
- Web: anh đã deploy **v119-93** (bao gồm FE F v119-92: nút Dùng số này + Kiểm Zalo + khối bình luận thêm) ⇒ chuỗi F trọn từ máy chủ tới web.
## 8. KẾT QUẢ KHỐI 2 — lần 1 ĐÃ CHẠY 13/09 13:22 VN (`node _lf_after.mjs`, 16′ sau deploy) — PASS cơ chế, số còn nhỏ
- Lượt quét 6 h 108, **6 lượt sau deploy đều có `cmtExtra/phoneFromCmt`**; Σ `cmtExtra` **2** (13:10: 6 bình luận → gom 1; 13:22: 1 bình luận → gom 1 = bình luận cùng tác giả được ghép vào bài) · Σ `phoneFromCmt` 0 (chưa có bình luận chứa SĐT trong 16′ — bình thường).
- Lead 24 h 54: `text_extra` 0 (2 bình luận gom vào bài chưa thành lead / lead cũ hơn 24 h) · có SĐT 2 (3,7 %), cả 2 `contact_source` "(trước F)", kiểm Zalo có 2.
- Lead 7 ngày 560: tỉ lệ có SĐT trước F 3,6 % (n 559) · bản F 0 % (n 1) → chưa đo được, chạy lại sau vài ngày.
- "Dùng số này" 7 ngày: 0 (web v119-93 đã deploy, chờ sales dùng).
- Bước kế: chạy lại `node _lf_after.mjs` sau 1–2 ngày có bình luận; kỳ vọng `phoneFromCmt` > 0 và tỉ lệ SĐT bản F ≥ 3,6 %.
