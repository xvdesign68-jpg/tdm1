# LỆNH #43 (10/09/2026) — CHỈ ĐỌC: đo sức khoẻ mảng automation "Tiếp cận" (engine · worker · nội dung)

Mục đích: có SỐ THẬT để kết luận báo cáo rà soát automation 10/09 (`docs/rasoat-automation-2026-09-10/`): brand bật ↔ nick sống ↔ VPS online, task/thread kẹt, van hôm nay (reserve vs đã làm, có bị âm không), lý do fail/skip/paused 7 ngày, lead máy đã chạm / đã inbox / khách phản hồi, chồng lấn người thật, backlog lead chưa chạm, hiệu quả nội dung. **Không ghi gì vào Firestore, không in secret.**

Script: `docs/lenh-2026-09-10-43-auto-health.mjs` (đã dry-run với fake firebase-admin có ca lỗi mẫu). Đặt trong `~/firebase-s13/functions` (bài học: `/tmp` không thấy `firebase-admin`).

```bash
cat > /tmp/l43.sh <<'L43'
set -u
cd ~/firebase-s13/functions || { echo 'KHONG VAO DUOC ~/firebase-s13/functions'; exit 1; }
cat > _auto_health.mjs <<'EOF_H'
__DÁN NGUYÊN NỘI DUNG FILE docs/lenh-2026-09-10-43-auto-health.mjs VÀO ĐÂY__
EOF_H
node --check _auto_health.mjs && echo 'SYNTAX OK' || { echo 'SYNTAX LOI'; exit 1; }
echo "=== 7 ngày, mọi brand ==="; node _auto_health.mjs --days=7
echo "=== 30 ngày, chỉ hscl-01 ==="; node _auto_health.mjs --days=30 --brand=hscl-01
echo "=== (b) dump 4 hàm engine AdsPower để em đối chiếu (chỉ đọc, che chuỗi dài) ==="
for fn in stepNickAdspower apEnqueueFunnel apEnqueueTask tryConsume; do
  awk -v f="$fn" 'index($0,"function "f"(")||index($0,"const "f" ="){p=1;d=0} p{print NR": "$0; n=gsub(/[{]/,"{"); m=gsub(/[}]/,"}"); d+=n-m; if(d<=0&&(n||m)){p=0}}' outreach.js | sed -E 's/[A-Za-z0-9_-]{40,}/<…>/g' > ~/engine-dump-$fn.txt
  echo "$fn: $(wc -l < ~/engine-dump-$fn.txt) dòng"
done
cat ~/engine-dump-stepNickAdspower.txt ~/engine-dump-apEnqueueFunnel.txt ~/engine-dump-apEnqueueTask.txt ~/engine-dump-tryConsume.txt > ~/engine-dump-0910.txt
echo "tổng: $(wc -l < ~/engine-dump-0910.txt) dòng → tải về: cloudshell download ~/engine-dump-0910.txt (rồi upload vào chat)"
cloudshell download ~/engine-dump-0910.txt 2>/dev/null || echo "(tải tay: cat ~/engine-dump-0910.txt)"
L43
bash /tmp/l43.sh
```

Đọc output:
- Mục 2: dòng `⚠ BẬT nhưng 0 nick sống` / `⚠ nick sống nhưng VPS offline` = brand đang chờ vô ích (lead vào phễu không ai chạy).
- Mục 3: `queued chờ VPS offline/chưa gán` > 0 = task mồ côi (nick gán VPS đã tắt); `running >30′` > 0 = phiên treo (reaper 27′ mới nhặt).
- Mục 4: `thread ACTIVE trên nick TẮT/needLogin` = thread kẹt cần `_l34_move.mjs` hoặc đăng nhập lại nick; `quá hạn nextAt >2h` = engine không re-enqueue được (nick không due / brand tắt / task đang queued không ai nhận).
- Mục 5: `⚠ ÂM` = refund quá tay (worker hoàn suất bước chưa reserve) → van bị nới ngoài ý.
- Mục 7: `người thật đã chăm TRƯỚC khi máy chạm` = máy chồng lên sales (đề xuất R-1 trong báo cáo); `vai người bán/chủ bài bị chạm` phải = 0 sau LỆNH #31; `backlog … CHƯA có thread` = lead hợp lệ bị bỏ (đói) vì engine chỉ lấy lead MỚI NHẤT.
- Mục 8: `rep/sent` theo mode/cta/variant = dữ liệu A/B thật (n nhỏ thì chưa kết luận).

Mục (b) dump 4 hàm engine (stepNickAdspower · apEnqueueFunnel · apEnqueueTask · tryConsume) — em cần để chốt 3 câu hỏi báo cáo còn ghi "Suy luận": (1) engine re-enqueue khi task cùng id đang `running` có ghi đè status không; (2) lead loop AdsPower có bỏ lead đã có `assignee/first_care_at/stage≠new/closed` không; (3) van reserve lại khi re-enqueue không. `awk` cắt theo dấu ngoặc nên hàm 1 dòng cũng ra được; chuỗi ≥40 ký tự bị che (không lộ token).

Gửi em nguyên output + file dump → em điền số vào báo cáo + chốt việc cần sửa.
