# LỆNH #40 (08/09/2026) — SLA đạt theo ngưỡng riêng brand (cohort ngày phát hiện) + Rules `stage_log` cho Dòng thời gian 360°

**Vì sao cần backend** (2 mục "cần backend" trong báo cáo rà số liệu `docs/rasoat-solieu-2026-09-08/`):
1. **SLA đạt ở Bảng brand**: bộ đếm `daily_stats` (LỆNH #17) chỉ ghi bucket `careLe15/30/60/120` theo **ngày chăm** → FE phải làm tròn LÊN mốc (ngưỡng 45′ tính như 60′)
   và kẹp 100% vì tử số/mẫu số khác cohort. Sửa: CF `statsOnLead` ghi thêm **`slaN`** (lead hợp lệ phát hiện ngày D đã được chăm lần đầu) + **`slaOk`** (chăm trong
   ≤ `slaBadMin` của brand — `brands/{code}.slaBadMin` > `config/app.slaBadMin` > 60′) lên doc **ngày phát hiện**. FE v119-80 tự ưu tiên `slaOk/slaN` khi có, trước đó vẫn dùng bucket.
2. **Dòng thời gian 360°**: lead chỉ lưu mốc giai đoạn hiện tại → hành trình mới → inbox → phản hồi → hẹn chỉ hiện 1 dòng. FE v119-80 ghi **`stage_log`** (mảng
   `{from,to,at,by,undo}`, tối đa 40) RIÊNG, best-effort. Rules whitelist field vận hành (LỆNH #8) chưa có `stage_log` → user thường ghi bị từ chối (FE chỉ console.warn,
   không chặn đổi giai đoạn; Super Admin ghi được ngay). LỆNH này thêm `'stage_log'` vào whitelist.

Harness thuần `docs/harness-lenh40-2026-09-08.mjs` (dựng stats.js = LỆNH #17 + #36 + #40 rồi kiểm 21 mốc) PASS; dry-run trọn `.sh` với firebase/gcloud giả 2 lần (idempotent).
Không sửa index.js. Không có secret trong bất kỳ file nào.

## KHỐI 0 — dán 3 file vào `~/firebase-s13/functions` (heredoc quoted, không expand `!`)
```bash
cd ~/firebase-s13/functions
cat > _l40_stats.cjs <<'EOF_L40S'
<dán nguyên nội dung docs/lenh-2026-09-08-40-stats-patch.cjs>
EOF_L40S
cat > _l40_rules.cjs <<'EOF_L40R'
<dán nguyên nội dung docs/lenh-2026-09-08-40-rules.cjs>
EOF_L40R
cat > _l40_backfill.mjs <<'EOF_L40B'
<dán nguyên nội dung docs/lenh-2026-09-08-40-backfill.mjs>
EOF_L40B
node --check _l40_stats.cjs && node --check _l40_rules.cjs && node --check _l40_backfill.mjs && echo OK
```

## KHỐI 1 — chạy script (dán `docs/lenh-2026-09-08-40.sh` thành `/tmp/l40.sh` rồi `bash /tmp/l40.sh`)
```bash
cat > /tmp/l40.sh <<'EOF_L40'
<dán nguyên nội dung docs/lenh-2026-09-08-40.sh>
EOF_L40
cd ~/firebase-s13/functions && bash /tmp/l40.sh 2>&1 | tee ~/l40-$(date +%Y%m%d-%H%M).log
```
**Mong đợi**: `PATCH OK stats.js (LENH #40…)` · `SYNTAX OK` · `IMPORT OK · statsOnLead = object · slaN/slaOk theo ngày phát hiện ✓` · `PATCH OK firestore.rules: + 'stage_log'…`
· deploy `statsOnLead` + rules `Deploy complete` · describe `ACTIVE google.cloud.firestore.document.v1.written <updateTime>` · DRY: `backfill slaN/slaOk: quét N lead → D doc` +
mỗi brand `ngưỡng X′ · ngày · đã chăm · đạt (%)` · THẬT: `ĐÃ GHI D doc daily_stats` · (g) liệt kê doc có `slaN`. Anh dán output cho em check (không có secret).

**Lưu ý**: chạy lại LỆNH bao nhiêu lần cũng an toàn (patch/Rules idempotent, backfill ghi tuyệt đối). Sự kiện chăm xảy ra trong vài giây giữa lúc backfill đọc và ghi
có thể lệch 1 — chấp nhận (như LỆNH #17). Lead rác (`temp junk`) không tính (mẫu số `new` cũng không tính rác). Ngưỡng brand đổi sau này → sự kiện mới áp ngưỡng mới,
số cũ giữ nguyên (muốn đối soát lại: `node _l40_backfill.mjs --days=60`).

## Sau khi chạy
Deploy zip **v119-80** (FE đọc `slaOk/slaN` + ghi `stage_log`): Bảng brand cột "SLA đạt" hiện `≤N′` = đúng ngưỡng brand; mở lead đã đổi giai đoạn vài lần →
Dòng thời gian 360° hiện đủ "Chuyển sang … (từ …) · ai". Chưa chạy LỆNH thì v119-80 vẫn chạy bình thường (SLA theo bucket như cũ, stage_log chỉ Super Admin ghi được).
