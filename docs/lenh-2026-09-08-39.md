# LỆNH #39 (08/09/2026) — user brand thấy "Bài đã quét" của brand mình (counter `daily_stats.scanned`)

**Vì sao cần backend**: ô "Bài đã quét" ở Bảng điều khiển tính từ nhật ký `scans` — dữ liệu vận hành nội bộ (chi phí/token) chỉ Super Admin đọc (Rules)
→ user brand không có nguồn số. Giải pháp: CF **`scanStatsOnRun`** (trigger `onDocumentCreated scans/{id}`) đọc `bySource[]` của mỗi lượt, map
`sources.url → sources.brand`, cộng `FieldValue.increment` vào `daily_stats/{brand}__{YYYY-MM-DD giờ VN}`: `scanned` (bài lấy về), `scannedComments`,
`scanRuns`. `daily_stats` brand user ĐÃ đọc được theo Rules (LỆNH #17), FE v119-78 subscribe `where brandCode == brand` (1 field, không cần index).
Không sửa index.js/scanAll (không patch nội dung mong manh) — chỉ thêm file + 1 dòng export như stats.js. Harness thuần `docs/harness-scanstats-2026-09-08.mjs` 10/10.

## KHỐI 0 — dán 2 file vào `~/firebase-s13/functions` (heredoc quoted, không expand `!`)
```bash
cd ~/firebase-s13/functions
cat > scanstats.js <<'EOF_SS'
<dán nguyên nội dung docs/lenh-2026-09-08-39-scanstats.js>
EOF_SS
cat > _scanstats_backfill.mjs <<'EOF_BF'
<dán nguyên nội dung docs/lenh-2026-09-08-39-backfill.mjs>
EOF_BF
node --check scanstats.js && node --check _scanstats_backfill.mjs && echo OK
```

## KHỐI 1 — chạy script (dán `docs/lenh-2026-09-08-39.sh` thành `/tmp/l39.sh` rồi `bash /tmp/l39.sh`)
```bash
cat > /tmp/l39.sh <<'EOF_L39'
<dán nguyên nội dung docs/lenh-2026-09-08-39.sh>
EOF_L39
cd ~/firebase-s13/functions && bash /tmp/l39.sh 2>&1 | tee ~/l39-$(date +%Y%m%d-%H%M).log
```
**Mong đợi**: `SYNTAX OK` · `IMPORT OK · scanStatsOnRun = object · scheduledScan = function` · `Deploy complete` · describe `ACTIVE google.cloud.firestore.document.v1.created <updateTime>`
· DRY: `lượt quét đọc: N | không khớp brand nào: k | Σposts≠postsFetched: m | doc ngày<hôm nay: D | brand hôm nay (trước deploy): B` + 6 doc mẫu
· THẬT: `ĐÃ GHI W doc daily_stats` · (f) liệt kê doc có `scanned`. Anh dán output cho em check (che gì cũng không cần — không có secret).

**Lưu ý**: `Σposts≠postsFetched` > 0 chỉ là thống kê tham khảo (postsFetched cấp lượt có thể gồm bài không thuộc nguồn nào/đã lọc) — không phải lỗi.
Nguồn đã tắt/xoá sau này → lượt cũ của nó không quy được brand (bỏ qua). Backfill chạy lại bao nhiêu lần cũng ra cùng số cho ngày < hôm nay.

## Sau khi chạy
Deploy zip **v119-78** (FE đọc counter) → đăng nhập tài khoản brand: Bảng điều khiển ô 1 = "Bài đã quét" (14 ngày · so với 14 ngày trước),
Lead mới: thanh nhịp quét có "Bài đã quét hôm nay". Chưa chạy LỆNH thì FE vẫn hiện "Đã hẹn tư vấn" (v119-77), không bao giờ "—".
