# HANDOFF.md — Gói bàn giao để tiếp tục SmartLead ở phiên/gói Claude Code khác

> Đọc file này + `CLAUDE.md` là đủ để một phiên Claude Code MỚI (gói khác, máy khác, vẫn của anh Vinh) tiếp tục làm việc đúng "gu" mà không mất ngữ cảnh.
> **TUYỆT ĐỐI không đưa secret (token/API key/mật khẩu/IP) vào file này, vào git, vào zip, vào chat.**
> Bản này cập nhật **12/09/2026 16:30 VN** (bản 11/09 và 07/09 lưu trong lịch sử git).

## 0. TL;DR (đọc 30 giây)
- **`CLAUDE.md` = bộ nhớ toàn dự án** (bắt buộc đọc hết, ~700 dòng). Nó đi theo git repo `xvdesign68-jpg/tdm1`, **nhánh `claude/chao-ban-8d6f9r`** (repo CHỈ có nhánh này). Phiên mới **nối cùng repo + nhánh** → tự có `CLAUDE.md` + `HANDOFF.md` + `docs/` (335 file: mọi LỆNH đã chạy + script + output, fixture mã đang chạy, harness, báo cáo rà soát, dump backend đã che secret).
- **Code app THẬT KHÔNG nằm trong git.** Phiên mới cần **4 file ngoài git**: (1) zip frontend mới nhất **`smartleads17deploy-v119-90.zip`** (cây IIFE) + (2) bản riêng **`smartleads17deploy-v120-esm-an.zip`** (cây ES module), (3) **`worker.mjs` bản `2026-09-11`** (149.873 byte), (4) **`config.json`** của worker. Cả 4 nằm trong gói **`smartlead-handoff-2026-09-12.zip`** (thư mục `fe/` + `worker/`) — em gửi 12/09 chiều. Gói cũ 11/09 và 07/09 bỏ.
- **Backend (engine/Rules/.env) nằm ở Cloud Shell `~/firebase-s13/functions`** — phiên mới KHÔNG cần file: mã đang chạy TRƯỚC LỆNH #48 đã được dựng nguyên văn trong git (`docs/lenh-2026-09-12-48-fixture/` + dump `docs/lenh-2026-09-12-47-dump/`), bản SAU #48 = fixture + `docs/lenh-2026-09-12-48-patch.cjs` áp lên (harness làm đúng việc đó). Cần thêm gì thì soạn LỆNH dump (mục 6, bước 3).
- **Secret KHÔNG bao giờ qua chat.** Chúng ở Secret Manager / `functions/.env` / VPS. Phiên mới chỉ cần biết *chúng ở đâu* (mục 4). **3 số BrightData dashboard (trần API, đơn giá, có notify không) KHÔNG phải secret** — anh cho thẳng trong chat.
- **Việc đang dở lúc bàn giao**: **LỆNH #48 (LỆNH A "AI hardening") HOÀN TẤT 12/09** (KHỐI 1 15:53 + KHỐI 2 PASS 16:07 VN). **Kế tiếp theo kế hoạch anh đã chốt: LỆNH B (hướng B "1 group nhiều brand" + tốc độ + PC-4) → LỆNH C → zip FE → Đợt 2/3.** **LỆNH B: KHỐI 1 ĐÃ CHẠY OK 12/09 19:11 VN (8 function ACTIVE + Rules `lead_links` + TTL `outreach_locks`; `docs/lenh-2026-09-12-b.md` mục 7) + bước (e) tagLeadBrand 19:28 VN → KHỐI 1 trọn 9 function; **KHỐI 2 ĐÃ CHẠY 19:34 VN — PASS cơ chế** (mục 8) + **LẦN 2 13/09 04:18 VN PASS** (mục 9: seen.brands 156/156 sau vá 4b, 18 lead sau B đúng dấu, 9 h đêm ổn định); **còn duy nhất khối 4c `node _lb_lag.mjs 24` (file đã tạo trên Cloud Shell; lần 1 04:37 VN chưa đủ mẫu n 7) chạy lại 11–12 h hoặc 14–16 h VN 13/09 chốt LAG ban ngày** (`docs/lenh-2026-09-12-b-lag.mjs`; PASS khi ≥20 mẫu, trung vị ≤8′, p95 ≤25′)**; 3 số BrightData chưa có → mặc định an toàn (sàn 5′, không notify, record lỗi coi là tính tiền), có số thì sửa `.env` (mục 0 của doc). Phía anh còn: nghiệm thu worker `2026-09-11`, deploy zip v119-89, đăng nhập lại 4 nick.

## 1. Bản đồ "cái gì ở đâu"
| Thành phần | Nơi ở THẬT | Có trong git tdm1? | Phiên mới lấy bằng cách |
|---|---|---|---|
| Bộ nhớ dự án (`CLAUDE.md` + `HANDOFF.md`) | git repo `xvdesign68-jpg/tdm1` | ✅ CÓ | Tự có nếu nối cùng repo; không thì copy từ gói zip |
| **Frontend SmartLead** (app.html, `src/app/*.js` 16 part, `assets/js/live.js`, `assets/css/*`, `tools/build.mjs`, `tools/smoke.js`, eslint, `_redirects`, `netlify.toml`, `sw.js`, `manifest.webmanifest`) | Máy anh (source gốc) + **zip khứ hồi** (zip CHỨA nguyên source, `_redirects` chặn công khai `/src /assets/js /tools`) | ❌ KHÔNG | `fe/smartleads17deploy-v119-90.zip` (+ `v120-esm-am`) trong gói → giải nén vào thư mục làm việc |
| **Worker AdsPower** (`worker.mjs`, `config.json`, `run.bat`, `capture.mjs`, `install-autostart.bat`, `serviceAccount.json`) | VPS Windows `vps-1` | ❌ KHÔNG | `worker/worker.mjs` (= 2026-09-11) + `worker/config.json` trong gói; `run.bat`/`capture.mjs`/SA key KHÔNG cần (ở VPS) |
| **Engine backend** (`index.js` scanAll, `lib/scorer.js`, `lib/scraper.js`, `lib/config.js`, `lib/filter.js`, `lib/multitouch.js`, `lib/zaloCheck.js`, `lib/alerts/*`, `outreach.js`, `content.js`, `stats.js`, `push.js`, `admin.js`, `cfgpriv.js`, `fbaccounts.js`, `firestore.rules`, `.env`) | Cloud Shell `~/firebase-s13/functions` (+ `~/codebase2` Sheet/`tagLeadBrand`, `~/smartlead-zalo-fn` Zalo) | ❌ KHÔNG (nhưng ĐÃ DUMP) | `docs/lenh-2026-09-12-47-dump/` (5 file text 12/09, che secret) + fixture `docs/lenh-2026-09-12-48-fixture/` (index.js 148–908 · lib/scorer · lib/scraper · lib/config · `stub48.mjs` Firestore giả) + `docs/lenh-2026-09-11-46-fixture/` + `docs/lenh-2026-09-10-43-engine-dump.txt` (outreach.js) + `docs/lenh-2026-09-11-44a.md` |
| **Lịch sử LỆNH** (`docs/lenh-*.md` + `.sh/.cjs/.mjs`) · **script tái lập FE** (`docs/fe-*.py`, `docs/fe-2026-09-07-v119-61/` m1–m31 + `lib.py` + README) · **harness** (`docs/harness-*.mjs`) · **báo cáo rà soát** (`docs/rasoat-2026-09-03/`, `rasoat-ui-2026-09-07/`, `rasoat-solieu-2026-09-08/`, `rasoat-automation-2026-09-10/`, **`rasoat-scan-2026-09-11/`** gồm thiết kế B) | git repo `tdm1` | ✅ CÓ | Tự có — nguồn để soạn LỆNH/patch mới đúng pattern |
| Secrets (func token, LLM_API_KEY, FUNC_WEBHOOK_SECRET, serviceAccount.json, BrightData/Telegram, cookie nick) | Secret Manager / `functions/.env` / VPS | ❌ KHÔNG (đúng) | Không cần — phiên mới chỉ thao tác *tham chiếu* |

> ⚠ **Repo tdm1 có rác cũ:** `index.html`, `css/styles.css`, `js/main.js`, `fb-worker/*` là **scaffold v0.1 CŨ, không dùng** — KHÔNG phải app thật. App thật = zip frontend; worker thật = `worker.mjs` (không phải `fb-worker/worker.js`).

## 2. File anh đưa cho phiên mới (checklist)
1. ☐ **Nối repo `xvdesign68-jpg/tdm1`, nhánh `claude/chao-ban-8d6f9r`** (cách tốt nhất — có sẵn CLAUDE.md/HANDOFF.md/docs). Không nối được → đưa **`smartlead-handoff-2026-09-12.zip`** (bên trong: `CLAUDE.md`, `HANDOFF.md`, `docs/` đủ 335 file, `fe/` 2 zip, `worker/` 2 file, `README-START.txt`).
2. ☐ **`fe/smartleads17deploy-v119-90.zip`** — zip FE MỚI NHẤT cần deploy (cây IIFE, 13/09 sáng, smoke 142/142). **`fe/smartleads17deploy-v120-esm-an.zip`** — bản riêng cây ES module, cùng tính năng, deploy sau khi v119-89 ổn. Mọi FE mới phải áp cho **cả 2 cây** (script `mNN.py` trong `docs/fe-2026-09-07-v119-61/`, mốc mới nhất `m31.py`).
3. ☐ **`worker/worker.mjs`** bản `2026-09-11` (149.873 byte; `WORKER_VERSION = '2026-09-11'`; harness `docs/harness-worker-2026-09-11.mjs` 67/67). Đây là bản anh cần chép đè VPS (nghiệm thu theo `docs/worker-2026-09-11.md` §4). Bản đang chạy trên VPS lúc bàn giao (chưa xác nhận đã chép) = `2026-09-06d`.
4. ☐ **`worker/config.json`** (KHÔNG phải secret: `workerId`, `adspowerBase` local, `graphql.reactDocId 27646120298312844` · `commentDocId/replyDocId 28781864408106143` · `friendDocId 28400389149651601`, `maxConcurrent 1`, `profiles []`, `inbox {enabled true, maxThreads 5, everyMin 60, days 7}`; chưa khai `hardCaps`/`safety`/`dryRun`/`stopGraceMs`/`replyLoop`/`inbox.backoffH` → mặc định).
5. ☐ **3 số từ BrightData dashboard** (cho LỆNH B): trần snapshot đồng thời / request-phút · đơn giá posts + comments USD/1k record + record lỗi (`dead_page`) có tính tiền không · gói có `notify` webhook không. Chưa có → LỆNH B soạn với sàn 5′ + không notify.
6. ☐ (khi debug automation) 1–2 ảnh `errors/*.png` + `capture-<ts>.log` từ VPS.

Sau khi giải nén zip FE: `npm i playwright-core esbuild eslint` 1 lần (Chromium sẵn ở `/opt/pw-browsers` trên môi trường web) để build + smoke. Harness backend (`docs/harness-lenh48-2026-09-12.mjs`) chạy thẳng bằng `node` không cần cài gì.

## 3. Thông tin hạ tầng / tài khoản (không phải secret)
- **Super Admin:** xuanvinhsc68.work@gmail.com
- **Firebase/GCP project:** `smartlead-z15` · **Region functions:** `asia-southeast1` (BẮT BUỘC mọi function)
- **Cloud Shell:** account `xuanvinh.marketingpartners` · thư mục chính `~/firebase-s13` (Rules + engine); phụ `~/codebase2` (Sheet sync + `tagLeadBrand`), `~/smartlead-zalo-fn` (Zalo ZBS `notifyBrandZalo`)
- **Netlify site (deploy FE):** `smartlead.z15miracle.com.vn` (kéo-thả zip; nhắc Cmd+Shift+R / Ctrl+F5)
- **func.vn:** account_pid FB User = `fbu100013727043931` (nằm trên URL, không phải secret) — kênh func hiện KHÔNG dùng (nick chạy AdsPower)
- **AdsPower:** Local API `http://local.adspower.net:50325` trên VPS Windows `vps-1` (proxy dân cư); nick AdsPower doc id `ap<profileId>`
- **Nguồn lead:** BrightData (dataset bài + comment; gieo/gặt snapshot mỗi ~10′/nguồn; Scheduler `scheduledScan` every 3 minutes, timeout 1800) · 23 nguồn đang bật / 4 brand (test-agency, hscl-01, z15mrc-tts-1, z15mrc-tuyendung1) · AI chấm điểm OpenAI `gpt-5.6-sol` + tầng 1 `gpt-5-nano` (≈ $7/ngày)
- **funcWebhook URL:** `https://asia-southeast1-smartlead-z15.cloudfunctions.net/funcWebhook`
- **Alert email GCP** (LỆNH #9): policy "SmartLead - lỗi Cloud Functions/Run (severity>=ERROR)" → mail tài khoản gcloud. Các log kích alert: `[BRIGHTDATA-DOWN]` (#21), `[OUTREACH-NO-NICK]` (#44), `[LLM-DOWN]` (#46), `[LLM-PRE-DOWN]` / `[SCAN-ABORTED]` / `[LLM-CB]` (#48).

## 4. Secrets — CHỈ vị trí, KHÔNG giá trị
| Secret | Nơi lưu | Ghi chú |
|---|---|---|
| func Account Token (mỗi nick func) | Secret Manager `func-token-<pid>` | CF đọc runtime; FE chỉ gọi `SL_FB.*` |
| LLM_API_KEY / LLM_BASE_URL / LLM_MODEL (gpt-5.6-sol) / LLM_PREFILTER_MODEL (gpt-5-nano) | `~/firebase-s13/functions/.env` | scanner + content.js + aiText; đổi key = sed .env + deploy cả bộ functions. Các khoá `LLM_TRIES/LLM_TIMEOUT_MS/LLM_POST_BUDGET_MS/LLM_REASONING/LLM_MAX_TOKENS/SCAN_SOFT_DEADLINE_S/ZALO_CHECK_COLD` (#48) là cấu hình, không secret |
| FUNC_WEBHOOK_SECRET | `~/firebase-s13/functions/.env` | verify webhook func |
| BRIGHTDATA_TOKEN / DATASET_ID / COMMENTS_DATASET_ID | `~/firebase-s13/functions/.env` | scraper (dump trong git che `gd_<che>`) |
| VPS_URL / BROWSER_SVC_URL / SECRET | `~/firebase-s13/functions/.env` (LỆNH #22: code không còn IP literal) | quét nick (đang tắt) |
| serviceAccount.json (SA `outreach-worker@`) | trên VPS cạnh `worker.mjs` | worker đọc/ghi Firestore (Admin SDK) — anh chốt KHÔNG đụng kiến trúc này |
| Cookie/nick FB | trong AdsPower trên VPS | SmartLead chỉ ra lệnh, không giữ credential |
| Firebase web apiKey · VAPID_KEY (Web Push) | công khai trong FE (`firebase-config.js`) | **KHÔNG phải secret** (hợp lệ) |

> ⚠ **Cần làm khi rảnh:** anh đã lỡ dán **2 func token + 1 webhook secret** vào chat trước đây → **regenerate** cả 3 (đã đổi webhook secret 1 lần bằng LỆNH E; 2 func token chưa).

## 5. Trạng thái deploy (tính đến 12/09/2026 16:30 VN)
### 5.1 Frontend
- **Zip mới nhất cần deploy: `v119-90`** (13/09 sáng, `m32.py`; bản riêng `v120-esm-an`) = v119-89 + FE đi cùng LỆNH B/#48/C + PA-4 (thẻ comment-lead kể đủ chuyện) + PC-6 (AI giải thích, không giấu lead đang chăm, Loại có lý do, ai_feedback) + PA-11 (biểu đồ Lịch sử quét từ daily_stats); Nguồn quét chip Dùng chung/Nhịp/sức khoẻ + Cảnh báo hệ thống đọc `system_status/scan`, `source_health`, `group_state` → **cần Rules LỆNH C bản 2** (chưa deploy: chỉ console.warn). Smoke 142/142 cả 2 cây; README `docs/fe-2026-09-07-v119-61/README.md` mục v119-90.
- Trước đó **`v119-89`** (11/09 tối, `m31.py`) = v119-88 + FE đi cùng **LỆNH #46** (lead "Điểm tạm – AI chưa chấm"): chip "Điểm tạm – AI sẽ chấm lại" mọi vai trò, Nên gọi tiếp theo −30, Bài đã quét "Chờ AI chấm lại", `system_status/llm` → Cảnh báo hệ thống + thẻ vàng + chip thanh nhịp quét; bản riêng **`v120-esm-am`**. Smoke **135/135** cả 2 cây; tái lập byte-identical; ảnh `docs/fe-2026-09-07-v119-61/shots-v119-89/`.
- Trước đó **`v119-88`** (11/09 chiều, `m30.py`) = zip FE nhỏ Đợt 1 automation (nick FB realtime cho super, van kẹp trần cứng 80/30/20/15, tooltip Safety `safetyWhy`, thẻ brand "BẬT · 0/N nick", chip máy ghi tên nick) · **`v119-87`** (10/09) = trang Giá trị & ROI super toàn cảnh (anh chưa nhận xét bố cục). **Anh xác nhận đã deploy tới v119-80 (09/09)**; v119-81…88 bỏ qua, deploy thẳng 89.
- Nền tảng đã chốt (đừng đổi nếu anh không gọi): font menu Plus Jakarta Sans đậm + nội dung TikTok Sans 14,5px/450 + số TikTok Sans (`--font-num`) + mã JetBrains Mono; morph DOM realtime (v119-58) + `slChart` chữ ký + `FX_INTRO` (60); khởi động sạch chờ kênh lõi (69/70); menu 4 nhóm + hộp thoại app + icon SVG (61/62); kênh realtime tự đăng ký lại 4 lần (65); KPI 14 ngày theo `daily_stats` (80–82).
- **FE chưa làm, đã chốt sẽ làm sau LỆNH B/C (1 zip)**: PA-4 thẻ comment-lead kể đủ chuyện + Nhu cầu lên đầu + tìm theo bài gốc · PC-6 giải thích AI, không ẩn lead đang chăm, "Loại" có lý do (`dropped_reason`), chip Rác·Loại tách AI/người · PA-11 · B UI (form/wizard cho trùng group khác brand + hộp thoại + chip "Dùng chung" super + Cảnh báo/Hộp việc super) · chip `ai_flag`/`zalo_defer`, thẻ `llm.pre`/`cbOpen` ở Cảnh báo hệ thống, decision `no_text`/`ai_wait` ở Bài đã quét · ô `scanIntervalMin` (v119-50) chưa từng được lưu (config/app không có field).

### 5.2 Worker VPS
- **Bản mới nhất cần chép: `worker.mjs 2026-09-11`** (`docs/worker-2026-09-11.md` §1 thay đổi · §2 khoá config mới · §3 field Firestore mới · **§4 nghiệm thu** · patch `docs/worker-2026-09-11-patch.cjs` áp lên 06d · harness 67/67 + 8 harness cũ). Nội dung: R-15 bộ biến comment bài theo capture 06/09 (hết fail 20/20 bài group gid số) + `ambiguous` kiểm hiển thị trước DOM (chống đăng 2 lần) · R-9 lỗi giao diện không trừ Safety, lỗi FB đếm 1 lần/phiên, decay đúng, `safetyWhy` · R-8 `markDone` trước log + Ctrl+C tắt êm · R-2 `replyLoop` đọc inbox độc lập + backoff `replyNextAt` · R-1 `preflight` người thật trước mỗi bước · `dryRun` · hoàn van kẹp ≥0 cùng ngày · `nextMorningVN`.
- **Đang chạy trên VPS (chưa xác nhận đã chép bản mới): `2026-09-06d`** trên `vps-1` (1 VPS, `maxConcurrent 1`). **Nick 0/6 sống** — 4 needLogin + tắt (`apk1g9yjdv/apk1ge6dka/apk1gehnnq/apk1gm2jd2`), t7 `apk1gm6por` Safety tự tắt 09/09 (28 điểm vì 43 lỗi selector — bản 2026-09-11 tự di trú lên điểm cao, vẫn cần ✓ Đã xử lý), t1 func. **Engine (#44) đã làm hscl-01 "đứng im có kiểm soát"** (sweeper đóng 11 thread quá 72 h, 1 orphan) → bật nick lại SAU khi worker mới chạy dryRun OK.
- Nghiệm thu chuẩn (`docs/worker-2026-09-11.md` §4): chép worker (giữ config.json; Ctrl+C 1 lần = tắt êm) → đăng nhập lại 1 nick + ✓ Đã xử lý → `"dryRun": true` xem log `[DRY] … ✓` → tắt dryRun chạy thật 1 nick (`comment API: OK capture`) → dòng `💬 nick …: mở để đọc phản hồi` sau ≥ 2′ → van hscl-01 về 40/12/10/8.

### 5.3 Engine / Cloud Functions (asia-southeast1) — LỆNH mới nhất từng function
- **LỆNH B KHỐI 1 ĐÃ CHẠY 12/09 19:11 VN** (`docs/lenh-2026-09-12-b.sh`, sha256 4332f154…ff2f; (e) `b-e.sh` 19:28 VN; KHỐI 2 19:34 VN PASS cơ chế; lần 2 13/09 04:18 VN PASS — seen.brands 100 %, 18 lead sau B đúng; còn khối 4c `_lb_lag.mjs` chốt LAG ban ngày) — đã đổi `scheduledScan · manualScan · outreachTick · statsOnLead · scanStatsOnRun` + thêm `createSource · sourceOnWrite · bdReady` (file mới `sources.js`) + Rules `lead_links` + codebase2 `tagLeadBrand`; marker `LENH B` 7 file (`lib/config.js · lib/scraper.js · index.js · outreach.js · stats.js · scanstats.js · lib/multitouch.js`).
- **`scheduledScan` + `manualScan` = LỆNH #48** (12/09 08:53Z = 15:53 VN; backup `.bak-20260912-085129` ×4: `index.js · lib/scorer.js · lib/scraper.js · lib/config.js`, marker `LENH #48` 45/12/6/1). Trước đó #46 (11/09 10:10Z), #32/#31b/#23/#22/#21.
- **`outreachTick` = LỆNH #44** (11/09 04:10Z, rev `outreachtick-00030-bub`: E-16/E-17/R-1/E-4/E-5/E-7/R-6 sweeper + `system_status/outreach`) **+ #46** (11/09 10:10Z: bỏ lead `ai_scored:false`) · **`statsOnLead` = #44** (`humanTookOver` → `stopMachine`; trước đó #42 bỏ vai người bán, #40 SLA, #36 `content_stats`) · **`pushOnLead` = #46** (không push lead nóng điểm tạm; trước #36/#12) · `genContent` = #34 (06/09) · `funcWebhook` = S3 (01/09) · `scanStatsOnRun` = #39 · `setUserLock` = #10 · `saveFbAccount/deleteFbAccount` (28/08) · `assistantChat/assistantLogs` = #7b · `pushDueFollowups` = #36. Ngoài s13: `tagLeadBrand` (codebase2, 08-11) · `notifyBrandZalo`/`sendZaloZBS` (zalo-fn).
- Backup code trên Cloud Shell: mỗi file giữ 1 `.bak-<ts>` mới nhất + các `.bak` của #46/#48; script `_l3x_*.mjs`, `_l4x_*.mjs` (gồm `_l46_after.mjs`, `_l48_after.mjs`, `_l44_after.mjs`, `_l36_junk.mjs`, `_l34_move.mjs`) để nguyên trong `functions/` — chạy lại được bất kỳ lúc nào (chỉ đọc, trừ `_l36_junk.mjs --delete`).
- **Firestore**: Rules có block `leads` (whitelist 31 field non-super — **chưa có `dropped_reason`/`ai_feedback`/`phone`** → LỆNH C), `users`, `brands`, `config` (private chỉ super), `notes` CG, `fb_accounts`, `workers`, `worker_config`, `outreach_log/stats`, `daily_stats`, `content_stats`, `system_status` (super đọc: `brightdata/outreach/llm/scan`), `stage_log`, `audit_log`; KHÔNG có block `backfill_done/group_state/score_retry/cmt_scrape/seen/pending_snapshots` (Admin SDK ghi, client deny mặc định — đúng). Index đủ (leads brand+phone+detected_at, outreach_tasks workerId+status+createdAt, outreach_threads pid+active+nextAt, outreach_log brandCode+at & status+at, notes CG ×4). TTL `outreach_log.expireAt` ACTIVE. Backup hằng ngày giữ 7 ngày. Collection mới từ #46/#48: `score_retry` (kind server/badreq/lease), `system_status/llm` (+`win` cửa sổ 15′, `pre`, `cbOpen`), `system_status/scan` (phase/runId), lead field `ai_scored/rescored_at/ai_prev/ai_flag/base_score/rescore_lease/zalo_defer/author_uid`, scans field `llmOk/llmFail/llmDeferred/llmFallback/llmRescored/llmPreOk/pipeErrors/softStop/seenRace/leased/badreq/cbOpen/tokensReasoning/runId/status`.

### 5.4 Số thật đã đo (nền cho LỆNH B/C) — chi tiết trong docs
- **Quét/lọc/chấm (LỆNH #47 a–d + B KHỐI 0, 12/09; `docs/lenh-2026-09-12-47.md` KẾT QUẢ ĐẦY ĐỦ + `docs/lenh-2026-09-12-b0.md`)**: lag phát hiện trung vị **11′ / p95 36′** (mốc gốc tốc độ, mục tiêu B ~4–7′) · lượt quét trung vị 15 s, gap 3′ · **31 % lượt thuần skip vẫn ghi `scans`** · **84 % record comment BrightData rỗng** (12.413 record / 2.021 comment) · 31 % record bài không dùng · AI ≈ $7/ngày · `cmt_scrape` gieo lại 0/3000 (Q-3) · `recentIds` đầy 200 ở 16/26 group · nguồn 32/23 bật, id==gid 7/32, URL số 17/slug 12 · **N4-3 thực tế 0 lead đang chăm bị đụng** · 18 % lead-bài gần trùng (PC-4) · lead KHÔNG có `post_id` · 57 comment mồ côi (slug↔số) · record bài CÓ `profile_id` uid số + `num_comments` + `group_id` (#48 đã map) · record bình luận CÓ `commentator_profile_url` (#48 đã map) · `tagLeadBrand` (codebase2) dòng 75 = lưới dự phòng gán brand theo TÊN nguồn → B ghi `brand_hint` · ZBS `notifyBrandZalo` cần `justTagged` → B giữ 2 bước ghi brand.
- **Sau #48 (KHỐI 2, 16:07 VN)**: 5 lượt 8–17 s sạch (soft/pipe/seenRace/aborted 0), `rt` 59–269 token/lượt, 2/2 lead-bài mới có `author_uid` số, 0 link tracking, `ai_scored:false` 0 (backlog 252 của #46 xong). Theo dõi: comment-lead đầu tiên sau 15:53 VN phải có `author_uid`.
- **Automation (LỆNH #43/#43b 10/09 + #44 KHỐI 2 11/09; `docs/rasoat-automation-2026-09-10/`)**: hscl-01 BẬT nhưng 0/6 nick sống; 20/20 comment fail = bài group gid số (gốc = bộ biến `gqlComment` cũ, sửa ở worker 2026-09-11); lead máy chạm 43, inbox 4, phản hồi 1 (máy phát hiện 0); `content_stats` sent 3/rep 1; sau #44: sweeper expired 11 / orphan 1, `[OUTREACH-NO-NICK]` 1 lần rồi WARNING.

## 6. Cách phiên mới KHỞI ĐỘNG hiệu quả
**Bước 1 — nạp ngữ cảnh:** đọc `CLAUDE.md` (toàn bộ) + `HANDOFF.md`. Làm LỆNH B/C thì đọc thêm: `docs/rasoat-scan-2026-09-11/thiet-ke-group-nhieu-brand.md` (§0–§7 bản 1, **§8 bản 2 sau phản biện, §9 điều chỉnh sau #47**), `docs/rasoat-scan-2026-09-11/findings.json` + `README.md`, `docs/lenh-2026-09-12-47.md` (KẾT QUẢ ĐẦY ĐỦ + #47d), `docs/lenh-2026-09-12-b0.md`, `docs/lenh-2026-09-12-48.md`. Làm automation thì đọc `docs/rasoat-automation-2026-09-10/findings.json` + `docs/lenh-2026-09-11-44.md` + `docs/worker-2026-09-11.md`.

**Bước 2 — nạp code thật:** giải nén zip FE (cả 2 cây nếu cần build song song) vào thư mục làm việc; đặt `worker.mjs` + `config.json` vào thư mục worker; `npm i playwright-core esbuild eslint`. Backend: fixture #48 + dump #47 trong git là mã đang chạy (sha8 trước #48: index.js 14a3a823 · scorer 12223e1c · scraper 66714197 · config 81d91f79).

**Bước 3 — pattern chuẩn 1 LỆNH backend (đã dùng cho #44/#46/#48, giữ nguyên):** (1) LỆNH chỉ đọc dump mốc (che chuỗi ≥28 ký tự, IPv4, `sk-`; in số dòng; script Admin SDK đặt TRONG `functions/`, đọc collection lớn theo trang ≤300 + `select()`); (2) dựng **fixture** mã đang chạy từ dump vào `docs/lenh-…-fixture/` + stub Firestore; (3) viết **patch `.cjs` content-anchored, fail-closed NGUYÊN TỬ** (mọi mốc khớp mới ghi, marker `LENH #NN`, idempotent, `--anchors` in JSON mốc); (4) **harness** chạy trọn hàm trên fixture + đối chiếu từng mốc vào VĂN BẢN DUMP đúng 1 lần; (5) `.sh` KHỐI 1 (backup `.bak-<ts>` → patch → `node --check` → import test `.env` → gọi thử thật nếu có → deploy xích `&&` → describe) **dry-run ×2 với fake `firebase`/`gcloud`** (idempotent, exit 0); (6) sha256 khối dán chat; (7) `_lNN_after.mjs` KHỐI 2 chỉ đọc; (8) dán KHỐI 1 + KHỐI 2 THẲNG vào chat, file `docs/lenh-*.md` chỉ là bản lưu; (9) anh dán output → em check → ghi KẾT QUẢ vào doc + CLAUDE.md + commit/push.
```bash
# dump nhanh khi cần thêm mốc (chỉ đọc)
cd ~/firebase-s13/functions
grep -n "^export\|^async function\|^function\|^const [A-Z_]* =" index.js | head -80
sed -n 'a,bp' index.js | sed -E 's/[A-Za-z0-9_-]{28,}/<che>/g; s/([0-9]{1,3}\.){3}[0-9]{1,3}/<ip>/g'
gcloud functions list --project=smartlead-z15 --format="table(name,state,updateTime)" 2>/dev/null
```

**Bước 4 — FE: build + smoke trước khi gửi zip (cả 2 cây):**
```bash
cd <cây smartlead>
node tools/build.mjs --zip /tmp/smartleads17deploy-<tên>.zip
NODE_PATH=<nơi có node_modules> node tools/smoke.js   # PASS hết mới gửi
```
Mọi thay đổi `live.js` kiểm thêm bằng harness stub SDK (`docs/harness-boot-stub-2026-09-05.mjs`, `docs/harness-boot-timeline-2026-09-08.mjs`) vì smoke chạy demo offline. Mỗi bản FE = 1 script `mNN.py` fail-closed áp cho cả 2 cây + README `docs/fe-2026-09-07-v119-61/` + kiểm âm (check mới trên cây cũ phải FAIL) + tái lập byte-identical.

**Câu mở đầu mẫu để dán vào phiên mới** (cũng có trong `README-START.txt` của gói bàn giao):
> Em là Claude Code tiếp tục dự án SmartLead (Z15 Miracle) của anh Vinh. Đọc hết `CLAUDE.md` + `HANDOFF.md` (bản 12/09/2026) để nạp ngữ cảnh và "gu" làm việc; soạn LỆNH/patch thì theo đúng pattern `docs/lenh-2026-09-12-48*` (fixture + patch fail-closed + harness + dry-run + KHỐI 1/2 dán chat) và FE theo `docs/fe-2026-09-07-v119-61/README.md`. Anh đính kèm: zip frontend mới nhất `smartleads17deploy-v119-90.zip` (+ bản ESM `v120-esm-am`), `worker.mjs` 2026-09-11, `config.json`. Quy tắc: trả lời tiếng Việt, xưng em/anh; FE giao bằng zip mới (build + smoke PASS cả 2 cây); backend chỉ soạn LỆNH bash để anh dán Cloud Shell; worker giao file; không bao giờ đưa secret vào chat/git/zip; chỉ làm việc đã chốt. Việc kế tiếp: HANDOFF.md mục 6b — LỆNH B (hướng B 1 group nhiều brand + tốc độ) theo `docs/rasoat-scan-2026-09-11/thiet-ke-group-nhieu-brand.md` §8–§9; 3 số BrightData của anh là: [trần snapshot/req-phút …] · [đơn giá posts/comments … + dead_page có tính tiền …] · [notify webhook có/không].

## 6b. Việc đang treo lúc bàn giao (12/09/2026 16:30 VN) — theo thứ tự
**Danh sách task đầy đủ 13/09 (anh yêu cầu liệt kê hết, coi tất cả là task chính)**: (1) B chốt LAG + 3 số BrightData/notify · (2) LỆNH C (PB-10 · PB-6 + recount · Rules dropped_reason/ai_feedback/phone · ZBS guard · source_health · dọn group_state URL cũ) · (3) zip FE v119-90 (PA-4 · PC-6 · PA-11 · FE của B · FE của #48) · (4) Đợt 2 quét/chấm 8 mục PB-9+PB-12 → PA-1 → PC-2 → PA-5 → PC-3 → PC-7 → PC-1 → PB-7/PA-9 + PC-5 · (5) Đợt 3 PC-11 · (6) Automation R-3/R-4/R-7/R-10 rồi R-11/R-12/R-13/R-14 · (7) nghiệm thu dữ liệu thật (worker 2026-09-11, 4 nick, van, deploy v119-89, author_uid comment-lead, replyLoop, card Hiệu quả nội dung, push điện thoại) · (8) tuỳ chọn cũ. Chi tiết từng mục ngay dưới.
0. **Phía anh (không cần em)**: (a) **3 số BrightData dashboard** (mục 2 việc 5) → mở khoá LỆNH B; (b) **nghiệm thu worker `2026-09-11`** theo `docs/worker-2026-09-11.md` §4 rồi đăng nhập lại 4 nick + ✓ Đã xử lý (hoặc `node _l34_move.mjs --dry` chuyển thread) + van hscl-01 về 40/12/10/8; (c) **deploy zip `v119-89`** (Cmd+Shift+R) rồi bản riêng `v120-esm-am`; (d) theo dõi #48: khi có comment-lead mới → `node _l48_after.mjs` (mục 4 phải thấy bình luận có `author_uid`); (e) tuỳ chọn: `node _l36_junk.mjs --delete` (1 lead rác), Close alert cũ GCP, regenerate 2 func token + webhook secret.
1. **LỆNH B — hướng B "1 group nhiều brand" + tốc độ + PC-4 — KHỐI 1 ĐÃ CHẠY OK 12/09 19:11 VN + bước (e) tagLeadBrand 19:28 VN (9 function ACTIVE); KHỐI 2 19:34 VN PASS cơ chế + lần 2 13/09 04:18 VN PASS (seen.brands 100 %, 18 lead sau B đúng, đêm ổn định — mục 9); CHỜ ANH: khối 4c `node _lb_lag.mjs 24` (đã tạo; lần 1 04:37 VN n 7 chưa đủ mẫu — mục 9b) chạy lại 11–12 h hoặc 14–16 h VN 13/09 chốt LAG ban ngày (script tự kết luận; chưa đạt → `config/app.scanIntervalMaxMin: 15` nếu lead ở group slow/idle)** (`docs/lenh-2026-09-12-b.md` mục 0 = 3 số BrightData → mặc định; mục 5 = quay lui; fixture `docs/lenh-2026-09-12-b-fixture/`; harness `docs/harness-lenhb-2026-09-12.mjs` 136/136; **đã rà đối kháng 12/09 tối → 22 nhóm sửa, mục 6 doc**; sha256 `.sh` 4332f154…ff2f; anh dán output → em ghi KẾT QUẢ) (thiết kế `docs/rasoat-scan-2026-09-11/thiet-ke-group-nhieu-brand.md` §8 + §9.1–9.21; mốc mã trong `docs/lenh-2026-09-12-47-dump/` + fixture #48 đã patch). Phạm vi đã chốt: BrightData gieo/gặt **1 lần/group (gid số, chuẩn hoá lúc gặt, học gid từ record)** rồi fan-out ứng viên cho từng brand, mỗi brand chấm bằng Hồ sơ AI riêng → lead riêng; `seen/{post_id}.brands{}`; **lead id tất định `L_<post_id>__<brand>` + `create()`** (post_id = slug + fallback urlKey, bỏ `Math.random()`); object lead mang `brand/source_id/gid/brand_hint` TRƯỚC commitLeadNow nhưng GIỮ 2 bước ghi brand (ZBS `justTagged`); `score_retry` id theo brand; scanstats/bd_month khoá `source_id`; nguồn mới id `<gid>__<brand>` qua CF `createSource` (kiểm trùng, `sharedAt`, `system_status/sources`, push super, `[SOURCE-SHARED]`); tenancy `shared_post:true` + `lead_links` (super); **automation `acquireLocks` transaction (post_/cmt_/person_) ở CẢ stepNick lẫn apEnqueueFunnel TRƯỚC tryConsume/genForLead**, first-come; **tốc độ**: khoá lượt transaction (maxInstances 1 KHÔNG chống chồng), bỏ "gặt trong lượt" → `notify` webhook `bdReady` (nếu gói có) hoặc sàn 5′, nhịp thích ứng 3′/5′/10′/30′ EWMA + hysteresis, gộp gieo 1 trigger/lượt, deep sweep chỉ khi BrightData ngưng ≥30′, S8 sweep `start_date`, lượt thuần skip không ghi `scans`, watch_posts ≤5 bài/nguồn 2 lần/ngày 2 ngày (đo trước, gieo comment chỉ khi `num_comments>0`); **PC-4** vân tay văn bản chặn 18 % lead gần trùng (bỏ hash bài đã sửa); `tagLeadBrand` codebase2 dòng 75 `hint || getBrandForSource` (deploy `--only functions:tagLeadBrand` từ codebase2). Mục tiêu đo được: post → lead ~4–7′ (từ 11′/36′). KHỐI 0 (chỉ đọc) ĐÃ CHẠY 12/09 14:44 VN.
2. **LỆNH C — KHỐI 1 ĐÃ CHẠY OK 13/09 07:10 VN** (4 function + Rules ACTIVE; recount 2.361 lead → 106 doc, máy loại 26; `docs/lenh-2026-09-13-c.md` mục 6.1) — **CÒN: bước (e) bổ sung `docs/lenh-2026-09-13-c-e.sh` (deploy `notifyBrandZalo` qua đúng codebase/gcloud vì Firebase CLI báo "No function matches"; sha256 `0e5d0401…c8bb`) → KHỐI 2 `node _lc_after.mjs` → tuỳ chọn `node _lc_clean.mjs --apply` (26 doc group_state URL cũ)**. Gốc — ĐÃ SOẠN 13/09 (`docs/lenh-2026-09-13-c.md` + `-c.sh` sha256 `6061ed4a…5fa1`; harness 44/44; dry-run ×2): PB-10 hotGateC · PB-6 machineDroppedC + aiDropped + recount · Rules whitelist · ZBS guard · source_health + healthEvalC [SCAN-NO-LEAD]/[SCAN-HEALTH] · backfill_done theo brand · clean group_state (dry). Anh dán KHỐI 1 → ảnh → em ghi KẾT QUẢ → KHỐI 2 `_lc_after.mjs`. Nội dung gốc: PB-10 push "brand xuất hiện" (before không brand → after có, kiểu #41) + `brand_hint` · PB-6 `stats.js` nhánh AI hạ nhiệt/loại → trừ bộ đếm (anh chốt câu 1) + recount 60 ngày (số KPI sẽ GIẢM, báo trước) · Rules whitelist `dropped_reason`, `ai_feedback` (+ `phone` nếu FE cần) · tuỳ chọn ZBS guard (không bắn khi chỉ đổi score / dropped / `ai_scored:false`).
2b. **LỆNH D — ĐÃ SOẠN 13/09 (Đợt 2.1: TTL scans/seen + lưới bài quá cũ + hoãn AI không đẻ doc + prefiltered 14 ngày + bình luận ↔ bài cha fail-closed), CHẠY SAU C** (`docs/lenh-2026-09-13-d.md` + `-d.sh` sha256 `649534fa…e9c9`; harness 33/33; dry-run ×2). KHỐI 2 `node _ld_after.mjs` sau ≥15′ (PB-12 (0) đo tỉ lệ orphan: >10 % → nới urlKey).
3. **Zip FE** (1 zip cả 2 cây, script `m32.py`): mục 5.1 "FE chưa làm".
4. **Đợt 2 quét/chấm** (`docs/rasoat-scan-2026-09-11/README.md`; **lộ trình CHÍNH đã chốt 12/09, anh nhắc 13/09 — làm ngay sau zip FE**; `source_health` [SCAN-NO-LEAD] chưa vào B → gộp LỆNH C): PB-9 + PB-12 → PA-1 (prompt theo BRAND + vai `reseller` khi cờ "brand bán sỉ", `proxy` vẫn cho kết bạn/inbox; `_promptcmp` + công tắc) → PC-2 (6 tiêu chí con = 6 key weights, hotness tính bằng code, **chạy bóng 2 tuần**) → PA-5 → PC-3 (`author_memory` khoá theo profile key) → PC-7 (audit tầng 1 ≈ $0,4/ngày) → PC-1 (Sổ tay brand tự học) → PB-7/PA-9 → PB-8/PC-5 (P23 lịch quét thích ứng đã chốt BẬT — phần nhịp nằm trong B). **Đợt 3**: PC-11 lead lạnh = "Theo dõi" theo brand (sau khi đo).
5. **Automation Đợt 2/3** (`docs/rasoat-automation-2026-09-10/`; **anh xác nhận 13/09: thuộc lộ trình CHÍNH, làm sau Đợt 2 quét/chấm, không phải chờ gọi**): R-3 bắt phản hồi qua Thông báo/scanner · R-4 hàng đợi ưu tiên nóng + `queued_at` · R-7 giám sát push · R-10 quality gate nội dung · R-11 follow-up lần 2 · R-12 màn "Hàng đợi & kẹt" · R-13 dò FB đổi giao diện · R-14 hành vi giống người. Nghiệm thu còn thiếu: comment-lead API nội bộ trên lead thật, card "Hiệu quả nội dung" lên số, `replyLoop` phát hiện phản hồi thật, push FCM điện thoại.
6. Tuỳ chọn cũ (chỉ khi anh gọi): #30c regen thread cũ; `_l31_fix.mjs --rescore`; kiến thức brand có số / follow-up 24–48 h / CTA xin Zalo (nội dung); lịch sử phí gói theo tháng + cảnh báo gói hết hạn (ROI); brand-admin tự quản automation (mở Rules); bdwatch debounce 2 lượt.

## 7. Quy trình & "gu" (chi tiết trong CLAUDE.md — nhắc nhanh)
- Trả lời **tiếng Việt**, xưng **em** / gọi **anh**; thuật ngữ kỹ thuật giữ English. Chỉ implement việc đã **chốt**; thay đổi thẩm mỹ toàn cục → chụp trước/sau + hỏi anh trước. Anh **không** lo pháp lý/billing/self-service (đã chốt 03/09) — đừng đề xuất lại.
- **FE** = sửa `src/app/*.js` + `live.js` qua script `mNN.py` fail-closed áp cho **cả 2 cây** → build → smoke PASS → **gửi 1 zip mới** ("zip MỚI NHẤT cần deploy") → anh kéo-thả Netlify → Cmd+Shift+R.
- **Backend** = **soạn LỆNH bash** (KHỐI 1 làm + KHỐI 2 chỉ đọc) dán THẲNG vào chat cho anh dán Cloud Shell → anh dán output/ảnh → em check → ghi KẾT QUẢ. Không tự đụng, không gửi file thay khối lệnh (file `.sh` gửi kèm chỉ để dự phòng, có sha256 khớp khối chat).
- **Worker** = giao file `.mjs` (+ harness PASS + doc §4 nghiệm thu), anh chép đè VPS + chạy lại `run.bat`. Không đụng SA key/kiến trúc worker nếu chưa bàn.
- `?v=` tự hash trong build (đừng bump tay). eslint chạy trong build (lỗi = dừng). Mọi bản FE/LỆNH ghi vào CLAUDE.md ("Zip mới nhất" + mục ★ + TRẠNG THÁI) + commit/push nhánh `claude/chao-ban-8d6f9r` (message tiếng Việt, không ghi tên model).
- Automation: **van an toàn nick** theo ngày · **verify kết quả thật** (không tin HTTP 200) · **fail-closed** (thà bỏ lỡ còn hơn thao tác nhầm/bay nick) · checkpoint = tín hiệu DỪNG, không né · nick FB phải để Tiếng Việt · máy dừng khi người thật vào (R-1).
- Chấm điểm: **không fallback im lặng** — mọi tầng dự phòng ghi dấu (`ai_scored:false`, `ai_flag`, `zalo_defer`) + có đường chấm lại + cảnh báo `system_status/*`.

## 8. Bẫy đã gặp (đọc trước khi soạn LỆNH/patch)
- Cloud Shell bash tương tác history-expand `!` kể cả trong `"…"` → chuỗi lệnh dài ghi `.sh` qua heredoc quoted rồi `bash file.sh`; grep `!` ngoài heredoc = 0 trước khi gửi.
- Script `firebase-admin` để ở `/tmp` → `Cannot find package` → luôn đặt trong `~/firebase-s13/functions`.
- **Script chỉ đọc trên collection lớn** (`leads` 1.8k+, `scanned_posts`, `seen` 54k) đọc nguyên 1 request → `DEADLINE_EXCEEDED 300 s` (#36, #47 R3–R5, lặp 3 lần) → PHẢI trang ≤300 + `select()` field cần (kèm field orderBy); `scan_jobs` không có field thời gian chuẩn → đọc rồi sort client.
- Deploy không xích `&&` sau patch → patch fail vẫn deploy code cũ (#20). Mốc patch chọn `{` SAU `)` đóng tham số (default param `opts = {}` làm hụt thân hàm — #19, #46 fixture).
- **Chèn code vào dòng có comment đuôi `//` phải đặt TRƯỚC `//`** (harness #48 bắt được `await unlease48(x)` thành comment). **State "cửa sổ" phải lưu cả lần đầu** (doc chưa tồn tại). Harness phải chạy TRỌN hàm trên fixture + đối chiếu mốc vào văn bản dump — grep mốc thôi không đủ.
- `initializeApp()` top-level ở module mới → Firebase CLI "codebase could not be analyzed" → khởi tạo lười trong handler. Function thiếu `region:'asia-southeast1'` → us-central1 → FE "Failed to fetch".
- **`maxInstances: 1` KHÔNG chống 2 lượt chồng** (Cloud Run concurrency 80) → khoá lượt phải bằng transaction (LỆNH B). Số đo BrightData 45 s là phép thử KHÔNG có `posts_to_not_include`; đường live chín ~141 s.
- Mọi khoá theo TÊN/URL (brandBySource, srcByGroup, scanstats normUrl, bd_month.src, score_retry, seen, leadKey, backfill_done, pending P_<url>) gãy khi 1 group thuộc 2 brand → liệt kê hết khoá trước khi fan-out (B). Tenancy: không ghi mã brand khác lên doc brand user đọc được.
- `String.replace` chuỗi thay thế có `` $` `` / `$&` → dùng hàm `()=>b` (CLAUDE.md từng bị nhân đôi). `String.slice` cắt giữa cặp surrogate → JSON body hỏng → sanitize trước khi gửi API (#46).
- Smoke chạy demo offline → thay đổi `live.js` phải kiểm bằng harness stub SDK; kiểm âm trước khi tin check mới.
- Đổi thẩm mỹ toàn cục theo "chuẩn design-system" bị anh chê (v119-61) → gu đã chốt thắng chuẩn.
- Nhận diện ô soạn FB bằng nhãn sai (bài và trả lời dùng chung nhãn) → tín hiệu tin cậy = focus ngay sau cú bấm + node mới + vị trí DOM. Safety Score cộng lỗi selector của mình như FB phạt → nick khoẻ tự tắt (sửa ở worker 2026-09-11).
- Workflow đa agent: resume theo cache cần `args` GIỐNG HỆT (1 dấu chấm khác = 25 agent chạy lại); nested backtick trong template literal của script = parse error; máy 4 CPU → 2 agent song song/workflow.
