# LỆNH #47 — CHỈ ĐỌC (R1–R6 + hướng B + tốc độ) — 12/09/2026

Mục đích: trước khi vá Đợt 1 theo rà soát 11/09 (PB-5 sweeper giấu lead đang chăm · PB-2 chống chấm đôi/mất bài · PB-4 giám sát tầng 1 · PB-3 AI · PB-10 brand ở write đầu · PB-6 bộ đếm) **và** 2 việc anh chốt 12/09 (**câu 4 = hướng B: 1 group phục vụ nhiều brand** · **câu 5 = không bỏ lỡ lead, đẩy về nhanh nhất**), em cần **mã đang chạy** (7 file chưa từng dump: `lib/filter.js`, `lib/multitouch.js`, `lib/zaloCheck.js`, `lib/alerts.js`, `stats.js`, `push.js`, wrapper `scheduledScan/manualScan` + `firestore.rules` các block `sources/scanned_posts/scan_jobs/backfill_done/config/system_*`) và **số liệu thật** (nhật ký quét 7 ngày, kho lead 30 ngày, Bài đã quét 3 ngày, nguồn, group_state/pending/cmt_scrape, automation↔lead, push).

- Không ghi gì. Che: chuỗi ≥28 ký tự kiểu token trong nháy, IPv4, `sk-…`; `.env` chỉ in key whitelist (không `*_KEY/*_TOKEN/*_SECRET/URL`).
- Chạy ~2–4 phút. Kết quả: `~/scan-dump-0912.txt` (mã + hạ tầng + số liệu, ~2–3 nghìn dòng) → `cloudshell download ~/scan-dump-0912.txt` rồi upload vào chat. Phần số liệu cũng in ra màn hình (và lưu `~/scan-data-0912.txt`).
- File script: `docs/lenh-2026-09-12-47.sh` (đã nhúng `docs/lenh-2026-09-12-47-data.mjs`); dry-run cục bộ với fake gcloud + stub firebase-admin exit 0, che secret kiểm OK.

## Dán vào Cloud Shell (1 khối)

```bash
cat > /tmp/l47.sh <<'L47'
<nội dung docs/lenh-2026-09-12-47.sh>
L47
bash /tmp/l47.sh
```

(Khối đầy đủ đã dán trong chat 12/09 — bản này chỉ là bản lưu; nội dung thật = file `.sh` cạnh đây.)

## Sau khi có output

Em soạn theo thứ tự: **LỆNH A0** (PB-5 + PB-2a, nhỏ, deploy trước) → **LỆNH A** (PB-3 + PB-4 + PB-2 b/d, kèm A/B `reasoning_effort` 30 bài) → **LỆNH B** (hướng B: gieo 1 lần/group + fan-out theo brand + `seen.brands` + `outreach_locks` + `sourceOnWrite` báo super + kế hoạch tốc độ: nhịp thích ứng bật mặc định, gặt trong lượt, tick 1′, deep sweep sau sự cố, `source_health`) → **LỆNH C** (PB-10 sau khi đọc Gateway ZBS + PB-6 sau backlog #46 ≈ 0 + Rules whitelist `dropped_reason`, `ai_feedback`) → zip FE (PA-4 + PC-6 + PA-11 + FE của B: cho phép trùng group khác brand, chip "Dùng chung", Cảnh báo/Hộp việc super, ưu tiên brand).

## KẾT QUẢ (đang nhận, 12/09 03:39Z = 10:39 VN) — anh chạy OK, đang gửi ảnh
- Script chạy đúng (dòng `== LỆNH #47 R1 — mã đang chạy · 2026-09-12T03:39Z ==`); phần lớn nội dung chỉ nằm trong `~/scan-dump-0912.txt` (tee >/dev/null) → cần anh `cloudshell download` + upload file.
- Đọc được từ ảnh: `lib/alerts` là THƯ MỤC (`import { dispatch } from './lib/alerts/index.js'`) → `dumpf lib/alerts.js` báo KHONG CO → **LỆNH #47b** (`docs/lenh-2026-09-12-47b.sh`) dump `lib/alerts/*.js` + `scanstats.js` + `cfgpriv.js` + `lib/needsynth.js` + `onLeadCreated/synthNeedsOnUpdate/cleanupScannedPosts` + Gateway ZBS `~/smartlead-zalo-fn` (justTagged/brand_pending).
- `onLeadCreated` (index.js:877) = `onDocumentCreated('leads/{id}')` → `dispatch({id,...lead}, config.channels)` → backend CÓ đọc `config/app.channels`; sự kiện create xảy ra TRƯỚC write brand (2 bước) → alerts cũng nhận lead chưa có brand (cùng gốc PB-10).
- `synthNeedsOnUpdate` = `onDocumentUpdated('leads/{id}')` (index.js:112) → lead có 4 trigger (statsOnLead, pushOnLead, synthNeedsOnUpdate, onLeadCreated).
- `lib/zaloCheck.js:11` regex `/^[35789]\d{8}$/` → chỉ trích SĐT di động (9 số sau 0), số bàn 02x bị loại → D-3/F-E1: FE-only vô ích, cần sửa backend nếu muốn số bàn.
- Kích thước: index.js 1289 · scraper.js 360 · scorer.js 214 · multitouch.js 113 · stats.js 123 · push.js 74 · outreach.js 479 · content.js 162 · filter.js 18 · zaloCheck.js 92.
- scraper mốc khớp thiết kế: `pending_snapshots/P_<url>` :155, recentIds ghi lúc gặt :187, `C_` :280, harvestComments :316–340, bdWait comment 85×6000 :294 (đường không sow), posts_to_not_include slice 200 :98.

### Đợt ảnh 2 (12/09 ~10:50 VN) — mục N/G/R6 OK, R3/R4/R5 TIMEOUT → LỆNH #47c
- **R3 scans · R4 leads · R5 scanned_posts đều `DEADLINE_EXCEEDED: Deadline exceeded after 300s`** — lỗi của em: đọc nguyên collection lớn 1 request (scans limit 4000 / leads 3000 / scanned_posts 6000 đủ field) đúng bẫy LỆNH #36/#37 → **LỆNH #47c** (`docs/lenh-2026-09-12-47c.sh` nhúng `-47c-data.mjs`): đọc theo TRANG 200–300 doc + `select()` chỉ field cần (field orderBy có trong select để `startAfter` chạy), thêm N2 (liệt kê nguồn URL slug / không-group), G2 (scan_jobs không cần `startedAt`, bd_month). Dry-run stub Firestore (700 lead · 450 scan · 900 scanned_posts phân trang) exit 0, 0 ✗.
- **N (config/app + nguồn)**: `config/app` KHÔNG có `scanIntervalMin` (engine dùng mặc định 10′; ô "Nhịp gieo" v119-50 chưa từng được lưu), `aiMode` undefined (log ghi "saver" là mặc định code), `profileScanEnabled true / max 5`, `keywords`/`exclude` toàn cục = 0 (đúng F-A2: trang Từ khoá đang hiện bản GỘP nguồn); exclude gộp mọi nguồn chỉ 5 từ (tuyển dụng giáo viên · tự học · chia sẻ kinh nghiệm · tuyn dng full-time · share kinh nghim). Nguồn 32 / bật 23; **doc id == gid chỉ 7/32**; dạng URL: số 17 · slug 12 · không-group 3; thiếu "/" cuối 11 → H1 (slug ↔ số) có thật.
- **G (trạng thái)**: group_state 26 doc, `recentIds` đầy 200 ở **16/26** (N1-9 có thật: group sôi động bị mù bài cũ hơn 200 bài); pending P_ 5 / C_ 1; **`cmt_scrape` gieo lại ≥2 lần = 0/3000** → Q-3 (comment chỉ lấy 1 lần) XÁC NHẬN bằng số; `seen` 54.663 doc; `score_retry` 0 (LLM khoẻ); `scan_jobs` orderBy `startedAt` ra 0 (tên field khác → #47c sort client).
- **Rules**: có block seen(260) / sources(191) / scanned_posts(252) / scan_jobs(244) / config(204) / system_alerts(248) / system_status(15) / brands(213) / users(288); KHÔNG có block `backfill_done` / `group_state` / `score_retry` / `cmt_scrape` (mặc định deny — Admin SDK ghi, không ảnh hưởng). `leads` update whitelist dòng 177 (stage, stage_at, first_care_*, fu_at, fu_note, assignee*, assigned_*, dropped*, lost*, restored_*, closed_*, deal_val…) → zip FE ghi `dropped_reason`/`ai_feedback` cần LỆNH C.
- **Hạ tầng**: manualScan 540 s · 20 inst · 512Mi (rev 00076-nuj) — N3-10 "Quét quá khứ tất cả" vượt 540 s là thật; scheduledScan 1800/1/512Mi (rev 00071-xed); statsOnLead 60/10/256Mi; pushOnLead 60/20/256Mi; outreachTick 300/1/512Mi (rev 00031-hok); Scheduler every 3′ · 1800 s · ENABLED (retry trống). Index: `scanned_posts(brand,createdAt)` READY; `leads(identityKey,last_seen_ms)` (multitouch — hướng B cần thêm brand); `leads(brand,phone,detected_at)`. TTL chỉ `outreach_log`. `.env`: **LLM_PRICE_IN=5 / OUT=30** (G-2 bác đúng), prefilter 0.05/0.40, SCAN_COMMENTS=true, COMMENTS_PER_POST=30, POSTS_PER_GROUP=20, SCANNED_TTL_DAYS=3; KHÔNG có LLM_REASONING/TIMEOUT/TRIES/RESCORE_FALLBACK/PROBE_POSTS/FULLSWEEP_HOURS/BD_SOW_MODE/SCAN_SOURCE_INTERVAL_MIN (toàn mặc định code).
- **Log 24h**: `[LLM-RESCORE]` 11/09 10:14–11:48Z ~26 lượt, mỗi lượt chấm lại 12/12 → **loại 10–12** (≈90 % backlog 252 bị loại; 1 lượt "DỪNG sớm vì LLM lỗi"); `[BRIGHTDATA-DOWN]` 10/09 10:28Z (502 nginx) → UP 10:31Z; grep "429" chỉ dính id snapshot chứa 429 (không có 429 thật).
- **R6 (automation ↔ lead)**: thread active 0 · task queued 0 (sau LỆNH #44); log 7 ngày 385, comment-lead thất bại 105 (ô Bình luận 65 · nút Trả lời 30 · ô trả lời trong khung 6 · ô sau bấm 4 — gốc R-15, worker 2026-09-11 đã vá); `outreach_map` có, `outreach_locks` chưa (đúng thiết kế B chưa làm); **push "Lead nóng mới" 85 dòng đều sent>0** (sent 1 = nhiều khả năng chỉ token super) → PB-10 vẫn cần kiểm admin brand có token; daily_stats 16 doc / 3 ngày.
- **Còn chờ anh**: (1) upload `~/scan-dump-0912.txt` + `~/scan-dump-0912b.txt` (`cloudshell download`); (2) chạy LỆNH #47b nếu chưa; (3) chạy LỆNH #47c → dán output; (4) 3 số BrightData (trần snapshot đồng thời/request-phút · đơn giá posts/comments USD/1k record + dead_page có tính tiền · gói có `notify` webhook).

### KẾT QUẢ ĐẦY ĐỦ (12/09 ~11:45 VN) — anh upload `scan-dump-0912.txt` + `scan-dump-0912b.txt` (#47b) + `scan-data-0912c.txt` (#47c) → lưu `docs/lenh-2026-09-12-47-dump/` (đã quét secret: 0 dòng lộ token/IP/sk-)
**#47b (mã)**: `lib/alerts/` = 5 kênh telegram/email/webhook/sheet/zalo, `dispatch()` chỉ gửi khi CÓ token trong `.env` — `.env` KHÔNG có TELEGRAM/RESEND/SHEET/WEBHOOK/ZALO_OA → `onLeadCreated` (index.js:877) thực tế là no-op (đọc config/app rồi log "chưa cấu hình kênh") · `scanstats.js` (v119-78) khoá nguồn↔brand bằng `normUrl(url)` (thêm 1 khoá theo URL vào danh sách phải đổi ở hướng B) · `cfgpriv.js` OK · `needsynth.js` (multitouch B) gọi LLM thẳng `fetch` KHÔNG qua llmChat46 (không timeout/retry — nhỏ, chạy trong trigger riêng) · `synthNeedsOnUpdate` = cấp `leadNo` theo brand (transaction lead_counters) + tổng hợp nhu cầu · `cleanupScannedPosts` 24h xoá theo `expireAt` ≤ 20k · **Gateway ZBS `notifyBrandZalo` (zalo-fn, onDocumentUpdated leads): `justTagged = !before.brand && after.brand`, không justTagged & score không đổi → return** ⇒ **đưa `brand` vào write ĐẦU (create) sẽ LÀM CÂM ZBS** (create không phải update; update kế có before.brand rồi) → PB-10/B PHẢI giữ 2 bước ghi brand (hoặc vá zalo-fn thêm onDocumentCreated — codebase khác, để sau).
**#47c (số liệu, đọc theo trang OK: scans 4 s · leads 5 s · scanned_posts 18 s)**:
- **N2**: nguồn không-group 3 (đều tắt, test-agency: duhoc/fnb/spa) · slug 12 (HNInternship, businesskhoinghiep, cakhocacloaigiasi, chuquancapheekhach, chuyennhaban, tuyenthuctapmarketing, vieclammarketinghaiphong, vieclamsinhvienhaiphong.2, vieclamtotchosv + 3 tắt) → id nguồn = slug + `_` (vd `cakhocacloaigiasi_`), id không phải gid 25/32. BrightData trả `parent_url`/post_url theo gid SỐ → **57/1027 comment 3 ngày "parent_url không thuộc nguồn nào" = đúng lỗi slug↔số (H1)** (brand doc ≠ brand nguồn 0 → chưa sai brand, nhưng B fan-out theo gid PHẢI chuẩn hoá slug→số lúc gặt).
- **G2**: `scan_jobs` 31 doc toàn backfill (2 "running" từ 29/06 = rác vô hại) · `bd_month/2026-09` (12 ngày): bdPostRecords 23.325 · bdCommentRecords 18.213 · aiUsd 83,8 → **AI ≈ $7/ngày · BrightData ≈ 1,9k record bài + 1,5k record comment/ngày**.
- **R3 scans 7 ngày**: 3.346 lượt (toàn scheduled), duration trung vị 15 s · p95 39 s · max 108 s; **khoảng cách 2 lượt trung vị 3′ · p95 3,4′ · >6′ 0** (Scheduler đều, không lượt chồng thấy được) · Σ bdRecords 16.396 vs postsFetched 11.268 → **31 % record bài mua về không dùng** (dead_page/đã seen/stub) · skippedSeen 69 (posts_to_not_include làm việc) · candidates 11.147 → lead 583 · nóng 192 · costUsd 48,65/7 ngày · sweepRuns 912 (578 có record) · probeEscalated 673 · **comment: commentsFetched 2.021 nhưng bdCommentRecords 12.413 → 84 % record comment là placeholder/rỗng (bài 0 bình luận vẫn tốn 1 record)** · scrapeErrors 299/7 ngày (0 trong 24h) · llmOk 544 / llmFail 1 (từ #46) · **lượt thuần skip 1.044/3.346 = 31 %** (T-7: vẫn ghi doc `scans`) · lượt không theo lịch 0 · **theo giờ VN**: 1–5h 15–70 bài/giờ, 2–3 lead/giờ · 9–11h 900–1.060 bài/giờ, 26–43 lead/giờ · 14–16h 840–860 · 20–21h 540–580 → lịch thích ứng có chỗ giảm 60 % lượt đêm · theo ngày ~480 lượt · 1,7k bài · 2,6k record · 60–103 lead · **bySource 24h**: mỗi nguồn "gieo ok" 256–296/474 lượt (= gieo mỗi 10′ + gặt) → ~3,3k snapshot/ngày cho 23 nguồn; 4 nguồn ≥ 240 bài/ngày (Chợ Hải Sản Vân Đồn 292 · Intern 280 · Cá khô sỉ 1 257 · Việc làm SV HP 239), **3 nguồn 0 bài 24h (Hội chủ quán 2 – T2 · Hội chủ quán – T1 · kn2)** + 3 nguồn ≤ 8 bài/ngày (chuquancaphe2, edf, Content/Marketing HP) → S9 ứng viên giãn nhịp/tắt · H8 record comment ≥ 30/lượt chỉ 1 · lỗi BrightData 429/not active 0.
- **R4 leads 30 ngày**: 2.059 (tts-1 856 · hscl-01 686 · tuyendung1 370 · test-agency 147) · temp hot 480 / warm 792 / cold 514 / junk 273 · post 1.629 / comment 430 · **`ai_scored:false` còn 2 (mở 0) → backlog #46 XONG** · rescored 298 → loại 274 (92 %; rescore_role 248 + rescore 26) · **N4-3: lead AI chấm lại CÓ người thật chăm = 0** → mối lo "sweeper giấu lead đang chăm" chưa xảy ra thật (PB-5 vẫn làm nhưng KHÔNG khẩn) · stage: new 2.044 (99,3 %) · checked 7 · responded 4 · booked 1 · closed 1 · inbox 2 · lost 1 → sales gần như chưa dùng pipeline · group_count ≥ 2: 21 · identityKey rỗng 64,4 % · author_url có 33,2 % · uid số 9,4 % · multitouch chéo brand 0 · phone 12,8 % (Zalo true 253/false 11) · email 32 · **`post_id` rỗng 2.059/2.059** (lead không lưu post_id, chỉ post_url — B cần ghi `post_id` từ `x.post.post_id` để id tất định) · comment_id đều số · **Q-4 lag phát hiện (detected_at − time bài): trung vị 11′ · p95 36′ · < 15′ 69,6 % · > 60′ 3,2 %** (= mốc gốc trước khi làm tốc độ; theo nguồn 8–16′, riêng Việc làm Marketing HP 93′) · **Q-1: 161 cặp lead cùng brand ≤ 14 ngày text trùng ≥ 0,8 → 198/1.087 lead-bài (18 %) là bản gần trùng** (không gộp được vì 64 % không có identityKey → PC-4 vân tay văn bản có giá trị thật) · R6c lead nóng 7 ngày 192: zalo_notified true 192, brand_tagged_by scanner-inline 192, brand_pending 0 (ZBS đang chạy đúng qua đường 2 bước) · role buyer 546 · seller 248 · poster_self 158 · other 19 · rỗng 1.088 (lead trước #31) · outreach chạm 43 · replied 0 · lead/ngày: tts-1 43–68 · hscl-01 8–19 · tuyendung1 8–21 · test-agency 2–5.
- **R5 scanned_posts 3 ngày**: 6.099 doc · decision prefiltered_out 3.937 (64,5 %) · excluded 651 (10,7 %) · seller 528 · self_comment 446 · lead 309 · scored_low 228 · ai_wait 0 · **6/6 mẫu "excluded" có `text` RỖNG** → `isExcluded` trả true khi không có chữ (bài chỉ ảnh/video, hscl-01: Chợ Hải Sản Vân Đồn 273/967 = 28 %, Cá khô sỉ 1 183/619 = 30 %) → nhãn "excluded" đang gộp cả "không có chữ" (đề xuất tách `no_text`; OCR ảnh giá là chuyện sau) · post 5.072 / comment 1.027 · comment mồ côi 57 (H1) · bài ai_wait lặp 0 · bài >1 dòng 0 (không chấm đôi).
**Kết luận cho bước kế**: (1) PB-5 hạ từ khẩn xuống thường (backlog xong, 0 lead đang chăm bị đụng) → GỘP A0 vào LỆNH A; (2) PB-10 = cổng push kiểu #41 (brand xuất hiện) + GIỮ 2 bước ghi brand vì ZBS `justTagged`; (3) B thêm: ghi `post_id`, chuẩn hoá gid slug→số lúc gặt (57 orphan), khoá `scanstats.normUrl` cũng theo source_id; (4) tốc độ: mốc gốc 11′/36′; đòn bẩy = nhịp gieo 10′ → 5′ cho nguồn sôi động (chờ giá dead_page) + tick 1′ có khoá + bỏ 31 % lượt thuần skip ghi `scans` + giảm 84 % record comment rỗng bằng gieo comment CHỈ cho bài có bình luận (cần xem `normalizePost` có `num_comments` → #47d); (5) #47d dump nốt mốc còn thiếu (index.js 762–870, scorer 1–56/117–187, scraper 1–60, config.js, zalo-fn notifyBrandZalo/tagLeadBrand) rồi soạn A → B → C → zip.

### #47d — đợt ảnh 1 (12/09 ~12:00 VN): sha8 index.js=14a3a823 · scorer=12223e1c · scraper=66714197 · config=81d91f79 · stats=5dc6d521 · push=59a304aa · outreach=457c9290
Đọc RÕ từ ảnh: `index.js 148–211` (verifyCaller/loadSources/seenRef/loadConfig/computeRange/mapPool/seenDoc/urlKey/cmtScrapeDoc/groupStateDoc) · `index.js 762–870` (sweeper #46 nửa sau · `[LLM-DOWN]/[LLM-UP]` 793–808 · costUsd 810–812 · `summary` 813–825 · `scans.add` 828 · rollup `bd_month` 836–857 (khoá `srcMap['s_'+(url||name)]`) · job done 858–865 · return 867) · `lib/scorer.js 1–56` (brandAiCtx · SYS · PRE_SYS · normRole) · `lib/scorer.js 117–140` (llmChat46 nửa đầu). **Xác nhận từ mã**: (1) sweeper nhánh `human` (775) chỉ ghi `rescore_note`, KHÔNG dropped, nhưng vẫn `set({score:h, temp:t, …})` (776/782) → temp có thể thành `junk` cho lead đang chăm = đúng cơ chế N4-3 (thực tế chưa xảy ra vì human=0 trong 298 lead) → PB-5 = với human: giữ `temp` ≥ cold + thêm `ai_flag`, không hạ score dưới 40; (2) `stop46` chỉ đặt khi lỗi auth/quota/model/server/net/rate (770); (3) `[LLM-DOWN]` điều kiện `llmFail46 ≥ 2 && llmOk46 === 0` hoặc auth/quota/model (797) — chưa có cửa sổ trượt/tầng 1 (PB-4 mốc 793–808); (4) `scans.add(summary)` vô điều kiện (828) → T-7 mốc; (5) `bd_month.src` khoá theo URL/tên (847) → B đổi sang `source_id`; (6) SYS prompt vẫn "agency marketing tại Việt Nam" + brandAiCtx nối sau (L-2, PA-1 Đợt 2); (7) `brandAiOf46` đọc `brands/{code}.ai` có cache (763).
**CHƯA đọc được (ảnh 5–8 chữ quá nhỏ)**: `lib/scorer.js 141–187` (vòng thử lại/heuristic/weightsHint), `lib/scraper.js 1–60` (normalizePost — cần biết field `num_comments`), `lib/config.js` toàn bộ, `lib/zaloCheck.js 1–40`, `lib/logger.js`, zalo-fn `notifyBrandZalo`/`getBrandSales`/`buildTemplateData`, `tagLeadBrand` ở đâu, package.json/firebase.json → cần anh upload file `~/scan-dump-0912d.txt` (không chép mã từ ảnh mờ làm mốc patch).
