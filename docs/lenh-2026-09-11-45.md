# LỆNH #45 (11/09/2026) — CHỈ ĐỌC: vì sao lead hiện "Điểm tạm – AI chưa chấm" (ai_scored:false) dù credit OpenAI còn

> Bản lưu; **khối lệnh thật dán trong chat** (anh copy lên Cloud Shell). File script: `lenh-2026-09-11-45-ai.mjs` · wrapper `lenh-2026-09-11-45.sh` (nested heredoc quoted, không `!` ngoài heredoc). Đã dry-run với stub firebase-admin + stub gcloud: exit 0, che key đúng.

## Cơ chế (đọc từ `lib/scorer.js` thật + `index.js` scanAll)
- `scoreLead()` gọi OpenAI (`LLM_MODEL`, `response_format json_object`, **không timeout, không max_tokens**). Lỗi bất kỳ → **retry đúng 1 lần sau 1,5 s** (trừ `insufficient_quota`) → vẫn lỗi → `heuristic()` = bộ chấm từ khoá: 35 nền + 45 nếu có "cần tìm/thuê/tìm agency/báo giá/trọn gói/cần chạy/cần tư vấn…" + 10 nếu có "N tr/triệu/k" − 40 nếu có "khoá học/tuyển/2k/sinh viên…"; `intent` = "Cần mua / tìm nhà cung cấp" (≥60) · `service` = "Performance Marketing" khi nguồn có ngành · `industry` = ngành nguồn · `reply` rỗng · `_llm:false`.
- `index.js` Pha 3: `ai_scored: ai._llm !== false` → FE modal (Super Admin) chip "Điểm tạm – AI chưa chấm". Lead #399: "Thuê mb chỉ 5 triệu…" → 35+45+10 = **90 = nóng** — khớp ảnh.
- **Hệ quả**: lead rác thành NÓNG 90 → push "Lead nóng mới", Hộp việc, Nên gọi tiếp theo, KPI; **engine automation vẫn bốc** (chỉ lọc dropped/junk/vai/người thật) → nick chạm bài rác. **Im lặng hoàn toàn**: `scans.scoreErrors` vẫn 0 (hàm không ném lỗi), không cảnh báo, sales không thấy chip, không bao giờ chấm lại.

## Mọi đường rơi về bộ dự phòng
1. `LLM_API_KEY` trống trong env đã deploy → heuristic ngay (kể cả không retry).
2. Key sai/đã xoay ở OpenAI nhưng function chưa deploy lại → 401 `invalid_api_key` (không khớp regex quota → retry 1 lần → heuristic).
3. 429 rate limit (TPM/RPM): scanner 6 lượt chấm + 10 lượt sàng lọc song song, cùng key với `genContent` (14–16 s/lead, gpt-5.6-sol) + `_l34_regen`… → 1,5 s sau vẫn 429.
4. 5xx / mạng / treo (không timeout — 1 lời gọi treo giữ cả pool).
5. 400 tham số (model từ chối `response_format`…), 404 model không tồn tại/đổi tên.
6. `JSON.parse` lỗi: content rỗng, `finish_reason=length`, refusal, JSON kẹp ```json.
7. `insufficient_quota` (hết credit) → không retry → heuristic (ca cũ).

## LỆNH #45 in gì
1. `.env` cục bộ ↔ env đã deploy 4 service (key: độ dài + sha8, model, base) → phát hiện "xoay key chưa deploy".
2. Gọi thử OpenAI y hệt `scoreLead` bằng key ĐÃ DEPLOY (+ key .env nếu khác, + model sàng lọc, + `reasoning_effort=low`, + burst 3 lượt) → status/mã lỗi/finish/token/độ trễ/**rate-limit headers**.
3. Lead `ai_scored:false` 14 ngày: theo ngày (tỉ lệ), brand, nguồn, nhiệt độ; còn mở / sales đã chăm / **máy đã chạm**; 10 lead gần nhất; lead #399.
4. `scans` 48 h theo giờ: `scoreCalls=0` mà vẫn tạo lead = "AI IM LẶNG".
5. Cloud Logging 24 h: dòng "LLM lỗi / prefilter lỗi / heuristic / miss-reply" gom theo chữ ký lỗi (che key).
6. `GET /models/<LLM_MODEL>` với key đã deploy.

## Sau khi có output → LỆNH #46 (vá triệt để, không phụ thuộc nguyên nhân hôm nay)
- `lib/scorer.js`: helper `llmCall` chung (timeout 60 s, retry 3 lần lùi 2/6/15 s tôn trọng `Retry-After` cho 429/5xx/mạng, tự thích nghi tham số như `llmJson` content.js, đọc JSON khoan dung, phân loại lỗi `auth/quota/rate/net/format`).
- **Không tạo lead nóng từ bộ dự phòng**: LLM hỏng sau retry → ghi bài vào `score_retry/{postKey}` (kèm src/brand), KHÔNG tạo lead; đầu mỗi lượt quét chấm lại ≤ N bài chờ; quá 3 ngày vẫn hỏng → mới tạo lead dự phòng nhưng **kẹp ≤ 59 (lạnh)** + `ai_scored:false`.
- Sweeper `rescoreFallback`: lead `ai_scored:false` đang mở → AI chấm lại (ghi đè score/temp/intent/need/service/industry/reply, `ai_scored:true, rescored_at`); AI nói không phải lead → `dropped:true, dropped_by:'rescore'`. Script `_l46_rescore.mjs` chạy 1 lần cho lead cũ.
- Cổng: engine `stepNickAdspower/stepNick` + `push.js` lead nóng mới **bỏ qua `ai_scored===false`**.
- Giám sát: `scans` thêm `llmFallback/llmErrClass`; ≥3 lỗi LLM & 0 thành công trong lượt → `console.log({severity:'ERROR', message:'[LLM-DOWN] …'})` (alert #9) + `system_status/llm {ok,since,sample}` → FE Cảnh báo hệ thống + chip thanh nhịp quét; `[LLM-UP]` khi hồi.
- FE v119-89: chip "Điểm tạm" hiện cho MỌI vai trò trên thẻ + modal; "Nên gọi tiếp theo"/Hộp việc hạ ưu tiên lead chưa AI chấm.
