# Rà soát toàn diện mảng automation "Tiếp cận" (10/09/2026) — CHỈ ĐÁNH GIÁ, chưa sửa code, chờ anh chốt

- **Báo cáo (artifact)**: https://claude.ai/code/artifact/85bfaa3e-b299-46ba-a92b-1f3878c571cf — bản HTML cùng thư mục: `report.html` (sinh từ `findings.json` bằng `python3 build.py`).
- **Kết luận**: chưa "oke hết". Lớp an toàn nick vững (3 vòng rà trước); 3 lỗ hệ thống: (1) vòng phản hồi khách gần như mù (worker chỉ đọc inbox khi nick có việc mới, bỏ thread sau 6 giờ, không bắt phản hồi qua bình luận → `replyFound = 0`); (2) engine chọn lead & kế toán van (chạm chồng sales đang chăm/đã chốt, lead nóng không ưu tiên, phễu kẹt qua ngày chạy lại = vượt van 2×, thread mồ côi khi nick chết); (3) không giám sát chủ động (4/5 nick chết 1 tuần chỉ phát hiện khi chạy script; web không realtime cờ nick). 2 đường còn có thể đăng bình luận 2 lần (API mơ hồ → DOM; tắt worker giữa bước); cách đếm lỗi nick sẽ tự tắt nick khoẻ khi nâng số nick cùng lúc.
- **Số liệu**: 50 phát hiện (11 Cao · 21 Vừa · 18 Thấp) — Worker W-1…17 · Engine/nội dung E-1…15 · Web F-1…18; 14 đề xuất R-1…14; lộ trình 3 đợt.
- **Phương pháp**: 3 agent đối kháng chỉ-đọc song song (A worker.mjs 2026-09-06d toàn bộ · B FE 45-outreach + live.js · C engine dựng lại từ 14 patch/harness + CLAUDE.md) → em kiểm chứng từng mục Cao/Vừa trên code; mục chỉ suy từ tài liệu ghi "Suy luận" + có mục kiểm trong LỆNH #43. Báo cáo gốc agent: `agent-A-worker.md`, `agent-B-fe.md`, `agent-C-engine.md`.
- **★ LỆNH #43 ĐÃ CHẠY 10/09 15:45 VN** → số thật + dump đã điền vào báo cáo (mục "Kết quả LỆNH #43", `docs/lenh-2026-09-10-43.md` phần KẾT QUẢ, dump `docs/lenh-2026-09-10-43-engine-dump.txt`): hscl-01 BẬT nhưng 0/6 nick sống · 12 thread kẹt 5–10 ngày · comment vào bài fail 82 % (105/23) · Safety t7 = 28 do 43 lỗi selector (không checkpoint) → tự tắt 09/09 · 160 log "Tạm hoãn" nick needLogin · backlog 133 lead 30 ngày (32 nóng). Chốt: E-2 CHẮC (limit 40), E-4 CHẮC, E-7 → Thấp; mới W-18/W-19/E-16/E-17 + đề xuất R-15. Tổng 53 phát hiện (13 Cao) · 15 đề xuất. Còn cần từ VPS: config.json doc_id · 2 ảnh errors comment · 1 dòng console comment API.
- **Bước kế (cũ)**: anh chạy **LỆNH #43** (`docs/lenh-2026-09-10-43.md`, chỉ đọc, ~2 phút: sức khoẻ automation + dump 4 hàm engine) → em điền số, chốt 3 mục "Suy luận" (E-2 limit · E-4 reserve lại · E-7 re-enqueue khi running) và danh sách sửa đợt 1 (worker 2026-09-10 + LỆNH #44 engine nhỏ + zip FE nhỏ: R-1, R-2, R-6, R-8, R-9, F-1, F-2, F-6).
